---
phase: quick-260318-m5y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/articulos/articulo-sheet.tsx
autonomous: true
requirements: [QUICK-IMG-REDESIGN]

must_haves:
  truths:
    - 'Always 9 slots visible in a single horizontal row: 6 producto + separator + 3 etiqueta'
    - 'Slots with images show the image thumbnail, slots without show a placeholder'
    - 'No conditional rendering — same layout whether 0 or 9 images exist'
    - 'Clicking an image opens the lightbox for its type'
  artifacts:
    - path: 'apps/web/src/components/articulos/articulo-sheet.tsx'
      provides: 'Unified horizontal image slots layout'
      contains: 'imagenesProducto'
  key_links:
    - from: 'articulo-sheet.tsx image slots'
      to: 'openLightboxForType'
      via: 'onClick handler on image buttons'
      pattern: 'openLightboxForType'
---

<objective>
Redesign the images section in articulo-sheet.tsx detail view to always show 6 product slots + vertical separator + 3 etiqueta slots in a single horizontal row, with placeholders for empty positions.

Purpose: Consistent, predictable layout regardless of how many images exist. No more conditional empty state vs populated state.
Output: Modified articulo-sheet.tsx with unified horizontal image layout.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/components/articulos/articulo-sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace images section with unified horizontal slot layout</name>
  <files>apps/web/src/components/articulos/articulo-sheet.tsx</files>
  <action>
Replace lines 194-266 (the entire images section IIFE) with a new layout that:

1. Remove the IIFE and all conditional rendering (no more `hasAnyImages` check, no separate empty/populated states).

2. Replace with a single `div` structure:

```tsx
{
  /* Images section */
}
;<div>
  <div className="flex items-center gap-0.5 text-[10px] text-muted-foreground mb-1">
    <span className="flex-1 text-center">Producto</span>
    <span className="w-px" />
    <span className="flex-1 text-center">Etiqueta</span>
  </div>
  <div className="flex items-stretch gap-1 h-14">
    {articulo.imagenesProducto.map((url, i) =>
      url ? (
        <button
          key={`p-${i}`}
          onClick={() => openLightboxForType('producto', url)}
          className="flex-1 min-w-0 aspect-square rounded-sm overflow-hidden cursor-pointer border border-border hover:opacity-80 transition-opacity"
        >
          <img
            src={API_BASE_URL + getThumbUrl(url)}
            className="w-full h-full object-cover"
            alt=""
          />
        </button>
      ) : (
        <div
          key={`p-${i}`}
          className="flex-1 min-w-0 aspect-square rounded-sm bg-muted flex items-center justify-center"
        >
          <ImageIcon className="h-4 w-4 text-muted-foreground/40" />
        </div>
      )
    )}
    {/* Vertical separator */}
    <div className="w-px bg-border self-stretch mx-0.5" />
    {articulo.imagenesEtiqueta.map((url, i) =>
      url ? (
        <button
          key={`e-${i}`}
          onClick={() => openLightboxForType('etiqueta', url)}
          className="flex-1 min-w-0 aspect-square rounded-sm overflow-hidden cursor-pointer border border-border hover:opacity-80 transition-opacity"
        >
          <img
            src={API_BASE_URL + getThumbUrl(url)}
            className="w-full h-full object-cover"
            alt=""
          />
        </button>
      ) : (
        <div
          key={`e-${i}`}
          className="flex-1 min-w-0 aspect-square rounded-sm bg-muted flex items-center justify-center"
        >
          <ImageIcon className="h-4 w-4 text-muted-foreground/40" />
        </div>
      )
    )}
  </div>
</div>
```

Key design decisions:

- `flex items-stretch` + `flex-1 min-w-0` on each slot distributes evenly across full width
- `aspect-square` keeps slots square
- `h-14` on the container constrains height so slots are compact within the 480-640px sheet
- Separator is a `w-px bg-border self-stretch` div with `mx-0.5` for subtle spacing
- Labels row uses same flex distribution: "Producto" spans 6 slots, "Etiqueta" spans 3 slots — use `flex-[6]` and `flex-[3]` respectively for the label spans, with the `w-px` spacer between them
- No SectionHeader — the labels replace it
- Placeholders use same `bg-muted` + `ImageIcon` pattern as current empty state but properly sized

Correction on labels row — use proportional flex instead of flex-1:

```tsx
<div className="flex items-center gap-0.5 text-[10px] text-muted-foreground mb-1">
  <span className="flex-[6] text-center">Producto</span>
  <span className="w-px" />
  <span className="flex-[3] text-center">Etiqueta</span>
</div>
```

This ensures "Producto" label centers over 6 slots and "Etiqueta" over 3 slots.
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
</verify>
<done>Images section always renders 9 slots (6+3) in a horizontal row with separator, showing images or placeholders. No conditional empty/populated states. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- Open articulo detail sheet for an articulo WITH images — see images in their slots, placeholders in empty slots
- Open articulo detail sheet for an articulo WITHOUT images — see 9 placeholders with separator
- Click an image — lightbox opens for correct type (producto/etiqueta)
- Layout fits within sheet width (480px mobile, 640px desktop)
</verification>

<success_criteria>

- Single horizontal row of 6 producto slots + separator + 3 etiqueta slots always visible
- Images render in slots that have data, placeholders in slots that don't
- Lightbox functionality preserved
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/quick/260318-m5y-redise-ar-secci-n-im-genes-vista-detalle/260318-m5y-SUMMARY.md`
</output>
