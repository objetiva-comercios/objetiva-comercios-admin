---
phase: quick
plan: 260318-2l2
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/hooks/use-ui-scale.ts
  - apps/web/src/components/ui-scale-init.tsx
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/(dashboard)/settings/appearance/page.tsx
autonomous: true
requirements: [quick-task]
must_haves:
  truths:
    - 'La interfaz se renderiza al 90% por defecto (font-size 90% en html)'
    - 'El usuario puede cambiar la escala desde Settings > Apariencia'
    - 'La escala persiste entre recargas via localStorage'
  artifacts:
    - path: 'apps/web/src/hooks/use-ui-scale.ts'
      provides: 'Hook para leer y escribir la escala UI'
    - path: 'apps/web/src/components/ui-scale-init.tsx'
      provides: 'Componente client que aplica la escala al montar'
  key_links:
    - from: 'apps/web/src/app/layout.tsx'
      to: 'apps/web/src/components/ui-scale-init.tsx'
      via: 'import and render UiScaleInit'
    - from: 'apps/web/src/app/(dashboard)/settings/appearance/page.tsx'
      to: 'apps/web/src/hooks/use-ui-scale.ts'
      via: 'useUiScale hook for controls'
---

<objective>
Agregar escala UI global configurable con default 90%. Usa `html { font-size: X% }` para escalar todo lo que use rem (Tailwind). Persistencia en localStorage, controles en Settings > Apariencia.

Purpose: Emular zoom del navegador al 90% para una UI mas compacta, configurable por el usuario.
Output: Hook useUiScale, componente UiScaleInit, controles en pagina de Apariencia.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/app/layout.tsx
@apps/web/src/app/(dashboard)/settings/appearance/page.tsx

<interfaces>
<!-- Root layout structure (from apps/web/src/app/layout.tsx): -->
<!-- RootLayout wraps children in ThemeProvider + Toaster -->
<!-- UiScaleInit goes inside <body> alongside ThemeProvider -->

<!-- Appearance page (from apps/web/src/app/(dashboard)/settings/appearance/page.tsx): -->
<!-- Uses next-themes useTheme, RadioGroup for theme selection -->
<!-- New scale section goes after the theme section div -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear hook useUiScale y componente UiScaleInit, integrar en root layout</name>
  <files>apps/web/src/hooks/use-ui-scale.ts, apps/web/src/components/ui-scale-init.tsx, apps/web/src/app/layout.tsx</files>
  <action>
1. Crear `apps/web/src/hooks/use-ui-scale.ts`:
   - STORAGE_KEY = 'ui-scale', DEFAULT_SCALE = 90
   - useState inicializa en DEFAULT_SCALE
   - useEffect en mount: lee localStorage, parsea int, aplica a document.documentElement.style.fontSize = `${value}%`
   - Funcion updateScale(value: number): clamp entre 80 y 100, setea state, guarda en localStorage, aplica al DOM
   - Exporta { scale, setScale: updateScale }

2. Crear `apps/web/src/components/ui-scale-init.tsx`:
   - Componente client ('use client') que retorna null
   - useEffect en mount: lee localStorage key 'ui-scale', default 90, aplica document.documentElement.style.fontSize = `${value}%`
   - Este componente solo aplica la escala al cargar. No expone controles.

3. Editar `apps/web/src/app/layout.tsx`:
   - Importar UiScaleInit de '@/components/ui-scale-init'
   - Renderizar `<UiScaleInit />` dentro del body, antes o despues de ThemeProvider (no importa el orden, renderiza null)
     </action>
     <verify>
     <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter web build 2>&1 | tail -5</automated>
     </verify>
     <done>La app compila sin errores. UiScaleInit se monta en el root layout y aplica font-size 90% por defecto al html.</done>
     </task>

<task type="auto">
  <name>Task 2: Agregar controles de escala en Settings > Apariencia</name>
  <files>apps/web/src/app/(dashboard)/settings/appearance/page.tsx</files>
  <action>
Editar `apps/web/src/app/(dashboard)/settings/appearance/page.tsx`:

1. Importar useUiScale de '@/hooks/use-ui-scale'
2. Importar ZoomIn de 'lucide-react' (icono para la seccion)
3. En el componente, llamar `const { scale, setScale } = useUiScale()`

4. Despues del div.rounded-lg.border existente (seccion Tema), agregar una nueva seccion con la misma estructura visual:

```
<div className="rounded-lg border p-6">
  <div className="space-y-4">
    <div>
      <h3 className="text-lg font-medium">Escala de interfaz</h3>
      <p className="text-sm text-muted-foreground">Ajusta el tamano general de la interfaz.</p>
    </div>

    <div className="flex flex-wrap gap-2">
      {[80, 85, 90, 95, 100].map((value) => (
        <button
          key={value}
          onClick={() => setScale(value)}
          className={cn(
            "inline-flex items-center justify-center rounded-md border px-4 py-2 text-sm font-medium transition-colors",
            scale === value
              ? "border-primary bg-primary text-primary-foreground"
              : "border-input bg-background hover:bg-accent hover:text-accent-foreground"
          )}
        >
          {value}%
        </button>
      ))}
    </div>

    <p className="text-xs text-muted-foreground">
      Escala actual: {scale}%. El valor por defecto es 90%.
    </p>
  </div>
</div>
```

5. Importar `cn` de '@/lib/utils' si no esta importado ya.

Los botones de preset son toggle-style: el activo tiene estilo primary, los demas estilo outline. Al hacer click se aplica inmediatamente (el hook actualiza localStorage y DOM).
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter web build 2>&1 | tail -5</automated>
</verify>
<done>La pagina de Apariencia muestra seccion "Escala de interfaz" con botones 80%-100%. Seleccionar un valor lo aplica inmediatamente, se marca como activo, y persiste al recargar.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter web build` compila sin errores
2. Abrir Settings > Apariencia: se ve seccion de tema y seccion de escala
3. Click en 85%: la interfaz se achica, el boton 85% queda marcado
4. Recargar pagina: la escala sigue en 85%
5. Click en 90%: vuelve al default
</verification>

<success_criteria>

- La app arranca con font-size 90% en el html element
- Los controles de escala funcionan con feedback visual inmediato
- La preferencia persiste en localStorage entre sesiones
- Build pasa sin errores ni warnings
  </success_criteria>

<output>
After completion, create `.planning/quick/260318-2l2-agregar-escala-ui-90-global-configurable/260318-2l2-SUMMARY.md`
</output>
