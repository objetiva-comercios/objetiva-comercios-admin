---
phase: quick
plan: 260317-qz6
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/tables/data-table.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - 'Las tablas de ventas, pedidos y compras usan el mismo tamaño de fuente y padding que la tabla de artículos'
  artifacts:
    - path: 'apps/web/src/components/tables/data-table.tsx'
      provides: 'DataTable con text-sm py-2 en TableHead y TableCell'
  key_links: []
---

<objective>
Igualar el tamaño de fuente y padding de DataTable (ventas, pedidos, compras) con ServerDataTable (artículos).

Purpose: Consistencia visual entre todas las tablas de la aplicación.
Output: DataTable con clases text-sm py-2 en headers y celdas.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/components/tables/data-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar text-sm py-2 a TableHead y TableCell en DataTable</name>
  <files>apps/web/src/components/tables/data-table.tsx</files>
  <action>
En `apps/web/src/components/tables/data-table.tsx`:

1. Linea 92 — Agregar className al TableHead de datos (NO al empty-state):
   Cambiar: `<TableHead key={header.id}>`
   A: `<TableHead key={header.id} className="text-sm py-2">`

2. Linea 112 — Agregar className al TableCell de datos (NO al empty-state de linea 120):
   Cambiar: `<TableCell key={cell.id}>`
   A: `<TableCell key={cell.id} className="text-sm py-2">`

Esto iguala el estilo con ServerDataTable que ya tiene estas mismas clases.
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && grep -n 'text-sm py-2' apps/web/src/components/tables/data-table.tsx | wc -l | grep -q '3' && echo "PASS: 3 instancias de text-sm py-2 (2 nuevas + 1 empty state existente)" || echo "FAIL"</automated>
</verify>
<done>TableHead y TableCell en DataTable tienen className="text-sm py-2", igualando el estilo compacto de ServerDataTable</done>
</task>

</tasks>

<verification>
- grep confirma que DataTable tiene text-sm py-2 en TableHead y TableCell
- Build compila sin errores: `cd apps/web && pnpm build`
</verification>

<success_criteria>
Las tablas de ventas, pedidos y compras se renderizan con el mismo tamaño de fuente (text-sm) y padding (py-2) que la tabla de artículos.
</success_criteria>

<output>
After completion, create `.planning/quick/260317-qz6-igualar-tama-o-de-fuente-de-lista-de-art/260317-qz6-SUMMARY.md`
</output>
