---
phase: 29-catalogos-de-atributos
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: []
autonomous: true
requirements: [CAT-01, CAT-03, CAT-04]
tags: [drizzle, migrate, postgres, propiedades, blocking, schema-push]

must_haves:
  truths:
    - "Las 6 tablas prop_* existen físicamente en la base de datos PostgreSQL local"
    - "Cada tabla tiene la shape correcta verificable vía `\\d prop_marca` (etc.)"
    - "Los 4 índices por tabla (UNIQUE LOWER nombre, UNIQUE abrev, index activo + PK serial) están aplicados"
    - "El CHECK constraint sobre abrev rechaza inserts inválidos (verificable con un INSERT de prueba)"
    - "El custom SQL 0005_phase29_cache_trigger.sql se aplicó como no-op sin errores"
    - "drizzle-kit considera la DB como up-to-date — `db:generate --check` o equivalente no muestra diff"
  artifacts:
    - path: "DATABASE: prop_marca, prop_color, prop_talle, prop_material, prop_presentacion, prop_objeto"
      provides: "6 tablas físicamente creadas en Postgres"
      contains: "psql -c \"\\dt prop_*\" muestra 6 tablas"
  key_links:
    - from: "apps/backend/drizzle/0004_phase29_propiedades.sql"
      to: "Postgres database"
      via: "drizzle-kit migrate"
      pattern: "applied"
    - from: "apps/backend/drizzle/0005_phase29_cache_trigger.sql"
      to: "Postgres database"
      via: "drizzle-kit migrate (no-op SQL)"
      pattern: "applied"
---

<objective>
**[BLOCKING SCHEMA PUSH]** Aplicar contra la base de datos Postgres local los dos migrations creados en Plan 01 (`0004_phase29_propiedades.sql` + `0005_phase29_cache_trigger.sql`) y verificar que las 6 tablas, sus constraints y sus índices existen y se comportan correctamente.

Purpose: Sin este paso, el módulo backend del Plan 03 no puede correr. Drizzle types compilan al ver `schema.ts` (Plan 01) pero las queries del service fallarían en runtime con `relation "prop_marca" does not exist`. Este es el gate técnico de la fase.

Output: 6 tablas físicas en Postgres con todos sus constraints + verificación que el CHECK constraint rechaza valores inválidos.

Convención del repo: usar `db:migrate` (no `db:push`) porque el migration ya está versionado en `apps/backend/drizzle/`. Esto preserva el historial y mantiene `_journal.json` consistente con producción.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-catalogos-de-atributos/29-CONTEXT.md
@.planning/phases/29-catalogos-de-atributos/29-01-SUMMARY.md

<interfaces>
<!-- Comandos del repo (definidos en apps/backend/package.json) -->

```jsonc
{
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate":  "drizzle-kit migrate",
    "db:push":     "drizzle-kit push",
    "db:studio":   "drizzle-kit studio"
  }
}
```

`db:migrate` ejecuta los archivos SQL en `apps/backend/drizzle/` que aún no estén registrados en la tabla `__drizzle_migrations` de Postgres. Es la opción canónica del repo (preserva historial).

<!-- Variables de entorno relevantes (apps/backend/.env) -->

`DATABASE_URL=postgresql://...` — apunta a la DB local de desarrollo (PostgreSQL 16).

<!-- Stack dockerizado del proyecto (memoria) -->

El proyecto corre dockerizado: `docker compose up`. Los migrations se aplican típicamente desde dentro del contenedor backend (que tiene acceso a la DB) o ejecutando localmente con `DATABASE_URL` apuntando al Postgres de Docker.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Aplicar migrations 0004 y 0005 contra Postgres local</name>
  <files></files>
  <read_first>
    - apps/backend/package.json (scripts db:migrate, db:generate, db:push)
    - apps/backend/drizzle.config.ts (config — confirma que apunta a apps/backend/drizzle/)
    - apps/backend/drizzle/0004_phase29_propiedades.sql (creado en Plan 01 — verificar que existe)
    - apps/backend/drizzle/0005_phase29_cache_trigger.sql (creado en Plan 01 — verificar que existe y todo está comentado)
    - .planning/phases/29-catalogos-de-atributos/29-01-SUMMARY.md (output del Plan 01)
  </read_first>
  <behavior>
    - Después de correr `db:migrate`, las 6 tablas existen físicamente en Postgres.
    - Drizzle Kit reporta los 2 migrations como aplicados (0004 + 0005).
    - El comando NO ejecutó ningún DROP / TRUNCATE — verificable inspeccionando el output del comando (no debe contener `DROP TABLE`, `DROP INDEX` ni `TRUNCATE` en el SQL aplicado).
    - El archivo 0005_phase29_cache_trigger.sql se aplicó como no-op (solo comentarios), sin error.
  </behavior>
  <action>
1) Pre-flight: confirmar que los dos archivos del Plan 01 existen y que el journal los registra:

```bash
ls -la apps/backend/drizzle/0004_phase29_propiedades.sql apps/backend/drizzle/0005_phase29_cache_trigger.sql
grep -E "phase29_propiedades|phase29_cache_trigger" apps/backend/drizzle/meta/_journal.json
```

Si alguno falta, ABORTAR — Plan 01 no fue completado correctamente.

2) Pre-flight de seguridad (regla del proyecto: NUNCA borrar tablas de producción sin autorización explícita):

Inspeccionar manualmente el SQL del migration auto-generado para confirmar que es PURAMENTE ADITIVO:

```bash
grep -E "^DROP|^TRUNCATE|^DELETE FROM" apps/backend/drizzle/0004_phase29_propiedades.sql apps/backend/drizzle/0005_phase29_cache_trigger.sql
```

Si retorna ALGO, ABORTAR y notificar al usuario antes de continuar — el migration nunca debe ser destructivo en Phase 29 (la fase solo agrega 6 tablas nuevas, ninguna preexistente).

3) Pre-flight Docker: por la convención dockerizada del proyecto (memoria — Web:3000, Backend:3001), correr el migrate DENTRO del contenedor backend, NO desde el host con `pnpm --filter`. Esto evita problemas de DATABASE_URL apuntando a `localhost` en host vs `postgres` (service name) dentro del contenedor.

```bash
docker compose ps backend | grep -q "Up" || (echo "Backend container not Up — start with: docker compose up backend -d" && exit 1)
```

4) Aplicar migrations contra Postgres local DESDE el contenedor backend:

```bash
docker compose exec -T backend pnpm db:migrate
```

Esto procesa los archivos `0004_phase29_propiedades.sql` y `0005_phase29_cache_trigger.sql` en orden. La conexión usa el `DATABASE_URL` interno del contenedor (apunta al service `postgres`).

Si el comando falla con `connection refused` u otro error de red:
- Confirmar que el servicio postgres está Up: `docker compose ps postgres`
- Verificar la red: `docker compose exec backend ping -c 1 postgres`
- Verificar `DATABASE_URL` interno: `docker compose exec backend env | grep DATABASE_URL`

4) Verificar que el comando reportó ambos migrations como aplicados (output típico de drizzle-kit muestra "applied" o equivalente).

5) Si Drizzle Kit detecta cambios destructivos inesperados (improbable porque solo se agregan tablas nuevas), ABORTAR el migrate con Ctrl+C y notificar al usuario antes de aceptar el prompt. Marcar este task como `autonomous: false` retroactivamente y escalar.

NO usar `db:push` (sobrescribe schema sin migration) salvo que el usuario explícitamente lo autorice.
  </action>
  <verify>
    <automated>docker compose ps backend | grep -q "Up" && docker compose exec -T backend pnpm db:migrate 2>&1 | tee /tmp/phase29-migrate.log && docker compose exec -T postgres psql -U postgres -d objetiva_dev -tAc "SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name LIKE 'prop\_%'" | grep -qx '6'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @objetiva/backend db:migrate` exits 0.
    - El output del comando reporta los migrations 0004 y 0005 como aplicados (palabras clave: "applied", "migrated", "complete" — varía por versión drizzle-kit).
    - El output del comando NO contiene "error", "failed", "aborted" (case-insensitive).
    - El output del comando NO contiene `DROP TABLE`, `DROP INDEX`, ni `TRUNCATE` (verificable: `! grep -iE "^DROP|^TRUNCATE" /tmp/phase29-migrate.log`).
  </acceptance_criteria>
  <done>
    drizzle-kit migrate completó sin error, los 2 migrations quedaron registrados en Postgres, y no se ejecutó ninguna operación destructiva.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verificar shape de las 6 tablas + comportamiento del CHECK constraint</name>
  <files></files>
  <read_first>
    - apps/backend/.env (DATABASE_URL para conectarse a Postgres local)
    - apps/backend/drizzle/0004_phase29_propiedades.sql (referencia de qué se debería ver en la DB)
    - .planning/phases/29-catalogos-de-atributos/29-CONTEXT.md (D-03, D-05, D-06 — shape esperada)
  </read_first>
  <behavior>
    - `psql ... -c "\\dt prop_*"` lista las 6 tablas.
    - `psql ... -c "\\d prop_marca"` muestra: 6 columnas (id, nombre, abrev, activo, created_at, updated_at), PK en id, CHECK constraint sobre abrev, los 3 indexes (UNIQUE LOWER(nombre), UNIQUE abrev, index activo).
    - INSERT con `abrev='shi'` (lowercase) FALLA con SQLSTATE 23514 (check_violation).
    - INSERT con `abrev='SHI'` SUCCEEDS y retorna `id=1`.
    - INSERT con `abrev='SHI'` por segunda vez FALLA con 23505 (unique_violation) — el UNIQUE INDEX abrev funciona.
    - INSERT con `nombre='Shimano'` y luego `nombre='shimano'` (mismo abrev distinto) FALLA con 23505 — el UNIQUE LOWER(nombre) funciona.
    - Cleanup: las filas de prueba se borran al final.
  </behavior>
  <action>
1) Extraer `DATABASE_URL` del `.env` del backend:

```bash
DB_URL=$(grep -E "^DATABASE_URL=" apps/backend/.env | head -1 | cut -d= -f2-)
echo "Using DB_URL=${DB_URL:0:30}..."  # mostrar prefijo solo
```

Si no hay psql instalado en el host, el repo (proyecto dockerizado) sugiere conectarse via el contenedor de Postgres:

```bash
# Alternativa via docker
docker compose exec -T postgres psql -U <user> -d <db> -c "\\dt prop_*"
```

Usar la opción que esté disponible (preferir psql nativo si está; sino docker exec).

2) Verificar que las 6 tablas existen:

```bash
psql "$DB_URL" -c "\\dt prop_*" 2>&1 | tee /tmp/phase29-dt.log
# o
docker compose exec -T postgres psql -U postgres -d objetiva_dev -c "\\dt prop_*"
```

Esperar 6 filas en la salida (prop_color, prop_marca, prop_material, prop_objeto, prop_presentacion, prop_talle — orden alfabético).

3) Verificar shape de UNA tabla representativa (prop_marca):

```bash
psql "$DB_URL" -c "\\d prop_marca" 2>&1 | tee /tmp/phase29-d-prop_marca.log
```

Inspeccionar el output:
- 6 columnas (id, nombre, abrev, activo, created_at, updated_at)
- "Indexes:" lista 4 entradas: prop_marca_pkey (PK), prop_marca_abrev_uniq (UNIQUE), prop_marca_nombre_lower_uniq (UNIQUE, functional con lower()), prop_marca_activo_idx (btree)
- "Check constraints:" lista `prop_marca_abrev_format_chk CHECK (abrev ~ '^[A-Z0-9]{1,8}$'::text)`

4) Behavior smoke test (CHECK + UNIQUE LOWER + UNIQUE abrev):

```bash
psql "$DB_URL" <<'SQL'
-- 1) INSERT válido — debe funcionar
INSERT INTO prop_marca (nombre, abrev) VALUES ('TestSmoke', 'TS');

-- 2) INSERT con abrev lowercase — debe FALLAR con CHECK (23514)
DO $$
BEGIN
  BEGIN
    INSERT INTO prop_marca (nombre, abrev) VALUES ('TestLower', 'ts');
    RAISE EXCEPTION 'EXPECTED CHECK violation but insert succeeded';
  EXCEPTION WHEN check_violation THEN
    RAISE NOTICE 'OK: CHECK rejected lowercase abrev as expected';
  END;
END $$;

-- 3) INSERT con abrev duplicado — debe FALLAR con UNIQUE (23505)
DO $$
BEGIN
  BEGIN
    INSERT INTO prop_marca (nombre, abrev) VALUES ('OtraMarca', 'TS');
    RAISE EXCEPTION 'EXPECTED UNIQUE violation but insert succeeded';
  EXCEPTION WHEN unique_violation THEN
    RAISE NOTICE 'OK: UNIQUE abrev rejected duplicate as expected';
  END;
END $$;

-- 4) INSERT con nombre duplicado case-insensitive — debe FALLAR con UNIQUE (23505)
DO $$
BEGIN
  BEGIN
    INSERT INTO prop_marca (nombre, abrev) VALUES ('TESTSMOKE', 'TX');
    RAISE EXCEPTION 'EXPECTED UNIQUE LOWER violation but insert succeeded';
  EXCEPTION WHEN unique_violation THEN
    RAISE NOTICE 'OK: UNIQUE LOWER(nombre) rejected case-insensitive duplicate';
  END;
END $$;

-- 5) Cleanup: borrar fila de prueba
DELETE FROM prop_marca WHERE abrev = 'TS';

-- 6) Confirmar que el SELECT no devuelve filas
SELECT COUNT(*) AS remaining FROM prop_marca WHERE abrev = 'TS';
SQL
```

5) Si CUALQUIERA de los 3 expected-failure tests no fue rechazado, ALGO está mal con el migration. Reportar y volver a Plan 01.
  </action>
  <verify>
    <automated>docker compose exec -T postgres psql -U postgres -d objetiva_dev -tAc "SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name LIKE 'prop\_%'" | grep -qx '6'</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM information_schema.tables WHERE table_name LIKE 'prop\\_%'` retorna `6`.
    - `\d prop_marca` muestra los 6 columns esperados (id serial, nombre text NOT NULL, abrev text NOT NULL, activo boolean DEFAULT true NOT NULL, created_at timestamp NOT NULL DEFAULT now(), updated_at timestamp NOT NULL DEFAULT now()).
    - `\d prop_marca` muestra el CHECK constraint con regex `abrev ~ '^[A-Z0-9]{1,8}$'`.
    - `\d prop_marca` muestra los 4 indexes (PK + 2 UNIQUE + 1 normal).
    - INSERT smoke test: válido (uppercase abrev) succeeds, abrev lowercase rechazado por CHECK (23514), abrev duplicado rechazado por UNIQUE (23505), nombre case-different duplicado rechazado por UNIQUE LOWER (23505).
    - Cleanup confirmado: la fila de prueba se borró.
  </acceptance_criteria>
  <done>
    Las 6 tablas existen físicamente, los constraints (CHECK + UNIQUE LOWER + UNIQUE abrev) funcionan correctamente, y la DB está lista para que Plan 03 (backend module) escriba contra ella.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @objetiva/backend db:migrate` exits 0.
- 6 tablas listadas via `\dt prop_*`.
- 1 tabla representativa (prop_marca) tiene shape correcta.
- 3 smoke tests de constraints (CHECK + 2 UNIQUE) rechazan correctamente.
- 1 fila de prueba borrada al final (cleanup).
</verification>

<success_criteria>
- 6 tablas prop_* existen físicamente en Postgres con shape D-03 + UNIQUE LOWER + CHECK + UNIQUE abrev.
- El CHECK constraint rechaza inserts con abrev inválido (lowercase o caracteres no [A-Z0-9]).
- El UNIQUE LOWER(nombre) rechaza duplicados case-insensitive.
- El custom SQL del trigger se aplicó como no-op (sin error).
- Plan 03 (backend module) puede correr queries contra estas tablas.
</success_criteria>

<output>
After completion, create `.planning/phases/29-catalogos-de-atributos/29-02-SUMMARY.md` con:
- Output del comando `db:migrate` (resumen, no full log)
- Output del `\dt prop_*` confirmando 6 tablas
- Resumen de los smoke tests de constraint behavior (PASS/FAIL para cada uno)
- Confirmación de que ninguna operación fue destructiva (no DROP, no TRUNCATE)
</output>
