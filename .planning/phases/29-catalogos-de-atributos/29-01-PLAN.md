---
phase: 29-catalogos-de-atributos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/db/schema.ts
  - apps/backend/drizzle/0004_phase29_propiedades.sql
  - apps/backend/drizzle/0005_phase29_cache_trigger.sql
autonomous: true
requirements: [CAT-01, CAT-03, CAT-04]
tags: [drizzle, schema, postgres, propiedades, prop_marca, unique-lower, check-constraint]

must_haves:
  truths:
    - "Las 6 tablas prop_* (prop_marca, prop_color, prop_talle, prop_material, prop_presentacion, prop_objeto) están declaradas en apps/backend/src/db/schema.ts"
    - "Cada tabla tiene la shape exacta: id SERIAL PK + nombre TEXT NOT NULL + abrev TEXT NOT NULL + activo BOOLEAN DEFAULT true + created_at + updated_at"
    - "Cada tabla tiene UNIQUE INDEX functional sobre LOWER(nombre)"
    - "Cada tabla tiene UNIQUE INDEX sobre abrev"
    - "Cada tabla tiene CHECK constraint con regex ^[A-Z0-9]{1,8}$ sobre abrev"
    - "Cada tabla tiene index normal sobre activo"
    - "Cada tabla expone tipos PropMarca / NewPropMarca (y análogos) vía $inferSelect / $inferInsert"
    - "El migration generado por Drizzle contiene las 6 CREATE TABLE + CHECK + UNIQUE LOWER + UNIQUE abrev + index activo"
    - "Existe un archivo SQL custom 0005_phase29_cache_trigger.sql con el bloque del trigger comentado (Opción A del research) — listo para Phase 30/31"
  artifacts:
    - path: "apps/backend/src/db/schema.ts"
      provides: "Declaración Drizzle de 6 tablas prop_* + types exportados"
      contains: "propMarca = pgTable('prop_marca'"
    - path: "apps/backend/drizzle/0004_phase29_propiedades.sql"
      provides: "Migration auto-generado con 6 CREATE TABLE + indexes + CHECK"
      contains: "CREATE TABLE \"prop_marca\""
    - path: "apps/backend/drizzle/0005_phase29_cache_trigger.sql"
      provides: "Custom SQL con trigger comentado para Phase 30/31"
      contains: "BLOQUE COMENTADO"
  key_links:
    - from: "apps/backend/src/db/schema.ts"
      to: "drizzle-orm/pg-core"
      via: "import { check, uniqueIndex, index } from 'drizzle-orm/pg-core'"
      pattern: "check|uniqueIndex|index"
    - from: "apps/backend/drizzle/0004_phase29_propiedades.sql"
      to: "schema.ts declarations"
      via: "drizzle-kit generate"
      pattern: "lower\\(\"nombre\"\\)"
---

<objective>
Declarar las 6 tablas `prop_*` en `schema.ts` siguiendo el patrón Drizzle declarativo (UNIQUE LOWER + CHECK regex + index activo) y generar el migration auto + un custom SQL adjunto con el trigger de cache de nombre **comentado** (Opción A del research, gated para Phase 30/31).

Purpose: Establecer la base de datos para todo el ABM de propiedades. Implementa D-01 (una tabla por propiedad), D-03 (schema mínimo), D-05 (UNIQUE LOWER), D-06 (CHECK regex), D-07 (6 tablas, sin calificador), D-04 (prefijo prop_), y D-02 (trigger preparado pero no conectado).

Output: schema.ts con 6 tablas + 1 migration auto-generado + 1 migration custom con el trigger SQL comentado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/29-catalogos-de-atributos/29-CONTEXT.md
@.planning/phases/29-catalogos-de-atributos/29-RESEARCH.md
@.planning/phases/29-catalogos-de-atributos/29-PATTERNS.md

<interfaces>
<!-- El schema.ts existente ya tiene todo el patrón base. Lo único nuevo es agregar 6 tablas más + import de `check`. -->

Imports actuales relevantes en apps/backend/src/db/schema.ts (líneas 1-15) — el ejecutor DEBE agregar `check` al destructuring:

```typescript
import {
  pgTable, serial, integer, varchar, text, doublePrecision,
  timestamp, index, uniqueIndex, numeric, boolean, jsonb, primaryKey,
  // AGREGAR para Phase 29:
  check,
} from 'drizzle-orm/pg-core'
import { sql, type AnyPgColumn } from 'drizzle-orm'
```

Patrón canónico de tabla soft-delete con activo (depositos, líneas 261-273 — ejemplo a imitar):

```typescript
export const depositos = pgTable(
  'depositos',
  {
    id: serial('id').primaryKey(),
    nombre: varchar('nombre', { length: 100 }).notNull(),
    direccion: varchar('direccion', { length: 255 }),
    descripcion: text('descripcion'),
    activo: boolean('activo').notNull().default(true),
    createdAt: timestamp('created_at').notNull().defaultNow(),
    updatedAt: timestamp('updated_at').notNull().defaultNow(),
  },
  table => [index('depositos_activo_idx').on(table.activo)]
)
```

Type exports actuales (final del archivo, líneas 478-494) — el ejecutor agrega tipos para las 6 tablas con el mismo patrón:

```typescript
export type Deposito = typeof depositos.$inferSelect
export type NewDeposito = typeof depositos.$inferInsert
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Agregar 6 tablas prop_* a schema.ts con UNIQUE LOWER + CHECK + index activo</name>
  <files>apps/backend/src/db/schema.ts</files>
  <read_first>
    - apps/backend/src/db/schema.ts (mirar imports líneas 1-15, patrón depositos líneas 261-273, patrón type exports líneas 478-494)
    - .planning/phases/29-catalogos-de-atributos/29-RESEARCH.md (Pattern 1, líneas 312-389 — factory definePropTable)
    - .planning/phases/29-catalogos-de-atributos/29-PATTERNS.md (sección "apps/backend/src/db/schema.ts" — match exact)
    - .planning/phases/29-catalogos-de-atributos/29-CONTEXT.md (D-01, D-03, D-04, D-05, D-06, D-07 — schema decisions)
  </read_first>
  <behavior>
    - Las 6 tablas (prop_marca, prop_color, prop_talle, prop_material, prop_presentacion, prop_objeto) se declaran usando el factory `definePropTable(tableName, indexPrefix)` que retorna `pgTable(...)` con la shape mínima (id, nombre, abrev, activo, created_at, updated_at) y el array de constraints `[uniqueIndex on LOWER(nombre), uniqueIndex on abrev, check abrev regex, index activo]`.
    - Helper local `lower(col: AnyPgColumn)` retorna `sql\`lower(${col})\`` y se usa una sola vez para garantizar consistencia textual entre las 6 tablas (mitiga Pitfall 1 del research).
    - Variable `ABREV_REGEX_SQL = sql\`abrev ~ '^[A-Z0-9]{1,8}$'\`` se reusa en los 6 `check()` calls (un solo string, ergo un solo diff potencial al regenerar).
    - Cada tabla exporta dos types: `PropMarca = typeof propMarca.$inferSelect` y `NewPropMarca = typeof propMarca.$inferInsert` (y los 5 análogos).
    - El bloque nuevo se agrega al FINAL del archivo schema.ts (después de los exports actuales) bajo un comentario `// === Phase 29: Propiedades (catálogos de atributos) ===`. NO modificar las tablas existentes.
  </behavior>
  <action>
Implementación EXACTA a copiar del research (Pattern 1, líneas 312-367 del RESEARCH.md).

1) Modificar el bloque de imports en la cabecera (líneas ~1-15) para agregar `check` al import de `drizzle-orm/pg-core` y agregar `import { sql, type AnyPgColumn } from 'drizzle-orm'` si no existe (verificar; ya hay `import { sql, ... } from 'drizzle-orm'` en algún punto del archivo — agregar `AnyPgColumn` a ese mismo import si ya está, o crear el import nuevo si no).

2) Al FINAL del archivo schema.ts (después del último `export type ... = typeof ....$inferInsert` actual), agregar el bloque:

```typescript
// === Phase 29: Propiedades (catálogos de atributos) ===
// 6 tablas independientes (D-01) con shape mínima (D-03), prefijo prop_ (D-04),
// UNIQUE LOWER(nombre) (D-05) y CHECK abrev ~ '^[A-Z0-9]{1,8}$' (D-06).

function lower(col: AnyPgColumn) {
  return sql`lower(${col})`
}

const ABREV_REGEX_SQL = sql`abrev ~ '^[A-Z0-9]{1,8}$'`

function definePropTable(tableName: string, indexPrefix: string) {
  return pgTable(
    tableName,
    {
      id: serial('id').primaryKey(),
      nombre: text('nombre').notNull(),
      abrev: text('abrev').notNull(),
      activo: boolean('activo').notNull().default(true),
      createdAt: timestamp('created_at').notNull().defaultNow(),
      updatedAt: timestamp('updated_at').notNull().defaultNow(),
    },
    (table) => [
      uniqueIndex(`${indexPrefix}_nombre_lower_uniq`).on(lower(table.nombre)),
      uniqueIndex(`${indexPrefix}_abrev_uniq`).on(table.abrev),
      check(`${indexPrefix}_abrev_format_chk`, ABREV_REGEX_SQL),
      index(`${indexPrefix}_activo_idx`).on(table.activo),
    ]
  )
}

export const propMarca         = definePropTable('prop_marca',         'prop_marca')
export const propColor         = definePropTable('prop_color',         'prop_color')
export const propTalle         = definePropTable('prop_talle',         'prop_talle')
export const propMaterial      = definePropTable('prop_material',      'prop_material')
export const propPresentacion  = definePropTable('prop_presentacion',  'prop_presentacion')
export const propObjeto        = definePropTable('prop_objeto',        'prop_objeto')

export type PropMarca         = typeof propMarca.$inferSelect
export type NewPropMarca      = typeof propMarca.$inferInsert
export type PropColor         = typeof propColor.$inferSelect
export type NewPropColor      = typeof propColor.$inferInsert
export type PropTalle         = typeof propTalle.$inferSelect
export type NewPropTalle      = typeof propTalle.$inferInsert
export type PropMaterial      = typeof propMaterial.$inferSelect
export type NewPropMaterial   = typeof propMaterial.$inferInsert
export type PropPresentacion  = typeof propPresentacion.$inferSelect
export type NewPropPresentacion = typeof propPresentacion.$inferInsert
export type PropObjeto        = typeof propObjeto.$inferSelect
export type NewPropObjeto     = typeof propObjeto.$inferInsert
```

NO instalar paquetes nuevos. Drizzle 0.45.1 ya soporta `check()` y `uniqueIndex().on(sql\`lower(...)\`)` declarativos (verificado en research).

3) Correr type-check para confirmar que TypeScript compila:

```bash
pnpm --filter @objetiva/backend type-check
```
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter @objetiva/backend type-check && grep -c "definePropTable" apps/backend/src/db/schema.ts && grep -E "export const prop(Marca|Color|Talle|Material|Presentacion|Objeto)" apps/backend/src/db/schema.ts | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @objetiva/backend type-check` exits 0 (TS compila sin errores).
    - `grep -c "definePropTable" apps/backend/src/db/schema.ts` retorna >= 7 (1 declaración del helper + 6 invocaciones).
    - `grep -E "export const prop(Marca|Color|Talle|Material|Presentacion|Objeto)" apps/backend/src/db/schema.ts | grep -v '^#' | wc -l` retorna 6.
    - `grep -c "ABREV_REGEX_SQL" apps/backend/src/db/schema.ts` retorna 2 (1 declaración + 1 uso en factory).
    - `grep "import { sql" apps/backend/src/db/schema.ts` muestra que `AnyPgColumn` está importado de `drizzle-orm`.
    - `grep "check," apps/backend/src/db/schema.ts` confirma que `check` está en el import desde `drizzle-orm/pg-core`.
    - `grep -E "export type (PropMarca|PropColor|PropTalle|PropMaterial|PropPresentacion|PropObjeto)\\b" apps/backend/src/db/schema.ts | grep -v '^#' | wc -l` retorna >= 6.
  </acceptance_criteria>
  <done>
    schema.ts tiene 6 tablas prop_* declaradas con factory, helper lower(), constante ABREV_REGEX_SQL, types exportados, y type-check pasa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generar migration auto-emit con `pnpm db:generate`</name>
  <files>apps/backend/drizzle/0004_phase29_propiedades.sql</files>
  <read_first>
    - apps/backend/drizzle.config.ts (config del kit — verificar que apunta a src/db/schema.ts)
    - apps/backend/drizzle/0002_kind_jamie_braddock.sql (analog: cómo se ven los CREATE TABLE generados)
    - .planning/phases/29-catalogos-de-atributos/29-RESEARCH.md (Pattern 1 — verificación obligatoria post-`db:generate`)
    - apps/backend/src/db/schema.ts (recién modificado en Task 1)
  </read_first>
  <behavior>
    - `pnpm --filter @objetiva/backend db:generate` produce un archivo nuevo en `apps/backend/drizzle/` con nombre incremental (`0004_*.sql`).
    - El archivo contiene 6 `CREATE TABLE "prop_*"` con la shape correcta (id, nombre, abrev, activo, created_at, updated_at) y la cláusula `CONSTRAINT "prop_*_abrev_format_chk" CHECK (abrev ~ '^[A-Z0-9]{1,8}$')`.
    - 6 `CREATE UNIQUE INDEX "prop_*_nombre_lower_uniq" ON "prop_*" USING btree (lower("nombre"))` (functional UNIQUE).
    - 6 `CREATE UNIQUE INDEX "prop_*_abrev_uniq" ON "prop_*" USING btree ("abrev")`.
    - 6 `CREATE INDEX "prop_*_activo_idx" ON "prop_*" USING btree ("activo")`.
    - El nombre del archivo es renombrado a `0004_phase29_propiedades.sql` (humano, descriptivo) si Drizzle generó algo aleatorio (ej: `0004_perfect_blue_marvel.sql` → `0004_phase29_propiedades.sql`). Si Drizzle ya genera con el `--name` flag, mejor.
  </behavior>
  <action>
1) Desde `apps/backend/`, correr:

```bash
cd apps/backend && pnpm db:generate --name=phase29_propiedades
```

(Si el flag `--name` no está soportado en esta versión de drizzle-kit, correr `pnpm db:generate` y luego renombrar manualmente el archivo generado a `0004_phase29_propiedades.sql`.)

2) Inspeccionar el archivo generado:

```bash
ls -t apps/backend/drizzle/*.sql | head -1
cat <archivo_generado>
```

3) Verificar que el archivo cumple TODOS estos puntos (Pattern 1, "Verificación obligatoria post-`db:generate`"):
   - 6 `CREATE TABLE "prop_*"` (uno por cada propiedad)
   - cada uno con `CONSTRAINT "prop_*_abrev_format_chk" CHECK (abrev ~ '^[A-Z0-9]{1,8}$')` LITERAL — el regex no debe estar escapado raro
   - `CREATE UNIQUE INDEX "prop_*_nombre_lower_uniq" ... lower("nombre")` SIN doble escapado
   - `CREATE UNIQUE INDEX "prop_*_abrev_uniq"` y `CREATE INDEX "prop_*_activo_idx"`

4) También se debe haber actualizado `apps/backend/drizzle/meta/_journal.json` (drizzle-kit lo gestiona automáticamente). NO editar manualmente.

5) Si Drizzle generó un nombre aleatorio, renombrar:

```bash
mv apps/backend/drizzle/0004_<random>.sql apps/backend/drizzle/0004_phase29_propiedades.sql
# Editar apps/backend/drizzle/meta/_journal.json y reemplazar el "tag":"0004_<random>" por "tag":"0004_phase29_propiedades"
```

NO crear el SQL a mano: la fuente de verdad es `schema.ts`. Si la generación falla, el problema está en schema.ts (Task 1) y hay que volver atrás.

6) NO ejecutar `db:migrate` ni `db:push` en este task — eso es Plan 02 (BLOCKING).
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && [ -f apps/backend/drizzle/0004_phase29_propiedades.sql ] && grep -c "CREATE TABLE \"prop_" apps/backend/drizzle/0004_phase29_propiedades.sql && grep -c "abrev ~ '\\^\\[A-Z0-9\\]{1,8}\\$'" apps/backend/drizzle/0004_phase29_propiedades.sql && grep -c "lower(\"nombre\")" apps/backend/drizzle/0004_phase29_propiedades.sql</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/drizzle/0004_phase29_propiedades.sql` existe.
    - `grep -c "CREATE TABLE \"prop_" apps/backend/drizzle/0004_phase29_propiedades.sql` retorna 6.
    - `grep -c "abrev ~ '\\^\\[A-Z0-9\\]{1,8}\\$'" apps/backend/drizzle/0004_phase29_propiedades.sql` retorna 6 (uno por cada CHECK).
    - `grep -c "lower(\"nombre\")" apps/backend/drizzle/0004_phase29_propiedades.sql` retorna 6 (functional UNIQUE LOWER).
    - `grep -c "_abrev_uniq\" ON \"prop_" apps/backend/drizzle/0004_phase29_propiedades.sql` retorna 6.
    - `grep -c "_activo_idx\" ON \"prop_" apps/backend/drizzle/0004_phase29_propiedades.sql` retorna 6.
    - El archivo NO contiene `DROP TABLE` ni `DROP INDEX` (validación destructiva — el migration debe ser puramente aditivo). Verificar: `! grep -E "^DROP" apps/backend/drizzle/0004_phase29_propiedades.sql`.
    - `apps/backend/drizzle/meta/_journal.json` referencia el tag nuevo (verificar con `grep phase29_propiedades apps/backend/drizzle/meta/_journal.json`).
  </acceptance_criteria>
  <done>
    Migration auto-generado existe, contiene las 6 CREATE TABLE + CHECK + UNIQUE LOWER + UNIQUE abrev + index activo, y el journal refleja el cambio. Listo para Plan 02 (db:migrate / db:push).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear custom SQL para trigger de cache de nombre (comentado, gated para Phase 30/31)</name>
  <files>apps/backend/drizzle/0005_phase29_cache_trigger.sql</files>
  <read_first>
    - apps/backend/drizzle/0003_add_columna_inv_articulos.sql (analog: único custom SQL del repo, header descriptivo, idempotente)
    - .planning/phases/29-catalogos-de-atributos/29-RESEARCH.md (Pattern 3, líneas 640-685 — Opción A: bloque comentado)
    - .planning/phases/29-catalogos-de-atributos/29-CONTEXT.md (D-02 — trigger preparado pero NO conectado)
  </read_first>
  <behavior>
    - Existe un archivo SQL custom `apps/backend/drizzle/0005_phase29_cache_trigger.sql` con header descriptivo en español que explica:
      1) Phase 29 deja el SQL preparado pero NO crea el trigger (TODO el bloque queda comentado entre `/* ... */`).
      2) Phase 30/31 lo descomentará cuando `articulos.<prop>_id` exista como FK.
    - El bloque comentado contiene la función PL/pgSQL `cache_nombre_prop()` (genérica con `TG_ARGV` como en el research) y los 6 `CREATE TRIGGER` para prop_marca / prop_color / prop_talle / prop_material / prop_presentacion / prop_objeto.
    - Como el bloque está comentado, al aplicar el migration con `db:migrate` Postgres ejecuta un no-op (solo procesa los comentarios SQL). Esto satisface D-02 ("trigger preparado pero NO conectado").
    - El archivo se registra en `apps/backend/drizzle/meta/_journal.json` mediante `pnpm db:generate --custom` para que `db:migrate` lo procese en orden.
  </behavior>
  <action>
1) Generar el archivo custom usando drizzle-kit:

```bash
cd apps/backend && pnpm db:generate --custom --name=phase29_cache_trigger
```

Esto crea un archivo VACÍO `apps/backend/drizzle/0005_phase29_cache_trigger.sql` y registra el tag en `_journal.json`. Si la versión de drizzle-kit no acepta `--custom` con `--name`, generar sin nombre y renombrar después (ver Task 2 para el patrón de rename).

2) Escribir el contenido completo del archivo (`apps/backend/drizzle/0005_phase29_cache_trigger.sql`):

```sql
-- 0005_phase29_cache_trigger.sql
-- Generado vía `drizzle-kit generate --custom --name=phase29_cache_trigger`
-- Phase 29 deja preparado el SQL del trigger de cache de nombre pero NO lo crea.
-- Phase 30/31 reactivará este bloque cuando articulos.<prop>_id exista como FK.
-- Si se prefiere aplicar el SQL como no-op desde ya, descomentar TODO el bloque.
--
-- Decisión: D-02 (CONTEXT.md) — trigger preparado pero NO conectado a articulos.
-- Pitfall mitigado: P-11 (PITFALLS.md milestone) — denorm trigger silent failure.
-- Si el trigger se activa antes de que articulos.<prop>_id exista, el UPDATE
-- en articulos fallará con `column "<prop>_id" does not exist`. Por eso queda
-- comentado hasta que Phase 30/31 cree las FK fuente.
--
-- Idempotencia: este archivo es un no-op SQL (solo comentarios) — safe to re-run.

-- -----------------------------------------------------------------------------
-- BLOQUE COMENTADO — activar en Phase 30/31
-- -----------------------------------------------------------------------------
/*
CREATE OR REPLACE FUNCTION cache_nombre_prop()
RETURNS TRIGGER AS $$
BEGIN
  -- Solo actuar si el nombre cambió.
  IF NEW.nombre IS DISTINCT FROM OLD.nombre THEN
    EXECUTE format(
      'UPDATE articulos SET %I = $1 WHERE %I = $2',
      TG_ARGV[0],          -- nombre de columna cache (ej: 'marca')
      TG_ARGV[1]           -- nombre de columna FK    (ej: 'marca_id')
    ) USING NEW.nombre, NEW.id;
  END IF;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_prop_marca_cache_nombre
  AFTER UPDATE OF nombre ON prop_marca
  FOR EACH ROW EXECUTE FUNCTION cache_nombre_prop('marca', 'marca_id');

CREATE TRIGGER trg_prop_color_cache_nombre
  AFTER UPDATE OF nombre ON prop_color
  FOR EACH ROW EXECUTE FUNCTION cache_nombre_prop('color', 'color_id');

CREATE TRIGGER trg_prop_talle_cache_nombre
  AFTER UPDATE OF nombre ON prop_talle
  FOR EACH ROW EXECUTE FUNCTION cache_nombre_prop('talle', 'talle_id');

CREATE TRIGGER trg_prop_material_cache_nombre
  AFTER UPDATE OF nombre ON prop_material
  FOR EACH ROW EXECUTE FUNCTION cache_nombre_prop('material', 'material_id');

CREATE TRIGGER trg_prop_presentacion_cache_nombre
  AFTER UPDATE OF nombre ON prop_presentacion
  FOR EACH ROW EXECUTE FUNCTION cache_nombre_prop('presentacion', 'presentacion_id');

CREATE TRIGGER trg_prop_objeto_cache_nombre
  AFTER UPDATE OF nombre ON prop_objeto
  FOR EACH ROW EXECUTE FUNCTION cache_nombre_prop('objeto', 'objeto_id');
*/
```

3) Verificar que el archivo se registró en `apps/backend/drizzle/meta/_journal.json` con tag `0005_phase29_cache_trigger` y `breakpoints: true` o `breakpoints: false` según el patrón del repo.

NO escribir código SQL fuera del bloque `/* ... */`. El archivo aplicado contra Postgres debe ser literalmente un no-op.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && [ -f apps/backend/drizzle/0005_phase29_cache_trigger.sql ] && grep -q "BLOQUE COMENTADO" apps/backend/drizzle/0005_phase29_cache_trigger.sql && grep -q "/\\*" apps/backend/drizzle/0005_phase29_cache_trigger.sql && grep -q "\\*/" apps/backend/drizzle/0005_phase29_cache_trigger.sql && grep -q "phase29_cache_trigger" apps/backend/drizzle/meta/_journal.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/drizzle/0005_phase29_cache_trigger.sql` existe.
    - El archivo contiene el header descriptivo con la frase "BLOQUE COMENTADO".
    - El archivo contiene exactamente un `/*` y un `*/` (bloque de comentario único, todo el SQL adentro).
    - `grep -E "^CREATE (OR REPLACE )?(FUNCTION|TRIGGER)" apps/backend/drizzle/0005_phase29_cache_trigger.sql | wc -l` retorna 0 (todo está adentro del comentario, ningún SQL ejecutable fuera).
    - `apps/backend/drizzle/meta/_journal.json` contiene el tag `phase29_cache_trigger` (o equivalente — `grep phase29_cache_trigger apps/backend/drizzle/meta/_journal.json`).
    - El archivo es idempotente: aplicarlo dos veces consecutivas en Postgres no produce side effects (verificable manualmente; no es bloqueante para este task).
  </acceptance_criteria>
  <done>
    Custom SQL con trigger comentado existe, registrado en journal, sin SQL ejecutable. Listo para Plan 02 (db:migrate aplicará el no-op sin error).
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @objetiva/backend type-check` pasa.
- `grep -E "export const prop(Marca|Color|Talle|Material|Presentacion|Objeto)" apps/backend/src/db/schema.ts | wc -l` retorna 6.
- `apps/backend/drizzle/0004_phase29_propiedades.sql` tiene 6 CREATE TABLE + 6 CHECK + 6 UNIQUE LOWER + 6 UNIQUE abrev + 6 index activo.
- `apps/backend/drizzle/0005_phase29_cache_trigger.sql` tiene el bloque del trigger comentado y 0 SQL ejecutable fuera del comentario.
- `_journal.json` lista los dos tags nuevos.
</verification>

<success_criteria>
- 6 tablas prop_* declaradas en schema.ts con shape exacta de D-03 + UNIQUE LOWER (D-05) + CHECK regex (D-06).
- 12 types exportados (Prop* + NewProp* para cada tabla).
- Migration auto-generado contiene los DDL correctos.
- Migration custom con trigger comentado existe (D-02 satisfecho — preparado pero no conectado).
- type-check pasa sin errores.
</success_criteria>

<output>
After completion, create `.planning/phases/29-catalogos-de-atributos/29-01-SUMMARY.md` con:
- Lista de tablas/types creados
- Path al migration auto-generado
- Path al migration custom
- Confirmación de que NO se aplicó nada en DB todavía (eso es Plan 02)
</output>
