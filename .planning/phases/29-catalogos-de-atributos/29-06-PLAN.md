---
phase: 29-catalogos-de-atributos
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - apps/web/e2e/propiedades.spec.ts
  - apps/web/playwright.config.ts
  - apps/web/package.json
  - apps/web/src/components/propiedades/propiedad-create-dialog.test.tsx
autonomous: false
requirements: [CAT-01, CAT-02, CAT-03, CAT-04]
tags: [e2e, playwright, propiedades, verification, smoke-test, ui-spec-compliance, contract-test, vitest, jsdom]

must_haves:
  truths:
    - "Wave 0: Playwright instalado en apps/web (playwright.config.ts + chromium browser + script test:e2e)"
    - "Wave 0: las credenciales E2E_ADMIN_EMAIL/E2E_ADMIN_PASSWORD están disponibles (preparadas en Plan 04 Task 0)"
    - "Existe un test E2E Playwright que ejecuta el flujo completo end-to-end del ABM de propiedades"
    - "El E2E cubre: login admin → navegar a /propiedades → ver los 6 tabs → cambiar de tab → crear marca con auto-suggest abrev → ver fila en tabla → editar marca → desactivar marca → toggle 'Mostrar inactivos' → reactivar marca"
    - "El E2E prueba el caso de duplicado case-insensitive: crear 'Shimano' luego 'SHIMANO' → 409 + FormMessage inline en el campo nombre con texto 'ya existe' (cubre B-1 / CAT-03)"
    - "Existe un component test (Vitest + @testing-library/react) que monta PropiedadCreateDialog y verifica que onCreated se invoca con { id, nombre, abrev } — esta es la signature pública que Phase 32 consumirá"
    - "El E2E corre con `pnpm --filter @objetiva/web test:e2e` y pasa"
    - "El component test corre con `pnpm --filter @objetiva/web test --run propiedad-create-dialog.test` y pasa"
    - "Hay un screenshot capturado del estado final (tabla con marca creada) para review humano"
    - "Existe un checkpoint humano final que confirma: D-19 cumplido (PropiedadCreateDialog NO cableado a ArticuloForm), UI-SPEC visual compliance, copywriting via grep gates (creada/actualizada/desactivada/Vas a desactivar), no regresiones en sidebar/header"
  artifacts:
    - path: "apps/web/e2e/propiedades.spec.ts"
      provides: "Spec Playwright completa del flujo /propiedades con caso duplicado case-insensitive"
      contains: "test('"
    - path: "apps/web/playwright.config.ts"
      provides: "Config Playwright"
      contains: "defineConfig"
    - path: "apps/web/src/components/propiedades/propiedad-create-dialog.test.tsx"
      provides: "Component test de la signature pública (contrato Phase 32)"
      contains: "onCreated"
  key_links:
    - from: "apps/web/e2e/propiedades.spec.ts"
      to: "http://localhost:3000/propiedades"
      via: "page.goto + login flow"
      pattern: "/propiedades"
    - from: "apps/web/src/components/propiedades/propiedad-create-dialog.test.tsx"
      to: "apps/web/src/components/propiedades/propiedad-create-dialog.tsx"
      via: "import + render + userEvent"
      pattern: "PropiedadCreateDialog"
---

<objective>
Cerrar la Phase 29 con tres niveles de verificación: (1) un component test Vitest que ancla la signature pública de `PropiedadCreateDialog` (contrato D-19 / Phase 32), (2) un E2E Playwright que ejercita el flujo completo del ABM de propiedades incluyendo el caso duplicado case-insensitive (CAT-03), y (3) un checkpoint humano que verifica UI-SPEC compliance + copywriting via grep gates + ausencia de regresiones.

Purpose: Verificación goal-backward final — confirmar que CAT-01..04 funcionan en el navegador real (E2E), que el contrato consumido por Phase 32 está pinneado a tests automatizados (component test), y que la estética/UX cumple UI-SPEC (checkpoint humano).

Output: 3 archivos nuevos (E2E spec + Playwright config + component test) + 1 modificación a package.json + 1 checkpoint humano de validación visual.

Camino mínimo viable elegido (research §Validation Architecture): Playwright como único E2E del repo + Vitest component test como contract anchor. Backend Jest sigue diferido a v1.4 (CAT-03 se ejerce E2E aquí).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-catalogos-de-atributos/29-CONTEXT.md
@.planning/phases/29-catalogos-de-atributos/29-RESEARCH.md
@.planning/phases/29-catalogos-de-atributos/29-UI-SPEC.md
@.planning/phases/29-catalogos-de-atributos/29-VALIDATION.md
@.planning/phases/29-catalogos-de-atributos/29-05-SUMMARY.md

<interfaces>
<!-- Memoria del proyecto: usar la skill `playwright-testing` (NO el MCP) -->

Per `MEMORY.md` feedback: usar la skill `playwright-testing` para verificación visual; NO usar el MCP de Playwright para tests automatizados. La skill maneja headless + screenshots + assertions de forma consistente.

<!-- Servicios dockerizados (memoria del proyecto) -->

- Web: `http://localhost:3000` (frontend Next.js)
- Backend: `http://localhost:3001/api` (NestJS)
- Backend route prefix: `/api` (verificar `apps/backend/src/main.ts` por `app.setGlobalPrefix('api')`)

<!-- Auth: Supabase (admin user testing) -->

Las credenciales E2E ya fueron preparadas en Plan 04 Task 0 (Wave 0). El runner las consume desde `apps/web/.env.test`:
- `E2E_ADMIN_EMAIL=e2e-admin@test.local`
- `E2E_ADMIN_PASSWORD=...` (provisto por Wave 0 — leer desde `.env.test`)

Si las credenciales NO existen al ejecutar este plan, eso es una falla del Wave 0; reportar y volver atrás a Plan 04 Task 0. **NO escalar a humano** desde este plan — Plan 04 Task 0 garantiza la disponibilidad.

<!-- Selectores estables del UI-SPEC -->

- Sidebar entry: text "Propiedades" (link to /propiedades)
- Tab triggers: text "Marcas", "Colores", "Talles", "Materiales", "Presentaciones", "Objetos"
- "Nueva [singular]" button (per tab activo): role=button, text contains "Nueva"
- Dialog title create: "Nueva [Singular]" (e.g. "Nueva Marca")
- Dialog inputs: label="Nombre", label="Abreviación"
- Submit button: text "Crear marca" (etc.)
- Toast: "Marca creada correctamente"
- Row actions: button "Abrir menú" (sr-only) → DropdownMenu items "Editar", "Desactivar", "Reactivar"
- AlertDialog confirm: title contains "Desactivar", action button "Desactivar"
- Switch "Mostrar inactivos": label "Mostrar inactivos"

<!-- Component test infrastructure -->

`apps/web/vitest.config.ts` actualmente está configurado con `environment: 'node'` (Plan 04 — suggestAbrev no toca DOM). Para el component test del Plan 06 se necesita `jsdom` + React Testing Library. Task 0 instala las dependencias y agrega un segundo proyecto Vitest o cambia el environment a `jsdom` con un toggle por archivo (preferido: `// @vitest-environment jsdom` en el head del test file).

<!-- PropiedadCreateDialog signature (D-19 contract) -->

```typescript
export interface PropiedadCreateDialogProps {
  propTipo: PropTipo
  onCreated?: (created: Propiedad) => void
  trigger?: ReactNode
  open?: boolean
  onOpenChange?: (open: boolean) => void
}
```

El component test pinea `onCreated(created)` recibiendo `{ id: number, nombre: string, abrev: string, activo: boolean, createdAt, updatedAt }` (Propiedad). Phase 32 consumirá `created.id`, `created.nombre`, `created.abrev`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: [WAVE 0] Instalar Playwright + jsdom/RTL + scripts package.json</name>
  <files>
    apps/web/playwright.config.ts,
    apps/web/package.json
  </files>
  <read_first>
    - apps/web/package.json (scripts actuales)
    - apps/web/.env.test.example (preparado en Plan 04 Task 0 — formato de credenciales)
    - apps/web/vitest.config.ts (preparado en Plan 04 Task 0)
  </read_first>
  <behavior>
    - Playwright (`@playwright/test`) está en `apps/web/devDependencies`.
    - El browser chromium está descargado (`pnpm exec playwright install chromium`).
    - `apps/web/playwright.config.ts` existe con `baseURL: 'http://localhost:3000'`, browser chromium, headless por default.
    - Para el component test: `jsdom`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom` están en `apps/web/devDependencies`.
    - `apps/web/package.json` tiene `"test:e2e": "playwright test"`.
    - Las credenciales E2E del Plan 04 Task 0 son verificables: `apps/web/.env.test` existe (gitignored) o las env vars están exportadas.
  </behavior>
  <action>
1) Instalar Playwright en `apps/web`:

```bash
pnpm --filter @objetiva/web add -D @playwright/test
pnpm --filter @objetiva/web exec playwright install chromium
```

2) Instalar deps para el component test:

```bash
pnpm --filter @objetiva/web add -D jsdom @testing-library/react @testing-library/user-event @testing-library/jest-dom
```

3) Crear `apps/web/playwright.config.ts`:

```typescript
import { defineConfig, devices } from '@playwright/test'
import { config as dotenvConfig } from 'dotenv'

// Cargar .env.test si existe (credenciales E2E preparadas en Plan 04 Task 0)
dotenvConfig({ path: '.env.test' })

export default defineConfig({
  testDir: './e2e',
  timeout: 30_000,
  retries: 0,
  fullyParallel: false,
  reporter: [['list']],
  use: {
    baseURL: process.env.E2E_BASE_URL ?? 'http://localhost:3000',
    headless: true,
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
    trace: 'retain-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
})
```

(Si `dotenv` no está instalado, agregarlo: `pnpm --filter @objetiva/web add -D dotenv`. O cargar `.env.test` manualmente — pero `dotenv` es la opción simple.)

4) Modificar `apps/web/package.json`:

```jsonc
{
  "scripts": {
    // existentes (incluido "test": "vitest run" del Plan 04)
    "test:e2e": "playwright test"
  }
}
```

5) Verificar que las credenciales del Wave 0 (Plan 04 Task 0) están disponibles:

```bash
[ -f apps/web/.env.test ] && grep -q "E2E_ADMIN_EMAIL=" apps/web/.env.test || echo "ERROR: .env.test missing — Plan 04 Task 0 incompleto"
```

Si falta, ABORTAR este plan y volver a Plan 04 Task 0.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && [ -f apps/web/playwright.config.ts ] && grep -q '"test:e2e"' apps/web/package.json && grep -q "@playwright/test" apps/web/package.json && grep -q "@testing-library/react" apps/web/package.json && grep -q "jsdom" apps/web/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/playwright.config.ts` existe.
    - `apps/web/package.json` contiene `"test:e2e": "playwright test"`.
    - `@playwright/test`, `jsdom`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom` están en devDependencies.
    - El browser chromium está instalado (`pnpm exec playwright install --dry-run chromium` reporta "already installed" o equivalente).
    - `.env.test` está disponible (creado en Plan 04 Task 0) o las env vars `E2E_ADMIN_EMAIL` / `E2E_ADMIN_PASSWORD` están exportadas en el shell.
  </acceptance_criteria>
  <done>
    Wave 0 del Plan 06 lista: Playwright + jsdom/RTL instalados, config presente, scripts agregados. Credenciales del Plan 04 Task 0 verificadas.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Component test de PropiedadCreateDialog (contrato Phase 32) + E2E Playwright completo + caso duplicado case-insensitive</name>
  <files>
    apps/web/src/components/propiedades/propiedad-create-dialog.test.tsx,
    apps/web/e2e/propiedades.spec.ts
  </files>
  <read_first>
    - apps/web/src/components/propiedades/propiedad-create-dialog.tsx (creado en Plan 05 Task 1 — selectores y signature)
    - apps/web/src/components/propiedades/propiedad-table.tsx (creado en Plan 05 Task 2 — selectores)
    - apps/web/src/components/propiedades/propiedades-page.tsx (creado en Plan 05 Task 2 — tabs)
    - apps/web/src/components/propiedades/propiedad-edit-dialog.tsx (creado en Plan 05 Task 1)
    - apps/web/src/components/propiedades/propiedad-deactivate-dialog.tsx (creado en Plan 05 Task 1)
    - .planning/phases/29-catalogos-de-atributos/29-UI-SPEC.md (§"Copywriting Contract" — copy exacto de buttons / titles / toasts)
    - apps/backend/src/main.ts (confirmar prefijo `/api` y CORS abierto a localhost:3000)
  </read_first>
  <behavior>
    **Component test (`propiedad-create-dialog.test.tsx`):**
    - Usa `// @vitest-environment jsdom` en la primera línea para overridear el environment global (que es node).
    - Mockea `createPropiedad` desde `@/lib/api.client` para retornar un objeto `Propiedad` válido.
    - Monta `<PropiedadCreateDialog propTipo="marca" onCreated={spy} open={true} onOpenChange={() => {}} />`.
    - Llena el form (Nombre + Abreviación) y submit.
    - Espera y asserta que `spy` fue invocado **exactamente una vez** con un argumento que tiene `{ id: number, nombre: string, abrev: string }` (la signature que Phase 32 consume).
    - Verifica que la signature exportada `PropiedadCreateDialogProps` tiene los 5 props requeridos (test type-level via `expectTypeOf` de Vitest).

    **E2E Playwright (`propiedades.spec.ts`):**
    - Importa Playwright via `import { test, expect } from '@playwright/test'`.
    - Lee credenciales desde `process.env.E2E_ADMIN_EMAIL` / `E2E_ADMIN_PASSWORD` (cargadas por playwright.config.ts via dotenv).
    - Test único con `test.step()` cubriendo: login → navegación → 6 tabs visibles → tab default = Marcas → crear marca con auto-suggest → ver fila → **caso duplicado case-insensitive (Shimano vs SHIMANO)** → editar → desactivar (AlertDialog) → toggle inactivos → reactivar → cambio de tab → screenshot final.
    - El step de duplicado case-insensitive verifica que el segundo intento (nombre 'SHIMANO' uppercase tras crear 'Shimano') es rechazado con FormMessage inline mencionando "ya existe" (la UNIQUE LOWER(nombre) del Plan 01-02 lo garantiza a nivel DB; el handleUniqueViolation del Plan 03 lo traduce a 409; el dialog del Plan 05 lo mapea a `form.setError('nombre')` con el message del backend).
  </behavior>
  <action>
1) Crear `apps/web/src/components/propiedades/propiedad-create-dialog.test.tsx`:

```tsx
// @vitest-environment jsdom
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import '@testing-library/jest-dom/vitest'

import { PropiedadCreateDialog } from './propiedad-create-dialog'
import type { PropiedadCreateDialogProps } from './propiedad-create-dialog'
import type { Propiedad } from '@/types/propiedad'

// Mock del fetcher
vi.mock('@/lib/api.client', () => ({
  createPropiedad: vi.fn(),
}))

// Mock del hook de toast (no testing toasts aquí — solo el callback contract)
vi.mock('@/hooks/use-toast', () => ({
  useToast: () => ({ toast: vi.fn() }),
}))

import { createPropiedad as mockCreatePropiedad } from '@/lib/api.client'

describe('PropiedadCreateDialog — public contract for Phase 32 (D-19)', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('expone la signature de props requerida por Phase 32', () => {
    // Test type-level: si la signature cambia, el compilador (y este assignment) rompe.
    const props: PropiedadCreateDialogProps = {
      propTipo: 'marca',
      onCreated: (created: Propiedad) => {
        // Phase 32 lee created.id, created.nombre, created.abrev
        const _id: number = created.id
        const _nombre: string = created.nombre
        const _abrev: string = created.abrev
        void _id; void _nombre; void _abrev
      },
      trigger: undefined,
      open: false,
      onOpenChange: () => {},
    }
    expect(props.propTipo).toBe('marca')
  })

  it('invoca onCreated con { id, nombre, abrev } al submit exitoso', async () => {
    const user = userEvent.setup()
    const onCreatedSpy = vi.fn()

    const fakeCreated: Propiedad = {
      id: 42,
      nombre: 'TestBrand',
      abrev: 'TEST',
      activo: true,
      createdAt: '2026-04-30T00:00:00Z',
      updatedAt: '2026-04-30T00:00:00Z',
    }
    vi.mocked(mockCreatePropiedad).mockResolvedValueOnce(fakeCreated)

    render(
      <PropiedadCreateDialog
        propTipo="marca"
        open={true}
        onOpenChange={() => {}}
        onCreated={onCreatedSpy}
      />,
    )

    // Llenar el form
    const nombreInput = await screen.findByLabelText(/Nombre/i)
    await user.clear(nombreInput)
    await user.type(nombreInput, 'TestBrand')

    const abrevInput = await screen.findByLabelText(/Abreviación/i)
    // El auto-suggest puede haber poblado abrev — limpiar y forzar valor conocido
    await user.clear(abrevInput)
    await user.type(abrevInput, 'TEST')

    // Submit
    const submitBtn = screen.getByRole('button', { name: /Crear marca/i })
    await user.click(submitBtn)

    // Esperar al callback
    await waitFor(() => {
      expect(onCreatedSpy).toHaveBeenCalledTimes(1)
    })

    const arg = onCreatedSpy.mock.calls[0][0]
    expect(arg).toMatchObject({
      id: expect.any(Number),
      nombre: expect.any(String),
      abrev: expect.any(String),
    })
    expect(arg.id).toBe(42)
    expect(arg.nombre).toBe('TestBrand')
    expect(arg.abrev).toBe('TEST')
  })
})
```

2) Crear `apps/web/e2e/propiedades.spec.ts`:

```typescript
import { test, expect } from '@playwright/test'

const ADMIN_EMAIL = process.env.E2E_ADMIN_EMAIL
const ADMIN_PASSWORD = process.env.E2E_ADMIN_PASSWORD

// Nombres únicos para evitar colisiones en re-runs
const TS = Date.now()
const TEST_MARCA_NOMBRE = `E2EMarca_${TS}`
const TEST_MARCA_ABREV = 'E2ETST' // 6 chars [A-Z0-9]
const DUP_BRAND_LOWER = `Shimano_${TS}`        // primera fila
const DUP_BRAND_UPPER = `SHIMANO_${TS}`        // misma cosa case-insensitive — debe ser rechazada

test.beforeAll(() => {
  if (!ADMIN_EMAIL || !ADMIN_PASSWORD) {
    throw new Error(
      'E2E_ADMIN_EMAIL y E2E_ADMIN_PASSWORD deben estar definidos. ' +
      'Plan 04 Task 0 (Wave 0) prepara las credenciales — ver apps/web/.env.test.example.',
    )
  }
})

test('Phase 29 — flujo completo ABM /propiedades (CAT-01..04)', async ({ page }) => {
  // ─── Login ──────────────────────────────────────────────────────────────
  await test.step('Login admin via Supabase', async () => {
    await page.goto('/auth/login')
    await page.fill('input[name="email"], input[type="email"]', ADMIN_EMAIL!)
    await page.fill('input[name="password"], input[type="password"]', ADMIN_PASSWORD!)
    await page.click('button[type="submit"]')
    await page.waitForURL(/dashboard|\/$/, { timeout: 10_000 })
  })

  // ─── Navegar a /propiedades ─────────────────────────────────────────────
  await test.step('Sidebar contiene "Propiedades" y navega a /propiedades', async () => {
    await page.click('a:has-text("Propiedades")')
    await expect(page).toHaveURL(/\/propiedades/)
    await expect(page.getByRole('heading', { name: 'Propiedades' })).toBeVisible()
  })

  // ─── Verificar 6 tabs ───────────────────────────────────────────────────
  await test.step('Los 6 tabs están presentes', async () => {
    for (const label of ['Marcas', 'Colores', 'Talles', 'Materiales', 'Presentaciones', 'Objetos']) {
      await expect(page.getByRole('tab', { name: label })).toBeVisible()
    }
  })

  await test.step('Tab activo inicial = Marcas', async () => {
    const marcasTab = page.getByRole('tab', { name: 'Marcas' })
    await expect(marcasTab).toHaveAttribute('aria-selected', 'true')
  })

  // ─── Crear una marca con auto-suggest ───────────────────────────────────
  await test.step('Crear marca con auto-suggest abrev', async () => {
    await page.click('button:has-text("Nueva")')
    await expect(page.getByRole('dialog', { name: /Nueva Marca/i })).toBeVisible()

    const nombreInput = page.getByLabel('Nombre')
    const abrevInput = page.getByLabel('Abreviación')

    await nombreInput.fill(TEST_MARCA_NOMBRE)
    await expect(abrevInput).toHaveValue(/^[A-Z0-9]{1,8}$/)
    await abrevInput.fill(TEST_MARCA_ABREV)
    await expect(abrevInput).toHaveValue(TEST_MARCA_ABREV)

    await page.click('button:has-text("Crear marca")')
    await expect(page.getByText('Marca creada correctamente')).toBeVisible({ timeout: 5_000 })
  })

  // ─── Ver fila creada en la tabla ────────────────────────────────────────
  await test.step('La marca aparece en la tabla', async () => {
    await expect(page.getByRole('cell', { name: TEST_MARCA_NOMBRE })).toBeVisible()
    await expect(page.getByRole('cell', { name: TEST_MARCA_ABREV })).toBeVisible()
    await expect(
      page.getByRole('row', { name: new RegExp(TEST_MARCA_NOMBRE) })
        .getByText('Activo'),
    ).toBeVisible()
  })

  // ─── Caso duplicado case-insensitive: Shimano vs SHIMANO (B-1 / CAT-03) ─
  await test.step('Crear "Shimano" y luego intentar "SHIMANO" → 409 + FormMessage inline en nombre', async () => {
    // 1) Crear "Shimano_<ts>" (lowercase first letter, rest lower)
    await page.click('button:has-text("Nueva")')
    await expect(page.getByRole('dialog', { name: /Nueva Marca/i })).toBeVisible()
    await page.getByLabel('Nombre').fill(DUP_BRAND_LOWER)
    // El auto-suggest puebla abrev; sobreescribir para que el conflict sea SOLO por nombre
    await page.getByLabel('Abreviación').fill('SHIM1')
    await page.click('button:has-text("Crear marca")')
    await expect(page.getByText('Marca creada correctamente')).toBeVisible({ timeout: 5_000 })

    // 2) Intentar crear "SHIMANO_<ts>" (uppercase) — UNIQUE LOWER(nombre) debe rechazarlo
    await page.click('button:has-text("Nueva")')
    await expect(page.getByRole('dialog', { name: /Nueva Marca/i })).toBeVisible()
    await page.getByLabel('Nombre').fill(DUP_BRAND_UPPER)
    await page.getByLabel('Abreviación').fill('SHIM2') // abrev único — el conflict es SOLO por nombre
    await page.click('button:has-text("Crear marca")')

    // El FormMessage debería aparecer inline bajo el campo nombre
    // (handleUniqueViolation del backend retorna mensaje "Ya existe una marca con el nombre ...")
    const errorLocator = page.getByText(/ya existe/i)
    await expect(errorLocator.first()).toBeVisible({ timeout: 5_000 })

    // No debe haber toast de éxito (no se creó)
    await expect(page.getByText('Marca creada correctamente').last()).not.toBeVisible()

    // Cerrar el dialog
    await page.click('button:has-text("Cancelar")')
  })

  // ─── Editar marca ───────────────────────────────────────────────────────
  const NUEVO_NOMBRE = `${TEST_MARCA_NOMBRE}_edit`
  await test.step('Editar la marca creada', async () => {
    const row = page.getByRole('row', { name: new RegExp(TEST_MARCA_NOMBRE) })
    await row.getByRole('button', { name: /Abrir menú/i }).click()
    await page.click('text=Editar')
    await expect(page.getByRole('dialog', { name: /Editar Marca/i })).toBeVisible()
    await page.getByLabel('Nombre').fill(NUEVO_NOMBRE)
    await page.click('button:has-text("Guardar cambios")')
    await expect(page.getByText('Marca actualizada correctamente')).toBeVisible({ timeout: 5_000 })
    await expect(page.getByRole('cell', { name: NUEVO_NOMBRE })).toBeVisible()
  })

  // ─── Desactivar marca ───────────────────────────────────────────────────
  await test.step('Desactivar marca via AlertDialog', async () => {
    const row = page.getByRole('row', { name: new RegExp(NUEVO_NOMBRE) })
    await row.getByRole('button', { name: /Abrir menú/i }).click()
    await page.click('text=Desactivar')
    await expect(page.getByRole('alertdialog')).toBeVisible()
    await expect(page.getByText(/Vas a desactivar/)).toBeVisible()
    await expect(page.getByText(/¿Confirmás\?/)).toBeVisible()
    await page.getByRole('button', { name: 'Desactivar' }).click()
    await expect(page.getByText('Marca desactivada')).toBeVisible({ timeout: 5_000 })
    await expect(page.getByRole('cell', { name: NUEVO_NOMBRE })).not.toBeVisible()
  })

  // ─── Toggle "Mostrar inactivos" ─────────────────────────────────────────
  await test.step('Toggle Mostrar inactivos revela la marca', async () => {
    await page.getByLabel('Mostrar inactivos').click()
    await expect(page.getByRole('cell', { name: NUEVO_NOMBRE })).toBeVisible()
    await expect(
      page.getByRole('row', { name: new RegExp(NUEVO_NOMBRE) })
        .getByText('Inactivo'),
    ).toBeVisible()
  })

  // ─── Reactivar marca ────────────────────────────────────────────────────
  await test.step('Reactivar marca (sin AlertDialog — flip directo)', async () => {
    const row = page.getByRole('row', { name: new RegExp(NUEVO_NOMBRE) })
    await row.getByRole('button', { name: /Abrir menú/i }).click()
    await page.click('text=Reactivar')
    await expect(page.getByText('Marca reactivada')).toBeVisible({ timeout: 5_000 })
    await expect(
      page.getByRole('row', { name: new RegExp(NUEVO_NOMBRE) })
        .getByText('Activo'),
    ).toBeVisible()
  })

  // ─── Cambio de tab ──────────────────────────────────────────────────────
  await test.step('Cambiar a tab Colores y volver — datos persisten', async () => {
    await page.getByRole('tab', { name: 'Colores' }).click()
    await expect(page.getByRole('tab', { name: 'Colores' })).toHaveAttribute('aria-selected', 'true')
    await page.getByRole('tab', { name: 'Marcas' }).click()
    await expect(page.getByRole('cell', { name: NUEVO_NOMBRE })).toBeVisible()
  })

  // ─── Screenshot final ───────────────────────────────────────────────────
  await test.step('Captura screenshot del estado final', async () => {
    await page.screenshot({ path: 'apps/web/e2e/screenshots/phase29-final.png', fullPage: true })
  })

  // ─── Cleanup ────────────────────────────────────────────────────────────
  await test.step('Cleanup: marca rastros para inspección', async () => {
    // Phase 29 NO expone DELETE endpoint (solo soft via toggle).
    // Los nombres son únicos por timestamp, no chocan con re-runs.
    console.log(`Test rows left in DB: ${NUEVO_NOMBRE} / ${DUP_BRAND_LOWER}`)
  })
})
```

3) Pre-flight check: confirmar que web y backend están corriendo:

```bash
docker compose ps web backend 2>/dev/null
curl -sf http://localhost:3000/ > /dev/null && echo "Web up"
curl -sf http://localhost:3001/api/propiedades/marca > /dev/null 2>&1 || echo "Backend reachable (may require auth)"
```

4) Ejecutar el component test (Vitest):

```bash
pnpm --filter @objetiva/web test --run propiedad-create-dialog.test 2>&1 | tee /tmp/phase29-component-test.log
grep -qE "passed" /tmp/phase29-component-test.log
```

5) Ejecutar el E2E:

```bash
pnpm --filter @objetiva/web test:e2e 2>&1 | tee /tmp/phase29-e2e.log
```

(Las credenciales se leen de `apps/web/.env.test` automáticamente por dotenv en playwright.config.ts.)

6) Si el E2E falla, inspeccionar `playwright-report/` y `test-results/` para video / trace y ajustar selectores. Reportar fallas en el SUMMARY como bloqueantes.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && [ -f apps/web/e2e/propiedades.spec.ts ] && [ -f apps/web/playwright.config.ts ] && [ -f apps/web/src/components/propiedades/propiedad-create-dialog.test.tsx ] && [ "$(grep -c 'test.step' apps/web/e2e/propiedades.spec.ts)" -ge 10 ] && grep -q "SHIMANO" apps/web/e2e/propiedades.spec.ts && grep -q "ya existe" apps/web/e2e/propiedades.spec.ts && grep -q "onCreated" apps/web/src/components/propiedades/propiedad-create-dialog.test.tsx && grep -q "test:e2e" apps/web/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/propiedades/propiedad-create-dialog.test.tsx` existe y contiene el spy `onCreated` con assertion `expect.objectContaining({ id, nombre, abrev })` (o equivalente vía `toMatchObject`).
    - `apps/web/e2e/propiedades.spec.ts` contiene >= 10 `test.step()` cubriendo: login, navegación, ver 6 tabs, crear con auto-suggest, ver fila, **caso duplicado case-insensitive (Shimano → SHIMANO)**, editar, desactivar via AlertDialog (con copy "Vas a desactivar"), toggle inactivos, reactivar, cambio de tab.
    - El spec verifica explícitamente que el segundo intento (SHIMANO uppercase) muestra inline "ya existe" — `grep -q "ya existe" apps/web/e2e/propiedades.spec.ts`.
    - El spec usa el caso `Shimano_<ts>` lowercase y luego `SHIMANO_<ts>` uppercase — `grep -q "SHIMANO" apps/web/e2e/propiedades.spec.ts`.
    - `apps/web/package.json` contiene `"test:e2e": "playwright test"` y `@playwright/test` en devDependencies.
    - Component test: `pnpm --filter @objetiva/web test --run propiedad-create-dialog.test` exits 0 y reporta "passed".
    - E2E: `pnpm --filter @objetiva/web test:e2e` exits 0 con todos los steps PASS (asumiendo backend + web corriendo y credenciales del Plan 04 Task 0 disponibles).
    - Screenshot capturado en `apps/web/e2e/screenshots/phase29-final.png`.
  </acceptance_criteria>
  <done>
    Component test ancla el contrato Phase 32 (onCreated invocado con `{ id, nombre, abrev }`). E2E Playwright completo cubre CAT-01..04 incluyendo el caso duplicado case-insensitive (B-1). Screenshot capturado para referencia humana.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint humano final — copywriting via grep + verificación visual + D-19 + UI-SPEC compliance + no regresiones</name>
  <what-built>
    Phase 29 completa: 6 tablas prop_* en Postgres, módulo NestJS PropiedadesModule con CRUD parametrizado + RBAC, página /propiedades con 6 tabs lazy + PropiedadCreateDialog standalone reusable + Edit/Deactivate dialogs + helper suggestAbrev con tests Vitest (RED+GREEN) + entry sidebar + spec Playwright E2E (incluyendo caso duplicado case-insensitive) + component test del contrato Phase 32. Build de web pasa. Tests Vitest pasan (10 abrev + component test). E2E Playwright pasa.
  </what-built>
  <action>
N/A — este es un checkpoint:human-verify. La verificación es manual, descrita en `<how-to-verify>` abajo. Los gates automatizados de copywriting (Parte A) los corre el ejecutor antes de pasar al review humano.
  </action>
  <how-to-verify>
**Parte A — Copywriting (gates automatizados via grep, debe pasar antes de la verificación visual):**

Estos greps reemplazan el punto 5 de la versión anterior del checkpoint (W-8). Ejecutar cada uno y confirmar exit 0:

```bash
grep -q "creada correctamente" apps/web/src/components/propiedades/propiedad-create-dialog.tsx
grep -q "actualizada correctamente" apps/web/src/components/propiedades/propiedad-edit-dialog.tsx
grep -q "desactivada" apps/web/src/components/propiedades/propiedad-deactivate-dialog.tsx
grep -q "Vas a desactivar" apps/web/src/components/propiedades/propiedad-deactivate-dialog.tsx
```

Si ALGUNO falla, BLOQUEAR el checkpoint hasta que el copy se corrija.

**Parte B — Verificación visual (humano + Playwright skill):**

1. **Sidebar:** abrir `http://localhost:3000` (con docker compose corriendo) y confirmar que el sidebar muestra "Propiedades" entre "Artículos" y "Compras", con icono Tags. Color Tabler en estado inactivo.

2. **Navegación:** click en "Propiedades" → URL pasa a `/propiedades`. La página muestra:
   - Header: `<h2>Propiedades</h2>` + subtítulo "Gestión de propiedades de artículos".
   - 6 tabs: Marcas (activo por default), Colores, Talles, Materiales, Presentaciones, Objetos.
   - Tab activo: tabla con 5 columnas (ID | Nombre | Abrev | Estado | empty header) + toolbar con Switch "Mostrar inactivos" y botón "Nueva [singular]".
   - Empty state si no hay datos: "Sin marcas. Usá el botón Nueva marca para agregar la primera."

3. **Lazy tabs:** click en "Colores" → solo se monta la tabla de colores (network tab muestra 1 fetch a `/api/propiedades/color`, no a las otras 5). Click de vuelta a "Marcas" → datos persisten.

4. **Auto-suggest abrev:**
   - Click "Nueva marca". Dialog abre con title "Nueva Marca", input Nombre (placeholder "Ej: Shimano"), input Abreviación (placeholder "Ej: SHI (auto-sugerido)").
   - Tipear "Shimano" en Nombre → el campo Abrev se llena automáticamente con "SHIM".
   - Editar manualmente Abrev → seguir tipeando en Nombre NO sobrescribe Abrev (lock activo).
   - El input abrev fuerza UPPERCASE: tipear "shi" se transforma en "SHI" inmediatamente.

5. **Submit + toast** (gate copy en Parte A): Click "Crear marca" → toast "Marca creada correctamente" → fila aparece en la tabla.

6. **Error duplicado case-insensitive:** intentar crear otra marca con nombre case-different (ej: la prueba "Shimano" → "SHIMANO" del E2E) → FormMessage inline bajo el campo nombre con texto que contenga "ya existe".

7. **Editar:** click en MoreHorizontal del row → DropdownMenu abre con "Editar" + "Desactivar". Click "Editar" → Dialog "Editar Marca" con valores pre-cargados. Cambiar Nombre, click "Guardar cambios" → toast "Marca actualizada correctamente" → fila se actualiza.

8. **Desactivar (AlertDialog):** DropdownMenu → "Desactivar" → AlertDialog abre con title "Desactivar 'XXX'" + description que comienza con "Vas a desactivar" y termina en "¿Confirmás?". Click "Desactivar" (botón rojo destructivo) → toast "Marca desactivada" → la fila DESAPARECE.

9. **Toggle inactivos:** click en Switch "Mostrar inactivos" → la fila reaparece con Badge "Inactivo" y el row tiene `text-muted-foreground`.

10. **Reactivar (sin AlertDialog):** DropdownMenu de la fila inactiva → "Reactivar" → flip directo SIN AlertDialog → toast "Marca reactivada" → Badge vuelve a "Activo".

11. **D-19 verification (CAT-02 parcial):** abrir `apps/web/src/components/articulos/articulo-form.tsx` y confirmar que NO importa `PropiedadCreateDialog`. Verificar via grep:
    ```bash
    ! grep -q "PropiedadCreateDialog" apps/web/src/components/articulos/articulo-form.tsx
    ```
    Si retorna match (PropiedadCreateDialog está importado), eso es violación de D-19 y debe revertirse.

12. **PropiedadCreateDialog signature pública:** abrir `apps/web/src/components/propiedades/propiedad-create-dialog.tsx` y confirmar que `interface PropiedadCreateDialogProps` exporta los 5 props: `propTipo`, `onCreated?`, `trigger?`, `open?`, `onOpenChange?`. El component test del Task 1 ancla esto automáticamente; este punto es solo confirmación humana.

13. **No regresiones:**
    - `/articulos` carga y muestra los datos.
    - `/dashboard` carga sin errores.
    - `/settings/depositos` sigue funcional.

14. **Tests:**
    - `pnpm --filter @objetiva/web test --run` pasa (Vitest: 10 abrev + 2 component tests).
    - `pnpm --filter @objetiva/web type-check` pasa.
    - `pnpm --filter @objetiva/web build` pasa.
    - `pnpm --filter @objetiva/backend type-check` pasa.
    - `pnpm --filter @objetiva/backend build` pasa.
    - `pnpm --filter @objetiva/web test:e2e` pasa.

15. **Estética Tabler (visual):**
    - Dialog con `rounded-md` (6px) — NO `rounded-xl`.
    - Inputs con `h-9` (36px).
    - Padding tabla con `text-sm` (14px) en cells.
    - Badge "Activo" con tamaño compacto `px-1.5 py-0 text-[11px]`.
    - DropdownMenu trigger con button ghost `h-6 w-6 p-0` y MoreHorizontal `h-3.5 w-3.5`.
  </how-to-verify>
  <resume-signal>Type "approved" si los 4 grep gates de Parte A pasan + los 15 puntos visuales de Parte B pasan, o describe cuál falla.</resume-signal>
</task>

</tasks>

<verification>
- 1 component test (Vitest + jsdom) ancla la signature pública `onCreated` (contrato Phase 32).
- 1 spec Playwright cubriendo el flujo completo + caso duplicado case-insensitive (B-1).
- Checkpoint humano valida copywriting via 4 grep gates + UI-SPEC compliance visual + D-19.
- Tests Vitest siguen verdes (10 abrev + 2 component test = 12+ totales).
- Build de web pasa.
- No hay regresiones en otras rutas.
</verification>

<success_criteria>
- CAT-01 verificado: admin puede crear, ver, editar, soft-delete propiedades en las 6 tablas (E2E paso 5-10).
- CAT-02 (parcial) verificado: PropiedadCreateDialog tiene signature pública correcta — pinneado por component test (no es manual). NO está cableado al ArticuloForm (D-19 cumplido — verificable via `! grep PropiedadCreateDialog apps/web/src/components/articulos/articulo-form.tsx`).
- CAT-03 verificado: error de duplicado case-insensitive (Shimano vs SHIMANO) muestra inline "ya existe" en el campo nombre — automatizado en el E2E (B-1 fix).
- CAT-04 verificado: soft-delete via toggle preserva la fila; "Mostrar inactivos" la revela; reactivar la flippea de vuelta.
- UI-SPEC compliance: copywriting validado via greps automatizados (W-8 fix); estética Tabler validada visualmente.
- Phase 29 lista para `/gsd-verify-work 29`.
</success_criteria>

<output>
After completion, create `.planning/phases/29-catalogos-de-atributos/29-06-SUMMARY.md` con:
- Output del component test (PASS/FAIL).
- Output del E2E (PASS/FAIL por step).
- Resultado de los 4 grep gates de Parte A del checkpoint.
- Checklist de los 15 puntos visuales de Parte B.
- Path al screenshot capturado.
- Confirmación de que CAT-01..04 están cubiertos.
- Confirmación de que D-19 sigue cumplido (PropiedadCreateDialog standalone, no cableado al ArticuloForm).
- Lista de cualquier follow-up identificado.
</output>
