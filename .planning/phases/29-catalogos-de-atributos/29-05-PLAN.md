---
phase: 29-catalogos-de-atributos
plan: 05
type: execute
wave: 5
depends_on: [03, 04]
files_modified:
  - apps/web/src/components/propiedades/propiedad-create-dialog.tsx
  - apps/web/src/components/propiedades/propiedad-edit-dialog.tsx
  - apps/web/src/components/propiedades/propiedad-deactivate-dialog.tsx
  - apps/web/src/components/propiedades/propiedad-table.tsx
  - apps/web/src/components/propiedades/propiedades-page.tsx
  - apps/web/src/app/(dashboard)/propiedades/page.tsx
autonomous: true
requirements: [CAT-01, CAT-02, CAT-04]
tags: [web, ui, propiedades, tabs, dialog, alert-dialog, dropdown-menu, shadcn-tabler, rhf, zod]

must_haves:
  truths:
    - "La ruta /propiedades renderiza una página con header (icon Tags + título 'Propiedades' + subtítulo) y 6 tabs (Marcas, Colores, Talles, Materiales, Presentaciones, Objetos)"
    - "Cada tab muestra una tabla con columnas: ID | Nombre | Abrev | Estado | Acciones (5 columnas)"
    - "Solo el tab activo monta su PropiedadTable (lazy via Radix Tabs default behavior)"
    - "Cada tab tiene un toggle 'Mostrar inactivos' (Switch) que cambia el filtro activo=true ↔ activo=all"
    - "El botón 'Nueva [Singular]' del tab activo abre el PropiedadCreateDialog (RHF + zod) con auto-suggest de abrev"
    - "El componente PropiedadCreateDialog acepta props { propTipo, onCreated?, trigger?, open?, onOpenChange? } — listo para reuso en Phase 32 (D-19)"
    - "El auto-suggest se desactiva (lock) cuando el usuario edita manualmente el campo abrev"
    - "El input abrev fuerza UPPERCASE on change"
    - "Errores 409 del backend se mapean al campo correspondiente (nombre o abrev) vía form.setError"
    - "DropdownMenu del row tiene Editar / Desactivar (o Reactivar si activo=false)"
    - "AlertDialog confirma la desactivación con copy específico (UI-SPEC §Copywriting)"
    - "Reactivar NO requiere confirmación (UI-SPEC) — flip directo desde DropdownMenuItem"
    - "Build de la app web (next build) pasa sin errores"
  artifacts:
    - path: "apps/web/src/components/propiedades/propiedad-create-dialog.tsx"
      provides: "Dialog reusable export para Phase 32"
      contains: "export interface PropiedadCreateDialogProps"
    - path: "apps/web/src/components/propiedades/propiedad-edit-dialog.tsx"
      provides: "Dialog de edición"
      contains: "PropiedadEditDialog"
    - path: "apps/web/src/components/propiedades/propiedad-deactivate-dialog.tsx"
      provides: "AlertDialog de confirmación soft-delete"
      contains: "AlertDialog"
    - path: "apps/web/src/components/propiedades/propiedad-table.tsx"
      provides: "Tabla genérica parametrizada por propTipo"
      contains: "interface PropiedadTableProps"
    - path: "apps/web/src/components/propiedades/propiedades-page.tsx"
      provides: "Client Component con Tabs lazy"
      contains: "Tabs"
    - path: "apps/web/src/app/(dashboard)/propiedades/page.tsx"
      provides: "Server Component shell"
      contains: "PropiedadesPage"
  key_links:
    - from: "apps/web/src/components/propiedades/propiedad-table.tsx"
      to: "apps/web/src/lib/api.client.ts (fetchPropiedades, togglePropiedadActivo)"
      via: "import + useEffect"
      pattern: "fetchPropiedades\\("
    - from: "apps/web/src/components/propiedades/propiedad-create-dialog.tsx"
      to: "apps/web/src/lib/abrev.ts (suggestAbrev)"
      via: "import + form.watch onEffect"
      pattern: "suggestAbrev\\("
    - from: "apps/web/src/components/propiedades/propiedades-page.tsx"
      to: "@/components/ui/tabs (Radix)"
      via: "Tabs / TabsList / TabsTrigger / TabsContent"
      pattern: "TabsContent"
    - from: "apps/web/src/app/(dashboard)/propiedades/page.tsx"
      to: "apps/web/src/components/propiedades/propiedades-page.tsx"
      via: "import + render"
      pattern: "PropiedadesPage"
---

<objective>
Construir todos los componentes UI bajo `apps/web/src/components/propiedades/` y la ruta `apps/web/src/app/(dashboard)/propiedades/page.tsx` siguiendo el contrato visual del UI-SPEC (Tabs lazy, DropdownMenu actions, AlertDialog confirmación, RHF + zod, Tabler aesthetics) y la arquitectura del research (Pattern 4 Tabs, Pattern 5 Dialog reusable).

Purpose: Cumplir CAT-01 (CRUD UI completo) + CAT-02 parcial (componente PropiedadCreateDialog standalone listo para Phase 32 — D-19) + CAT-04 (soft-delete via toggle con confirmación AlertDialog).

Output: 6 archivos web nuevos. Reutilizan los fetchers, helper y types creados en Plan 04. Patrón canónico: `DepositoDialog` + `DepositosList` + `articulos-columns` (DropdownMenu).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-catalogos-de-atributos/29-CONTEXT.md
@.planning/phases/29-catalogos-de-atributos/29-RESEARCH.md
@.planning/phases/29-catalogos-de-atributos/29-PATTERNS.md
@.planning/phases/29-catalogos-de-atributos/29-UI-SPEC.md
@.planning/phases/29-catalogos-de-atributos/29-03-SUMMARY.md
@.planning/phases/29-catalogos-de-atributos/29-04-SUMMARY.md
@apps/web/src/components/depositos/deposito-dialog.tsx
@apps/web/src/components/depositos/depositos-list.tsx
@apps/web/src/components/articulos/articulos-columns.tsx
@apps/web/src/app/(dashboard)/settings/depositos/page.tsx

<interfaces>
<!-- Imports listos del Plan 04 -->

```typescript
import type { Propiedad, PropTipo } from '@/types/propiedad'
import { PROP_TIPOS, PROP_LABELS, PROP_NOMBRE_PLACEHOLDERS } from '@/types/propiedad'
import { suggestAbrev } from '@/lib/abrev'
import {
  fetchPropiedades, createPropiedad, updatePropiedad, togglePropiedadActivo,
} from '@/lib/api.client'
```

<!-- Primitives shadcn ya instaladas -->

- `@/components/ui/tabs` (Tabs, TabsList, TabsTrigger, TabsContent — lazy default)
- `@/components/ui/dialog` (Dialog, DialogContent, DialogHeader, DialogFooter, DialogTitle)
- `@/components/ui/alert-dialog` (AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle)
- `@/components/ui/form` (Form, FormControl, FormDescription, FormField, FormItem, FormLabel, FormMessage)
- `@/components/ui/input` (Input)
- `@/components/ui/button` (Button)
- `@/components/ui/badge` (Badge)
- `@/components/ui/switch` (Switch)
- `@/components/ui/dropdown-menu` (DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem)
- `@/components/ui/table` (Table, TableHeader, TableHead, TableBody, TableRow, TableCell)
- `@/components/ui/skeleton` (Skeleton)
- `@/hooks/use-toast` (useToast hook)
- `lucide-react` (Plus, Pencil, MoreHorizontal, Loader2, Tags, ToggleLeft, ToggleRight)

<!-- Patrón canónico DepositoDialog (lo que se replica) -->

`apps/web/src/components/depositos/deposito-dialog.tsx` — RHF + zod + Dialog + Form + useToast + Loader2 + isEditing rama.

<!-- Patrón DropdownMenu del row (lo que se replica) -->

`apps/web/src/components/articulos/articulos-columns.tsx:21-50` — `MoreHorizontal h-3.5 w-3.5` + DropdownMenu align="end" + DropdownMenuItem con stopPropagation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear PropiedadCreateDialog (standalone reusable Phase 32) + PropiedadEditDialog + PropiedadDeactivateDialog</name>
  <files>
    apps/web/src/components/propiedades/propiedad-create-dialog.tsx,
    apps/web/src/components/propiedades/propiedad-edit-dialog.tsx,
    apps/web/src/components/propiedades/propiedad-deactivate-dialog.tsx
  </files>
  <read_first>
    - apps/web/src/components/depositos/deposito-dialog.tsx (analog canónico — 171 líneas, RHF + zod + Dialog + Form, useEffect reset, Loader2, useToast)
    - .planning/phases/29-catalogos-de-atributos/29-RESEARCH.md (Pattern 5 — PropiedadCreateDialog completo, líneas 762-935)
    - .planning/phases/29-catalogos-de-atributos/29-PATTERNS.md (sección "PropiedadCreateDialog" + "PropiedadEditDialog" + "PropiedadDeactivateDialog")
    - .planning/phases/29-catalogos-de-atributos/29-UI-SPEC.md (§"PropiedadCreateDialog / PropiedadEditDialog — Form Contract" + §"Copywriting Contract" + §"PropiedadDeactivateDialog")
    - .planning/phases/29-catalogos-de-atributos/29-CONTEXT.md (D-17 reusable, D-19 standalone)
    - apps/web/src/types/propiedad.ts (creado en Plan 04 — PROP_LABELS, PROP_NOMBRE_PLACEHOLDERS)
    - apps/web/src/lib/abrev.ts (creado en Plan 04 — suggestAbrev)
    - apps/web/src/lib/api.client.ts (creado en Plan 04 — createPropiedad, updatePropiedad)
  </read_first>
  <behavior>
    **PropiedadCreateDialog:**
    - Es un Client Component (`'use client'`) con la signature exportada del research:
      ```typescript
      export interface PropiedadCreateDialogProps {
        propTipo: PropTipo
        onCreated?: (created: Propiedad) => void
        trigger?: ReactNode
        open?: boolean
        onOpenChange?: (open: boolean) => void
      }
      ```
    - Soporta modo controlled (parent provee `open` + `onOpenChange`) y uncontrolled (estado interno + `trigger`).
    - Form usa RHF + zod con schema `{ nombre: z.string().trim().min(1).max(255), abrev: z.string().regex(/^[A-Z0-9]{1,8}$/) }`.
    - Auto-suggest abrev: `useEffect` escucha `form.watch('nombre')`, llama `suggestAbrev(nombre)` y hace `form.setValue('abrev', ...)` SOLO si `abrevManuallyEdited === false`.
    - El input de abrev `onChange={(e) => { setAbrevManuallyEdited(true); field.onChange(e.target.value.toUpperCase()) }}` — fuerza uppercase y desactiva auto-suggest al primer edit manual.
    - Reset del form + flag `abrevManuallyEdited` cuando `open` pasa a `true`.
    - Submit: `await createPropiedad(propTipo, values)` → toast success → `onCreated?.(created)` → `onOpenChange(false)`.
    - Error 409: parsear `error.message` y `form.setError('nombre' | 'abrev', { message })` según contenga "nombre" o "abrev"/"abreviación".
    - Copy: title "Nueva [PROP_LABELS[tipo].singular]", placeholder nombre desde `PROP_NOMBRE_PLACEHOLDERS[tipo]`, abrev placeholder "Ej: SHI (auto-sugerido)", description "1-8 caracteres, solo mayúsculas y dígitos", submit button "Crear [singular]".

    **PropiedadEditDialog:**
    - Misma forma que Create pero con prop `propiedad: Propiedad` requerida.
    - Title "Editar [singular]", submit "Guardar cambios", llama `updatePropiedad(propTipo, propiedad.id, values)`.
    - **Edit dialog NO auto-sugiere abrev por diseño — el usuario edita manualmente. D-17 cumple solo en Create.** En Create el abrev se sugiere a partir del nombre (lock al primer edit manual); en Edit el abrev ya existe, el usuario lo modifica si lo desea, sin asistencia automática.
    - SIN auto-suggest (el abrev ya existe; usuario edita manualmente).
    - Reset con valores de `propiedad` cuando `open=true`.

    **PropiedadDeactivateDialog:**
    - Usa `AlertDialog` (no Dialog).
    - Title "Desactivar '[nombre]'", description del UI-SPEC: "Vas a desactivar '[nombre]'. Los artículos existentes que la usan no se modifican. ¿Confirmás?".
    - Cancel button "Cancelar" (default), Action button "Desactivar" con `bg-destructive text-destructive-foreground`.
    - Llama callback `onConfirm()` que el parent provee (la lógica del toggle vive en PropiedadTable).
    - Reactivación NO usa este dialog — es flip directo desde el DropdownMenuItem (UI-SPEC).
  </behavior>
  <action>
1) Crear `apps/web/src/components/propiedades/propiedad-create-dialog.tsx`:

```typescript
'use client'

import { useEffect, useState, type ReactNode } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import * as z from 'zod'
import { createPropiedad } from '@/lib/api.client'
import { suggestAbrev } from '@/lib/abrev'
import {
  Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle,
} from '@/components/ui/dialog'
import {
  Form, FormControl, FormDescription, FormField, FormItem, FormLabel, FormMessage,
} from '@/components/ui/form'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'
import { useToast } from '@/hooks/use-toast'
import { Loader2 } from 'lucide-react'
import { PROP_LABELS, PROP_NOMBRE_PLACEHOLDERS, type Propiedad, type PropTipo } from '@/types/propiedad'

const schema = z.object({
  nombre: z.string().trim().min(1, 'El nombre es requerido').max(255, 'Máximo 255 caracteres'),
  abrev: z.string().regex(
    /^[A-Z0-9]{1,8}$/,
    'La abreviación debe tener 1 a 8 caracteres en mayúsculas o dígitos',
  ),
})
type FormValues = z.infer<typeof schema>

export interface PropiedadCreateDialogProps {
  propTipo: PropTipo
  /** Phase 32 reuse hook — invocado tras éxito con la fila creada. */
  onCreated?: (created: Propiedad) => void
  /** Trigger custom — si no se provee, el padre controla open/onOpenChange. */
  trigger?: ReactNode
  open?: boolean
  onOpenChange?: (open: boolean) => void
}

export function PropiedadCreateDialog({
  propTipo,
  onCreated,
  trigger,
  open: controlledOpen,
  onOpenChange: controlledOnOpenChange,
}: PropiedadCreateDialogProps) {
  const { toast } = useToast()
  const [internalOpen, setInternalOpen] = useState(false)
  const open = controlledOpen ?? internalOpen
  const onOpenChange = controlledOnOpenChange ?? setInternalOpen

  const [isLoading, setIsLoading] = useState(false)
  const [abrevManuallyEdited, setAbrevManuallyEdited] = useState(false)

  const form = useForm<FormValues>({
    resolver: zodResolver(schema),
    defaultValues: { nombre: '', abrev: '' },
  })

  // Auto-suggest abrev mientras el usuario no la haya editado manualmente
  const nombre = form.watch('nombre')
  useEffect(() => {
    if (abrevManuallyEdited) return
    const suggested = suggestAbrev(nombre)
    if (suggested !== form.getValues('abrev')) {
      form.setValue('abrev', suggested, { shouldValidate: false })
    }
  }, [nombre, abrevManuallyEdited, form])

  // Reset al abrir
  useEffect(() => {
    if (open) {
      form.reset({ nombre: '', abrev: '' })
      setAbrevManuallyEdited(false)
    }
  }, [open, form])

  const label = PROP_LABELS[propTipo]

  async function onSubmit(values: FormValues) {
    setIsLoading(true)
    try {
      const created = await createPropiedad(propTipo, values)
      toast({ title: `${label.singular} creada correctamente` })
      onCreated?.(created)
      onOpenChange(false)
    } catch (err) {
      const message = err instanceof Error ? err.message : 'Error desconocido'
      const lower = message.toLowerCase()
      if (lower.includes('nombre')) {
        form.setError('nombre', { message })
      } else if (lower.includes('abreviación') || lower.includes('abrev')) {
        form.setError('abrev', { message })
      } else {
        toast({
          title: `No se pudo crear la ${label.singular.toLowerCase()}`,
          description: message,
          variant: 'destructive',
        })
      }
    } finally {
      setIsLoading(false)
    }
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      {trigger}
      <DialogContent className="sm:max-w-md">
        <DialogHeader>
          <DialogTitle>Nueva {label.singular}</DialogTitle>
        </DialogHeader>
        <Form {...form}>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
            <FormField
              control={form.control}
              name="nombre"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>Nombre</FormLabel>
                  <FormControl>
                    <Input placeholder={PROP_NOMBRE_PLACEHOLDERS[propTipo]} {...field} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            <FormField
              control={form.control}
              name="abrev"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>Abreviación</FormLabel>
                  <FormControl>
                    <Input
                      placeholder="Ej: SHI (auto-sugerido)"
                      {...field}
                      onChange={(e) => {
                        setAbrevManuallyEdited(true)
                        field.onChange(e.target.value.toUpperCase())
                      }}
                    />
                  </FormControl>
                  <FormDescription className="text-xs">
                    1-8 caracteres, solo mayúsculas y dígitos
                  </FormDescription>
                  <FormMessage />
                </FormItem>
              )}
            />
            <DialogFooter>
              <Button type="button" variant="outline" size="sm" onClick={() => onOpenChange(false)}>
                Cancelar
              </Button>
              <Button type="submit" size="sm" disabled={isLoading}>
                {isLoading && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}
                Crear {label.singular.toLowerCase()}
              </Button>
            </DialogFooter>
          </form>
        </Form>
      </DialogContent>
    </Dialog>
  )
}
```

2) Crear `apps/web/src/components/propiedades/propiedad-edit-dialog.tsx`:

```typescript
'use client'

import { useEffect, useState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import * as z from 'zod'
import { updatePropiedad } from '@/lib/api.client'
import {
  Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle,
} from '@/components/ui/dialog'
import {
  Form, FormControl, FormDescription, FormField, FormItem, FormLabel, FormMessage,
} from '@/components/ui/form'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'
import { useToast } from '@/hooks/use-toast'
import { Loader2 } from 'lucide-react'
import { PROP_LABELS, type Propiedad, type PropTipo } from '@/types/propiedad'

const schema = z.object({
  nombre: z.string().trim().min(1, 'El nombre es requerido').max(255, 'Máximo 255 caracteres'),
  abrev: z.string().regex(
    /^[A-Z0-9]{1,8}$/,
    'La abreviación debe tener 1 a 8 caracteres en mayúsculas o dígitos',
  ),
})
type FormValues = z.infer<typeof schema>

export interface PropiedadEditDialogProps {
  propTipo: PropTipo
  propiedad: Propiedad
  open: boolean
  onOpenChange: (open: boolean) => void
  onSuccess: () => void
}

export function PropiedadEditDialog({
  propTipo, propiedad, open, onOpenChange, onSuccess,
}: PropiedadEditDialogProps) {
  const { toast } = useToast()
  const [isLoading, setIsLoading] = useState(false)
  const label = PROP_LABELS[propTipo]

  const form = useForm<FormValues>({
    resolver: zodResolver(schema),
    defaultValues: { nombre: propiedad.nombre, abrev: propiedad.abrev },
  })

  useEffect(() => {
    if (open) {
      form.reset({ nombre: propiedad.nombre, abrev: propiedad.abrev })
    }
  }, [open, propiedad, form])

  async function onSubmit(values: FormValues) {
    setIsLoading(true)
    try {
      await updatePropiedad(propTipo, propiedad.id, values)
      toast({ title: `${label.singular} actualizada correctamente` })
      onOpenChange(false)
      onSuccess()
    } catch (err) {
      const message = err instanceof Error ? err.message : 'Error desconocido'
      const lower = message.toLowerCase()
      if (lower.includes('nombre')) form.setError('nombre', { message })
      else if (lower.includes('abreviación') || lower.includes('abrev')) form.setError('abrev', { message })
      else {
        toast({
          title: 'No se pudieron guardar los cambios',
          description: message,
          variant: 'destructive',
        })
      }
    } finally {
      setIsLoading(false)
    }
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-md">
        <DialogHeader>
          <DialogTitle>Editar {label.singular}</DialogTitle>
        </DialogHeader>
        <Form {...form}>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
            <FormField
              control={form.control}
              name="nombre"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>Nombre</FormLabel>
                  <FormControl><Input {...field} /></FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            <FormField
              control={form.control}
              name="abrev"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>Abreviación</FormLabel>
                  <FormControl>
                    <Input
                      {...field}
                      onChange={(e) => field.onChange(e.target.value.toUpperCase())}
                    />
                  </FormControl>
                  <FormDescription className="text-xs">
                    1-8 caracteres, solo mayúsculas y dígitos
                  </FormDescription>
                  <FormMessage />
                </FormItem>
              )}
            />
            <DialogFooter>
              <Button type="button" variant="outline" size="sm" onClick={() => onOpenChange(false)}>
                Cancelar
              </Button>
              <Button type="submit" size="sm" disabled={isLoading}>
                {isLoading && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}
                Guardar cambios
              </Button>
            </DialogFooter>
          </form>
        </Form>
      </DialogContent>
    </Dialog>
  )
}
```

3) Crear `apps/web/src/components/propiedades/propiedad-deactivate-dialog.tsx`:

```typescript
'use client'

import {
  AlertDialog, AlertDialogAction, AlertDialogCancel,
  AlertDialogContent, AlertDialogDescription, AlertDialogFooter,
  AlertDialogHeader, AlertDialogTitle,
} from '@/components/ui/alert-dialog'
import type { Propiedad } from '@/types/propiedad'

export interface PropiedadDeactivateDialogProps {
  propiedad: Propiedad
  open: boolean
  onOpenChange: (open: boolean) => void
  onConfirm: () => void
}

export function PropiedadDeactivateDialog({
  propiedad, open, onOpenChange, onConfirm,
}: PropiedadDeactivateDialogProps) {
  return (
    <AlertDialog open={open} onOpenChange={onOpenChange}>
      <AlertDialogContent>
        <AlertDialogHeader>
          <AlertDialogTitle>Desactivar &lsquo;{propiedad.nombre}&rsquo;</AlertDialogTitle>
          <AlertDialogDescription>
            Vas a desactivar &lsquo;{propiedad.nombre}&rsquo;. Los artículos existentes que la usan
            no se modifican. ¿Confirmás?
          </AlertDialogDescription>
        </AlertDialogHeader>
        <AlertDialogFooter>
          <AlertDialogCancel>Cancelar</AlertDialogCancel>
          <AlertDialogAction
            onClick={onConfirm}
            className="bg-destructive text-destructive-foreground hover:bg-destructive/90"
          >
            Desactivar
          </AlertDialogAction>
        </AlertDialogFooter>
      </AlertDialogContent>
    </AlertDialog>
  )
}
```

4) Validar TS:

```bash
pnpm --filter @objetiva/web type-check
```
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter @objetiva/web type-check && [ -f apps/web/src/components/propiedades/propiedad-create-dialog.tsx ] && [ -f apps/web/src/components/propiedades/propiedad-edit-dialog.tsx ] && [ -f apps/web/src/components/propiedades/propiedad-deactivate-dialog.tsx ] && grep -q "export interface PropiedadCreateDialogProps" apps/web/src/components/propiedades/propiedad-create-dialog.tsx && grep -q "abrevManuallyEdited" apps/web/src/components/propiedades/propiedad-create-dialog.tsx && grep -q "suggestAbrev" apps/web/src/components/propiedades/propiedad-create-dialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Los 3 archivos existen con el patrón canónico de DepositoDialog (RHF + zod + Dialog/AlertDialog).
    - PropiedadCreateDialog tiene la signature exacta del UI-SPEC: `{ propTipo, onCreated?, trigger?, open?, onOpenChange? }`. Verificar: `grep -E "(propTipo|onCreated|trigger|open|onOpenChange)" apps/web/src/components/propiedades/propiedad-create-dialog.tsx | wc -l` retorna >= 5.
    - PropiedadCreateDialog importa `suggestAbrev` y lo usa en `useEffect` con flag `abrevManuallyEdited`.
    - El input abrev tiene `field.onChange(e.target.value.toUpperCase())` (force uppercase) y `setAbrevManuallyEdited(true)`.
    - PropiedadEditDialog NO importa `suggestAbrev` (solo PropiedadCreateDialog lo usa).
    - PropiedadDeactivateDialog usa `AlertDialog` (no `Dialog`) y la copy contiene "¿Confirmás?".
    - `pnpm --filter @objetiva/web type-check` exits 0.
  </acceptance_criteria>
  <done>
    Los 3 dialogs creados, signatures correctas, suggestAbrev cableado a CreateDialog, copy en español argentino. PropiedadCreateDialog listo para reuso en Phase 32.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear PropiedadTable + PropiedadesPage (Tabs lazy)</name>
  <files>
    apps/web/src/components/propiedades/propiedad-table.tsx,
    apps/web/src/components/propiedades/propiedades-page.tsx
  </files>
  <read_first>
    - apps/web/src/components/depositos/depositos-list.tsx (analog — 381 líneas, patrón loadData callback + setState refresh)
    - apps/web/src/components/dispositivos/dispositivos-list.tsx (analog — 157 líneas, versión más simple con header bar)
    - apps/web/src/components/articulos/articulos-columns.tsx:21-50 (DropdownMenu pattern para row actions)
    - .planning/phases/29-catalogos-de-atributos/29-RESEARCH.md (Pattern 4 — Tabs lazy líneas 686-753; Code Examples — PropiedadTable líneas 1184+)
    - .planning/phases/29-catalogos-de-atributos/29-PATTERNS.md (sección "propiedad-table.tsx" — header bar, table, DropdownMenu, empty state)
    - .planning/phases/29-catalogos-de-atributos/29-UI-SPEC.md (§"PropiedadTable Column Contract" + §"Page Layout" + §"Interaction States" + §"Copywriting")
    - .planning/phases/29-catalogos-de-atributos/29-CONTEXT.md (D-15, D-16, D-18)
  </read_first>
  <behavior>
    **PropiedadTable:**
    - Client Component que recibe `propTipo: PropTipo` como única prop.
    - Estado local: `propiedades: Propiedad[]`, `loading: boolean`, `showInactivos: boolean`, `togglingId: number | null`, `editing: Propiedad | null`, `editOpen: boolean`, `deactivating: Propiedad | null`, `deactivateOpen: boolean`, `createOpen: boolean`.
    - `useEffect` carga datos al montar y cuando cambia `showInactivos` (con `fetchPropiedades(propTipo, { activo: showInactivos ? 'all' : true })`).
    - Header bar con `[Switch Mostrar inactivos]` a la derecha + `[+ Nueva {singular}]` button a la izquierda (o ambos a la derecha; alineado con UI-SPEC).
    - Tabla con 5 columnas (UI-SPEC):
      - ID (font-mono, text-muted-foreground, ~60px)
      - Nombre (font-medium)
      - Abrev (font-mono)
      - Estado (Badge default/secondary "Activo"/"Inactivo")
      - Acciones (DropdownMenu con MoreHorizontal h-3.5 w-3.5)
    - Filas inactivas: `text-muted-foreground` aplicado al row.
    - DropdownMenuItems: "Editar" (siempre), "Desactivar" (si activo) o "Reactivar" (si inactivo). Reactivar es flip directo (sin AlertDialog); Desactivar abre PropiedadDeactivateDialog.
    - Loading inicial: 5 rows de Skeleton (h-8 cada uno).
    - Empty state: "Sin {plural}. Usá el botón Nueva {singular} para agregar la primera."
    - Toggle handler usa `togglingId` flag para mostrar `Loader2` en la fila afectada.
    - Tras crear/editar/toggle exitoso, llamar `loadData()` para refrescar la tabla.

    **PropiedadesPage:**
    - Client Component (`'use client'`) que renderiza `<Tabs value={activeTab} onValueChange={setActiveTab}>` con 6 `<TabsTrigger>` y 6 `<TabsContent>`. Cada `<TabsContent>` envuelve un `<PropiedadTable propTipo={tipo} />`.
    - Radix Tabs default: solo monta el TabsContent activo (lazy de facto).
    - State inicial `activeTab='marca'`.
    - Tab labels desde `PROP_LABELS[tipo].plural`.
  </behavior>
  <action>
1) Crear `apps/web/src/components/propiedades/propiedad-table.tsx`:

```typescript
'use client'

import { useCallback, useEffect, useState } from 'react'
import {
  fetchPropiedades, togglePropiedadActivo,
} from '@/lib/api.client'
import type { Propiedad, PropTipo } from '@/types/propiedad'
import { PROP_LABELS } from '@/types/propiedad'
import {
  Table, TableBody, TableCell, TableHead, TableHeader, TableRow,
} from '@/components/ui/table'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Switch } from '@/components/ui/switch'
import { Skeleton } from '@/components/ui/skeleton'
import {
  DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem,
} from '@/components/ui/dropdown-menu'
import { useToast } from '@/hooks/use-toast'
import { Plus, MoreHorizontal, Pencil, Loader2 } from 'lucide-react'
import { PropiedadCreateDialog } from './propiedad-create-dialog'
import { PropiedadEditDialog } from './propiedad-edit-dialog'
import { PropiedadDeactivateDialog } from './propiedad-deactivate-dialog'

export interface PropiedadTableProps {
  propTipo: PropTipo
}

export function PropiedadTable({ propTipo }: PropiedadTableProps) {
  const { toast } = useToast()
  const label = PROP_LABELS[propTipo]

  const [propiedades, setPropiedades] = useState<Propiedad[]>([])
  const [loading, setLoading] = useState(true)
  const [showInactivos, setShowInactivos] = useState(false)
  const [togglingId, setTogglingId] = useState<number | null>(null)

  const [createOpen, setCreateOpen] = useState(false)
  const [editing, setEditing] = useState<Propiedad | null>(null)
  const [editOpen, setEditOpen] = useState(false)
  const [deactivating, setDeactivating] = useState<Propiedad | null>(null)
  const [deactivateOpen, setDeactivateOpen] = useState(false)

  const loadData = useCallback(async () => {
    setLoading(true)
    try {
      const data = await fetchPropiedades(propTipo, {
        activo: showInactivos ? 'all' : true,
      })
      setPropiedades(data)
    } catch (err) {
      toast({
        title: `No se pudieron cargar las ${label.plural.toLowerCase()}`,
        description: err instanceof Error ? err.message : 'Error desconocido',
        variant: 'destructive',
      })
    } finally {
      setLoading(false)
    }
  }, [propTipo, showInactivos, toast, label.plural])

  useEffect(() => {
    loadData()
  }, [loadData])

  function handleEdit(p: Propiedad) {
    setEditing(p)
    setEditOpen(true)
  }

  function handleDeactivateRequest(p: Propiedad) {
    setDeactivating(p)
    setDeactivateOpen(true)
  }

  async function handleConfirmDeactivate() {
    if (!deactivating) return
    setTogglingId(deactivating.id)
    setDeactivateOpen(false)
    try {
      await togglePropiedadActivo(propTipo, deactivating.id)
      toast({ title: `${label.singular} desactivada` })
      await loadData()
    } catch (err) {
      toast({
        title: 'Error al desactivar',
        description: err instanceof Error ? err.message : 'Error desconocido',
        variant: 'destructive',
      })
    } finally {
      setTogglingId(null)
      setDeactivating(null)
    }
  }

  async function handleReactivate(p: Propiedad) {
    setTogglingId(p.id)
    try {
      await togglePropiedadActivo(propTipo, p.id)
      toast({ title: `${label.singular} reactivada` })
      await loadData()
    } catch (err) {
      toast({
        title: 'Error al reactivar',
        description: err instanceof Error ? err.message : 'Error desconocido',
        variant: 'destructive',
      })
    } finally {
      setTogglingId(null)
    }
  }

  return (
    <div className="space-y-3">
      {/* Toolbar */}
      <div className="flex items-center justify-between gap-3 px-1">
        <div className="flex items-center gap-2">
          <Switch
            id={`show-inactive-${propTipo}`}
            checked={showInactivos}
            onCheckedChange={setShowInactivos}
          />
          <label
            htmlFor={`show-inactive-${propTipo}`}
            className="text-sm text-muted-foreground cursor-pointer"
          >
            Mostrar inactivos
          </label>
        </div>
        <Button size="sm" onClick={() => setCreateOpen(true)}>
          <Plus className="mr-2 h-4 w-4" />
          Nueva {label.singular.toLowerCase()}
        </Button>
      </div>

      {/* Table */}
      <div className="rounded-md border">
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead className="w-[60px]">ID</TableHead>
              <TableHead>Nombre</TableHead>
              <TableHead className="w-[100px]">Abrev</TableHead>
              <TableHead className="w-[80px]">Estado</TableHead>
              <TableHead className="w-[40px] text-right"></TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {loading ? (
              Array.from({ length: 5 }).map((_, i) => (
                <TableRow key={`sk-${i}`}>
                  <TableCell><Skeleton className="h-5 w-8" /></TableCell>
                  <TableCell><Skeleton className="h-5 w-40" /></TableCell>
                  <TableCell><Skeleton className="h-5 w-12" /></TableCell>
                  <TableCell><Skeleton className="h-5 w-16" /></TableCell>
                  <TableCell></TableCell>
                </TableRow>
              ))
            ) : propiedades.length === 0 ? (
              <TableRow>
                <TableCell colSpan={5} className="text-center py-8 text-sm text-muted-foreground">
                  Sin {label.plural.toLowerCase()}. Usá el botón Nueva {label.singular.toLowerCase()} para agregar la primera.
                </TableCell>
              </TableRow>
            ) : (
              propiedades.map((p) => (
                <TableRow key={p.id} className={p.activo ? '' : 'text-muted-foreground'}>
                  <TableCell className="font-mono text-sm text-muted-foreground">{p.id}</TableCell>
                  <TableCell className="font-medium text-sm">{p.nombre}</TableCell>
                  <TableCell className="font-mono text-sm">{p.abrev}</TableCell>
                  <TableCell>
                    <Badge variant={p.activo ? 'default' : 'secondary'} className="px-1.5 py-0 text-[11px]">
                      {p.activo ? 'Activo' : 'Inactivo'}
                    </Badge>
                  </TableCell>
                  <TableCell className="text-right">
                    {togglingId === p.id ? (
                      <Loader2 className="ml-auto h-3.5 w-3.5 animate-spin" />
                    ) : (
                      <DropdownMenu>
                        <DropdownMenuTrigger asChild>
                          <Button variant="ghost" className="h-6 w-6 p-0">
                            <span className="sr-only">Abrir menú</span>
                            <MoreHorizontal className="h-3.5 w-3.5" />
                          </Button>
                        </DropdownMenuTrigger>
                        <DropdownMenuContent align="end">
                          <DropdownMenuItem onClick={() => handleEdit(p)}>
                            <Pencil className="mr-2 h-4 w-4" />
                            Editar
                          </DropdownMenuItem>
                          {p.activo ? (
                            <DropdownMenuItem onClick={() => handleDeactivateRequest(p)}>
                              Desactivar
                            </DropdownMenuItem>
                          ) : (
                            <DropdownMenuItem onClick={() => handleReactivate(p)}>
                              Reactivar
                            </DropdownMenuItem>
                          )}
                        </DropdownMenuContent>
                      </DropdownMenu>
                    )}
                  </TableCell>
                </TableRow>
              ))
            )}
          </TableBody>
        </Table>
      </div>

      {/* Create Dialog (controlled) */}
      <PropiedadCreateDialog
        propTipo={propTipo}
        open={createOpen}
        onOpenChange={setCreateOpen}
        onCreated={() => loadData()}
      />

      {/* Edit Dialog */}
      {editing && (
        <PropiedadEditDialog
          propTipo={propTipo}
          propiedad={editing}
          open={editOpen}
          onOpenChange={(o) => {
            setEditOpen(o)
            if (!o) setEditing(null)
          }}
          onSuccess={loadData}
        />
      )}

      {/* Deactivate Dialog */}
      {deactivating && (
        <PropiedadDeactivateDialog
          propiedad={deactivating}
          open={deactivateOpen}
          onOpenChange={(o) => {
            setDeactivateOpen(o)
            if (!o) setDeactivating(null)
          }}
          onConfirm={handleConfirmDeactivate}
        />
      )}
    </div>
  )
}
```

2) Crear `apps/web/src/components/propiedades/propiedades-page.tsx`:

```typescript
'use client'

import { useState } from 'react'
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
import { PropiedadTable } from './propiedad-table'
import { PROP_TIPOS, PROP_LABELS, type PropTipo } from '@/types/propiedad'

export function PropiedadesPage() {
  const [activeTab, setActiveTab] = useState<PropTipo>('marca')

  return (
    <Tabs
      value={activeTab}
      onValueChange={(v) => setActiveTab(v as PropTipo)}
      className="w-full"
    >
      <TabsList className="w-full justify-start">
        {PROP_TIPOS.map((tipo) => (
          <TabsTrigger key={tipo} value={tipo}>
            {PROP_LABELS[tipo].plural}
          </TabsTrigger>
        ))}
      </TabsList>
      {PROP_TIPOS.map((tipo) => (
        <TabsContent key={tipo} value={tipo} className="mt-4">
          {/* Radix monta esto solo cuando value === activeTab — lazy automático */}
          <PropiedadTable propTipo={tipo} />
        </TabsContent>
      ))}
    </Tabs>
  )
}
```

3) Validar TS:

```bash
pnpm --filter @objetiva/web type-check
```
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter @objetiva/web type-check && [ -f apps/web/src/components/propiedades/propiedad-table.tsx ] && [ -f apps/web/src/components/propiedades/propiedades-page.tsx ] && grep -q "DropdownMenu" apps/web/src/components/propiedades/propiedad-table.tsx && grep -q "MoreHorizontal" apps/web/src/components/propiedades/propiedad-table.tsx && grep -q "Switch" apps/web/src/components/propiedades/propiedad-table.tsx && grep -q "Skeleton" apps/web/src/components/propiedades/propiedad-table.tsx && grep -c "TabsContent" apps/web/src/components/propiedades/propiedades-page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/propiedades/propiedad-table.tsx` existe.
    - `apps/web/src/components/propiedades/propiedades-page.tsx` existe.
    - PropiedadTable importa los 3 dialogs del Task 1: `grep -E "PropiedadCreateDialog|PropiedadEditDialog|PropiedadDeactivateDialog" apps/web/src/components/propiedades/propiedad-table.tsx | wc -l` retorna >= 3.
    - PropiedadTable usa DropdownMenu + MoreHorizontal + Switch + Skeleton (UI-SPEC compliance).
    - Tabla tiene 5 columnas: ID, Nombre, Abrev, Estado, (Acciones empty header). Verificar `grep -cE "<TableHead" apps/web/src/components/propiedades/propiedad-table.tsx` retorna 5.
    - Empty state copy contiene "Sin" y "Usá el botón Nueva".
    - PropiedadesPage usa `<Tabs>`, `<TabsList>`, `<TabsTrigger>`, `<TabsContent>` (Radix lazy).
    - PropiedadesPage hace map sobre `PROP_TIPOS` (6 elementos) generando 6 triggers + 6 contents.
    - `pnpm --filter @objetiva/web type-check` exits 0.
  </acceptance_criteria>
  <done>
    PropiedadTable + PropiedadesPage creados. Tabla genérica reusable para los 6 tipos. Tabs lazy via Radix. Toolbar con Switch + Nueva button.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear ruta /propiedades (Server Component shell) + build smoke</name>
  <files>apps/web/src/app/(dashboard)/propiedades/page.tsx</files>
  <read_first>
    - apps/web/src/app/(dashboard)/settings/depositos/page.tsx (analog Server Component shell — 13 líneas)
    - apps/web/src/app/(dashboard)/layout.tsx (verificar layout en (dashboard))
    - .planning/phases/29-catalogos-de-atributos/29-RESEARCH.md (Pattern 4 — page.tsx, líneas 696-715)
    - .planning/phases/29-catalogos-de-atributos/29-UI-SPEC.md (§"Page Layout Contract" — header con Tags icon)
    - .planning/phases/29-catalogos-de-atributos/29-PATTERNS.md (sección "page.tsx" — el path canónico es (dashboard) NO (admin))
  </read_first>
  <behavior>
    - El archivo está bajo `apps/web/src/app/(dashboard)/propiedades/page.tsx` (path canónico verificado vs settings/depositos/page.tsx).
    - Es un Server Component (Next 14 default — sin `'use client'`).
    - Renderiza un wrapper `<div className="space-y-4">` con:
      - Header `<div>` con `<h2 className="text-lg font-medium">Propiedades</h2>` y `<p className="text-sm text-muted-foreground">Gestión de propiedades de artículos</p>`.
      - El componente Client `<PropiedadesPage />`.
    - El icono Tags ya lo pinta el sidebar (Plan 04). El header de la página NO necesita repetirlo (UI-SPEC permite icon en header pero no es obligatorio; el sidebar lo cubre).
    - `pnpm --filter @objetiva/web build` (next build) pasa sin errores.
  </behavior>
  <action>
1) Crear `apps/web/src/app/(dashboard)/propiedades/page.tsx`:

```typescript
import { PropiedadesPage } from '@/components/propiedades/propiedades-page'

export default function PropiedadesRoute() {
  return (
    <div className="space-y-4">
      <div>
        <h2 className="text-lg font-medium">Propiedades</h2>
        <p className="text-sm text-muted-foreground">
          Gestión de propiedades de artículos
        </p>
      </div>
      <PropiedadesPage />
    </div>
  )
}
```

2) Validar TS + build:

```bash
pnpm --filter @objetiva/web type-check
pnpm --filter @objetiva/web build
```

El build de Next 14 valida que:
- Todos los Client Components tienen `'use client'`.
- Todos los imports resuelven.
- No hay tipos rotos.
- La ruta `/propiedades` se incluye en el manifest.

Si el build falla, leer el error con cuidado — generalmente es un `'use client'` faltante en algún archivo, una primitiva no instalada, o un type mismatch.

3) (Opcional, recomendable) Visitar la URL en navegador local si está corriendo el web app vía docker compose:

```bash
docker compose ps web 2>/dev/null
# Visitar http://localhost:3000/propiedades — debe cargar sin errores
# Probar:
#  - Sidebar tiene "Propiedades" entre Artículos y Compras
#  - Click en cada uno de los 6 tabs muestra la tabla del tipo correspondiente
#  - Click en "Nueva [singular]" abre el dialog
#  - Auto-suggest funciona al tipear en nombre
```

Esta validación visual es opcional para este task — el Plan 06 (E2E Playwright) la cubrirá formalmente.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter @objetiva/web type-check && pnpm --filter @objetiva/web build 2>&1 | tee /tmp/phase29-build.log && grep -E "Compiled successfully|generating static pages" /tmp/phase29-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/(dashboard)/propiedades/page.tsx` existe.
    - El archivo importa `PropiedadesPage` desde `@/components/propiedades/propiedades-page`.
    - Contiene `<h2>Propiedades</h2>` y subtitulo "Gestión de propiedades de artículos".
    - `pnpm --filter @objetiva/web type-check` exits 0.
    - `pnpm --filter @objetiva/web build` exits 0.
    - El output del build contiene "Compiled successfully" o "generating static pages".
    - El output del build NO contiene "Error" (case-insensitive, excluyendo nombres de archivos `error.tsx` legítimos).
  </acceptance_criteria>
  <done>
    Ruta /propiedades creada como Server Component shell. Build de Next 14 pasa. La aplicación web compila correctamente con todo el ABM de propiedades.
  </done>
</task>

</tasks>

<verification>
- 6 archivos web nuevos creados.
- `pnpm --filter @objetiva/web type-check` exits 0.
- `pnpm --filter @objetiva/web build` exits 0.
- PropiedadCreateDialog tiene la signature pública requerida (D-19, listo Phase 32).
- Estructura: page.tsx → PropiedadesPage (Tabs lazy) → PropiedadTable (per tipo) → 3 Dialogs (Create/Edit/Deactivate).
</verification>

<success_criteria>
- CAT-01 cubierto a nivel UI: el admin entra a /propiedades, ve 6 tabs, gestiona CRUD completo.
- CAT-02 parcial: PropiedadCreateDialog standalone exportado con `{ propTipo, onCreated, trigger, open, onOpenChange }`. Phase 32 podrá importarlo sin cambios.
- CAT-04: soft-delete via Switch toggle + AlertDialog confirmación; reactivación directa via DropdownMenuItem.
- UI-SPEC compliance: DropdownMenu actions, Skeleton loading, Badge estado, Switch inactivos, copy es-MX argentino.
- Auto-suggest abrev funciona y se desactiva al primer edit manual.
</success_criteria>

<output>
After completion, create `.planning/phases/29-catalogos-de-atributos/29-05-SUMMARY.md` con:
- Lista de los 6 archivos creados
- Output del `next build` (exitoso, exit 0)
- Sample de `PropiedadCreateDialog` props signature
- Confirmación: D-19 cumplido (componente standalone listo, NO cableado a ArticuloForm)
- Confirmación: UI-SPEC compliance (DropdownMenu + AlertDialog + Skeleton + Switch)
</output>
