---
phase: 29-catalogos-de-atributos
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - apps/backend/src/modules/propiedades/propiedades.constants.ts
  - apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts
  - apps/backend/src/modules/propiedades/dto/update-propiedad.dto.ts
  - apps/backend/src/modules/propiedades/propiedades.service.ts
  - apps/backend/src/modules/propiedades/propiedades.controller.ts
  - apps/backend/src/modules/propiedades/propiedades.module.ts
  - apps/backend/src/app.module.ts
autonomous: true
requirements: [CAT-01, CAT-03, CAT-04]
tags: [nestjs, propiedades, crud, rbac, conflict-exception, soft-delete, parametrized-controller]

must_haves:
  truths:
    - "Existe el módulo PropiedadesModule registrado en AppModule"
    - "Hay un controller con 5 endpoints: GET /:tipo (list), GET /:tipo/:id (one), POST /:tipo (create), PATCH /:tipo/:id (update), PATCH /:tipo/:id/toggle (soft-delete/reactivate)"
    - "Endpoints write (POST/PATCH) están protegidos con @Roles('admin') vía RolesGuard — viewer recibe 403"
    - "Endpoints read (GET) son accesibles tanto a admin como a viewer (auth global pero sin @Roles)"
    - "El service detecta error.code === '23505' y traduce a ConflictException con mensaje específico per-constraint (UNIQUE LOWER nombre vs UNIQUE abrev)"
    - "El controller valida el :tipo URL param contra PROP_TIPOS y retorna NotFoundException si es inválido"
    - "GET /:tipo acepta query param activo=true|false|all (default true)"
    - "El DTO Create exige nombre (1..255) y abrev (regex ^[A-Z0-9]{1,8}$)"
    - "El DTO Update permite ambos campos opcionales (sin @nestjs/mapped-types — patrón actual del repo)"
    - "El backend levanta sin errores (build + start)"
  artifacts:
    - path: "apps/backend/src/modules/propiedades/propiedades.module.ts"
      provides: "DI registration"
      exports: ["PropiedadesService"]
    - path: "apps/backend/src/modules/propiedades/propiedades.controller.ts"
      provides: "5 endpoints REST"
      contains: "@Controller('propiedades')"
    - path: "apps/backend/src/modules/propiedades/propiedades.service.ts"
      provides: "CRUD genérico parametrizado por tipo"
      contains: "tableFor(tipo: PropTipo)"
    - path: "apps/backend/src/modules/propiedades/propiedades.constants.ts"
      provides: "PROP_TIPOS, PROP_TABLES, PROP_LABELS"
      contains: "export const PROP_TIPOS"
    - path: "apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts"
      provides: "CreatePropiedadDto con @Matches regex"
      contains: "@Matches(/^[A-Z0-9]{1,8}$/"
    - path: "apps/backend/src/modules/propiedades/dto/update-propiedad.dto.ts"
      provides: "UpdatePropiedadDto con campos opcionales"
      contains: "@IsOptional()"
    - path: "apps/backend/src/app.module.ts"
      provides: "Registro del PropiedadesModule"
      contains: "PropiedadesModule"
  key_links:
    - from: "apps/backend/src/modules/propiedades/propiedades.service.ts"
      to: "apps/backend/src/db/schema.ts (propMarca, propColor, ...)"
      via: "import + tableFor(tipo)"
      pattern: "PROP_TABLES\\["
    - from: "apps/backend/src/modules/propiedades/propiedades.controller.ts"
      to: "RolesGuard + @Roles('admin')"
      via: "@UseGuards(RolesGuard) + decorator"
      pattern: "@Roles\\('admin'\\)"
    - from: "apps/backend/src/app.module.ts"
      to: "PropiedadesModule"
      via: "import + array imports"
      pattern: "PropiedadesModule"
---

<objective>
Crear el módulo NestJS `Propiedades` parametrizado por `:tipo` que expone CRUD completo para las 6 tablas prop_* con RBAC (admin escribe, viewer lee), manejo de UNIQUE violations 23505 con mensajes per-constraint, soft-delete via toggle y query param `activo` para listados.

Purpose: Exponer las 6 tablas como API REST consumible por el frontend (Plan 04/05) y por Phase 32 (cuando se cabla al ArticuloForm). Implementa CAT-01 (CRUD), CAT-03 (UNIQUE legible), CAT-04 (soft-delete).

Output: 1 módulo + 1 controller + 1 service + 2 DTOs + 1 constants file + registro en AppModule. Patrón canónico replicado de `DispositivosModule`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-catalogos-de-atributos/29-CONTEXT.md
@.planning/phases/29-catalogos-de-atributos/29-RESEARCH.md
@.planning/phases/29-catalogos-de-atributos/29-PATTERNS.md
@.planning/phases/29-catalogos-de-atributos/29-01-SUMMARY.md
@.planning/phases/29-catalogos-de-atributos/29-02-SUMMARY.md
@apps/backend/src/modules/dispositivos/dispositivos.module.ts
@apps/backend/src/modules/dispositivos/dispositivos.controller.ts
@apps/backend/src/modules/dispositivos/dispositivos.service.ts
@apps/backend/src/modules/dispositivos/dto/create-dispositivo.dto.ts
@apps/backend/src/modules/dispositivos/dto/update-dispositivo.dto.ts
@apps/backend/src/app.module.ts

<interfaces>
<!-- Imports relevantes desde schema.ts (creados en Plan 01) -->

```typescript
import {
  propMarca, propColor, propTalle, propMaterial,
  propPresentacion, propObjeto,
} from '../../db/schema'
```

<!-- Auth/Guards globales (ya configurados en main.ts y app.module.ts) -->

```typescript
import { RolesGuard } from '../../common/guards/roles.guard'
import { Roles } from '../../common/decorators/roles.decorator'
// El JwtAuthGuard global cubre todo; RolesGuard se aplica per-endpoint con @UseGuards
```

<!-- ValidationPipe global (main.ts líneas 22-28) — confirma que DTOs se validan automáticamente -->

```typescript
app.useGlobalPipes(new ValidationPipe({
  transform: true, whitelist: true, forbidNonWhitelisted: true,
}))
```

<!-- DrizzleService (apps/backend/src/db/index.ts) — patrón usado por todos los services del repo -->

```typescript
import { DrizzleService } from '../../db/index'
// constructor(private readonly drizzle: DrizzleService) {}
// uso: this.drizzle.db.select().from(table)
```

<!-- AppRole type para @Roles -->

```typescript
import { AppRole } from '@objetiva/types'  // 'admin' | 'viewer'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear constants + DTOs (PROP_TIPOS, PROP_TABLES, PROP_LABELS, Create/Update DTOs)</name>
  <files>
    apps/backend/src/modules/propiedades/propiedades.constants.ts,
    apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts,
    apps/backend/src/modules/propiedades/dto/update-propiedad.dto.ts
  </files>
  <read_first>
    - apps/backend/src/modules/dispositivos/dto/create-dispositivo.dto.ts (analog DTO Create — 17 líneas)
    - apps/backend/src/modules/dispositivos/dto/update-dispositivo.dto.ts (analog DTO Update — 17 líneas; patrón canónico SIN @nestjs/mapped-types)
    - apps/backend/src/db/schema.ts (verificar exports propMarca..propObjeto creados en Plan 01)
    - .planning/phases/29-catalogos-de-atributos/29-RESEARCH.md (Pattern 2, líneas 400-426 — constants; líneas 611-636 — DTOs)
    - .planning/phases/29-catalogos-de-atributos/29-PATTERNS.md (sección "DTOs" — el patrón canónico del repo NO usa @nestjs/mapped-types)
  </read_first>
  <behavior>
    - `PROP_TIPOS` es un readonly tuple de los 6 tipos: `['marca', 'color', 'talle', 'material', 'presentacion', 'objeto'] as const`.
    - `PropTipo` es el tipo derivado: `typeof PROP_TIPOS[number]`.
    - `PROP_TABLES` es un mapa `PropTipo -> Drizzle table` con las 6 tablas importadas de schema.
    - `PROP_LABELS` mapea cada tipo a `{ singular, plural }` en español: marca→{marca, marcas}, color→{color, colores}, talle→{talle, talles}, material→{material, materiales}, presentacion→{presentación, presentaciones}, objeto→{objeto, objetos}.
    - `CreatePropiedadDto` exige `nombre: string` (1..255 chars, trim auto) y `abrev: string` con `@Matches(/^[A-Z0-9]{1,8}$/)`.
    - `UpdatePropiedadDto` tiene los mismos 2 campos pero `@IsOptional()` cada uno (NO usar `@nestjs/mapped-types` — el repo no lo tiene instalado, replicar el patrón actual).
  </behavior>
  <action>
1) Crear `apps/backend/src/modules/propiedades/propiedades.constants.ts`:

```typescript
import {
  propMarca, propColor, propTalle, propMaterial,
  propPresentacion, propObjeto,
} from '../../db/schema'

export const PROP_TIPOS = [
  'marca',
  'color',
  'talle',
  'material',
  'presentacion',
  'objeto',
] as const

export type PropTipo = typeof PROP_TIPOS[number]

export const PROP_TABLES = {
  marca: propMarca,
  color: propColor,
  talle: propTalle,
  material: propMaterial,
  presentacion: propPresentacion,
  objeto: propObjeto,
} as const

// Etiquetas en español (es-MX) para mensajes de error y respuestas
export const PROP_LABELS: Record<PropTipo, { singular: string; plural: string }> = {
  marca:         { singular: 'marca',         plural: 'marcas' },
  color:         { singular: 'color',         plural: 'colores' },
  talle:         { singular: 'talle',         plural: 'talles' },
  material:      { singular: 'material',      plural: 'materiales' },
  presentacion:  { singular: 'presentación',  plural: 'presentaciones' },
  objeto:        { singular: 'objeto',        plural: 'objetos' },
}
```

2) Crear `apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts`:

```typescript
import { IsString, IsNotEmpty, MaxLength, Matches } from 'class-validator'
import { Transform } from 'class-transformer'

export class CreatePropiedadDto {
  @IsString()
  @IsNotEmpty()
  @MaxLength(255)
  @Transform(({ value }) =>
    typeof value === 'string' ? value.trim() : value,
  )
  nombre!: string

  @IsString()
  @Matches(/^[A-Z0-9]{1,8}$/, {
    message: 'La abreviación debe tener 1 a 8 caracteres en mayúsculas o dígitos',
  })
  abrev!: string
}
```

3) Crear `apps/backend/src/modules/propiedades/dto/update-propiedad.dto.ts` (NO usar `@nestjs/mapped-types` — patrón actual del repo, ver UpdateDispositivoDto):

```typescript
import { IsString, IsOptional, MaxLength, Matches } from 'class-validator'
import { Transform } from 'class-transformer'

export class UpdatePropiedadDto {
  @IsOptional()
  @IsString()
  @MaxLength(255)
  @Transform(({ value }) =>
    typeof value === 'string' ? value.trim() : value,
  )
  nombre?: string

  @IsOptional()
  @IsString()
  @Matches(/^[A-Z0-9]{1,8}$/, {
    message: 'La abreviación debe tener 1 a 8 caracteres en mayúsculas o dígitos',
  })
  abrev?: string
}
```

4) Confirmar que TS compila:

```bash
pnpm --filter @objetiva/backend type-check
```
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter @objetiva/backend type-check && [ -f apps/backend/src/modules/propiedades/propiedades.constants.ts ] && [ -f apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts ] && [ -f apps/backend/src/modules/propiedades/dto/update-propiedad.dto.ts ] && grep -q "export const PROP_TIPOS" apps/backend/src/modules/propiedades/propiedades.constants.ts && grep -q "@Matches(/\\^\\[A-Z0-9\\]{1,8}\\$/" apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/modules/propiedades/propiedades.constants.ts` existe.
    - `grep "export const PROP_TIPOS = " apps/backend/src/modules/propiedades/propiedades.constants.ts` muestra el tuple readonly con 6 elementos.
    - `grep "presentación" apps/backend/src/modules/propiedades/propiedades.constants.ts` muestra que el plural usa tilde correcta (es-MX).
    - `grep "PROP_TABLES" apps/backend/src/modules/propiedades/propiedades.constants.ts` retorna el mapa con 6 entradas.
    - `apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts` existe con `@Matches(/^[A-Z0-9]{1,8}$/`.
    - `apps/backend/src/modules/propiedades/dto/update-propiedad.dto.ts` existe con `@IsOptional()` en ambos campos.
    - `! grep "@nestjs/mapped-types" apps/backend/src/modules/propiedades/dto/update-propiedad.dto.ts` (NO se debe importar este paquete).
    - `pnpm --filter @objetiva/backend type-check` exits 0.
  </acceptance_criteria>
  <done>
    constants + 2 DTOs creados, todos compilan, regex y trim configurados correctamente.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear PropiedadesService (genérico parametrizado por tipo, con handleUniqueViolation)</name>
  <files>apps/backend/src/modules/propiedades/propiedades.service.ts</files>
  <read_first>
    - apps/backend/src/modules/dispositivos/dispositivos.service.ts (analog canónico — 100 líneas, patrón UNIQUE 23505)
    - apps/backend/src/db/index.ts (DrizzleService — confirmar shape del export)
    - .planning/phases/29-catalogos-de-atributos/29-RESEARCH.md (Pattern 2 — service completo, líneas 429-535)
    - .planning/phases/29-catalogos-de-atributos/29-CONTEXT.md (D-18 — soft-delete, listado default activo=true)
    - apps/backend/src/modules/propiedades/propiedades.constants.ts (creado en Task 1)
  </read_first>
  <behavior>
    - El service tiene 5 métodos públicos: `findAll(tipo, opts)`, `findOne(tipo, id)`, `create(tipo, dto)`, `update(tipo, id, dto)`, `toggleActive(tipo, id)`.
    - Helper privado `tableFor(tipo: PropTipo)` resuelve la tabla del map y lanza `BadRequestException` si el tipo es inválido (defensivo — el controller también valida).
    - `findAll` acepta `opts: { activo?: boolean }`: si `activo` es boolean, filtra `WHERE activo = $value`; si es `undefined`, retorna TODOS (sin filtro).
    - `create` y `update` envuelven el insert/update en try/catch y delegan a `handleUniqueViolation(error, tipo, dto)` que lanza `ConflictException` con mensaje en español específico per-constraint (parsing `error.constraint_name`).
    - `update` con `id` no encontrado lanza `NotFoundException` con mensaje en español usando PROP_LABELS[tipo].singular.
    - `toggleActive` invierte el flag `activo` y actualiza `updatedAt` a `new Date()` (patrón del repo).
  </behavior>
  <action>
Crear `apps/backend/src/modules/propiedades/propiedades.service.ts` con el código completo del Pattern 2 del research (RESEARCH.md líneas 429-535). Copiar el código verbatim adaptando solamente los imports si la ruta de DrizzleService difiere.

```typescript
import { Injectable, ConflictException, NotFoundException, BadRequestException } from '@nestjs/common'
import { eq, asc } from 'drizzle-orm'
import { DrizzleService } from '../../db/index'
import { PROP_TABLES, PROP_LABELS, type PropTipo } from './propiedades.constants'
import { CreatePropiedadDto } from './dto/create-propiedad.dto'
import { UpdatePropiedadDto } from './dto/update-propiedad.dto'

@Injectable()
export class PropiedadesService {
  constructor(private readonly drizzle: DrizzleService) {}

  private tableFor(tipo: PropTipo) {
    const table = PROP_TABLES[tipo]
    if (!table) {
      throw new BadRequestException(`Tipo de propiedad inválido: ${tipo}`)
    }
    return table
  }

  async findAll(tipo: PropTipo, opts: { activo?: boolean } = {}) {
    const table = this.tableFor(tipo)
    const query = this.drizzle.db.select().from(table)
    if (opts.activo !== undefined) {
      return query.where(eq(table.activo, opts.activo)).orderBy(asc(table.nombre))
    }
    return query.orderBy(asc(table.nombre))
  }

  async findOne(tipo: PropTipo, id: number) {
    const table = this.tableFor(tipo)
    const rows = await this.drizzle.db.select().from(table).where(eq(table.id, id))
    return rows[0] ?? null
  }

  async create(tipo: PropTipo, dto: CreatePropiedadDto) {
    const table = this.tableFor(tipo)
    try {
      const rows = await this.drizzle.db
        .insert(table)
        .values({ nombre: dto.nombre, abrev: dto.abrev })
        .returning()
      return rows[0]
    } catch (error: unknown) {
      this.handleUniqueViolation(error, tipo, dto)
      throw error
    }
  }

  async update(tipo: PropTipo, id: number, dto: UpdatePropiedadDto) {
    const table = this.tableFor(tipo)
    try {
      const updateData: Record<string, unknown> = { updatedAt: new Date() }
      if (dto.nombre !== undefined) updateData.nombre = dto.nombre
      if (dto.abrev !== undefined) updateData.abrev = dto.abrev

      const rows = await this.drizzle.db
        .update(table)
        .set(updateData)
        .where(eq(table.id, id))
        .returning()

      if (!rows[0]) {
        const label = PROP_LABELS[tipo].singular
        throw new NotFoundException(`${capitalize(label)} con ID ${id} no encontrada`)
      }
      return rows[0]
    } catch (error: unknown) {
      if (error instanceof NotFoundException) throw error
      this.handleUniqueViolation(error, tipo, dto)
      throw error
    }
  }

  async toggleActive(tipo: PropTipo, id: number) {
    const table = this.tableFor(tipo)
    const existing = await this.findOne(tipo, id)
    if (!existing) {
      const label = PROP_LABELS[tipo].singular
      throw new NotFoundException(`${capitalize(label)} con ID ${id} no encontrada`)
    }
    const rows = await this.drizzle.db
      .update(table)
      .set({ activo: !existing.activo, updatedAt: new Date() })
      .where(eq(table.id, id))
      .returning()
    return rows[0]
  }

  // ─── Helpers ───────────────────────────────────────────────────────────────

  private handleUniqueViolation(
    error: unknown,
    tipo: PropTipo,
    dto: { nombre?: string; abrev?: string },
  ) {
    if (
      !(error instanceof Error) ||
      !('code' in error) ||
      (error as Record<string, unknown>).code !== '23505'
    ) {
      return
    }

    const detail = String((error as Record<string, unknown>).detail ?? '')
    const constraint = String(
      (error as Record<string, unknown>).constraint_name ??
        (error as Record<string, unknown>).constraint ??
        '',
    )
    const label = PROP_LABELS[tipo]

    // UNIQUE LOWER(nombre)
    if (constraint.includes('nombre_lower_uniq') || detail.toLowerCase().includes('lower')) {
      throw new ConflictException(
        `Ya existe una ${label.singular} con el nombre "${dto.nombre}"`,
      )
    }
    // UNIQUE(abrev)
    if (constraint.includes('abrev_uniq') || detail.includes('abrev')) {
      throw new ConflictException(
        `La abreviación "${dto.abrev}" ya existe en ${label.plural}`,
      )
    }
    // Fallback genérico
    throw new ConflictException(`Conflicto de unicidad en ${label.plural}`)
  }
}

function capitalize(s: string) {
  return s.charAt(0).toUpperCase() + s.slice(1)
}
```

Confirmar que TS compila:

```bash
pnpm --filter @objetiva/backend type-check
```

> **Nota Pitfall 1 del research:** `error.constraint_name` puede no estar siempre presente en `postgres.js v3.4`. El helper `handleUniqueViolation` chequea ambos `constraint_name` y `constraint` como fallback, y como último recurso parsea `detail`. Esto cubre el shape variado del error según versión de postgres.js.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter @objetiva/backend type-check && [ -f apps/backend/src/modules/propiedades/propiedades.service.ts ] && grep -c "private tableFor" apps/backend/src/modules/propiedades/propiedades.service.ts && grep -c "handleUniqueViolation" apps/backend/src/modules/propiedades/propiedades.service.ts && grep -c "code === '23505'" apps/backend/src/modules/propiedades/propiedades.service.ts && grep -E "async (findAll|findOne|create|update|toggleActive)" apps/backend/src/modules/propiedades/propiedades.service.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/modules/propiedades/propiedades.service.ts` existe.
    - `grep -c "private tableFor" propiedades.service.ts` retorna 1.
    - `grep -c "handleUniqueViolation" propiedades.service.ts` retorna >= 3 (definición + 2+ usos).
    - `grep "code === '23505'" propiedades.service.ts` retorna >= 1 (detección del SQLSTATE).
    - `grep -E "async (findAll|findOne|create|update|toggleActive)" propiedades.service.ts | wc -l` retorna 5.
    - `grep "ConflictException" propiedades.service.ts` retorna >= 4 (3 throw distintos + import).
    - `grep "nombre_lower_uniq" propiedades.service.ts` retorna >= 1 (parsing del constraint name).
    - `pnpm --filter @objetiva/backend type-check` exits 0.
  </acceptance_criteria>
  <done>
    PropiedadesService creado con 5 métodos públicos, helper de detección 23505 con mensajes per-constraint, y compila sin errores.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear Controller + Module + registrar PropiedadesModule en AppModule</name>
  <files>
    apps/backend/src/modules/propiedades/propiedades.controller.ts,
    apps/backend/src/modules/propiedades/propiedades.module.ts,
    apps/backend/src/app.module.ts
  </files>
  <read_first>
    - apps/backend/src/modules/dispositivos/dispositivos.controller.ts (analog — 56 líneas, RBAC pattern)
    - apps/backend/src/modules/dispositivos/dispositivos.module.ts (analog — 10 líneas, DI)
    - apps/backend/src/app.module.ts (mismo archivo a modificar — patrón de import + array imports)
    - apps/backend/src/common/guards/roles.guard.ts (verificar firma)
    - apps/backend/src/common/decorators/roles.decorator.ts (verificar firma de `@Roles`)
    - .planning/phases/29-catalogos-de-atributos/29-RESEARCH.md (Pattern 2 — controller completo líneas 540-607)
    - .planning/phases/29-catalogos-de-atributos/29-PATTERNS.md (sección "app.module.ts" — patrón de registro)
  </read_first>
  <behavior>
    - El controller usa `@Controller('propiedades')` y declara 5 endpoints:
      - `GET    :tipo`         — list (con query `activo=true|false|all`)
      - `GET    :tipo/:id`     — find one
      - `POST   :tipo`         — create        (admin only)
      - `PATCH  :tipo/:id`     — update        (admin only)
      - `PATCH  :tipo/:id/toggle` — toggle activo (admin only)
    - Helper privado `assertValidTipo(tipo: string)` valida el URL param contra `PROP_TIPOS`. Si inválido, lanza `NotFoundException`.
    - Endpoints write usan `@UseGuards(RolesGuard) + @Roles('admin')` (patrón del repo). Endpoints read NO usan guards (auth global cubre).
    - El query `activo` se parsea: `activo=true` → boolean true, `activo=false` → boolean false, `activo=all` → undefined (no filtro), undefined (no query) → boolean true (default per D-18).
    - El módulo exporta `PropiedadesService` (por si Phase 30/31 lo necesita reusar).
    - `AppModule` importa `PropiedadesModule` (agregado al array `imports` después de `ArticulosModule`).
  </behavior>
  <action>
1) Crear `apps/backend/src/modules/propiedades/propiedades.controller.ts`:

```typescript
import {
  Controller, Get, Post, Patch, Param, Query, Body,
  ParseIntPipe, UseGuards, NotFoundException,
} from '@nestjs/common'
import { PropiedadesService } from './propiedades.service'
import { CreatePropiedadDto } from './dto/create-propiedad.dto'
import { UpdatePropiedadDto } from './dto/update-propiedad.dto'
import { PROP_TIPOS, type PropTipo } from './propiedades.constants'
import { RolesGuard } from '../../common/guards/roles.guard'
import { Roles } from '../../common/decorators/roles.decorator'

@Controller('propiedades')
export class PropiedadesController {
  constructor(private readonly service: PropiedadesService) {}

  @Get(':tipo')
  findAll(@Param('tipo') tipo: string, @Query('activo') activo?: string) {
    this.assertValidTipo(tipo)
    // 'all' → undefined (sin filtro), 'true'/'false' → boolean, undefined → true (default)
    let activoFilter: boolean | undefined
    if (activo === undefined) activoFilter = true
    else if (activo === 'all') activoFilter = undefined
    else if (activo === 'true') activoFilter = true
    else if (activo === 'false') activoFilter = false
    else activoFilter = true // valor desconocido → default seguro
    return this.service.findAll(tipo as PropTipo, { activo: activoFilter })
  }

  @Get(':tipo/:id')
  async findOne(@Param('tipo') tipo: string, @Param('id', ParseIntPipe) id: number) {
    this.assertValidTipo(tipo)
    const row = await this.service.findOne(tipo as PropTipo, id)
    if (!row) {
      throw new NotFoundException(`Propiedad con ID ${id} no encontrada en ${tipo}`)
    }
    return row
  }

  @UseGuards(RolesGuard)
  @Roles('admin')
  @Post(':tipo')
  create(@Param('tipo') tipo: string, @Body() dto: CreatePropiedadDto) {
    this.assertValidTipo(tipo)
    return this.service.create(tipo as PropTipo, dto)
  }

  @UseGuards(RolesGuard)
  @Roles('admin')
  @Patch(':tipo/:id')
  update(
    @Param('tipo') tipo: string,
    @Param('id', ParseIntPipe) id: number,
    @Body() dto: UpdatePropiedadDto,
  ) {
    this.assertValidTipo(tipo)
    return this.service.update(tipo as PropTipo, id, dto)
  }

  @UseGuards(RolesGuard)
  @Roles('admin')
  @Patch(':tipo/:id/toggle')
  toggleActive(@Param('tipo') tipo: string, @Param('id', ParseIntPipe) id: number) {
    this.assertValidTipo(tipo)
    return this.service.toggleActive(tipo as PropTipo, id)
  }

  private assertValidTipo(tipo: string): asserts tipo is PropTipo {
    if (!(PROP_TIPOS as readonly string[]).includes(tipo)) {
      throw new NotFoundException(`Tipo de propiedad inválido: ${tipo}`)
    }
  }
}
```

2) Crear `apps/backend/src/modules/propiedades/propiedades.module.ts`:

```typescript
import { Module } from '@nestjs/common'
import { PropiedadesController } from './propiedades.controller'
import { PropiedadesService } from './propiedades.service'

@Module({
  controllers: [PropiedadesController],
  providers: [PropiedadesService],
  exports: [PropiedadesService],
})
export class PropiedadesModule {}
```

3) Modificar `apps/backend/src/app.module.ts` agregando import y registro:

   a) En el bloque de imports (después del último `import ... from './modules/.../*.module'`):

   ```typescript
   import { PropiedadesModule } from './modules/propiedades/propiedades.module'
   ```

   b) En el array `imports: [...]` del `@Module({...})`, agregar `PropiedadesModule` después de `ArticulosModule` y antes de `DepositosModule` (agrupación conceptual: Artículos → Propiedades → Depósitos).

4) Build + smoke test del backend:

```bash
pnpm --filter @objetiva/backend build
pnpm --filter @objetiva/backend type-check
```

5) Levantar el backend brevemente para confirmar que el módulo se carga:

```bash
# Si el proyecto está dockerizado, alguno de estos:
docker compose restart backend
docker compose logs --tail=50 backend
# Esperar mensajes tipo "Mapped {/api/propiedades/:tipo, GET}"
```

Si el log muestra los 5 endpoints `/api/propiedades/...` mapeados, el módulo está OK.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter @objetiva/backend type-check && pnpm --filter @objetiva/backend build && [ -f apps/backend/src/modules/propiedades/propiedades.controller.ts ] && [ -f apps/backend/src/modules/propiedades/propiedades.module.ts ] && grep -c "@Roles('admin')" apps/backend/src/modules/propiedades/propiedades.controller.ts && grep -c "PropiedadesModule" apps/backend/src/app.module.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/modules/propiedades/propiedades.controller.ts` existe.
    - `apps/backend/src/modules/propiedades/propiedades.module.ts` existe con `exports: [PropiedadesService]`.
    - `apps/backend/src/app.module.ts` contiene el import `PropiedadesModule` Y el módulo está en el array `imports: [...]`. Verificar: `grep -c "PropiedadesModule" apps/backend/src/app.module.ts` retorna >= 2 (1 import + 1 en array imports).
    - `grep -c "@Roles('admin')" apps/backend/src/modules/propiedades/propiedades.controller.ts` retorna 3 (POST, PATCH, PATCH toggle).
    - `grep -c "@Get" apps/backend/src/modules/propiedades/propiedades.controller.ts` retorna 2 (list + findOne).
    - `grep -c "@Post" apps/backend/src/modules/propiedades/propiedades.controller.ts` retorna 1.
    - `grep -c "@Patch" apps/backend/src/modules/propiedades/propiedades.controller.ts` retorna 2 (update + toggle).
    - `grep "assertValidTipo" apps/backend/src/modules/propiedades/propiedades.controller.ts | wc -l` retorna >= 6 (definición + 5 usos).
    - `pnpm --filter @objetiva/backend type-check` exits 0.
    - `pnpm --filter @objetiva/backend build` exits 0.
  </acceptance_criteria>
  <done>
    Controller + Module + registro en AppModule completos. Backend compila y construye sin errores.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Smoke test — backend levanta y la ruta /api/propiedades/marca está mapeada</name>
  <files></files>
  <read_first>
    - apps/backend/.env (ports, DATABASE_URL)
    - apps/backend/src/modules/propiedades/propiedades.controller.ts (recién creado en Task 3)
  </read_first>
  <behavior>
    - El contenedor backend está Up y mapeó el módulo PropiedadesModule durante el bootstrap (verificable en logs: "Mapped {/api/propiedades/:tipo, GET}").
    - El endpoint `GET /api/propiedades/marca` responde con HTTP 401 (auth requerida) o 200 (si auth global lo permite). Ambos son válidos para confirmar que la ruta existe.
    - Un 404 NotFound en esta ruta indicaría que el módulo NO se cargó — eso ES un fallo.
    - Los 11 sub-tests funcionales (POST/PATCH/toggle/RBAC/duplicate/etc.) se ejercen E2E en Plan 06 con credenciales reales preparadas en Wave 0 del Plan 04 Task 0. **Este task NO los ejecuta** — su función es solo confirmar que el módulo está cargado.
  </behavior>
  <action>
1) Confirmar que el backend está corriendo:

```bash
docker compose ps backend | grep -q "Up" || (echo "Backend not Up — start with: docker compose up backend -d" && exit 1)
```

2) Verificar que el módulo PropiedadesModule está mapeado en los logs de startup:

```bash
docker compose logs --tail=200 backend 2>&1 | grep -E "Mapped.*/api/propiedades" | head -5
```

Esperar al menos 1 línea (idealmente 5: GET, GET/:id, POST, PATCH/:id, PATCH/:id/toggle). Si no aparece ninguna, el módulo NO se registró — ABORTAR y revisar Plan 03 Task 3.

3) Ping al endpoint base — confirmar que la ruta existe (independiente de auth):

```bash
HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/api/propiedades/marca)
echo "HTTP $HTTP_CODE"
# Esperar 401 (auth requerida) o 200 (si la ruta tiene auth bypass durante dev).
# Un 404 significa que el módulo NO está cargado — fallo.
[ "$HTTP_CODE" = "404" ] && (echo "FAIL: route not registered" && exit 1)
```

4) Documentar en `29-03-SUMMARY.md`:
   - Output del `docker compose logs` con las rutas mapeadas.
   - Código HTTP devuelto por el ping.
   - Confirmación de que los 11 sub-tests funcionales (CRUD, RBAC, duplicate, lowercase abrev, tipo inválido) se exercerán E2E en Plan 06 — Plan 04 Task 0 prepara las credenciales de testing.

> **Nota intencional:** este task ya NO consume tokens admin/viewer. La detección de inválidos (lowercase abrev, duplicate, RBAC viewer 403) se hace via el E2E del Plan 06 con un usuario admin real. Esto evita el escalation a humano por credenciales que reportaba el plan original (revisión B-4).
  </action>
  <verify>
    <automated>docker compose ps backend | grep -q "Up" && [ "$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3001/api/propiedades/marca)" != "404" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ps backend` retorna estado `Up`.
    - `curl http://localhost:3001/api/propiedades/marca` retorna HTTP code distinto de 404 (típico: 401 auth required o 200). Un 404 = falla del task (módulo no cargado).
    - Los logs del backend muestran al menos 1 línea `Mapped.*/api/propiedades` (preferiblemente 5).
    - El SUMMARY documenta explícitamente que los 11 sub-tests funcionales se difieren a Plan 06 (E2E con credenciales reales del Wave 0).
  </acceptance_criteria>
  <done>
    Smoke confirmado: módulo cargado, ruta accesible, ningún token requerido en este task. Plan 06 con credenciales del Plan 04 Task 0 ejerce el comportamiento funcional completo.
  </done>
</task>


</tasks>

<verification>
- 5 endpoints accesibles bajo `/api/propiedades/:tipo*`.
- DTO valida `abrev` regex y rechaza lowercase con 400.
- Service traduce 23505 a 409 con mensaje específico per-constraint.
- Soft-delete via toggle preserva la fila.
- RBAC: admin escribe / viewer lee.
- Tipo inválido devuelve 404.
</verification>

<success_criteria>
- 5 endpoints REST funcionan correctamente.
- Backend levanta sin errores y mapea las rutas en startup logs.
- CAT-01 (CRUD), CAT-03 (UNIQUE legible), CAT-04 (soft-delete) cubiertos a nivel API.
- Listo para Plan 04 (web infra) y Plan 05 (web UI) que consumen estos endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/29-catalogos-de-atributos/29-03-SUMMARY.md` con:
- Lista de archivos creados (controller, service, module, 2 DTOs, constants)
- Patches aplicados (app.module.ts agregando PropiedadesModule)
- Resultado de los 11 sub-tests del smoke E2E
- Sample del JSON devuelto por GET /api/propiedades/marca (1-2 filas)
- Confirmación de que el backend levanta limpio
</output>
