---
phase: 14-schema-foundation-articulos-depositos
plan: 05
type: execute
wave: 3
depends_on: ['14-02']
files_modified:
  - apps/web/src/types/deposito.ts
  - apps/web/src/lib/api.ts
  - apps/web/src/lib/api.client.ts
  - apps/web/src/components/depositos/depositos-list.tsx
  - apps/web/src/components/depositos/deposito-dialog.tsx
  - apps/web/src/app/(dashboard)/settings/depositos/page.tsx
  - apps/web/src/app/(dashboard)/settings/layout.tsx
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
  - DEP-04
  - MIG-06

must_haves:
  truths:
    - 'User can view depositos list within Settings showing nombre, direccion, and stock count badges (0/0)'
    - 'User can create a new deposito via inline dialog with 3 fields (nombre, direccion, descripcion)'
    - 'User can edit an existing deposito via the same dialog pattern'
    - 'User can toggle active/inactive on a deposito'
  artifacts:
    - path: 'apps/web/src/components/depositos/depositos-list.tsx'
      provides: 'Depositos table within settings page'
      min_lines: 40
    - path: 'apps/web/src/components/depositos/deposito-dialog.tsx'
      provides: 'Create/edit dialog for depositos'
      min_lines: 50
    - path: 'apps/web/src/app/(dashboard)/settings/depositos/page.tsx'
      provides: 'Settings sub-page for depositos management'
  key_links:
    - from: 'apps/web/src/components/depositos/depositos-list.tsx'
      to: 'apps/web/src/lib/api.client.ts'
      via: 'fetchDepositosClient for data loading'
      pattern: 'fetchDepositos'
    - from: 'apps/web/src/app/(dashboard)/settings/layout.tsx'
      to: 'apps/web/src/app/(dashboard)/settings/depositos/page.tsx'
      via: 'Settings nav tab linking to depositos sub-page'
      pattern: 'depositos'
---

<objective>
Build the depositos management UI as a sub-section within Settings, using inline dialogs for create/edit and showing placeholder stock count badges.

Purpose: Delivers depositos CRUD within settings (DEP-01 through DEP-04) and installs the dialog shadcn component.
Output: Working depositos management in Settings with dialog-based create/edit.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-schema-foundation-articulos-depositos/14-CONTEXT.md
@.planning/phases/14-schema-foundation-articulos-depositos/14-RESEARCH.md
@.planning/phases/14-schema-foundation-articulos-depositos/14-02-SUMMARY.md

@apps/web/src/app/(dashboard)/settings/layout.tsx
@apps/web/src/app/(dashboard)/settings/business/business-form.tsx
@apps/web/src/lib/api.ts
@apps/web/src/lib/api.client.ts

<interfaces>
<!-- API endpoints from Plan 02 -->
GET /api/depositos — returns { id, nombre, direccion, descripcion, activo, stockSummary: { totalArticulos: 0, totalUnidades: 0 }, createdAt, updatedAt }[]
POST /api/depositos — body: { nombre, direccion?, descripcion? } — returns Deposito
PATCH /api/depositos/:id — body: partial fields — returns Deposito
PATCH /api/depositos/:id/toggle — no body — returns Deposito
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Deposito type, API functions, install dialog component</name>
  <files>apps/web/src/types/deposito.ts, apps/web/src/lib/api.ts, apps/web/src/lib/api.client.ts</files>
  <action>
**Install shadcn dialog component:**
Run `cd apps/web && npx shadcn@latest add dialog` before creating files. This is needed for the deposito create/edit dialog (Pitfall 4 from RESEARCH.md).

**deposito.ts type:**

```typescript
export interface Deposito {
  id: number
  nombre: string
  direccion: string | null
  descripcion: string | null
  activo: boolean
  createdAt: string
  updatedAt: string
  stockSummary: {
    totalArticulos: number
    totalUnidades: number
  }
}
```

**api.ts (server-side):** Add `fetchDepositos(): Promise<Deposito[]>` — simple GET /depositos with fetchWithAuth. No pagination needed. Import Deposito type.

**api.client.ts additions** (add to existing file):

- `fetchDepositosClient(): Promise<Deposito[]>` — GET /depositos with browser auth
- `createDeposito(data: { nombre: string; direccion?: string; descripcion?: string }): Promise<Deposito>` — POST /depositos
- `updateDeposito(id: number, data: Partial<...>): Promise<Deposito>` — PATCH /depositos/:id
- `toggleDepositoActivo(id: number): Promise<Deposito>` — PATCH /depositos/:id/toggle
  </action>
  <verify>
  <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin/apps/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Deposito type defined. API functions for server and client-side exist. Dialog component installed from shadcn.</done>
  </task>

<task type="auto">
  <name>Task 2: Build depositos settings sub-page with list and dialog</name>
  <files>apps/web/src/components/depositos/depositos-list.tsx, apps/web/src/components/depositos/deposito-dialog.tsx, apps/web/src/app/(dashboard)/settings/depositos/page.tsx, apps/web/src/app/(dashboard)/settings/layout.tsx</files>
  <action>
**deposito-dialog.tsx:** Create/edit dialog for depositos (per locked decision: "inline dialog with 3 fields"):
- Uses shadcn Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter
- Props: `{ deposito?: Deposito; open: boolean; onOpenChange: (open: boolean) => void; onSuccess: () => void }`
- React Hook Form + Zod validation:
  - nombre: required, max 100 chars
  - direccion: optional, max 255 chars
  - descripcion: optional, Textarea
- Title: "Nuevo Deposito" or "Editar Deposito" based on `deposito` prop
- Submit calls `createDeposito` or `updateDeposito` from api.client.ts
- On success: toast, close dialog, call onSuccess
- On error: toast with error message
- Size: `sm:max-w-md`
- Per RESEARCH.md deposito dialog pattern

**depositos-list.tsx:** Table within settings showing depositos:

- Props: `{ initialDepositos?: Deposito[] }`
- Fetches depositos client-side via `fetchDepositosClient()` on mount
- Columns: nombre, direccion (truncated), stock badges, estado, acciones
- Stock badges per deposito: Show `{totalArticulos} articulos` and `{totalUnidades} unidades` as muted badges (will show "0 articulos · 0 unidades" until Phase 15)
- Estado: Badge "Activo" / "Inactivo"
- Actions: Edit button (opens dialog), Toggle active/inactive button
- "Nuevo Deposito" button at top-right (opens dialog in create mode)
- Simple HTML table or shadcn Table (no need for TanStack Table — max ~10 rows)
- After create/edit/toggle: refetch list to show updated data

**depositos/page.tsx:** Settings sub-page:

- Simple wrapper that renders DepositosList
- Page heading: "Depositos" with description "Gestion de depositos y almacenes"

**settings/layout.tsx:** Update to add a new tab/link for "Depositos":

- Find existing settings navigation (likely tabs: Negocio, Apariencia, Perfil)
- Add "Depositos" tab linking to `/settings/depositos`
- Keep all existing tabs/links unchanged

Follow Tabler aesthetic: text-sm, h-8 buttons, rounded-sm borders, reduced padding.
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin/apps/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
</verify>
<done>Settings has a "Depositos" tab. Depositos page shows table with stock badges (0/0). "Nuevo Deposito" opens dialog. Edit and toggle active work. All deposito CRUD functional through UI.</done>
</task>

</tasks>

<verification>
- Web app compiles: `cd apps/web && npx tsc --noEmit` passes
- Settings page has "Depositos" tab in navigation
- `/settings/depositos` renders depositos list with seeded data
- Each deposito shows "0 articulos · 0 unidades" badges
- "Nuevo Deposito" button opens dialog, submitting creates a deposito
- Edit button opens dialog with pre-populated fields
- Toggle active/inactive updates status badge
</verification>

<success_criteria>

- Depositos accessible from Settings navigation
- List shows nombre, direccion, stock badges (0/0 placeholder), estado
- Create dialog has 3 fields (nombre, direccion, descripcion) with validation
- Edit dialog pre-populates existing values
- Active/inactive toggle works
- Dialog closes on success with toast notification
- List refreshes after any mutation
  </success_criteria>

<output>
After completion, create `.planning/phases/14-schema-foundation-articulos-depositos/14-05-SUMMARY.md`
</output>
