---
phase: 16-downstream-migration-dashboard-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/db/schema.ts
  - apps/backend/src/db/seed.ts
  - apps/backend/src/db/generators/order.generator.ts
  - apps/backend/src/db/generators/sale.generator.ts
  - apps/backend/src/db/generators/purchase.generator.ts
  - apps/backend/src/app.module.ts
  - apps/web/src/types/order.ts
  - apps/web/src/types/sale.ts
  - apps/web/src/types/purchase.ts
autonomous: true
requirements:
  - MIG-03
must_haves:
  truths:
    - 'orderItems, saleItems, purchaseItems reference articuloCodigo (text FK) instead of productId (integer FK)'
    - 'products and inventory tables are removed from Drizzle schema entirely'
    - 'Seed runs successfully with articulos data flowing into order/sale/purchase item generation'
    - 'Backend starts without errors after old modules are deleted'
  artifacts:
    - path: 'apps/backend/src/db/schema.ts'
      provides: 'Updated item table FKs, removed products+inventory tables and type exports'
      contains: 'articuloCodigo'
    - path: 'apps/backend/src/db/generators/order.generator.ts'
      provides: 'Order generation using articulo data'
      contains: 'articuloCodigo'
    - path: 'apps/backend/src/db/generators/sale.generator.ts'
      provides: 'Sale generation using articulo data'
      contains: 'articuloCodigo'
    - path: 'apps/backend/src/db/generators/purchase.generator.ts'
      provides: 'Purchase generation using articulo data'
      contains: 'articuloCodigo'
  key_links:
    - from: 'apps/backend/src/db/schema.ts'
      to: 'articulos.codigo'
      via: 'FK reference on orderItems/saleItems/purchaseItems'
      pattern: "references.*articulos\\.codigo"
    - from: 'apps/backend/src/db/seed.ts'
      to: 'apps/backend/src/db/generators/order.generator.ts'
      via: 'passes articulosData to generators'
      pattern: 'generateOrders.*articulos'
---

<objective>
Migrate item table FKs from productId (integer) to articuloCodigo (text), drop old products/inventory tables from schema, update seed generators, delete old backend modules, and update web item types.

Purpose: Complete the data model migration so all downstream modules reference the new articulos model instead of the removed products model.
Output: Clean schema with no products/inventory tables, working seed, no old backend modules.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-downstream-migration-dashboard-navigation/16-CONTEXT.md
@.planning/phases/16-downstream-migration-dashboard-navigation/16-RESEARCH.md

<interfaces>
<!-- Current schema item table pattern (same for orderItems, saleItems, purchaseItems): -->
From apps/backend/src/db/schema.ts:
```typescript
// orderItems (line 66-83) — productId + productName columns to replace
productId: integer('product_id').notNull().references(() => products.id, { onDelete: 'restrict' }),
productName: varchar('product_name', { length: 255 }).notNull(),

// articulos table PK:
codigo: text('codigo').primaryKey(),
nombre: varchar('nombre', { length: 255 }).notNull(),
sku: varchar('sku', { length: 50 }),

````

From apps/backend/src/db/generators/order.generator.ts:
```typescript
import type { GeneratedProduct } from './product.generator'
export interface GeneratedOrderItem {
  productId: number; productName: string; sku: string; quantity: number; price: number; subtotal: number;
}
export function generateOrders(count: number, products: GeneratedProduct[]): GeneratedOrder[]
````

<!-- Same pattern in sale.generator.ts and purchase.generator.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update schema, generators, seed, and delete old backend modules</name>
  <files>
    apps/backend/src/db/schema.ts,
    apps/backend/src/db/seed.ts,
    apps/backend/src/db/generators/order.generator.ts,
    apps/backend/src/db/generators/sale.generator.ts,
    apps/backend/src/db/generators/purchase.generator.ts,
    apps/backend/src/app.module.ts
  </files>
  <action>
    **Schema (schema.ts):**
    1. DELETE the entire `products` table definition (lines 17-38) and its comment header.
    2. DELETE the entire `inventory` table definition (lines 87-108) and its comment header.
    3. On `orderItems`: replace `productId: integer('product_id').notNull().references(() => products.id, { onDelete: 'restrict' })` with `articuloCodigo: text('articulo_codigo').notNull().references(() => articulos.codigo, { onDelete: 'restrict' })`. Replace `productName: varchar('product_name', { length: 255 }).notNull()` with `articuloNombre: varchar('articulo_nombre', { length: 255 }).notNull()`.
    4. Apply the SAME column renames on `saleItems` and `purchaseItems`.
    5. DELETE type exports: `Product`, `NewProduct`, `InventoryItem`, `NewInventoryItem`. Keep all other type exports. The OrderItem/SaleItem/PurchaseItem types will automatically update through Drizzle inference since we changed the columns.

    **Generators:**
    6. `order.generator.ts`: Remove `import type { GeneratedProduct }`. Create a local interface `type ArticuloRef = { codigo: string; nombre: string; sku: string | null }`. Change `GeneratedOrderItem` to use `articuloCodigo: string` and `articuloNombre: string` instead of `productId` and `productName`. Update `generateOrder` and `generateOrders` to accept `articulos: ArticuloRef[]` instead of `products: GeneratedProduct[]`. Inside the loop, pick random articulo and use `articulo.codigo`, `articulo.nombre`, `articulo.sku ?? ''`.
    7. Apply SAME changes to `sale.generator.ts` (GeneratedSaleItem, generateSale, generateSales).
    8. Apply SAME changes to `purchase.generator.ts` (GeneratedPurchaseItem, generatePurchase, generatePurchases). Note: purchase items use `unitCost` not `price` — use a random cost value via `faker.number.float({ min: 10, max: 500, fractionDigits: 2 })` since articulos don't have a `cost` field accessible in the generator (the ArticuloRef only has codigo/nombre/sku).

    **Seed (seed.ts):**
    9. Remove imports: `generateProducts`, `generateInventory`.
    10. Remove the entire "Products (v1.0 compat)" section (lines 109-136 — the products insertion and idMap creation).
    11. Remove the entire "Inventory" section (lines 138-157).
    12. Update TRUNCATE SQL: remove `inventory, products` from the list. Keep: `purchase_items, sale_items, order_items, purchases, sales, orders, existencias, articulos, depositos, business_settings`.
    13. Update order generation: change `generateOrders(80, productsData)` to `generateOrders(80, articulosData)`. In the order insertion loop, change `productId: idMap.get(item.productId)!` to `articuloCodigo: item.articuloCodigo`, and `productName: item.productName` to `articuloNombre: item.articuloNombre`.
    14. Apply SAME changes to sales seeding: `generateSales(60, articulosData)`, update item insertion to use `articuloCodigo`/`articuloNombre`.
    15. Apply SAME changes to purchases seeding: `generatePurchases(20, articulosData)`, update item insertion to use `articuloCodigo`/`articuloNombre`.

    **Delete old backend modules:**
    16. DELETE directories: `apps/backend/src/modules/products/` and `apps/backend/src/modules/inventory/`.
    17. DELETE generator files: `apps/backend/src/db/generators/product.generator.ts` and `apps/backend/src/db/generators/inventory.generator.ts`.
    18. `app.module.ts`: Remove `ProductsModule` and `InventoryModule` from imports array and their import statements at top. Keep all other modules.

    **CRITICAL ORDER:** Update app.module.ts BEFORE or AT SAME TIME as deleting the module directories.

  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && cd apps/backend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Schema has no products/inventory tables, three item tables reference articuloCodigo, generators accept articulos data, seed uses articulosData for all item generation, old modules deleted, TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update web item types and run db:push + db:seed</name>
  <files>
    apps/web/src/types/order.ts,
    apps/web/src/types/sale.ts,
    apps/web/src/types/purchase.ts,
    apps/web/src/components/tables/orders/order-sheet.tsx,
    apps/web/src/components/tables/sales/sale-sheet.tsx,
    apps/web/src/components/tables/purchases/purchase-sheet.tsx
  </files>
  <action>
    **Web types:**
    1. `order.ts`: In `OrderItem`, replace `productId: number` with `articuloCodigo: string`, replace `productName: string` with `articuloNombre: string`.
    2. `sale.ts`: Same changes on `SaleItem`.
    3. `purchase.ts`: Same changes on `PurchaseItem`.

    **Web sheet components that display item.productName:**
    4. `order-sheet.tsx` (line 81): Change `item.productName` to `item.articuloNombre`.
    5. `sale-sheet.tsx` (line 76): Change `item.productName` to `item.articuloNombre`.
    6. `purchase-sheet.tsx` (line 78): Change `item.productName` to `item.articuloNombre`.

    **Database reset:**
    7. Run `cd apps/backend && pnpm db:push` to push updated schema (drops products+inventory, updates item table columns).
    8. Run `cd apps/backend && pnpm db:seed` to re-seed with articulos data flowing into orders/sales/purchases.

    Verify seed completes without errors and the item tables contain articuloCodigo references.

  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin/apps/backend && pnpm db:push 2>&1 | tail -5 && pnpm db:seed 2>&1 | tail -5</automated>
  </verify>
  <done>Web item types use articuloCodigo/articuloNombre, sheet components display articuloNombre, database schema pushed successfully with no products/inventory tables, seed completes with all item tables referencing articuloCodigo.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in apps/backend (no type errors)
- `pnpm db:push` succeeds (schema applied)
- `pnpm db:seed` succeeds (data inserted with articulo references)
- No imports of products/inventory modules remain in backend code
- Web types OrderItem/SaleItem/PurchaseItem have articuloCodigo+articuloNombre fields
</verification>

<success_criteria>
Order items, sale items, and purchase items reference articuloCodigo (text FK) in schema, seed, generators, web types, and display components. Old products and inventory tables and backend modules are completely removed. Database re-seeded successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/16-downstream-migration-dashboard-navigation/16-01-SUMMARY.md`
</output>
