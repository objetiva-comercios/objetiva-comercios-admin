---
phase: 38
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase38-preflight-backup.sh
  - /var/backups/erp_sanchez/backup-YYMMDD-HHMM.dump
autonomous: false
requirements:
  - SC-PRE
tags:
  - drizzle
  - postgresql
  - backup
  - migrations
  - prod
security_enforcement: true
user_setup:
  - service: vps-shell
    why: 'Pre-flight backup ejecuta pg_dump dentro del container postgres del VPS y deja el .dump en /var/backups/erp_sanchez del host. Requiere acceso SSH al VPS y privilegios para escribir en /var/backups/erp_sanchez/.'
    env_vars: []
    dashboard_config:
      - task: 'Verificar que el path /var/backups/erp_sanchez/ existe en el VPS y es escribible (mkdir -p si no)'
        location: 'VPS shell (host del docker compose)'
      - task: 'Confirmar que docker exec postgres tiene rol sanchez con permiso de createdb/dropdb'
        location: 'VPS shell'

must_haves:
  truths:
    - 'Existe un backup pg_dump -F c de erp_sanchez con timestamp del día de la fase en /var/backups/erp_sanchez/ (D-05, D-15)'
    - 'El backup fue restaurado exitosamente en una DB temporal erp_restore_test antes de tocar nada de prod (D-05)'
    - 'Los row counts de las 6 tablas críticas (articulos, existencias, inventarios_articulos, comprobantes_cabecera, comprobantes_detalle, comprobantes_pagos) matchean exactamente entre erp_sanchez y erp_restore_test (D-05)'
    - 'La DB temporal erp_restore_test fue dropeada después de la verificación (no queda como ruido) (D-05)'
    - 'El backup persiste en /var/backups/erp_sanchez/ con naming canónico backup-YYMMDD-HHMM.dump (D-15)'
    - 'Si el restore-test falla o los counts no matchean, la fase se ABORTA y se abre un nuevo todo (D-05)'
  artifacts:
    - path: 'scripts/phase38-preflight-backup.sh'
      provides: 'Script bash idempotente que ejecuta pg_dump + restore-test + count diff + drop temp DB + persistencia'
      contains: 'set -e'
    - path: '/var/backups/erp_sanchez/backup-YYMMDD-HHMM.dump'
      provides: 'Backup pg_dump -F c restorable de erp_sanchez previo a cualquier modificación de tracking'
      min_size_bytes: 1048576
  key_links:
    - from: 'scripts/phase38-preflight-backup.sh'
      to: 'docker exec postgres'
      via: 'comando bash'
      pattern: 'docker exec postgres'
    - from: 'scripts/phase38-preflight-backup.sh'
      to: '/var/backups/erp_sanchez/'
      via: 'redirección de stdout o cp final'
      pattern: '/var/backups/erp_sanchez'
  derived_from:
    - 'D-05 (Pre-flight obligatorio gate de avance)'
    - 'D-15 (Backups persisten 30 días en VPS, naming canónico)'
    - 'feedback_docker_compose.md (comandos vía docker exec, NUNCA pnpm dev)'
    - 'feedback_never_drop_tables.md (backup obligatorio antes de tocar prod)'
---

<objective>
Producir un backup verificado de la DB de producción `erp_sanchez` antes de cualquier paso que modifique tracking de migraciones o estado del schema. Este plan es el GATE de avance para todo el resto de Phase 38: si falla, la fase se aborta y se abre un nuevo todo.

Purpose: Implementar D-05 (pre-flight obligatorio) y D-15 (backups persisten 30 días con naming canónico). Sin restore-test exitoso no se toca `__drizzle_migrations` ni `_journal.json`.

Output:

- Script `scripts/phase38-preflight-backup.sh` reutilizable y idempotente.
- Backup `/var/backups/erp_sanchez/backup-YYMMDD-HHMM.dump` en VPS, restorable.
- Reporte de counts verificados (texto stdout que el usuario aprueba antes del avance).
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-CONTEXT.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-RESEARCH.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-PATTERNS.md
@CLAUDE.md
@scripts/test-auth.sh

<interfaces>
<!-- Existing pattern for bash scripts in this repo: scripts/test-auth.sh
     Reuse the same shape (set -e, ✓/❌ glyphs, banner separators, exit 1 on failure). -->

scripts/test-auth.sh (líneas 1-26):

```bash
#!/usr/bin/env bash
set -e
echo "Testing Backend Authentication (AUTH-05)"
echo "========================================="
echo ""
if ! curl -s http://localhost:3001/health > /dev/null 2>&1; then
  echo "❌ Backend is not running on localhost:3001"
  exit 1
fi
echo "✓ Backend is running"
```

Patrón de comandos PG (per CONTEXT.md §canonical_refs y feedback_docker_compose.md):

- `docker exec postgres psql -U sanchez -d erp_sanchez -c '...'`
- `docker exec postgres pg_dump -U sanchez -d erp_sanchez -F c -f /tmp/backup.dump`
- `docker exec postgres createdb -U sanchez erp_restore_test`
- `docker exec postgres pg_restore -U sanchez -d erp_restore_test /tmp/backup.dump`
- `docker exec postgres dropdb -U sanchez erp_restore_test`

Tablas críticas para count diff (D-05):

- articulos
- existencias
- inventarios_articulos
- comprobantes_cabecera
- comprobantes_detalle
- comprobantes_pagos
  </interfaces>
  </context>

<threat_model>

## Trust Boundaries

| Boundary                                             | Description                                                                                                                                                                                         |
| ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| VPS shell → docker exec postgres                     | Operador con SSH al VPS ejecuta comandos psql/pg_dump dentro del container postgres. Untrusted input no aplica (es operador humano), pero las credenciales del rol `sanchez` están en el container. |
| Container postgres → /var/backups/erp_sanchez (host) | El backup se mueve del FS del container al FS del host. Bind mount o `docker cp`.                                                                                                                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                                                                                                |
| --------- | ---------------------- | ----------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-01   | Tampering              | scripts/phase38-preflight-backup.sh | mitigate    | Script idempotente con `set -e`; cualquier paso que falle aborta toda la cadena (pg_dump, restore, count diff). El operador debe re-ejecutar limpio, no parchear estado intermedio.                            |
| T-38-02   | Information Disclosure | backup .dump file                   | mitigate    | El .dump contiene PII de comprobantes y artículos. Path canónico `/var/backups/erp_sanchez/` debe tener perms `750` o más restrictivo. Operador verifica con `ls -la /var/backups/erp_sanchez/` post-creación. |
| T-38-03   | Denial of Service      | postgres container durante pg_dump  | accept      | pg_dump genera carga de I/O; aceptable en ventana de baja actividad. Si la carga es prohibitiva, aplazar la fase (no es problema del script).                                                                  |
| T-38-04   | Repudiation            | backup naming colision              | mitigate    | Naming `backup-YYMMDD-HHMM.dump` (D-15). Si dos ejecuciones en el mismo minuto colisionan, el script aborta con error explícito (no overwrite silencioso).                                                     |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Crear script preflight backup + restore-test + count diff</name>
  <files>scripts/phase38-preflight-backup.sh</files>
  <read_first>
    - scripts/test-auth.sh (patrón canónico de bash scripts en este repo: shebang, set -e, banners, ✓/❌ glyphs)
    - .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-RESEARCH.md §Pitfall 1 (qualified schema drizzle.__drizzle_migrations) — para entender qué NO toca este script
    - .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-PATTERNS.md §"Bash script structure" y §"Docker-exec for all DB/host commands"
    - CLAUDE.md (commits en inglés conventional, comandos canónicos docker compose)
  </read_first>
  <action>
    Crear `scripts/phase38-preflight-backup.sh` siguiendo EXACTAMENTE el patrón de `scripts/test-auth.sh` (shebang, `set -e`, banners de `=========================================`, glifos `✓`/`❌`, exit 1 on failure).

    El script debe ejecutar la secuencia documentada en D-05:

    ```bash
    #!/usr/bin/env bash
    set -e

    echo "Phase 38 — Pre-flight backup + restore-test (D-05)"
    echo "============================================================"
    echo ""

    # 0) Computar nombre canónico (D-15): backup-YYMMDD-HHMM.dump
    TIMESTAMP="$(date +%y%m%d-%H%M)"
    BACKUP_NAME="backup-${TIMESTAMP}.dump"
    BACKUP_PATH_HOST="/var/backups/erp_sanchez/${BACKUP_NAME}"
    BACKUP_PATH_CTR="/tmp/${BACKUP_NAME}"

    echo "Backup target: ${BACKUP_PATH_HOST}"
    echo ""

    # 1) Verificar que /var/backups/erp_sanchez/ existe y es escribible
    if [ ! -d /var/backups/erp_sanchez ]; then
      echo "❌ /var/backups/erp_sanchez/ does not exist. Create it first: sudo mkdir -p /var/backups/erp_sanchez && sudo chown $(whoami) /var/backups/erp_sanchez"
      exit 1
    fi

    # 2) Verificar que no hay colisión de nombre (D-15: naming explícito, NO overwrite silencioso)
    if [ -f "${BACKUP_PATH_HOST}" ]; then
      echo "❌ Backup ${BACKUP_PATH_HOST} already exists. Refusing to overwrite. Wait 1 minute and re-run, or move the existing file."
      exit 1
    fi

    # 3) pg_dump dentro del container
    echo "Step 1: pg_dump erp_sanchez (custom format, restorable via pg_restore)"
    docker exec postgres pg_dump -U sanchez -d erp_sanchez -F c -f "${BACKUP_PATH_CTR}"
    echo "✓ pg_dump complete"
    echo ""

    # 4) Mover el .dump del container al host
    echo "Step 2: Move dump from container to host"
    docker cp "postgres:${BACKUP_PATH_CTR}" "${BACKUP_PATH_HOST}"
    docker exec postgres rm -f "${BACKUP_PATH_CTR}"
    echo "✓ Dump persisted at ${BACKUP_PATH_HOST}"
    ls -la "${BACKUP_PATH_HOST}"
    echo ""

    # 5) Crear DB temporal y restaurar
    echo "Step 3: Restore-test in erp_restore_test"
    docker exec postgres dropdb -U sanchez --if-exists erp_restore_test
    docker exec postgres createdb -U sanchez erp_restore_test
    docker cp "${BACKUP_PATH_HOST}" "postgres:${BACKUP_PATH_CTR}"
    # pg_restore puede emitir warnings no fatales (ej: roles ya existentes); -e hace que cualquier ERROR aborte
    docker exec postgres pg_restore -U sanchez -d erp_restore_test -e "${BACKUP_PATH_CTR}" || {
      echo "❌ pg_restore failed. Aborting Phase 38 — do NOT touch __drizzle_migrations or _journal.json."
      docker exec postgres dropdb -U sanchez --if-exists erp_restore_test
      docker exec postgres rm -f "${BACKUP_PATH_CTR}"
      exit 1
    }
    docker exec postgres rm -f "${BACKUP_PATH_CTR}"
    echo "✓ Restore-test complete"
    echo ""

    # 6) Diff de row counts en 6 tablas críticas
    echo "Step 4: Row count diff between erp_sanchez and erp_restore_test"
    TABLES=("articulos" "existencias" "inventarios_articulos" "comprobantes_cabecera" "comprobantes_detalle" "comprobantes_pagos")
    DIFF_FOUND=0
    for T in "${TABLES[@]}"; do
      C1=$(docker exec postgres psql -U sanchez -d erp_sanchez -tAc "SELECT count(*) FROM ${T}")
      C2=$(docker exec postgres psql -U sanchez -d erp_restore_test -tAc "SELECT count(*) FROM ${T}")
      if [ "$C1" = "$C2" ]; then
        echo "  ✓ ${T}: ${C1} (match)"
      else
        echo "  ❌ ${T}: erp_sanchez=${C1} erp_restore_test=${C2} (MISMATCH)"
        DIFF_FOUND=1
      fi
    done
    echo ""

    # 7) Drop DB temporal SIEMPRE (incluso si hay diff)
    echo "Step 5: Drop erp_restore_test"
    docker exec postgres dropdb -U sanchez erp_restore_test
    echo "✓ erp_restore_test dropped"
    echo ""

    if [ $DIFF_FOUND -ne 0 ]; then
      echo "❌ ABORT: row counts mismatch. Phase 38 must NOT proceed. Open a new todo and investigate."
      echo "   Backup is preserved at ${BACKUP_PATH_HOST} for forensics."
      exit 1
    fi

    echo "============================================================"
    echo "✓ ALL CHECKS PASSED"
    echo "  Backup:   ${BACKUP_PATH_HOST}"
    echo "  Tables:   6/6 row counts match between prod and restored"
    echo "  Next:     Plan 38-02 (local journal repair) is now safe to start"
    echo "============================================================"
    ```

    El script debe ser ejecutable: `chmod +x scripts/phase38-preflight-backup.sh`.

    NO ejecutarlo en este task — solo crear el archivo. La ejecución es Task 2 (gate humano).

  </action>
  <verify>
    <automated>test -x scripts/phase38-preflight-backup.sh &amp;&amp; bash -n scripts/phase38-preflight-backup.sh &amp;&amp; grep -q "set -e" scripts/phase38-preflight-backup.sh &amp;&amp; grep -q "pg_dump" scripts/phase38-preflight-backup.sh &amp;&amp; grep -q "erp_restore_test" scripts/phase38-preflight-backup.sh &amp;&amp; grep -q "/var/backups/erp_sanchez/" scripts/phase38-preflight-backup.sh &amp;&amp; grep -q "comprobantes_cabecera" scripts/phase38-preflight-backup.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/phase38-preflight-backup.sh` exits 0 (script es ejecutable)
    - `bash -n scripts/phase38-preflight-backup.sh` exits 0 (sintaxis válida sin ejecutar)
    - `grep -c '^#' scripts/phase38-preflight-backup.sh` no afecta — verificar contenido funcional con: `grep -v '^#' scripts/phase38-preflight-backup.sh | grep -c 'docker exec postgres'` retorna >= 5
    - `grep -q "BACKUP_NAME=\"backup-\${TIMESTAMP}.dump\"" scripts/phase38-preflight-backup.sh` (naming canónico D-15)
    - `grep -q "TABLES=" scripts/phase38-preflight-backup.sh` y la lista contiene las 6 tablas: `articulos`, `existencias`, `inventarios_articulos`, `comprobantes_cabecera`, `comprobantes_detalle`, `comprobantes_pagos`
    - El script NO contiene comandos contra `__drizzle_migrations` ni `_journal.json` (este plan es solo backup)
  </acceptance_criteria>
  <done>El archivo existe, es ejecutable, sintaxis válida, y contiene los 5 steps documentados (verificación de path, pg_dump, mover al host, restore-test, count diff con drop final). Listo para ser ejecutado en Task 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Ejecutar el script en VPS y aprobar el reporte</name>
  <files>/var/backups/erp_sanchez/backup-YYMMDD-HHMM.dump (en VPS)</files>
  <action>Ejecutar bash scripts/phase38-preflight-backup.sh en VPS shell. Validar que el output termina con "✓ ALL CHECKS PASSED" y que el backup persistió en /var/backups/erp_sanchez/. Aprobar/abortar según el resultado.</action>
  <verify>ls -la /var/backups/erp_sanchez/backup-*.dump retorna al menos 1 archivo del día actual; docker exec postgres psql -U sanchez -lqt | grep erp_restore_test retorna vacío.</verify>
  <done>Backup creado, restore-test pasó con 6/6 counts matching, DB temporal dropeada, operador aprobó "approved" en el resume signal.</done>
  <what-built>scripts/phase38-preflight-backup.sh listo para ejecutar contra el VPS de prod.</what-built>
  <how-to-verify>
    1. Loguearse al VPS por SSH (donde corre `docker compose` con el container `postgres` y la DB `erp_sanchez`).
    2. Asegurarse de que `/var/backups/erp_sanchez/` existe; si no:
       ```bash
       sudo mkdir -p /var/backups/erp_sanchez
       sudo chown $(whoami) /var/backups/erp_sanchez
       ```
    3. Ejecutar el script desde el repo en el VPS (asumiendo que el repo está cloneado allá; si no, copiar el script):
       ```bash
       bash scripts/phase38-preflight-backup.sh
       ```
    4. Verificar el output esperado al final:
       - "✓ ALL CHECKS PASSED"
       - "Backup: /var/backups/erp_sanchez/backup-YYMMDD-HHMM.dump" (con timestamp del día)
       - "Tables: 6/6 row counts match between prod and restored"
    5. Verificar manualmente que el backup persistió:
       ```bash
       ls -la /var/backups/erp_sanchez/backup-*.dump
       ```
       Tamaño esperado: > 1 MB (depende del volumen de prod, pero un dump vacío sería sospechoso).
    6. Verificar que la DB temporal NO quedó:
       ```bash
       docker exec postgres psql -U sanchez -lqt | grep erp_restore_test
       ```
       Output esperado: vacío (DB ya dropeada).
    7. **Si el script aborta con `❌`**: NO continuar con Phase 38. Reportar el error completo al desarrollador, abrir un nuevo todo en `.planning/todos/pending/` documentando el fallo.
  </how-to-verify>
  <resume-signal>Type "approved" si el backup pasó y los counts matchean. Type "abort" si el script falló (Phase 38 entera se aborta).</resume-signal>
</task>

</tasks>

<verification>
- Script existe, es ejecutable, sintaxis válida.
- Backup existe en `/var/backups/erp_sanchez/` con naming canónico (verificación humana en VPS).
- Counts de las 6 tablas matchean entre prod y restore-test (verificación humana).
- DB temporal `erp_restore_test` fue dropeada post-verificación.
- Aprobación humana explícita registrada antes de avanzar a Plan 38-02.
</verification>

<success_criteria>
SC-PRE: pg_dump + restore-test pasa con counts matching antes de tocar nada (D-05).

Medible:

- `ls /var/backups/erp_sanchez/backup-*.dump` retorna 1 archivo del día actual.
- Output del script termina con "✓ ALL CHECKS PASSED".
- Operador humano firma aprobación en el checkpoint para liberar Plan 38-02.
  </success_criteria>

<output>
After completion, create `.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-01-SUMMARY.md` containing:
- Backup path final (incluyendo timestamp real del backup, ej: `/var/backups/erp_sanchez/backup-260502-1530.dump`)
- Tamaño del backup en bytes/MB
- Counts de las 6 tablas críticas (snapshot del estado de prod en el momento del pre-flight)
- Confirmación de drop de `erp_restore_test`
- Próximo paso: Plan 38-02 desbloqueado
</output>
</content>
</invoke>
