---
phase: 38
plan: 03
type: execute
wave: 3
depends_on: ['38-01', '38-02']
files_modified:
  - scripts/audit-drizzle-migrations.sh
  - scripts/insert-drizzle-migrations.sql
  - drizzle.__drizzle_migrations (en VPS prod, vía docker exec — NO un archivo del repo)
autonomous: false
requirements:
  - SC-PROD-AUDIT
  - SC-PROD-INSERT
tags:
  - drizzle
  - postgresql
  - migrations
  - prod
  - audit
security_enforcement: true
user_setup:
  - service: vps-shell
    why: 'El INSERT en drizzle.__drizzle_migrations toca prod. Requiere SSH al VPS y permisos para ejecutar docker exec postgres con rol sanchez.'
    env_vars: []
    dashboard_config:
      - task: 'Confirmar que el backup de Plan 38-01 existe y es restorable antes de ejecutar el INSERT'
        location: 'VPS shell — ls /var/backups/erp_sanchez/'

must_haves:
  truths:
    - 'STAMP ONLY: 0006_baseline.sql NUNCA es ejecutado contra prod; solo se INSERTea su hash en drizzle.__drizzle_migrations (override del orchestrator §1)'
    - 'El AUDIT command consulta drizzle.__drizzle_migrations (schema-qualified, NO public.__drizzle_migrations) — el comando de CONTEXT.md línea 207-210 estaba mal y el plan lo corrige (override del orchestrator §2, RESEARCH §Pitfall 1)'
    - 'Existe scripts/audit-drizzle-migrations.sh que lee drizzle.__drizzle_migrations + calcula sha256 de cada migration file local + emite reporte estructurado de hashes registered vs computed (D-03 paso 1, override del orchestrator §3)'
    - 'Cada hash insertado en __drizzle_migrations matchea exactamente el output de sha256sum apps/backend/drizzle/NNNN_*.sql (override del orchestrator §3)'
    - 'El INSERT es idempotente vía WHERE NOT EXISTS (re-correr el SQL no duplica entries) (D-03 paso 3, RESEARCH §INSERT idempotente)'
    - 'El INSERT NUNCA usa UPDATE/DELETE/TRUNCATE sobre __drizzle_migrations (D-03, feedback_never_drop_tables.md)'
    - 'El plan tiene un human gate explícito ENTRE el AUDIT y el INSERT — el INSERT solo se ejecuta tras aprobación del usuario sobre el reporte AUDIT (D-03 paso 5)'
    - 'Si el AUDIT revela hashes mismatched en migrations 0000-0005 ya registradas (no solo missing), se ESCALA al usuario antes de tocar nada (D-03 paso 4)'
    - 'Post-INSERT, drizzle.__drizzle_migrations tiene >= 7 entries (0000..0006 — pueden ser >7 si Drizzle tiene entries duplicadas legacy, pero sin pérdida)'
    - 'Cada created_at en el INSERT usa el valor exacto de journal.entries[i].when para que drizzle-kit migrate skipee correctamente (RESEARCH §Pitfall 7 + Q2 RESOLVED)'
    - 'W2 fix: el SHA256 que se INSERTa para 0006 se computa contra apps/backend/drizzle/0006_baseline.sql AS PRESENT IN THE REPO post-38-02 (con el header "STAMPED, NOT EXECUTED" presente — el header es PARTE del archivo). El audit script lo calcula con `sha256sum` al runtime sin pre-computación, así que naturalmente honra esta regla. Re-correr drizzle-kit generate en el futuro sin preservar el header divergerá el hash del registrado en prod.'
    - 'W2 fix: el created_at insertado para 0006 es el `when` que drizzle-kit asignó al entry idx 6 de _journal.json durante 38-02 (no Date.now() al INSERT). Esto preserva el ordering del journal y permite que drizzle-kit migrate skipee correctamente.'
  artifacts:
    - path: 'scripts/audit-drizzle-migrations.sh'
      provides: 'Script bash idempotente, read-only, que produce el reporte AUDIT comparando hashes registered vs computed'
      contains: 'drizzle.__drizzle_migrations'
    - path: 'scripts/insert-drizzle-migrations.sql'
      provides: 'SQL idempotente con WHERE NOT EXISTS para INSERT de las entries faltantes en drizzle.__drizzle_migrations. Generado por el audit script con hashes computados al runtime.'
      contains: 'drizzle.__drizzle_migrations'
  key_links:
    - from: 'scripts/audit-drizzle-migrations.sh'
      to: 'drizzle.__drizzle_migrations (en VPS)'
      via: "docker exec postgres psql -c 'SELECT ... FROM drizzle.__drizzle_migrations'"
      pattern: "drizzle\\.__drizzle_migrations"
    - from: 'scripts/insert-drizzle-migrations.sql'
      to: 'drizzle.__drizzle_migrations (en VPS)'
      via: 'docker exec -i postgres psql -d erp_sanchez < scripts/insert-drizzle-migrations.sql'
      pattern: "INSERT INTO drizzle\\.__drizzle_migrations"
    - from: 'scripts/insert-drizzle-migrations.sql'
      to: 'apps/backend/drizzle/meta/_journal.json'
      via: 'created_at = journal.entries[i].when'
      pattern: 'created_at'
    - from: 'scripts/audit-drizzle-migrations.sh'
      to: 'apps/backend/drizzle/0006_baseline.sql (CON header W2)'
      via: 'sha256sum file (lee el archivo as-is del repo, header incluido)'
      pattern: 'sha256sum'
  derived_from:
    - 'D-03 (Audit-first + INSERT idempotente en __drizzle_migrations)'
    - 'Override del orchestrator §1 (STAMP ONLY — 0006_baseline.sql no se ejecuta)'
    - 'Override del orchestrator §2 (drizzle.__drizzle_migrations qualificado)'
    - 'Override del orchestrator §3 (sha256sum reproduce el hash de drizzle-kit)'
    - 'RESEARCH §1 (hash algorithm CONFIRMADO: sha256 del file completo, NO normalizado)'
    - 'RESEARCH §Pitfall 1 (schema qualification obligatoria)'
    - 'RESEARCH §Pitfall 7 (drizzle-kit migrate skipea por created_at, no por hash)'
    - 'RESEARCH §Open Questions Q2 RESOLVED (timestamps locked: 0003=1777809600000, 0004=1777989600000, 0005=1777989600001, 0006=el `when` autogenerado por drizzle-kit en 38-02)'
    - 'feedback_never_drop_tables.md (NUNCA UPDATE/DELETE/TRUNCATE contra __drizzle_migrations)'
    - 'W2 (revisión iter 1): truth de hash-includes-header añadida — alineada con la nueva truth de 38-02 sobre header preservation'
---

<objective>
Stampear las entries 0000..0006 en `drizzle.__drizzle_migrations` de prod usando INSERT idempotente, sin ejecutar ninguno de los archivos SQL. La fase de tracking queda alineada con el filesystem (post-Plan 38-02) y futuras `pnpm db:migrate` no re-ejecutarán las migrations ya aplicadas.

Purpose: Implementar D-03 con human gate explícito entre AUDIT (read-only) y INSERT (write). El AUDIT report se presenta al usuario; solo después de aprobación se ejecuta el INSERT.

Output:

- `scripts/audit-drizzle-migrations.sh`: produce reporte de hashes registered vs computed.
- `scripts/insert-drizzle-migrations.sql`: SQL idempotente con WHERE NOT EXISTS para cada entry faltante. Generado por el audit script con hashes calculados al runtime.
- `drizzle.__drizzle_migrations` en VPS: contiene >= 7 entries para tags 0000..0006 con hashes correctos (incluido 0006 que se computa contra el archivo CON header W2) y created_at = journal.when.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-CONTEXT.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-RESEARCH.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-PATTERNS.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-01-SUMMARY.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-02-SUMMARY.md
@apps/backend/drizzle/meta/_journal.json
@apps/backend/drizzle/0003_add_columna_inv_articulos.sql
@apps/backend/src/db/migration-prod.sql
@scripts/test-auth.sh

<interfaces>
<!-- Tabla destino: drizzle.__drizzle_migrations
     Schema: drizzle (NO public — RESEARCH §Pitfall 1 + override del orchestrator §2)
     Columnas relevantes (per RESEARCH §3 + drizzle-orm/src/pg-core/dialect.ts):
     - id        SERIAL PRIMARY KEY    (auto)
     - hash      TEXT                  (sha256 hex del SQL file)
     - created_at BIGINT               (epoch ms — debe ser >= journal.entries[i].when para que migrate skipee)
-->

<!-- Comando AUDIT canónico (RESEARCH §AUDIT prod) -->

```bash
docker exec postgres psql -U sanchez -d erp_sanchez \
  -c "SELECT id, hash, created_at, to_timestamp(created_at/1000) AS applied_at
      FROM drizzle.__drizzle_migrations
      ORDER BY created_at"
```

<!-- Hash algorithm CONFIRMADO (RESEARCH §1, override del orchestrator §3) -->
<!-- W2 fix: para 0006_baseline.sql, este sha256sum incluye el header "STAMPED, NOT EXECUTED" porque sha256sum lee el archivo as-is (header presente como parte del file). NO normalizar, NO strip-comments. -->

```bash
sha256sum apps/backend/drizzle/0000_open_anita_blake.sql | awk '{print $1}'
sha256sum apps/backend/drizzle/0006_baseline.sql | awk '{print $1}'  # incluye header W2
```

<!-- INSERT idempotente template (RESEARCH §INSERT idempotente, PATTERNS §insert-drizzle-migrations.sql) -->

```sql
INSERT INTO drizzle.__drizzle_migrations (hash, created_at)
SELECT '<sha256-hex>', <epoch-ms-from-journal.when>
WHERE NOT EXISTS (
  SELECT 1 FROM drizzle.__drizzle_migrations WHERE hash = '<sha256-hex>'
);
```

<!-- _journal.json post-Plan 38-02 (7 entries) — fuente de los created_at (Q2 RESOLVED) -->

- entries[0].when = 1772422128557 → 0000_open_anita_blake
- entries[1].when = 1772469220121 → 0001_brief_reaper
- entries[2].when = 1772627204469 → 0002_kind_jamie_braddock
- entries[3].when = 1777809600000 → 0003_add_columna_inv_articulos (Apr 29 14:00 UTC)
- entries[4].when = 1777989600000 → 0004_phase29_propiedades (May 1 16:00 UTC)
- entries[5].when = 1777989600001 → 0005_phase29_cache_trigger (May 1 16:00 UTC + 1ms)
- entries[6].when = <auto-by-drizzle-kit> → 0006_baseline (timestamp del momento en que 38-02 corrió drizzle-kit generate)

<!-- Patrón bash script (PATTERNS.md §Bash script structure) -->

- shebang `#!/usr/bin/env bash` + `set -e`
- Banner `=========================================`
- Glifos `✓` / `❌`
- Exit 1 on failure

<!-- IMPORTANTE: NO hay UNIQUE constraint en hash en __drizzle_migrations (PATTERNS.md §insert-drizzle-migrations.sql + RESEARCH §3) — usar WHERE NOT EXISTS, NO ON CONFLICT (hash) -->
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                                                                                 |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Operador VPS shell → drizzle.\_\_drizzle_migrations | Operador humano ejecuta INSERT con privilegios de rol sanchez. Sin auth runtime, gate enteramente humano.                                                   |
| audit script → repo files (lectura)                 | El script lee `apps/backend/drizzle/*.sql` del repo cloneado en el VPS para calcular hashes. Asume integridad del repo (git verificable).                   |
| insert script → docker exec postgres                | El SQL es ejecutado vía `docker exec -i postgres psql`. Cualquier modificación maliciosa al .sql sería ejecutada con privilegios completos del rol sanchez. |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                                                        |
| --------- | ---------------------- | ------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-05   | Tampering              | scripts/insert-drizzle-migrations.sql | mitigate    | El SQL es generado por el audit script con hashes computados al runtime; no se hardcodean valores literales en el repo. Operador inspecciona el SQL antes de ejecutar (human gate D-03 paso 5).                                        |
| T-38-06   | Elevation of Privilege | INSERT vía docker exec                | mitigate    | Rol sanchez tiene permisos amplios en erp_sanchez. Mitigación: SQL acota su scope a `drizzle.__drizzle_migrations` (schema-qualified) y usa BEGIN/COMMIT explícito; cualquier query fuera de ese scope dispara revisión humana.        |
| T-38-07   | Repudiation            | INSERT sin audit log                  | mitigate    | El SQL incluye un comentario header con timestamp y referencia a Phase 38; el reporte AUDIT post-INSERT registra el cambio de cardinalidad de la tabla; el .sql se commitea al repo para trazabilidad.                                 |
| T-38-08   | Tampering              | UPDATE/DELETE/TRUNCATE accidental     | mitigate    | El SQL hand-crafted contiene SOLO INSERT con WHERE NOT EXISTS. El audit script tiene grep gate explícito (`grep -E '^(UPDATE\|DELETE\|TRUNCATE)' scripts/insert-drizzle-migrations.sql` debe retornar 0 líneas) antes de la ejecución. |
| T-38-09   | Information Disclosure | hash de SQL file en logs CI           | accept      | Los hashes son SHA256 públicos de archivos SQL públicos (en el repo). No revelan secrets. Log público OK.                                                                                                                              |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Crear audit script + generar el insert SQL con hashes computados</name>
  <files>scripts/audit-drizzle-migrations.sh, scripts/insert-drizzle-migrations.sql</files>
  <read_first>
    - scripts/test-auth.sh (patrón canónico bash)
    - apps/backend/drizzle/meta/_journal.json (post-Plan 38-02 con 7 entries — fuente de los `when` para `created_at`)
    - apps/backend/drizzle/0003_add_columna_inv_articulos.sql (header explanatory pattern)
    - apps/backend/drizzle/0006_baseline.sql (W2 fix: contiene el header "STAMPED, NOT EXECUTED" — el sha256sum se computa contra el archivo CON header)
    - apps/backend/src/db/migration-prod.sql (transactional batch pattern + idempotency)
    - .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-RESEARCH.md §1 (hash algorithm), §3 (table location), §Pitfall 1 (schema qualification), §Pitfall 7 (created_at semantics)
    - .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-PATTERNS.md §audit-drizzle-migrations.sh y §insert-drizzle-migrations.sql
    - .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-02-SUMMARY.md (lista de hashes computados — si está disponible, ahorra trabajo; el hash de 0006 documentado allí incluye el header W2)
  </read_first>
  <action>
    **Crear `scripts/audit-drizzle-migrations.sh`** — script READ-ONLY que produce el reporte AUDIT y emite el SQL del INSERT como artefacto derivado. El script DEBE:

    1. Setear `set -e`, banners, glifos `✓`/`❌`.
    2. Verificar que el container postgres está running:
       ```bash
       docker exec postgres pg_isready -U sanchez -d erp_sanchez
       ```
    3. Verificar que el schema y la tabla existen (RESEARCH §Pitfall 1):
       ```bash
       docker exec postgres psql -U sanchez -d erp_sanchez -tAc \
         "SELECT count(*) FROM information_schema.tables WHERE table_schema='drizzle' AND table_name='__drizzle_migrations'"
       ```
       Esperado: `1`. Si retorna `0` → ABORT (la tabla no existe o no se llama así).
    4. Verificar line endings de los SQL files (RESEARCH §Pitfall 4):
       ```bash
       if file apps/backend/drizzle/0*.sql | grep -q "CRLF"; then
         echo "❌ CRLF line endings detected. Run: dos2unix apps/backend/drizzle/*.sql"; exit 1
       fi
       ```
    5. Listar las entries actualmente registradas en `drizzle.__drizzle_migrations`:
       ```bash
       echo "## Currently registered:"
       docker exec postgres psql -U sanchez -d erp_sanchez -c \
         "SELECT id, hash, created_at, to_timestamp(created_at/1000)::text AS applied_at
          FROM drizzle.__drizzle_migrations
          ORDER BY created_at"
       ```
    6. Para cada migration file `apps/backend/drizzle/0000..0006_*.sql`:
       - Calcular hash: `HASH=$(sha256sum "$f" | awk '{print $1}')` — **W2 fix**: para `0006_baseline.sql` esto naturalmente incluye el header porque sha256sum lee el archivo as-is (no strip ni normalización). El audit imprime el hash y el operador lo verifica contra el SUMMARY de 38-02 antes de aprobar.
       - Leer el `when` correspondiente del `_journal.json`:
         ```bash
         WHEN=$(node -e "const j=JSON.parse(require('fs').readFileSync('apps/backend/drizzle/meta/_journal.json'));const e=j.entries.find(x=>x.tag==='$TAG');console.log(e?e.when:'')")
         ```
       - Verificar si está registrada en prod:
         ```bash
         REGISTERED=$(docker exec postgres psql -U sanchez -d erp_sanchez -tAc \
           "SELECT count(*) FROM drizzle.__drizzle_migrations WHERE hash='$HASH'")
         ```
       - Imprimir línea: `"  ✓ 0003_add_columna_inv_articulos  hash=abcd1234... when=1777809600000 status=REGISTERED"` o `"❌ MISSING"`.
    7. Si hay hashes mismatched (algún hash registered NO matchea ningún hash computed) → escalar (echo + exit 1) per D-03 paso 4.
    8. Generar `scripts/insert-drizzle-migrations.sql` como artefacto derivado, con UN bloque INSERT por cada migration MISSING:
       ```sql
       -- Phase 38 D-03: stamp 0000-0006 entries into drizzle.__drizzle_migrations.
       -- Generated by scripts/audit-drizzle-migrations.sh on <DATE>.
       -- Idempotent: each INSERT guarded by WHERE NOT EXISTS so re-running this is safe.
       -- DOES NOT execute any of the 0000-0006 SQL files — pure metadata stamp.
       -- W2 NOTE: hash for 0006_baseline.sql includes the "STAMPED, NOT EXECUTED" header
       -- (sha256sum reads the file as-is). If 0006_baseline.sql is regenerated in the future
       -- without preserving the header, the hash will diverge from what's registered here.
       -- Run via: docker exec -i postgres psql -U sanchez -d erp_sanchez < scripts/insert-drizzle-migrations.sql

       BEGIN;

       -- 0003_add_columna_inv_articulos
       INSERT INTO drizzle.__drizzle_migrations (hash, created_at)
       SELECT '<computed-sha256>', <when-from-journal>
       WHERE NOT EXISTS (
         SELECT 1 FROM drizzle.__drizzle_migrations WHERE hash = '<computed-sha256>'
       );

       -- (un bloque por cada migration faltante)

       -- Verify post-insert:
       SELECT count(*) AS total FROM drizzle.__drizzle_migrations;
       -- Expected: >= 7 (0000..0006)

       COMMIT;
       ```
    9. Imprimir resumen final: `Generated scripts/insert-drizzle-migrations.sql with N missing entries. Review before executing.`

    **Crear `scripts/insert-drizzle-migrations.sql`**: el script en (8) lo emite. Si el directorio `scripts/` no tiene un `.gitignore` para `.sql`, asegurarse que el archivo se commitea para trazabilidad.

    **Hacer ambos archivos commiteables:**

    ```bash
    chmod +x scripts/audit-drizzle-migrations.sh
    git add scripts/audit-drizzle-migrations.sh
    # NO hacer commit todavía — el SQL se commitea después de su generación en Task 2
    ```

    NOTA: el script puede ejecutarse contra prod desde el VPS (asumiendo el repo cloneado allá) o localmente si el operador tiene tunnel SSH al docker postgres. Documentar ambas modalidades en el header del script.

  </action>
  <verify>
    <automated>test -x scripts/audit-drizzle-migrations.sh &amp;&amp; bash -n scripts/audit-drizzle-migrations.sh &amp;&amp; grep -q "set -e" scripts/audit-drizzle-migrations.sh &amp;&amp; grep -q "drizzle\.__drizzle_migrations" scripts/audit-drizzle-migrations.sh &amp;&amp; grep -q "sha256sum" scripts/audit-drizzle-migrations.sh &amp;&amp; ! grep -E "^[^-]*[[:space:]]*(UPDATE|DELETE|TRUNCATE)[[:space:]]+(FROM[[:space:]]+)?(drizzle\.)?__drizzle_migrations" scripts/audit-drizzle-migrations.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/audit-drizzle-migrations.sh` exits 0
    - `bash -n scripts/audit-drizzle-migrations.sh` exits 0 (sintaxis)
    - El audit script usa `drizzle.__drizzle_migrations` (qualified) en TODAS sus queries — verificable: `grep -c "drizzle\.__drizzle_migrations" scripts/audit-drizzle-migrations.sh` retorna >= 2
    - El audit script NO contiene `__drizzle_migrations` SIN el prefijo `drizzle.` — verificable: `grep -v "drizzle\.__drizzle_migrations" scripts/audit-drizzle-migrations.sh | grep -c "__drizzle_migrations"` retorna `0`
    - El audit script NO contiene `UPDATE`/`DELETE`/`TRUNCATE` contra `__drizzle_migrations` (verificable con grep — debe retornar 0 matches)
    - El audit script usa `sha256sum` (algoritmo canónico per RESEARCH §1) — para 0006_baseline.sql, este hash naturalmente incluye el header W2
    - El audit script invoca `node -e` para leer `when` desde `_journal.json` (no hardcodea timestamps)
    - El SQL generado contiene el bloque comment "W2 NOTE" advirtiendo del header dependency
  </acceptance_criteria>
  <done>El audit script existe, es ejecutable, sintaxis válida, usa schema qualification correcta, computa hashes con sha256sum directo (incluye header W2 para 0006), y NO contiene operaciones destructivas. Listo para ejecutar en Task 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Ejecutar AUDIT en prod, presentar reporte, aprobar el INSERT, ejecutarlo</name>
  <files>scripts/insert-drizzle-migrations.sql (generado en runtime), drizzle.__drizzle_migrations en VPS prod</files>
  <action>Ejecutar bash scripts/audit-drizzle-migrations.sh en VPS shell para generar reporte AUDIT + scripts/insert-drizzle-migrations.sql. Inspeccionar el SQL generado verificando schema-qualified y sin DML destructivo. Verificar que el hash de 0006_baseline.sql en el reporte matchea el documentado en 38-02-SUMMARY.md (W2 cross-check). Presentar reporte al desarrollador. Tras aprobación, ejecutar docker exec -i postgres psql -U sanchez -d erp_sanchez < scripts/insert-drizzle-migrations.sql. Re-ejecutar el AUDIT para confirmar 0 MISSING. Test de idempotencia re-ejecutando el INSERT (debe reportar INSERT 0 0).</action>
  <verify>docker exec postgres psql -U sanchez -d erp_sanchez -c "SELECT count(*) FROM drizzle.__drizzle_migrations" retorna >= 7; re-ejecución del AUDIT muestra "0 missing entries"; re-ejecución del INSERT muestra "INSERT 0 0" en cada bloque.</verify>
  <done>drizzle.__drizzle_migrations contiene >= 7 entries (0000..0006), idempotencia validada, hash de 0006 cross-checked contra 38-02-SUMMARY (W2 fix), commit de scripts/insert-drizzle-migrations.sql + scripts/audit-drizzle-migrations.sh creado.</done>
  <what-built>scripts/audit-drizzle-migrations.sh listo. La fase necesita ejecutar el audit, generar el insert SQL, presentar el reporte al usuario, y solo entonces ejecutar el insert.</what-built>
  <how-to-verify>
    **Paso 1 — Verificar el backup de Plan 38-01 está presente:**
    ```bash
    ls -la /var/backups/erp_sanchez/backup-*.dump
    ```
    Debe haber al menos 1 archivo `backup-YYMMDD-HHMM.dump` del día actual o reciente. Si NO hay → re-ejecutar Plan 38-01 antes de continuar.

    **Paso 2 — Ejecutar el AUDIT script (read-only, no toca nada):**
    ```bash
    bash scripts/audit-drizzle-migrations.sh
    ```

    Output esperado:
    1. Sección "Currently registered" mostrando los hashes ya en prod (puede haber 5-6 entries de las migraciones aplicadas en quick tasks 260428-mig y 260429-rec).
    2. Sección "Per-file analysis" mostrando para cada `0000..0006_*.sql`:
       - hash computado
       - status REGISTERED (ya en prod) o MISSING
       - timestamp `when` que se usaría para el INSERT
    3. Sección "Mismatches" — debe estar vacía. Si NO está vacía → escalar al desarrollador, NO continuar.
    4. Mensaje final: "Generated scripts/insert-drizzle-migrations.sql with N missing entries."

    **Paso 2.5 — W2 cross-check del hash de 0006**:
    ```bash
    # El hash impreso por el audit script para 0006_baseline.sql DEBE matchear el documentado en
    # 38-02-SUMMARY.md (que se computó post-header-insertion).
    # Buscar en el SUMMARY:
    grep -A 5 "Hash SHA256" .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-02-SUMMARY.md
    grep "0006_baseline" /tmp/audit-output.txt  # o stdout del audit script
    ```
    Si los hashes NO matchean → ABORT, alguien modificó 0006_baseline.sql post-38-02 (header alterado u otra modificación). Re-ejecutar 38-02 desde Task 2 para regenerar.

    **Paso 3 — Inspeccionar el SQL generado:**
    ```bash
    cat scripts/insert-drizzle-migrations.sql
    ```

    Verificar:
    - Empieza con header comment + `BEGIN;`
    - Contiene el bloque "W2 NOTE" sobre el header dependency
    - Cada INSERT usa `INSERT INTO drizzle.__drizzle_migrations` (qualified)
    - Cada INSERT usa `WHERE NOT EXISTS`
    - NINGUNA línea contiene `UPDATE`, `DELETE`, `TRUNCATE`, `DROP`
    - Termina con `SELECT count(*)` + `COMMIT;`
    - Verificación shell:
      ```bash
      grep -E "^(UPDATE|DELETE|TRUNCATE|DROP)" scripts/insert-drizzle-migrations.sql
      ```
      Debe retornar VACÍO. Si tiene matches → ABORT.

    **Paso 4 — Presentar el reporte al desarrollador (humano):**
    Mostrar el output de Paso 2 + el contenido de Paso 3 + el resultado del cross-check W2 (Paso 2.5). Esperar aprobación explícita ("approved").

    **Paso 5 — Ejecutar el INSERT:**
    ```bash
    docker exec -i postgres psql -U sanchez -d erp_sanchez < scripts/insert-drizzle-migrations.sql
    ```

    Output esperado:
    - `BEGIN`
    - `INSERT 0 1` (× N entries, donde N = missing entries reportadas en Paso 2)
    - `total` con valor >= 7
    - `COMMIT`

    **Paso 6 — Re-ejecutar AUDIT post-INSERT (verificación):**
    ```bash
    bash scripts/audit-drizzle-migrations.sh
    ```

    Verificar:
    - Sección "Per-file analysis": las 7 migrations 0000..0006 ahora están REGISTERED.
    - Sección "Mismatches": vacía.
    - Mensaje final: "Generated scripts/insert-drizzle-migrations.sql with 0 missing entries." (idempotencia confirmada).

    **Paso 7 — Re-ejecutar el INSERT (test de idempotencia):**
    ```bash
    docker exec -i postgres psql -U sanchez -d erp_sanchez < scripts/insert-drizzle-migrations.sql
    ```
    Output esperado: cada INSERT reporta `INSERT 0 0` (no insertó nada). Si reporta `INSERT 0 1` → IDEMPOTENCY FAIL → ABORT y revisar el WHERE NOT EXISTS.

    **Paso 8 — Commit del SQL generado:**
    ```bash
    git add scripts/insert-drizzle-migrations.sql
    gsd-sdk query commit "chore(38): audit drizzle.__drizzle_migrations and insert missing entries (stamp-only)" scripts/audit-drizzle-migrations.sh scripts/insert-drizzle-migrations.sql
    ```

  </how-to-verify>
  <resume-signal>Type "approved" después de Paso 4 (reporte AUDIT + SQL inspeccionados + W2 cross-check OK). Type "abort" si hay mismatches, el SQL contiene comandos destructivos, o el hash de 0006 diverge del SUMMARY de 38-02.</resume-signal>
</task>

</tasks>

<verification>
- audit script existe, es ejecutable, sintaxis válida, qualified schema, no destructivo.
- audit script computa el hash de 0006 con sha256sum directo (header incluido per W2 fix).
- insert SQL existe (post Task 2), es idempotente, qualified schema, no destructivo.
- Insert SQL contiene el bloque "W2 NOTE" advirtiendo del header dependency.
- Post-INSERT en prod: drizzle.__drizzle_migrations tiene >= 7 entries con tags 0000..0006 stampeados.
- Re-ejecución del INSERT no duplica entries (idempotencia validada).
- Cross-check W2: hash de 0006 en el audit matchea el documentado en 38-02-SUMMARY.md.
- Commit creado: `chore(38): audit drizzle.__drizzle_migrations and insert missing entries (stamp-only)`.
</verification>

<success_criteria>

- SC-PROD-AUDIT: Reporte AUDIT producido + diff de hashes calculados vs registrados + cross-check W2 contra 38-02-SUMMARY.
- SC-PROD-INSERT: INSERTs idempotentes ejecutados solo después de aprobación humana sobre el reporte.
  </success_criteria>

<output>
After completion, create `.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-03-SUMMARY.md` containing:
- Output completo del AUDIT pre-INSERT (cuántas entries estaban registered, cuáles MISSING)
- Lista de hashes calculados (snapshot del estado del repo) — incluyendo el de 0006_baseline.sql con la nota W2 "computed against file with header"
- Cross-check W2: hash de 0006 del audit vs hash documentado en 38-02-SUMMARY (deben matchear)
- Lista de entries insertadas (tag, hash, created_at)
- Output del AUDIT post-INSERT (debe mostrar 0 MISSING)
- Confirmación de idempotencia (re-ejecución muestra INSERT 0 0)
- Conteo final de drizzle.__drizzle_migrations
- Próximo paso: Plan 38-04 desbloqueado (verificación triple)
</output>
</content>
</invoke>
