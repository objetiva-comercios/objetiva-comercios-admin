---
phase: 38
plan: 02
type: execute
wave: 2
depends_on: ['38-01']
files_modified:
  - apps/backend/drizzle/meta/_journal.json
  - apps/backend/drizzle/meta/0003_snapshot.json
  - apps/backend/drizzle/meta/0004_snapshot.json
  - apps/backend/drizzle/meta/0005_snapshot.json
  - apps/backend/drizzle/meta/0006_snapshot.json
  - apps/backend/drizzle/0006_baseline.sql
autonomous: false
requirements:
  - SC-JOURNAL
  - SC-SNAPS
  - SC-BASELINE
tags:
  - drizzle
  - migrations
  - journal
  - schema
security_enforcement: false

must_haves:
  truths:
    - 'STAMP ONLY: 0006_baseline.sql NUNCA es ejecutado contra prod; solo su hash se INSERTea en drizzle.__drizzle_migrations en el plan siguiente (D-02 paso 5, override del orchestrator)'
    - '_journal.json local tiene 7 entries (idx 0..6) con tags 0000_open_anita_blake, 0001_brief_reaper, 0002_kind_jamie_braddock, 0003_add_columna_inv_articulos, 0004_phase29_propiedades, 0005_phase29_cache_trigger, y el tag autogenerado de 0006_baseline (D-02 paso 1)'
    - 'Existen 4 snapshots nuevos en meta/ (0003, 0004, 0005 dummies + 0006 generado por drizzle-kit), cada uno con id UUID único y prevId encadenado a la entry anterior (D-02 paso 2, override del orchestrator §4)'
    - 'Los snapshots dummy 0003/0004/0005 son copias bit-a-bit del body de 0002_snapshot.json EXCEPTO los campos id y prevId (D-02 paso 2)'
    - 'El comando pnpm --filter backend db:generate corre exitosamente y emite 0006_baseline.sql + meta/0006_snapshot.json sin errores (D-02 paso 3)'
    - 'El contenido del 0006_baseline.sql es auditado por humano antes del commit; si contiene ALTERs estructurales no esperados se ESCALA al usuario antes de avanzar (D-02 paso 4)'
    - '0006_baseline.sql lleva un header comment explícito declarando STAMPED, NOT EXECUTED y referenciando el ADR (override del orchestrator §1, RESEARCH §2)'
    - 'drizzle-kit check exits 0 después de los cambios — la cadena de snapshots es internamente consistente (RESEARCH §Pitfall 3)'
  artifacts:
    - path: 'apps/backend/drizzle/meta/_journal.json'
      provides: 'Journal con 7 entries en orden por idx; bridge entre filesystem y __drizzle_migrations'
      contains: '0003_add_columna_inv_articulos'
    - path: 'apps/backend/drizzle/meta/0003_snapshot.json'
      provides: 'Snapshot dummy idéntico a 0002 en cuerpo, con UUID único e id prevId chain válida'
      min_size_bytes: 1024
    - path: 'apps/backend/drizzle/meta/0004_snapshot.json'
      provides: 'Snapshot dummy'
      min_size_bytes: 1024
    - path: 'apps/backend/drizzle/meta/0005_snapshot.json'
      provides: 'Snapshot dummy'
      min_size_bytes: 1024
    - path: 'apps/backend/drizzle/meta/0006_snapshot.json'
      provides: 'Snapshot real autogenerado por drizzle-kit reflejando schema.ts actual'
      min_size_bytes: 1024
    - path: 'apps/backend/drizzle/0006_baseline.sql'
      provides: 'SQL emitido por drizzle-kit (CREATE TABLE prop_* + ALTER columna). STAMPED, no ejecutado en prod.'
      contains: 'STAMPED, NOT EXECUTED'
  key_links:
    - from: 'apps/backend/drizzle/meta/_journal.json'
      to: 'apps/backend/drizzle/meta/0003_snapshot.json'
      via: 'tag 0003_add_columna_inv_articulos'
      pattern: '0003_add_columna_inv_articulos'
    - from: 'apps/backend/drizzle/meta/0003_snapshot.json'
      to: 'apps/backend/drizzle/meta/0002_snapshot.json'
      via: 'prevId UUID encadenado'
      pattern: '"prevId"'
    - from: 'apps/backend/drizzle/0006_baseline.sql'
      to: '.planning/decisions/0001-migration-discipline.md'
      via: 'header comment referencia ADR'
      pattern: 'ADR'
  derived_from:
    - 'D-02 (Forward marker + 0006 baseline para arreglar el journal local)'
    - 'Override del orchestrator §1 (NO schema-push auto-injection, STAMP ONLY)'
    - 'Override del orchestrator §4 (Snapshot dummy strategy: copia + regen UUID + re-chain prevId)'
    - 'RESEARCH §2 (drizzle-kit generate behavior con dummies copia-de-0002)'
    - 'RESEARCH Pitfall 3 (UUID chain rota por copy-paste)'
    - 'PATTERNS.md §0006_baseline.sql (header explanatory pattern de 0003_add_columna_inv_articulos.sql)'
---

<objective>
Reconstruir el journal local de Drizzle para que pase de 5 entries inconsistentes a 7 entries consistentes (0000..0006) y generar el `0006_baseline.sql` que será stampeado contra prod en Plan 38-03 SIN ser ejecutado.

Purpose: Implementar D-02 con stamp-only handling. El estado físico de prod ya está correcto (post-260428-mig + 260429-rec + manual May 1); este plan solo sincroniza el tracking local. Al terminar, el repo tiene un journal completo y un baseline auditable.

Output:

- `_journal.json` con 7 entries en orden idx 0..6.
- 3 snapshots dummy nuevos (0003, 0004, 0005) con UUIDs únicos y `prevId` correctamente encadenado.
- `0006_baseline.sql` + `0006_snapshot.json` autogenerados por `drizzle-kit generate`.
- Header comment "STAMPED, NOT EXECUTED" añadido a `0006_baseline.sql`.
- `pnpm --filter backend db:check` corre limpio (drizzle-kit check valida la cadena).
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-CONTEXT.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-RESEARCH.md
@.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-PATTERNS.md
@apps/backend/drizzle/meta/_journal.json
@apps/backend/drizzle/meta/0002_snapshot.json
@apps/backend/drizzle/0003_add_columna_inv_articulos.sql
@apps/backend/drizzle/0004_phase29_propiedades.sql
@apps/backend/drizzle/0005_phase29_cache_trigger.sql

<interfaces>
<!-- Estructura actual de _journal.json (5 entries, falta 0003) -->
<!-- VERIFICAR con Read antes de editar — el archivo cambió a 5 entries con timestamps reales -->

```json
{
  "version": "7",
  "dialect": "postgresql",
  "entries": [
    {
      "idx": 0,
      "version": "7",
      "when": 1772422128557,
      "tag": "0000_open_anita_blake",
      "breakpoints": true
    },
    {
      "idx": 1,
      "version": "7",
      "when": 1772469220121,
      "tag": "0001_brief_reaper",
      "breakpoints": true
    },
    {
      "idx": 2,
      "version": "7",
      "when": 1772627204469,
      "tag": "0002_kind_jamie_braddock",
      "breakpoints": true
    },
    {
      "idx": 3,
      "version": "7",
      "when": 1777569630018,
      "tag": "0004_phase29_propiedades",
      "breakpoints": true
    },
    {
      "idx": 4,
      "version": "7",
      "when": 1777569630019,
      "tag": "0005_phase29_cache_trigger",
      "breakpoints": true
    }
  ]
}
```

<!-- Header de 0002_snapshot.json (las 6 primeras líneas — el resto del body es bit-a-bit lo que copiaremos en los dummies) -->

```json
{
  "id": "8ef42715-a5e4-4fa7-a458-2cfa4f674439",
  "prevId": "af93b79c-8bb8-4ae4-9ca4-ae25dca1fd2f",
  "version": "7",
  "dialect": "postgresql",
  "tables": { ... }
}
```

<!-- Pattern de header comment para 0006_baseline.sql (sintetizado de 0003_add_columna_inv_articulos.sql) -->

```sql
-- 0006_baseline.sql — STAMPED, NOT EXECUTED in production.
-- Applied retroactively as INSERT into drizzle.__drizzle_migrations (Phase 38 D-03).
-- Schema state captured here was already in production via:
--   0003 (manual psql 2026-04-29, quick task 260429-rec)
--   0004 (manual psql 2026-05-01, smoke phase 29)
--   0005 (no-op trigger comment block, manual 2026-05-01)
-- Re-running this file against an aligned DB will fail with "relation already exists".
-- See ADR: .planning/decisions/0001-migration-discipline.md
--> statement-breakpoint
```

<!-- Comando para generar UUID v4 (RESEARCH §2) -->

```bash
node -e "console.log(crypto.randomUUID())"
```

<!-- Timestamps a usar en el journal (per CONTEXT D-02 paso 1 + observación de mtimes en RESEARCH §2 + PATTERNS §_journal.json) -->

- 0003: epoch-ms de 2026-04-29 (matching mtime real del file 0003_add_columna_inv_articulos.sql)
- 0004: epoch-ms de 2026-04-30 / 2026-05-01 (mantener el 1777569630018 actual o recomputar desde mtime real)
- 0005: epoch-ms de 2026-05-01 (mantener 1777569630019 actual o recomputar)
- 0006: AUTO — drizzle-kit generate lo escribe; NO hand-author
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Reparar _journal.json y crear 3 snapshots dummy con UUID chain válida</name>
  <files>
    apps/backend/drizzle/meta/_journal.json,
    apps/backend/drizzle/meta/0003_snapshot.json,
    apps/backend/drizzle/meta/0004_snapshot.json,
    apps/backend/drizzle/meta/0005_snapshot.json
  </files>
  <read_first>
    - apps/backend/drizzle/meta/_journal.json (estado actual antes de editar — confirmar las 5 entries)
    - apps/backend/drizzle/meta/0002_snapshot.json (será copiado 3 veces — verificar su id actual para el prevId chain)
    - apps/backend/drizzle/0003_add_columna_inv_articulos.sql (verificar su mtime con `stat` para timestamp del journal entry)
    - apps/backend/drizzle/0004_phase29_propiedades.sql
    - apps/backend/drizzle/0005_phase29_cache_trigger.sql
    - .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-RESEARCH.md §Pitfall 3 (UUID chain — sin esto drizzle-kit check rechaza)
    - .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-PATTERNS.md §_journal.json y §snapshots dummies
  </read_first>
  <action>
    **Paso A — Generar 3 UUIDs v4 nuevos (uno por snapshot dummy):**

    ```bash
    UUID_0003=$(node -e "console.log(crypto.randomUUID())")
    UUID_0004=$(node -e "console.log(crypto.randomUUID())")
    UUID_0005=$(node -e "console.log(crypto.randomUUID())")
    echo "0003: $UUID_0003"
    echo "0004: $UUID_0004"
    echo "0005: $UUID_0005"
    ```

    Capturar los 3 UUIDs (necesarios para Paso B y para el prevId chain).

    **Paso B — Leer el id actual de 0002_snapshot.json (es el `prevId` del 0003 dummy):**

    Usar Read sobre `apps/backend/drizzle/meta/0002_snapshot.json` y capturar el valor del campo `"id"` en la línea 2. Llamémoslo `UUID_0002`. Por inspección actual: `UUID_0002 = "8ef42715-a5e4-4fa7-a458-2cfa4f674439"` (verificar antes de usar — si no matchea, usar el real).

    **Paso C — Crear los 3 snapshots dummy:**

    ```bash
    cp apps/backend/drizzle/meta/0002_snapshot.json apps/backend/drizzle/meta/0003_snapshot.json
    cp apps/backend/drizzle/meta/0002_snapshot.json apps/backend/drizzle/meta/0004_snapshot.json
    cp apps/backend/drizzle/meta/0002_snapshot.json apps/backend/drizzle/meta/0005_snapshot.json
    ```

    **Paso D — Re-escribir id y prevId en cada dummy** (usando el Edit tool con un solo find/replace por archivo, o `node -e` con fs.readFile/JSON.parse/write):

    Cadena resultante:
    - `0003_snapshot.json`: `id = $UUID_0003`, `prevId = $UUID_0002`
    - `0004_snapshot.json`: `id = $UUID_0004`, `prevId = $UUID_0003`
    - `0005_snapshot.json`: `id = $UUID_0005`, `prevId = $UUID_0004`

    Comando reproducible (ejecutar uno por archivo, sustituyendo valores):

    ```bash
    node -e "
    const fs = require('fs');
    const f = process.argv[1];
    const newId = process.argv[2];
    const newPrev = process.argv[3];
    const j = JSON.parse(fs.readFileSync(f, 'utf8'));
    j.id = newId;
    j.prevId = newPrev;
    fs.writeFileSync(f, JSON.stringify(j, null, '\t'));
    " apps/backend/drizzle/meta/0003_snapshot.json "$UUID_0003" "$UUID_0002"

    node -e "..." apps/backend/drizzle/meta/0004_snapshot.json "$UUID_0004" "$UUID_0003"
    node -e "..." apps/backend/drizzle/meta/0005_snapshot.json "$UUID_0005" "$UUID_0004"
    ```

    **Paso E — Re-escribir _journal.json** para insertar la entry de 0003 entre 0002 y la antigua 0004 (renumerando idx):

    Estado actual (5 entries): idx 0,1,2,3 (= 0004),4 (= 0005).
    Estado objetivo (6 entries — el 0006 lo agregará drizzle-kit generate en Task 2):
    ```json
    {
      "version": "7",
      "dialect": "postgresql",
      "entries": [
        { "idx": 0, "version": "7", "when": 1772422128557, "tag": "0000_open_anita_blake", "breakpoints": true },
        { "idx": 1, "version": "7", "when": 1772469220121, "tag": "0001_brief_reaper", "breakpoints": true },
        { "idx": 2, "version": "7", "when": 1772627204469, "tag": "0002_kind_jamie_braddock", "breakpoints": true },
        { "idx": 3, "version": "7", "when": <epoch-ms-de-mtime-de-0003-sql>, "tag": "0003_add_columna_inv_articulos", "breakpoints": true },
        { "idx": 4, "version": "7", "when": 1777569630018, "tag": "0004_phase29_propiedades", "breakpoints": true },
        { "idx": 5, "version": "7", "when": 1777569630019, "tag": "0005_phase29_cache_trigger", "breakpoints": true }
      ]
    }
    ```

    Para calcular el `when` de 0003: `stat -c %Y apps/backend/drizzle/0003_add_columna_inv_articulos.sql` retorna epoch en segundos; multiplicar por 1000 para ms. Si el mtime no es confiable (file fue tocado por git), usar `Date.parse('2026-04-29T00:32:00Z')` que matchea el mtime histórico documentado en PATTERNS.md.

    Use el tool Write para crear el _journal.json final con las 6 entries en orden por idx (NO usar heredoc — usar Write).

    **Paso F — Verificar la cadena con drizzle-kit check:**

    ```bash
    pnpm --filter backend exec drizzle-kit check
    ```

    Debe exit 0 sin errores. Si reporta "snapshot chain broken", revisar UUIDs (Paso D mal aplicado).

    **Paso G — Commit (NO push):**

    ```bash
    git add apps/backend/drizzle/meta/_journal.json apps/backend/drizzle/meta/0003_snapshot.json apps/backend/drizzle/meta/0004_snapshot.json apps/backend/drizzle/meta/0005_snapshot.json
    gsd-sdk query commit "chore(38): repair local _journal.json with forward markers for 0003-0005" apps/backend/drizzle/meta/_journal.json apps/backend/drizzle/meta/0003_snapshot.json apps/backend/drizzle/meta/0004_snapshot.json apps/backend/drizzle/meta/0005_snapshot.json
    ```

  </action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('apps/backend/drizzle/meta/_journal.json')); if(j.entries.length !== 6) {console.error('expected 6 entries got '+j.entries.length); process.exit(1)} const tags=j.entries.map(e=>e.tag); const expected=['0000_open_anita_blake','0001_brief_reaper','0002_kind_jamie_braddock','0003_add_columna_inv_articulos','0004_phase29_propiedades','0005_phase29_cache_trigger']; if(JSON.stringify(tags)!==JSON.stringify(expected)) {console.error('tags mismatch:',tags); process.exit(1)} console.log('OK')" &amp;&amp; node -e "const fs=require('fs'); const ids=['0002','0003','0004','0005'].map(n=>JSON.parse(fs.readFileSync('apps/backend/drizzle/meta/'+n+'_snapshot.json')).id); if(new Set(ids).size!==4){console.error('duplicate UUIDs:',ids);process.exit(1)} const prevs=['0003','0004','0005'].map(n=>JSON.parse(fs.readFileSync('apps/backend/drizzle/meta/'+n+'_snapshot.json')).prevId); if(prevs[0]!==ids[0]||prevs[1]!==ids[1]||prevs[2]!==ids[2]){console.error('prevId chain broken:',{ids,prevs});process.exit(1)} console.log('UUID chain OK')" &amp;&amp; pnpm --filter backend exec drizzle-kit check</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('apps/backend/drizzle/meta/_journal.json')).entries.length"` retorna `6`
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('apps/backend/drizzle/meta/_journal.json')).entries[3].tag)"` retorna `0003_add_columna_inv_articulos`
    - Los 4 archivos `meta/0002_snapshot.json`, `0003_snapshot.json`, `0004_snapshot.json`, `0005_snapshot.json` tienen 4 UUIDs DISTINTOS en el campo `id`
    - `JSON.parse(0003_snapshot.json).prevId === JSON.parse(0002_snapshot.json).id`
    - `JSON.parse(0004_snapshot.json).prevId === JSON.parse(0003_snapshot.json).id`
    - `JSON.parse(0005_snapshot.json).prevId === JSON.parse(0004_snapshot.json).id`
    - El cuerpo `tables` de 0003/0004/0005 es bit-a-bit idéntico al de 0002 (verificable con `diff <(jq '.tables' 0002_snapshot.json) <(jq '.tables' 0003_snapshot.json)` exit 0)
    - `pnpm --filter backend exec drizzle-kit check` exits 0
    - Commit creado con mensaje `chore(38): repair local _journal.json with forward markers for 0003-0005`
  </acceptance_criteria>
  <done>El journal tiene 6 entries en orden, los 3 snapshots dummy existen con UUIDs únicos correctamente encadenados, drizzle-kit check pasa, y el commit está creado.</done>
</task>

<task type="auto">
  <name>Task 2: Generar 0006_baseline.sql + 0006_snapshot.json y stampear header explicativo</name>
  <files>
    apps/backend/drizzle/0006_baseline.sql,
    apps/backend/drizzle/meta/0006_snapshot.json,
    apps/backend/drizzle/meta/_journal.json
  </files>
  <read_first>
    - apps/backend/drizzle/meta/_journal.json (post-Task-1 con 6 entries — drizzle-kit generate añadirá la 7ª)
    - apps/backend/drizzle/meta/0005_snapshot.json (LAST snapshot que drizzle-kit usará para diff)
    - apps/backend/src/db/schema.ts (fuente de verdad — drizzle compara contra esto)
    - apps/backend/drizzle/0003_add_columna_inv_articulos.sql (patrón de header explanatory)
    - apps/backend/drizzle/0004_phase29_propiedades.sql (forma del SQL que drizzle 0.31.x emite — sin IF NOT EXISTS)
    - .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-RESEARCH.md §Pitfall 2 (Drizzle no emite IF NOT EXISTS — STAMP ONLY obligatorio)
    - .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-RESEARCH.md §2 (alternativa más limpia descartada — aceptamos dummies + stamp-only)
    - .planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-PATTERNS.md §0006_baseline.sql
  </read_first>
  <action>
    **Paso A — Ejecutar drizzle-kit generate desde apps/backend/:**

    ```bash
    cd apps/backend && pnpm drizzle-kit generate
    ```

    Output esperado:
    - Archivos creados: `apps/backend/drizzle/0006_<name>.sql` y `apps/backend/drizzle/meta/0006_snapshot.json`.
    - `_journal.json` actualizado a 7 entries (drizzle-kit añade la entry 0006 con su `when` autogenerado).
    - El nombre canónico de drizzle es `NNNN_<random_words>.sql` — si drizzle elige un nombre como `0006_petite_dragon_man.sql`, hay que renombrarlo a `0006_baseline.sql` Y actualizar el tag en `_journal.json` correspondientemente.

    **Paso B — Renombrar el SQL a `0006_baseline.sql` (si drizzle eligió otro nombre):**

    ```bash
    # Encontrar el archivo nuevo
    ls apps/backend/drizzle/0006_*.sql
    # Si NO es 0006_baseline.sql:
    mv apps/backend/drizzle/0006_<random>.sql apps/backend/drizzle/0006_baseline.sql
    # Editar el tag en _journal.json (entry idx 6) para cambiar "0006_<random>" por "0006_baseline"
    ```

    Editar `_journal.json` para que la entry idx 6 tenga `"tag": "0006_baseline"`. Use Edit tool sobre el archivo (NO heredoc).

    **Paso C — Auditar el contenido de 0006_baseline.sql:**

    ```bash
    cat apps/backend/drizzle/0006_baseline.sql
    ```

    Esperado (per RESEARCH §2):
    - 6 bloques `CREATE TABLE "prop_color"`, `prop_marca`, `prop_material`, `prop_objeto`, `prop_presentacion`, `prop_talle` (con sus indices).
    - `ALTER TABLE "inventarios_articulos" ADD COLUMN "columna" integer;`
    - Cada statement separado por `--> statement-breakpoint`.
    - **NO debe contener:** ALTERs estructurales sobre `articulos`, `existencias`, `comprobantes_*`, `business_settings` u otras tablas que ya existen en prod desde antes.

    Si contiene ALTERs INESPERADOS:
    - **DETENER el plan**.
    - Reportar el diff completo al usuario.
    - Esperar instrucciones (puede ser drift estructural adicional no documentado — escalar per D-02 paso 4).

    Si solo contiene los CREATE TABLE prop_* + ALTER columna esperados → continuar al Paso D.

    **Paso D — Insertar header comment "STAMPED, NOT EXECUTED" al inicio del archivo:**

    Use Edit tool para insertar este bloque ANTES de la primera línea SQL del archivo:

    ```sql
    -- 0006_baseline.sql — STAMPED, NOT EXECUTED in production.
    -- Applied retroactively as INSERT into drizzle.__drizzle_migrations (Phase 38 D-03).
    -- Schema state captured here was already in production via:
    --   0003 (manual psql 2026-04-29, quick task 260429-rec)
    --   0004 (manual psql 2026-05-01, smoke phase 29)
    --   0005 (no-op trigger comment block, manual 2026-05-01)
    -- Re-running this file against an aligned DB will fail with "relation already exists".
    -- See ADR: .planning/decisions/0001-migration-discipline.md
    --> statement-breakpoint
    ```

    Importante: el `--> statement-breakpoint` al final del header es necesario para que Drizzle no concatene el comentario con el primer `CREATE TABLE`.

    **Paso E — Verificar consistencia post-edit:**

    ```bash
    pnpm --filter backend exec drizzle-kit check
    ```

    Si pasa → la cadena snapshots+journal está consistente.

    **Paso F — Commit:**

    ```bash
    git add apps/backend/drizzle/0006_baseline.sql apps/backend/drizzle/meta/0006_snapshot.json apps/backend/drizzle/meta/_journal.json
    gsd-sdk query commit "feat(38): generate 0006_baseline migration to align with prod state (stamped not executed)" apps/backend/drizzle/0006_baseline.sql apps/backend/drizzle/meta/0006_snapshot.json apps/backend/drizzle/meta/_journal.json
    ```

    NO ejecutar el SQL contra ninguna DB en este task. El stamp en prod ocurre en Plan 38-03.

  </action>
  <verify>
    <automated>test -f apps/backend/drizzle/0006_baseline.sql &amp;&amp; test -f apps/backend/drizzle/meta/0006_snapshot.json &amp;&amp; grep -q "STAMPED, NOT EXECUTED" apps/backend/drizzle/0006_baseline.sql &amp;&amp; grep -q "0001-migration-discipline.md" apps/backend/drizzle/0006_baseline.sql &amp;&amp; node -e "const j=JSON.parse(require('fs').readFileSync('apps/backend/drizzle/meta/_journal.json')); if(j.entries.length!==7){console.error('expected 7 entries got '+j.entries.length);process.exit(1)} if(j.entries[6].tag!=='0006_baseline'){console.error('entry 6 tag is '+j.entries[6].tag);process.exit(1)} console.log('OK')" &amp;&amp; pnpm --filter backend exec drizzle-kit check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/drizzle/0006_baseline.sql` existe y contiene `STAMPED, NOT EXECUTED in production.` en las primeras 10 líneas
    - `apps/backend/drizzle/0006_baseline.sql` referencia `.planning/decisions/0001-migration-discipline.md` en el header
    - `apps/backend/drizzle/meta/0006_snapshot.json` existe con campo `id` UUID único y `prevId` = `id` de `0005_snapshot.json`
    - `apps/backend/drizzle/meta/_journal.json` tiene 7 entries (idx 0..6); la entry 6 tiene `tag: "0006_baseline"`
    - El contenido de `0006_baseline.sql` (excluyendo header comments) contiene `CREATE TABLE "prop_color"` y `ALTER TABLE "inventarios_articulos" ADD COLUMN "columna"` (verificable con `grep -E '^(CREATE TABLE|ALTER TABLE)' apps/backend/drizzle/0006_baseline.sql | wc -l` retorna >= 7)
    - El contenido de `0006_baseline.sql` NO contiene ALTERs sobre `articulos`, `existencias`, `comprobantes_*`, `business_settings` (verificable con `grep -E "ALTER TABLE \"(articulos|existencias|comprobantes|business_settings)\"" apps/backend/drizzle/0006_baseline.sql | wc -l` retorna `0`)
    - `pnpm --filter backend exec drizzle-kit check` exits 0
    - Commit creado con mensaje conventional commit prefijo `feat(38):`
  </acceptance_criteria>
  <done>0006_baseline.sql generado y stampeado con header "STAMPED, NOT EXECUTED", 0006_snapshot.json autogenerado, _journal.json tiene 7 entries, drizzle-kit check pasa, commit creado. Plan 38-03 puede consumir estos artefactos para calcular hashes y stampear en prod.</done>
</task>

</tasks>

<verification>
- 4 snapshots nuevos (0003, 0004, 0005 dummies + 0006 real) con UUID chain válida.
- _journal.json tiene 7 entries en orden por idx con tags correctos.
- 0006_baseline.sql contiene SOLO CREATE TABLE prop_* + ALTER columna (no otras ALTERs).
- 0006_baseline.sql lleva header explícito de STAMP-ONLY referenciando ADR.
- drizzle-kit check pasa.
- 2 commits (uno por task) en git log con prefijo `chore(38):` y `feat(38):`.
</verification>

<success_criteria>

- SC-JOURNAL: \_journal.json local tiene 7 entries (0000-0006) con campos correctos.
- SC-SNAPS: meta/0003, 0004, 0005 snapshot.json existen como dummies con UUIDs únicos y prevId chain válida.
- SC-BASELINE: 0006*baseline.sql generado por db:generate y auditado (contenido = CREATE TABLE prop*\* + ALTER columna como esperado, requiere stamp-only handling).
  </success_criteria>

<output>
After completion, create `.planning/phases/38-reconciliar-drift-sistemico-de-db-de-produccion/38-02-SUMMARY.md` containing:
- UUIDs generados para cada dummy snapshot
- Timestamp `when` finalizado para entry 0003 en _journal.json
- Output del audit de 0006_baseline.sql (lista de CREATE TABLE + ALTER emitidos)
- Confirmación de drizzle-kit check exit 0
- Hash SHA256 calculado de cada archivo SQL 0000..0006 (insumo directo para Plan 38-03):
  ```
  for f in apps/backend/drizzle/0*.sql; do
    echo "$(sha256sum "$f" | awk '{print $1}')  $(basename "$f")"
  done
  ```
- Próximo paso: Plan 38-03 desbloqueado
</output>
</content>
</invoke>
