---
phase: 03-web-application
plan: 03
type: execute
wave: 2
depends_on: ['03-01', '03-02']
files_modified:
  - apps/web/src/app/(dashboard)/layout.tsx
  - apps/web/src/components/layout/sidebar.tsx
  - apps/web/src/components/layout/header.tsx
  - apps/web/src/components/layout/mobile-nav.tsx
  - apps/web/src/components/layout/user-menu.tsx
  - apps/web/src/components/layout/theme-toggle.tsx
  - apps/web/src/components/ui/sheet.tsx
  - apps/web/src/components/ui/dropdown-menu.tsx
  - apps/web/src/components/ui/avatar.tsx
  - apps/web/src/components/ui/separator.tsx
autonomous: true

must_haves:
  truths:
    - 'Authenticated user sees persistent sidebar on desktop'
    - 'Sidebar collapses on tablet and uses hamburger on mobile'
    - 'All navigation sections visible in sidebar (Dashboard, Articles, Purchases, Sales, Orders, Inventory, Settings)'
    - 'User can toggle dark/light theme from header'
    - 'User can log out from user menu in header'
    - "Navigation state persists across page navigation (layout doesn't remount)"
  artifacts:
    - path: 'apps/web/src/app/(dashboard)/layout.tsx'
      provides: 'Dashboard route group layout'
      min_lines: 30
    - path: 'apps/web/src/components/layout/sidebar.tsx'
      provides: 'Responsive sidebar navigation'
      min_lines: 50
    - path: 'apps/web/src/components/layout/header.tsx'
      provides: 'Header with user menu and theme toggle'
      min_lines: 30
  key_links:
    - from: 'apps/web/src/app/(dashboard)/layout.tsx'
      to: 'apps/web/src/components/layout/sidebar.tsx'
      via: 'Sidebar component import'
      pattern: 'Sidebar'
    - from: 'apps/web/src/components/layout/user-menu.tsx'
      to: 'apps/web/src/lib/supabase/client.ts'
      via: 'signOut on logout'
      pattern: 'signOut'
---

<objective>
Create the dashboard layout shell with responsive sidebar navigation, header, and user menu.

Purpose: Establish the persistent layout structure that all operational sections (Dashboard, Articles, etc.) will render within. The sidebar provides navigation, header shows user actions, and the layout adapts automatically to screen sizes.

Output: Working dashboard layout with sidebar navigation showing all 7 sections, responsive behavior (full sidebar on desktop, collapsed on tablet, hamburger on mobile), user menu with logout, and theme toggle.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-application/03-RESEARCH.md
@.planning/phases/03-web-application/03-CONTEXT.md
@apps/web/src/lib/supabase/client.ts
@apps/web/src/components/providers/theme-provider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard layout and sidebar navigation</name>
  <files>
    apps/web/src/app/(dashboard)/layout.tsx
    apps/web/src/components/layout/sidebar.tsx
    apps/web/src/components/layout/nav-item.tsx
    apps/web/src/components/ui/sheet.tsx
    apps/web/src/components/ui/separator.tsx
    apps/web/src/components/ui/scroll-area.tsx
  </files>
  <action>
    1. Add required shadcn/ui components:
       ```bash
       cd apps/web && npx shadcn@latest add sheet separator scroll-area tooltip
       ```

    2. Create sidebar component at `src/components/layout/sidebar.tsx`:
       - "use client" directive (needs pathname for active state)
       - Import usePathname from next/navigation
       - Import lucide-react icons: LayoutDashboard, Package, ShoppingCart, ShoppingBag, ClipboardList, Warehouse, Settings
       - Define routes array with: label, icon, href for each section:
         - Dashboard: /dashboard, LayoutDashboard
         - Articles: /articles, Package
         - Purchases: /purchases, ShoppingCart
         - Sales: /sales, ShoppingBag
         - Orders: /orders, ClipboardList
         - Inventory: /inventory, Warehouse
         - Settings: /settings, Settings
       - Render sidebar with:
         - Logo/brand area at top (h-14 border-b)
         - Navigation links in ScrollArea
         - Active state: bg-primary text-primary-foreground
         - Hover state: hover:bg-accent
         - Icons with labels
       - Accept className prop for responsive visibility
       - Width: w-64 on desktop

    3. Create dashboard layout at `src/app/(dashboard)/layout.tsx`:
       - Server Component (no "use client" needed for layout structure)
       - Check auth state: create server Supabase client, get user
       - If no user: redirect to /login
       - Render layout structure:
         - Flex container h-screen overflow-hidden
         - Sidebar (hidden md:flex for responsive)
         - Main area with Header and content
       - Pass children to main content area
       - Content area: flex-1 overflow-y-auto with padding

    4. Create placeholder pages for each section (just to make routes work):
       - src/app/(dashboard)/dashboard/page.tsx - "Dashboard coming soon"
       - src/app/(dashboard)/articles/page.tsx - "Articles coming soon"
       - src/app/(dashboard)/purchases/page.tsx - "Purchases coming soon"
       - src/app/(dashboard)/sales/page.tsx - "Sales coming soon"
       - src/app/(dashboard)/orders/page.tsx - "Orders coming soon"
       - src/app/(dashboard)/inventory/page.tsx - "Inventory coming soon"
       - src/app/(dashboard)/settings/page.tsx - "Settings coming soon"
       - These are minimal placeholder pages that will be replaced in later plans

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - /dashboard route renders with sidebar
    - All 7 navigation items visible in sidebar
    - Active state highlights current route
    - Sidebar hidden on mobile (< md breakpoint)
  </verify>
  <done>
    Dashboard layout shell with persistent sidebar navigation. All 7 sections have routes and navigation links. Sidebar responsive - visible on desktop, hidden on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create header with user menu and mobile navigation</name>
  <files>
    apps/web/src/components/layout/header.tsx
    apps/web/src/components/layout/mobile-nav.tsx
    apps/web/src/components/layout/user-menu.tsx
    apps/web/src/components/layout/theme-toggle.tsx
    apps/web/src/components/ui/dropdown-menu.tsx
    apps/web/src/components/ui/avatar.tsx
    apps/web/src/components/ui/button.tsx
    apps/web/src/app/(dashboard)/layout.tsx
  </files>
  <action>
    1. Add required shadcn/ui components:
       ```bash
       cd apps/web && npx shadcn@latest add dropdown-menu avatar
       ```

    2. Create theme toggle at `src/components/layout/theme-toggle.tsx`:
       - "use client" directive
       - Import useTheme from next-themes
       - Import Sun, Moon icons from lucide-react
       - Render Button with ghost variant, icon size
       - Toggle between light/dark on click
       - Use CSS transitions for icon swap (Sun visible in light, Moon in dark)

    3. Create user menu at `src/components/layout/user-menu.tsx`:
       - "use client" directive
       - Import DropdownMenu components from shadcn/ui
       - Import useRouter from next/navigation
       - Import createClient from supabase/client
       - Accept user prop (email, name, avatar_url optional)
       - Render Avatar with user initials or image
       - DropdownMenu with:
         - Header showing user email
         - Separator
         - Profile link to /settings
         - Separator
         - Log out button
       - On logout:
         - Create Supabase client
         - Call supabase.auth.signOut()
         - router.push('/login')
         - router.refresh()

    4. Create mobile navigation at `src/components/layout/mobile-nav.tsx`:
       - "use client" directive
       - Import Sheet, SheetContent, SheetTrigger from shadcn/ui
       - Import Menu icon from lucide-react
       - Reuse routes array from sidebar (extract to shared config)
       - Render hamburger button (visible md:hidden)
       - Sheet opens from left with full navigation
       - Close sheet on navigation (use onOpenChange)

    5. Create header at `src/components/layout/header.tsx`:
       - Accept user prop for user menu
       - Render header bar with:
         - Mobile nav trigger (md:hidden)
         - Optional breadcrumb area (can be placeholder)
         - Spacer (flex-1)
         - Theme toggle
         - User menu
       - Sticky header with border-b

    6. Update dashboard layout:
       - Import Header component
       - Pass user data to Header
       - Render Header above main content
       - Integrate MobileNav for responsive navigation

    7. Create shared nav config at `src/config/navigation.ts`:
       - Export routes array used by both Sidebar and MobileNav
       - Single source of truth for navigation items

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Header renders with theme toggle and user menu
    - User menu shows user email and logout option
    - Logout redirects to /login
    - Mobile hamburger menu appears on small screens
    - Mobile nav sheet shows all navigation items
    - Theme toggle switches between light/dark
  </verify>
  <done>
    Complete layout shell with responsive navigation. Desktop shows sidebar, mobile shows hamburger menu. Header includes theme toggle and user menu with logout. All 7 sections navigable.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build --filter=@objetiva/web` succeeds
2. `pnpm dev --filter=@objetiva/web` runs without errors
3. /dashboard renders with sidebar on desktop
4. All 7 sections visible in sidebar navigation
5. Clicking navigation items changes route (active state updates)
6. Theme toggle changes between light and dark
7. User menu shows and logout works
8. Mobile view shows hamburger menu instead of sidebar
9. Mobile nav sheet opens with all navigation items
</verification>

<success_criteria>

- Dashboard layout renders for authenticated users
- Unauthenticated users redirected to /login
- Sidebar navigation with 7 sections (Dashboard, Articles, Purchases, Sales, Orders, Inventory, Settings)
- Active navigation state based on current route
- Responsive behavior: sidebar on desktop, hamburger menu on mobile
- Header with theme toggle and user menu
- Logout functionality works and redirects to /login
- Requirements covered: NAV-03 (sidebar), NAV-04 (consistent nav), NAV-05 (header), NAV-06 (content area), NAV-07 (all sections), AUTH-04 (logout)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-web-application/03-03-SUMMARY.md`
</output>
