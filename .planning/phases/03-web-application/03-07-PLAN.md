---
phase: 03-web-application
plan: 07
type: execute
wave: 3
depends_on: ['03-05']
files_modified:
  - apps/web/src/app/(dashboard)/sales/page.tsx
  - apps/web/src/app/(dashboard)/sales/loading.tsx
  - apps/web/src/components/tables/sales/columns.tsx
  - apps/web/src/components/tables/sales/sale-sheet.tsx
  - apps/web/src/app/(dashboard)/purchases/page.tsx
  - apps/web/src/app/(dashboard)/purchases/loading.tsx
  - apps/web/src/components/tables/purchases/columns.tsx
  - apps/web/src/components/tables/purchases/purchase-sheet.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/types/sale.ts
  - apps/web/src/types/purchase.ts
autonomous: true

must_haves:
  truths:
    - 'Sales page displays sales transactions from backend'
    - 'Purchases page displays purchase transactions from backend'
    - 'Both tables support sorting, filtering, and pagination'
    - 'Row clicks open detail side panels'
    - 'Transaction totals are formatted as currency'
  artifacts:
    - path: 'apps/web/src/app/(dashboard)/sales/page.tsx'
      provides: 'Sales page with data fetching'
      min_lines: 30
    - path: 'apps/web/src/app/(dashboard)/purchases/page.tsx'
      provides: 'Purchases page with data fetching'
      min_lines: 30
    - path: 'apps/web/src/components/tables/sales/columns.tsx'
      provides: 'Sales table column definitions'
      min_lines: 40
  key_links:
    - from: 'apps/web/src/app/(dashboard)/sales/page.tsx'
      to: 'http://localhost:3001/sales'
      via: 'fetch sales endpoint'
      pattern: 'fetch.*sales'
    - from: 'apps/web/src/app/(dashboard)/purchases/page.tsx'
      to: 'http://localhost:3001/purchases'
      via: 'fetch purchases endpoint'
      pattern: 'fetch.*purchases'
---

<objective>
Build the Sales and Purchases sections using the DataTable pattern.

Purpose: Complete the transactional data views. Sales tracks revenue transactions with customers, Purchases tracks cost transactions with suppliers. Both continue the DataTable pattern with section-specific columns and detail sheets.

Output: Working Sales and Purchases pages with tables, sorting, filtering, pagination, and detail side panels. Both fetch from backend API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-application/03-RESEARCH.md
@.planning/phases/02-backend-api-with-mock-data/02-04-SUMMARY.md
@apps/web/src/components/tables/data-table.tsx
@apps/web/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sales section with data table</name>
  <files>
    apps/web/src/app/(dashboard)/sales/page.tsx
    apps/web/src/app/(dashboard)/sales/loading.tsx
    apps/web/src/components/tables/sales/columns.tsx
    apps/web/src/components/tables/sales/sale-sheet.tsx
    apps/web/src/lib/api.ts
    apps/web/src/types/sale.ts
  </files>
  <action>
    1. Create Sale type at `src/types/sale.ts`:
       - Match backend Sale entity:
         - id: string
         - saleNumber: string
         - customerName: string
         - customerEmail: string
         - items: SaleItem[] (id, productId, productName, quantity, unitPrice, total)
         - subtotal: number
         - tax: number
         - discount: number
         - total: number
         - paymentMethod: 'cash' | 'card' | 'transfer' | 'credit'
         - status: 'completed' | 'refunded' | 'partial_refund'
         - notes: string
         - createdAt: string

    2. Update API client `src/lib/api.ts`:
       - Add fetchSales(params?) function:
         - Fetch from /sales endpoint
         - Support: page, limit, status, paymentMethod filters
         - Return typed response

    3. Create sales columns at `src/components/tables/sales/columns.tsx`:
       - "use client" directive
       - Define columns:
         - Sale Number: sortable
         - Customer: sortable
         - Items: count
         - Total: formatted currency, sortable
         - Payment Method: badge or text
         - Status: badge with colors:
           - completed: green
           - refunded: red
           - partial_refund: yellow
         - Date: formatted, sortable (createdAt)
       - Default sort by date descending

    4. Create SaleSheet at `src/components/tables/sales/sale-sheet.tsx`:
       - Display sale details:
         - Header: Sale number and status badge
         - Customer info
         - Items list with line totals
         - Subtotal, Tax, Discount, Total
         - Payment method
         - Notes (if any)
         - Timestamp
       - Read-only view

    5. Create Sales page at `src/app/(dashboard)/sales/page.tsx`:
       - Similar pattern to previous pages
       - Server fetches, client handles interactivity
       - SalesClient with table and sheet state
       - Page header: "Sales"
       - Optional: Total revenue summary at top

    6. Create loading state at `src/app/(dashboard)/sales/loading.tsx`:
       - Skeleton matching sales table layout

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Navigate to /sales
    - Table shows sales transactions
    - Currency formatting correct
    - Status badges have correct colors
    - Row click opens sale detail sheet
  </verify>
  <done>
    Sales page complete with transaction table. Shows sale number, customer, items, total, payment method, status, and date. Row click reveals full sale details.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Purchases section with data table</name>
  <files>
    apps/web/src/app/(dashboard)/purchases/page.tsx
    apps/web/src/app/(dashboard)/purchases/loading.tsx
    apps/web/src/components/tables/purchases/columns.tsx
    apps/web/src/components/tables/purchases/purchase-sheet.tsx
    apps/web/src/lib/api.ts
    apps/web/src/types/purchase.ts
  </files>
  <action>
    1. Create Purchase type at `src/types/purchase.ts`:
       - Match backend Purchase entity:
         - id: string
         - purchaseNumber: string
         - supplierName: string
         - supplierContact: string
         - items: PurchaseItem[] (id, productId, productName, quantity, unitCost, total)
         - subtotal: number
         - tax: number
         - shipping: number
         - total: number
         - status: 'pending' | 'ordered' | 'received' | 'cancelled'
         - expectedDelivery: string
         - receivedAt: string | null
         - notes: string
         - createdAt: string

    2. Update API client `src/lib/api.ts`:
       - Add fetchPurchases(params?) function:
         - Fetch from /purchases endpoint
         - Support: page, limit, status filter
         - Return typed response

    3. Create purchases columns at `src/components/tables/purchases/columns.tsx`:
       - "use client" directive
       - Define columns:
         - Purchase Number: sortable
         - Supplier: sortable
         - Items: count
         - Total: formatted currency, sortable
         - Status: badge with colors:
           - pending: yellow
           - ordered: blue
           - received: green
           - cancelled: red
         - Expected Delivery: formatted date
         - Date: formatted, sortable (createdAt)
       - Default sort by date descending

    4. Create PurchaseSheet at `src/components/tables/purchases/purchase-sheet.tsx`:
       - Display purchase details:
         - Header: Purchase number and status badge
         - Supplier info
         - Items list with line totals
         - Subtotal, Tax, Shipping, Total
         - Expected delivery date
         - Received date (if received)
         - Notes (if any)
         - Timestamps
       - Read-only view

    5. Create Purchases page at `src/app/(dashboard)/purchases/page.tsx`:
       - Similar pattern to Sales page
       - Server fetches, client handles interactivity
       - PurchasesClient with table and sheet state
       - Page header: "Purchases"
       - Optional: Pending purchases count at top

    6. Create loading state at `src/app/(dashboard)/purchases/loading.tsx`:
       - Skeleton matching purchases table layout

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Navigate to /purchases
    - Table shows purchase transactions
    - Currency formatting correct
    - Status badges have correct colors
    - Row click opens purchase detail sheet
  </verify>
  <done>
    Purchases page complete with transaction table. Shows purchase number, supplier, items, total, status, and dates. Row click reveals full purchase details.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build --filter=@objetiva/web` succeeds
2. Backend and web running
3. Navigate to /sales after login
4. Sales table renders with data from backend
5. Sale totals formatted as currency
6. Click sale row opens detail sheet
7. Navigate to /purchases
8. Purchases table renders with data
9. Purchase status badges have correct colors
10. Click purchase row opens detail sheet
11. All tables support sorting, filtering, pagination
</verification>

<success_criteria>

- Sales page displays sales from backend /sales endpoint
- Sales columns: Sale Number, Customer, Items, Total, Payment Method, Status, Date
- Sale status badges with appropriate colors
- Sale detail sheet shows items, totals, payment info
- Purchases page displays purchases from backend /purchases endpoint
- Purchases columns: Purchase Number, Supplier, Items, Total, Status, Expected Delivery, Date
- Purchase status badges with appropriate colors
- Purchase detail sheet shows items, totals, supplier info
- Currency formatting consistent across all monetary values
- Both sections reuse DataTable component pattern
  </success_criteria>

<output>
After completion, create `.planning/phases/03-web-application/03-07-SUMMARY.md`
</output>
