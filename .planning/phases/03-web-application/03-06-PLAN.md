---
phase: 03-web-application
plan: 06
type: execute
wave: 3
depends_on: ['03-05']
files_modified:
  - apps/web/src/app/(dashboard)/orders/page.tsx
  - apps/web/src/app/(dashboard)/orders/loading.tsx
  - apps/web/src/components/tables/orders/columns.tsx
  - apps/web/src/components/tables/orders/order-sheet.tsx
  - apps/web/src/app/(dashboard)/inventory/page.tsx
  - apps/web/src/app/(dashboard)/inventory/loading.tsx
  - apps/web/src/components/tables/inventory/columns.tsx
  - apps/web/src/components/tables/inventory/inventory-sheet.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/types/order.ts
  - apps/web/src/types/inventory.ts
autonomous: true

must_haves:
  truths:
    - 'Orders page displays orders table with data from backend'
    - 'Inventory page displays inventory table with data from backend'
    - 'Both tables support sorting, filtering, and pagination'
    - 'Row clicks open detail side panels'
    - 'Status badges show appropriate colors'
  artifacts:
    - path: 'apps/web/src/app/(dashboard)/orders/page.tsx'
      provides: 'Orders page with data fetching'
      min_lines: 30
    - path: 'apps/web/src/app/(dashboard)/inventory/page.tsx'
      provides: 'Inventory page with data fetching'
      min_lines: 30
    - path: 'apps/web/src/components/tables/orders/columns.tsx'
      provides: 'Order table column definitions'
      min_lines: 40
  key_links:
    - from: 'apps/web/src/app/(dashboard)/orders/page.tsx'
      to: 'http://localhost:3001/orders'
      via: 'fetch orders endpoint'
      pattern: 'fetch.*orders'
    - from: 'apps/web/src/app/(dashboard)/inventory/page.tsx'
      to: 'http://localhost:3001/inventory'
      via: 'fetch inventory endpoint'
      pattern: 'fetch.*inventory'
---

<objective>
Build the Orders and Inventory sections using the DataTable pattern established in Plan 05.

Purpose: Extend the data table pattern to two more operational sections. Orders tracks customer orders with status workflow, Inventory shows stock levels with low stock highlighting. Both reuse the DataTable component with section-specific columns and detail sheets.

Output: Working Orders and Inventory pages with tables, sorting, filtering, pagination, and detail side panels. Both fetch from backend API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-application/03-RESEARCH.md
@.planning/phases/02-backend-api-with-mock-data/02-03-SUMMARY.md
@apps/web/src/components/tables/data-table.tsx
@apps/web/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Orders section with data table</name>
  <files>
    apps/web/src/app/(dashboard)/orders/page.tsx
    apps/web/src/app/(dashboard)/orders/loading.tsx
    apps/web/src/components/tables/orders/columns.tsx
    apps/web/src/components/tables/orders/order-sheet.tsx
    apps/web/src/lib/api.ts
    apps/web/src/types/order.ts
  </files>
  <action>
    1. Create Order type at `src/types/order.ts`:
       - Match backend Order entity:
         - id: string
         - orderNumber: string
         - customerName: string
         - customerEmail: string
         - status: 'pending' | 'processing' | 'shipped' | 'delivered' | 'cancelled'
         - items: OrderItem[] (id, productId, productName, quantity, price)
         - subtotal: number
         - tax: number
         - total: number
         - shippingAddress: string
         - createdAt: string
         - updatedAt: string

    2. Update API client `src/lib/api.ts`:
       - Add fetchOrders(params?) function:
         - Fetch from /orders endpoint
         - Support: page, limit, status filter
         - Return typed response

    3. Create order columns at `src/components/tables/orders/columns.tsx`:
       - "use client" directive
       - Define columns:
         - Order Number: sortable (link-style text)
         - Customer: sortable
         - Items: count of items
         - Total: formatted currency, sortable
         - Status: badge with colors:
           - pending: yellow
           - processing: blue
           - shipped: purple
           - delivered: green
           - cancelled: red
         - Date: formatted, sortable (createdAt)
       - Sort by date descending by default (most recent first)

    4. Create OrderSheet at `src/components/tables/orders/order-sheet.tsx`:
       - Display order details:
         - Header: Order number and status badge
         - Customer info section
         - Items list with quantities and prices
         - Subtotal, Tax, Total
         - Shipping address
         - Timestamps
       - Read-only view

    5. Create Orders page at `src/app/(dashboard)/orders/page.tsx`:
       - Similar pattern to Articles page
       - Server fetches data, passes to client component
       - OrdersClient handles table and sheet state
       - Page header: "Orders"
       - Optional status filter tabs/badges above table

    6. Create loading state at `src/app/(dashboard)/orders/loading.tsx`:
       - Skeleton matching orders table layout

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Navigate to /orders
    - Table shows orders with all columns
    - Status badges have correct colors
    - Sorting and filtering work
    - Row click opens order detail sheet
  </verify>
  <done>
    Orders page complete with data table. Shows order number, customer, items, total, status, and date. Row click reveals full order details in side panel.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Inventory section with data table</name>
  <files>
    apps/web/src/app/(dashboard)/inventory/page.tsx
    apps/web/src/app/(dashboard)/inventory/loading.tsx
    apps/web/src/components/tables/inventory/columns.tsx
    apps/web/src/components/tables/inventory/inventory-sheet.tsx
    apps/web/src/lib/api.ts
    apps/web/src/types/inventory.ts
  </files>
  <action>
    1. Create Inventory type at `src/types/inventory.ts`:
       - Match backend Inventory entity:
         - id: string
         - productId: string
         - productName: string
         - sku: string
         - quantity: number
         - reservedQuantity: number
         - availableQuantity: number
         - reorderPoint: number
         - status: 'in_stock' | 'low_stock' | 'out_of_stock'
         - lastRestocked: string
         - updatedAt: string

    2. Update API client `src/lib/api.ts`:
       - Add fetchInventory(params?) function:
         - Fetch from /inventory endpoint
         - Support: page, limit, status filter
         - Return typed response
       - Add fetchLowStock() function:
         - Fetch from /inventory/low-stock endpoint
         - Returns items needing attention

    3. Create inventory columns at `src/components/tables/inventory/columns.tsx`:
       - "use client" directive
       - Define columns:
         - SKU: sortable
         - Product Name: sortable, filterable
         - Quantity: sortable, with visual indicator
         - Reserved: number
         - Available: calculated (quantity - reserved)
         - Reorder Point: number
         - Status: badge with colors:
           - in_stock: green
           - low_stock: yellow/warning
           - out_of_stock: red/destructive
         - Last Restocked: formatted date
       - Highlight rows with low_stock or out_of_stock status (optional background tint)

    4. Create InventorySheet at `src/components/tables/inventory/inventory-sheet.tsx`:
       - Display inventory details:
         - Header: Product name and SKU
         - Quantity breakdown (total, reserved, available)
         - Reorder point with comparison
         - Status badge
         - Last restocked date
         - Link to product (Articles section)
       - Read-only view

    5. Create Inventory page at `src/app/(dashboard)/inventory/page.tsx`:
       - Similar pattern to Orders page
       - Server fetches data, passes to client
       - InventoryClient handles table and sheet
       - Page header: "Inventory"
       - Optional: Quick filter for "Low Stock" items
       - Consider stats row at top: Total Items, Low Stock Count, Out of Stock Count

    6. Create loading state at `src/app/(dashboard)/inventory/loading.tsx`:
       - Skeleton matching inventory table layout

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Navigate to /inventory
    - Table shows inventory with all columns
    - Status badges have correct colors
    - Low stock rows are visually distinguishable
    - Sorting and filtering work
    - Row click opens inventory detail sheet
  </verify>
  <done>
    Inventory page complete with data table. Shows SKU, product, quantities, status. Low stock items highlighted for quick identification. Row click reveals full inventory details.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build --filter=@objetiva/web` succeeds
2. Backend and web running
3. Navigate to /orders after login
4. Orders table renders with data from backend
5. Order status badges have correct colors
6. Click order row opens detail sheet
7. Navigate to /inventory
8. Inventory table renders with data
9. Low stock items are visually highlighted
10. Click inventory row opens detail sheet
11. All tables support sorting, filtering, pagination
</verification>

<success_criteria>

- Orders page displays orders from backend /orders endpoint
- Orders columns: Order Number, Customer, Items, Total, Status, Date
- Order status badges with appropriate colors
- Order detail sheet shows full order information
- Inventory page displays inventory from backend /inventory endpoint
- Inventory columns: SKU, Product, Quantity, Reserved, Available, Reorder Point, Status, Last Restocked
- Inventory status badges (in_stock, low_stock, out_of_stock)
- Low stock items visually highlighted
- Inventory detail sheet shows quantity breakdown
- Both sections reuse DataTable component pattern
  </success_criteria>

<output>
After completion, create `.planning/phases/03-web-application/03-06-SUMMARY.md`
</output>
