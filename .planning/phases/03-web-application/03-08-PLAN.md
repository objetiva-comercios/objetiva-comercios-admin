---
phase: 03-web-application
plan: 08
type: execute
wave: 4
depends_on: ['03-03', '03-04', '03-05', '03-06', '03-07']
files_modified:
  - apps/web/src/app/(dashboard)/settings/page.tsx
  - apps/web/src/app/(dashboard)/settings/profile/page.tsx
  - apps/web/src/app/(dashboard)/settings/business/page.tsx
  - apps/web/src/components/settings/profile-form.tsx
  - apps/web/src/components/settings/settings-nav.tsx
  - apps/web/src/lib/api.ts
  - README.md
autonomous: false

must_haves:
  truths:
    - 'User can view their profile information'
    - 'User can update their profile (display name)'
    - 'Settings page has organized navigation'
    - 'Web app is responsive across desktop, tablet, mobile'
    - 'README documents running the web app'
  artifacts:
    - path: 'apps/web/src/app/(dashboard)/settings/page.tsx'
      provides: 'Settings page with navigation'
      min_lines: 30
    - path: 'apps/web/src/components/settings/profile-form.tsx'
      provides: 'Profile edit form'
      min_lines: 50
  key_links:
    - from: 'apps/web/src/components/settings/profile-form.tsx'
      to: 'apps/web/src/lib/supabase/client.ts'
      via: 'updateUser for profile updates'
      pattern: "updateUser|auth\\.update"
---

<objective>
Complete the Settings section with profile management and verify the entire web application.

Purpose: Deliver the final operational section (Settings) and perform comprehensive verification of the complete web application. This plan includes a human verification checkpoint to validate the UI, responsiveness, and user flows before marking Phase 3 complete.

Output: Working Settings page with profile view/edit and business settings. Updated README with web app documentation. Verified complete web application ready for Phase 4.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-application/03-RESEARCH.md
@.planning/phases/03-web-application/03-CONTEXT.md
@apps/web/src/lib/supabase/client.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Settings section with profile management</name>
  <files>
    apps/web/src/app/(dashboard)/settings/page.tsx
    apps/web/src/app/(dashboard)/settings/layout.tsx
    apps/web/src/app/(dashboard)/settings/profile/page.tsx
    apps/web/src/app/(dashboard)/settings/business/page.tsx
    apps/web/src/components/settings/profile-form.tsx
    apps/web/src/components/settings/settings-nav.tsx
    apps/web/src/components/ui/tabs.tsx
  </files>
  <action>
    1. Add tabs component:
       ```bash
       cd apps/web && npx shadcn@latest add tabs
       ```

    2. Create Settings navigation at `src/components/settings/settings-nav.tsx`:
       - "use client" directive
       - Tabs-style navigation or sidebar links
       - Sections:
         - Profile (user profile)
         - Business (business settings - placeholder)
         - Appearance (theme settings - optional)
       - Use Next.js Link for navigation
       - Highlight active section

    3. Create Settings layout at `src/app/(dashboard)/settings/layout.tsx`:
       - Render settings navigation
       - Content area for child pages
       - Consistent layout across settings sub-pages

    4. Create Settings index at `src/app/(dashboard)/settings/page.tsx`:
       - Redirect to /settings/profile or
       - Display overview with links to sub-sections

    5. Create Profile form at `src/components/settings/profile-form.tsx`:
       - "use client" directive
       - Use React Hook Form + Zod for validation
       - Fields:
         - Display name (editable)
         - Email (read-only, from auth)
         - Avatar URL (optional, placeholder for future)
       - On submit:
         - Create Supabase client
         - Call supabase.auth.updateUser({ data: { display_name } })
         - Show success/error toast
       - Pre-populate with current user data

    6. Create Profile page at `src/app/(dashboard)/settings/profile/page.tsx`:
       - Server Component fetches current user
       - Pass user data to ProfileForm client component
       - Page title: "Profile Settings"

    7. Create Business settings page at `src/app/(dashboard)/settings/business/page.tsx`:
       - Placeholder for business settings
       - Show form with fields:
         - Business name
         - Currency
         - Timezone
       - Note: These don't persist yet (Phase 5 database)
       - Show "Coming soon" or disabled state
       - Or: Allow local storage persistence for now

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Navigate to /settings
    - Settings navigation visible
    - Profile page shows user email
    - Profile form allows name update
    - Business settings page accessible
  </verify>
  <done>
    Settings section complete with profile management. User can view and update their display name. Business settings page ready for future database integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README and prepare for verification</name>
  <files>
    README.md
    apps/web/.env.example
  </files>
  <action>
    1. Update main README.md with web app documentation:
       - Add "Web Application" section under "Apps"
       - Document running the web app:
         - `pnpm dev --filter=@objetiva/web`
         - Available at http://localhost:3000
       - Document web-specific environment variables:
         - NEXT_PUBLIC_SUPABASE_URL
         - NEXT_PUBLIC_SUPABASE_ANON_KEY
         - NEXT_PUBLIC_API_URL (backend URL, defaults to localhost:3001)
       - Document available routes:
         - /login - Authentication
         - /signup - Registration
         - /dashboard - Main dashboard
         - /articles - Products management
         - /orders - Order tracking
         - /inventory - Stock levels
         - /sales - Sales transactions
         - /purchases - Purchase orders
         - /settings - User and business settings

    2. Ensure apps/web/.env.example is complete:
       - NEXT_PUBLIC_SUPABASE_URL=your-project-url
       - NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
       - NEXT_PUBLIC_API_URL=http://localhost:3001

    3. Verify all placeholder pages replaced:
       - Check each dashboard route has real content
       - No "coming soon" placeholders except business settings

    4. Final build verification:
       - Run `pnpm build` (full monorepo build)
       - Ensure no TypeScript errors
       - Ensure no build warnings for web app

  </action>
  <verify>
    - README documents web app running and routes
    - .env.example has all required variables
    - `pnpm build` succeeds for entire monorepo
    - No placeholder pages except intentional ones
  </verify>
  <done>
    Documentation updated, web app ready for human verification. All routes functional, build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete web admin interface with:
    - Authentication (login, signup, logout)
    - Responsive sidebar navigation (7 sections)
    - Dashboard with KPIs, charts, alerts
    - Articles (Products) table with filtering and details
    - Orders table with status tracking
    - Inventory table with low stock highlighting
    - Sales transaction table
    - Purchases transaction table
    - Settings with profile management
    - Dark/light theme toggle
  </what-built>
  <how-to-verify>
    1. Start backend: `pnpm dev --filter=@objetiva/backend`
    2. Start web: `pnpm dev --filter=@objetiva/web`
    3. Open http://localhost:3000

    **Authentication Flow:**
    - [ ] Signup page renders at /signup
    - [ ] Login page renders at /login
    - [ ] Can log in with valid credentials
    - [ ] Session persists after page refresh
    - [ ] Can log out from user menu

    **Navigation:**
    - [ ] Sidebar shows all 7 sections on desktop
    - [ ] Hamburger menu works on mobile (resize browser)
    - [ ] Active navigation state updates on route change

    **Dashboard:**
    - [ ] KPI cards show numbers from backend
    - [ ] Sales chart renders trend line
    - [ ] Low stock alerts show items
    - [ ] Recent orders show activity

    **Data Tables (check each section):**
    - [ ] Tables load data from backend
    - [ ] Sorting works (click column headers)
    - [ ] Filtering/search works
    - [ ] Pagination works (if >10 items)
    - [ ] Row click opens detail side panel

    **Theme:**
    - [ ] Theme toggle switches light/dark
    - [ ] Dark theme applies to all components

    **Responsiveness:**
    - [ ] Desktop (1200px+): Full sidebar visible
    - [ ] Tablet (768-1200px): Sidebar collapses or hamburger
    - [ ] Mobile (<768px): Hamburger menu, tables scroll horizontally

    **Settings:**
    - [ ] Profile page shows user email
    - [ ] Can update display name

    Report any issues found.

  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. All routes functional and loading data
2. Authentication flow complete (signup, login, logout, session persistence)
3. Navigation works on all screen sizes
4. All data tables functional with sorting, filtering, pagination
5. Dark theme works throughout
6. Settings profile update works
7. Build succeeds without errors
8. Human verification passed
</verification>

<success_criteria>

- Settings section accessible with profile and business pages
- User can view and update their profile
- README documents web app setup and usage
- All 7 dashboard sections render real data
- Authentication flow works end-to-end
- Navigation responsive across screen sizes
- Theme toggle works
- Human verification checkpoint passed
- Requirements covered: SET-01 (view profile), SET-02 (update profile), SET-03 (business settings), SET-04 (settings navigable), UI-03 (responsive), AUTH-03 (session persistence), DOC-03 (README)
- Phase 3 complete and ready for Phase 4 (Mobile Application)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-web-application/03-08-SUMMARY.md`
</output>
