---
phase: 03-web-application
plan: 04
type: execute
wave: 3
depends_on: ['03-03']
files_modified:
  - apps/web/src/app/(dashboard)/dashboard/page.tsx
  - apps/web/src/components/dashboard/stats-cards.tsx
  - apps/web/src/components/dashboard/sales-chart.tsx
  - apps/web/src/components/dashboard/low-stock-alerts.tsx
  - apps/web/src/components/dashboard/recent-orders.tsx
  - apps/web/src/components/dashboard/date-range-picker.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/components/ui/calendar.tsx
  - apps/web/src/components/ui/popover.tsx
  - apps/web/src/components/ui/skeleton.tsx
  - apps/web/src/app/(dashboard)/dashboard/loading.tsx
autonomous: true

must_haves:
  truths:
    - 'Dashboard displays KPI cards with metrics from backend'
    - 'Dashboard shows sales trend chart'
    - 'Dashboard shows low stock alerts'
    - 'Dashboard shows recent orders'
    - 'Dashboard data comes from backend API'
    - 'Loading state shows skeletons while data fetches'
  artifacts:
    - path: 'apps/web/src/app/(dashboard)/dashboard/page.tsx'
      provides: 'Dashboard page with data fetching'
      min_lines: 50
    - path: 'apps/web/src/components/dashboard/stats-cards.tsx'
      provides: 'KPI stat cards component'
      min_lines: 30
    - path: 'apps/web/src/components/dashboard/sales-chart.tsx'
      provides: 'Sales trend chart'
      min_lines: 40
    - path: 'apps/web/src/lib/api.ts'
      provides: 'API client for backend requests'
      exports: ['fetchDashboard', 'fetchProducts']
  key_links:
    - from: 'apps/web/src/app/(dashboard)/dashboard/page.tsx'
      to: 'http://localhost:3001/dashboard'
      via: 'fetch in Server Component'
      pattern: 'fetch.*localhost:3001.*dashboard'
    - from: 'apps/web/src/components/dashboard/sales-chart.tsx'
      to: 'recharts'
      via: 'LineChart component'
      pattern: 'LineChart|ResponsiveContainer'
---

<objective>
Build the dashboard page with KPI overview, trend visualization, and actionable alerts.

Purpose: Create the main landing page after login that gives users immediate visibility into business metrics. Dashboard displays key performance indicators, sales trends, low stock warnings, and recent orders - all fetched from the backend API built in Phase 2.

Output: Working dashboard page with stat cards, line chart for sales trends, low stock alerts table, and recent orders list. Data comes from backend /dashboard endpoint via parallel fetch.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-application/03-RESEARCH.md
@.planning/phases/03-web-application/03-CONTEXT.md
@.planning/phases/02-backend-api-with-mock-data/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API client and dashboard data fetching</name>
  <files>
    apps/web/src/lib/api.ts
    apps/web/src/app/(dashboard)/dashboard/page.tsx
    apps/web/src/app/(dashboard)/dashboard/loading.tsx
    apps/web/src/components/ui/skeleton.tsx
    apps/web/src/types/dashboard.ts
  </files>
  <action>
    1. Add skeleton component:
       ```bash
       cd apps/web && npx shadcn@latest add skeleton
       ```

    2. Create TypeScript types at `src/types/dashboard.ts`:
       - Define DashboardStats interface matching backend response:
         - totalProducts: number
         - totalOrders: number
         - totalRevenue: number
         - totalSales: number
         - pendingOrders: number
         - lowStockCount: number
       - Define LowStockItem interface:
         - id, productId, productName, quantity, status
       - Define RecentOrder interface:
         - id, orderNumber, customerName, total, status, createdAt
       - Define DashboardResponse interface combining all

    3. Create API client at `src/lib/api.ts`:
       - Define API_BASE_URL from env (NEXT_PUBLIC_API_URL or default localhost:3001)
       - Create async fetchDashboard() function:
         - Fetch from `${API_BASE_URL}/dashboard`
         - Include credentials: 'include' for cookies
         - Return typed DashboardResponse
       - Create async fetchWithAuth(endpoint) helper:
         - Handles base URL, credentials, error handling
         - Throws on non-ok response
       - Note: For Server Components, fetch happens server-side. For now, fetch without auth header (backend has dev mode or we'll add auth later)

    4. Create dashboard page at `src/app/(dashboard)/dashboard/page.tsx`:
       - Server Component (async function)
       - Fetch dashboard data using parallel Promise.all:
         - Stats data
         - Low stock items
         - Recent orders
       - Note: Backend /dashboard endpoint returns all this in one call
       - Render page with:
         - Page title "Dashboard"
         - StatsCards component with stats
         - Grid layout for charts and alerts
         - SalesChart component
         - LowStockAlerts component
         - RecentOrders component

    5. Create loading state at `src/app/(dashboard)/dashboard/loading.tsx`:
       - Export default Loading component
       - Use Skeleton components to match dashboard layout:
         - 4 skeleton cards for stats
         - Large skeleton for chart area
         - List skeleton for alerts/orders
       - Match the visual structure of the actual dashboard

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Types defined for dashboard data
    - API client exports fetchDashboard function
    - Dashboard page fetches data on server
  </verify>
  <done>
    Dashboard page structure with Server Component data fetching. API client configured for backend communication. Loading skeleton displays during data fetch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard visualization components</name>
  <files>
    apps/web/src/components/dashboard/stats-cards.tsx
    apps/web/src/components/dashboard/sales-chart.tsx
    apps/web/src/components/dashboard/low-stock-alerts.tsx
    apps/web/src/components/dashboard/recent-orders.tsx
    apps/web/src/components/dashboard/date-range-picker.tsx
    apps/web/src/components/ui/calendar.tsx
    apps/web/src/components/ui/popover.tsx
    apps/web/src/components/ui/badge.tsx
  </files>
  <action>
    1. Add required shadcn/ui components:
       ```bash
       cd apps/web && npx shadcn@latest add calendar popover badge
       ```

    2. Create StatsCards at `src/components/dashboard/stats-cards.tsx`:
       - Accept stats prop with dashboard metrics
       - Render grid of 4-6 stat cards (responsive: 2 cols mobile, 4 cols desktop)
       - Each card shows:
         - Label (Total Revenue, Total Orders, etc.)
         - Value (formatted appropriately - currency, number)
         - Optional trend indicator (up/down arrow with percentage)
       - Use Card component from shadcn/ui
       - Icons: DollarSign, ShoppingCart, Package, AlertTriangle from lucide-react
       - Format currency with Intl.NumberFormat

    3. Create SalesChart at `src/components/dashboard/sales-chart.tsx`:
       - "use client" directive (recharts needs client)
       - Accept data prop with sales trend data
       - Use Recharts: ResponsiveContainer, LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip
       - Chart shows sales over time (last 30 days or week)
       - Style with CSS variables for theme compatibility:
         - stroke="hsl(var(--primary))"
         - Grid stroke-muted
       - Responsive height (350px suggested)
       - Format dates on X axis, currency on Y axis

    4. Create LowStockAlerts at `src/components/dashboard/low-stock-alerts.tsx`:
       - Accept items prop with low stock products
       - Render table or list of items needing attention
       - Show: product name, current quantity, status badge
       - Status badge colors: warning for low_stock, destructive for out_of_stock
       - Limit to top 5 items (backend already limits)
       - Link each item to /inventory or /articles for action

    5. Create RecentOrders at `src/components/dashboard/recent-orders.tsx`:
       - Accept orders prop with recent order list
       - Render compact table with:
         - Order number
         - Customer name
         - Total (formatted currency)
         - Status badge
         - Relative time (e.g., "2 hours ago")
       - Use date-fns formatDistanceToNow for relative time
       - Link order number to /orders/{id} (placeholder)

    6. Create DateRangePicker at `src/components/dashboard/date-range-picker.tsx`:
       - "use client" directive
       - Use Calendar component with mode="range"
       - Popover trigger with formatted date range
       - Default to last 30 days
       - This is for future use - dashboard can filter by date range
       - For now, just render the component in header area

    7. Update dashboard page to use all components:
       - Layout: responsive grid
       - Stats cards at top (full width)
       - Chart and alerts side by side on desktop
       - Recent orders below

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Dashboard page renders with all components
    - Stats cards show formatted numbers
    - Chart renders with data (or placeholder if no data)
    - Low stock alerts show product names and badges
    - Recent orders show with status badges
    - Start backend: `pnpm dev --filter=@objetiva/backend`
    - Dashboard fetches and displays real mock data
  </verify>
  <done>
    Complete dashboard with KPI cards, sales trend chart, low stock alerts, and recent orders. All components receive data from backend API. Dense, admin-oriented layout following shadcn aesthetic.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build --filter=@objetiva/web` succeeds
2. Start backend: `pnpm dev --filter=@objetiva/backend`
3. Start web: `pnpm dev --filter=@objetiva/web`
4. Navigate to /dashboard after login
5. Dashboard shows stat cards with real numbers
6. Chart renders sales trend line
7. Low stock alerts table shows items
8. Recent orders list shows order data
9. Loading skeleton appears briefly during fetch
10. Layout is responsive (cards reflow on mobile)
</verification>

<success_criteria>

- Dashboard page fetches data from backend /dashboard endpoint
- Stat cards display: Total Products, Total Orders, Revenue, Sales, Pending Orders, Low Stock Count
- Sales chart shows trend visualization with Recharts
- Low stock alerts highlight items needing attention
- Recent orders show latest order activity
- Loading state shows skeleton while data fetches
- Layout is dense and admin-oriented (shadcn aesthetic)
- Requirements covered: DASH-01 (key metrics), DASH-02 (backend data), DASH-03 (layout density)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-web-application/03-04-SUMMARY.md`
</output>
