---
phase: 03-web-application
plan: 02
type: execute
wave: 1
depends_on: ['03-01']
files_modified:
  - apps/web/src/app/(auth)/layout.tsx
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/app/(auth)/signup/page.tsx
  - apps/web/src/app/auth/callback/route.ts
  - apps/web/src/app/page.tsx
  - apps/web/src/components/ui/toast.tsx
  - apps/web/src/components/ui/toaster.tsx
  - apps/web/src/hooks/use-toast.ts
autonomous: true

must_haves:
  truths:
    - 'User can navigate to /login and see login form'
    - 'User can navigate to /signup and see signup form'
    - 'User can submit login form with email/password'
    - 'User can submit signup form with email/password'
    - 'Invalid credentials show error message'
    - 'Successful login redirects to /dashboard'
  artifacts:
    - path: 'apps/web/src/app/(auth)/login/page.tsx'
      provides: 'Login page with form'
      min_lines: 50
    - path: 'apps/web/src/app/(auth)/signup/page.tsx'
      provides: 'Signup page with form'
      min_lines: 50
    - path: 'apps/web/src/app/auth/callback/route.ts'
      provides: 'OAuth callback handler'
      exports: ['GET']
  key_links:
    - from: 'apps/web/src/app/(auth)/login/page.tsx'
      to: 'apps/web/src/lib/supabase/client.ts'
      via: 'createClient for auth'
      pattern: 'createClient.*signInWithPassword'
    - from: 'apps/web/src/app/(auth)/signup/page.tsx'
      to: 'apps/web/src/lib/supabase/client.ts'
      via: 'createClient for signup'
      pattern: 'createClient.*signUp'
---

<objective>
Create authentication pages (Login and Signup) with form validation and Supabase Auth integration.

Purpose: Enable users to create accounts and sign in using email/password authentication via Supabase. This completes the auth flow that allows access to protected dashboard routes.

Output: Working login and signup pages with React Hook Form + Zod validation, proper error handling, and redirect to dashboard on success.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-application/03-RESEARCH.md
@apps/web/src/lib/supabase/client.ts
@apps/web/src/components/ui/button.tsx
@apps/web/src/components/ui/form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth route group and login page</name>
  <files>
    apps/web/src/app/(auth)/layout.tsx
    apps/web/src/app/(auth)/login/page.tsx
    apps/web/src/components/ui/toast.tsx
    apps/web/src/components/ui/toaster.tsx
    apps/web/src/hooks/use-toast.ts
    apps/web/src/app/layout.tsx
  </files>
  <action>
    1. Add toast components for error feedback:
       ```bash
       cd apps/web && npx shadcn@latest add toast sonner
       ```
       Note: Use sonner for simpler toast API if available, otherwise use shadcn toast.

    2. Update root layout to include Toaster component.

    3. Create auth layout at `src/app/(auth)/layout.tsx`:
       - Centered layout for auth pages
       - No sidebar (auth pages are unauthenticated)
       - Simple centered container with card-like appearance
       - Optional: subtle background pattern or gradient

    4. Create login page at `src/app/(auth)/login/page.tsx`:
       - "use client" directive (needs form interactivity)
       - Import useForm from react-hook-form
       - Import zodResolver from @hookform/resolvers/zod
       - Define loginSchema with zod:
         - email: z.string().email()
         - password: z.string().min(1, "Password is required")
       - Use shadcn/ui Form, FormField, FormItem, FormLabel, FormControl, FormMessage
       - Use shadcn/ui Input, Button, Card components
       - On submit:
         - Create Supabase browser client
         - Call supabase.auth.signInWithPassword({ email, password })
         - On success: router.push('/dashboard') and router.refresh()
         - On error: Show toast with error.message
       - Include "Don't have an account? Sign up" link to /signup
       - Include "Forgot password?" link (can be placeholder for now)
       - Style: Centered card, shadcn aesthetic, responsive

    5. Add loading state during form submission:
       - Disable submit button while loading
       - Show spinner or "Signing in..." text

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Navigate to /login shows login form
    - Form validates email format
    - Form shows error if password empty
  </verify>
  <done>
    Login page renders with email/password form, Zod validation, and Supabase signInWithPassword integration. Error states display via toast.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create signup page and auth callback</name>
  <files>
    apps/web/src/app/(auth)/signup/page.tsx
    apps/web/src/app/auth/callback/route.ts
    apps/web/src/app/page.tsx
    apps/web/src/middleware.ts
  </files>
  <action>
    1. Create signup page at `src/app/(auth)/signup/page.tsx`:
       - Similar structure to login page
       - Define signupSchema with zod:
         - email: z.string().email()
         - password: z.string().min(8, "Password must be at least 8 characters")
         - confirmPassword: z.string()
         - Add refine() to check password === confirmPassword
       - On submit:
         - Create Supabase browser client
         - Call supabase.auth.signUp({ email, password })
         - On success with user: Show success toast "Check your email for confirmation"
         - On success without user (email confirmation required): Show info toast
         - On error: Show toast with error.message
       - Include "Already have an account? Log in" link to /login
       - Style: Match login page design

    2. Create auth callback route at `src/app/auth/callback/route.ts`:
       - Export async GET handler
       - Get 'code' from searchParams
       - If code exists:
         - Create Supabase server client
         - Call supabase.auth.exchangeCodeForSession(code)
       - Redirect to /dashboard (or origin from searchParams)
       - This handles email confirmation redirects

    3. Update root page `src/app/page.tsx`:
       - Check if user is authenticated
       - If authenticated: redirect to /dashboard
       - If not authenticated: redirect to /login
       - This ensures root path has sensible behavior

    4. Update middleware to handle auth redirects:
       - If user NOT authenticated AND trying to access protected routes: redirect to /login
       - If user IS authenticated AND trying to access /login or /signup: redirect to /dashboard
       - Protected routes: everything under /(dashboard)/*
       - Public routes: /(auth)/*, /auth/callback, static files

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Navigate to /signup shows signup form
    - Form validates password length and confirmation match
    - /auth/callback route exists and handles code exchange
    - Root page (/) redirects appropriately
  </verify>
  <done>
    Complete auth flow: signup with email confirmation, auth callback for code exchange, and proper redirects based on auth state. Users can create accounts and sign in.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build --filter=@objetiva/web` succeeds
2. `pnpm dev --filter=@objetiva/web` runs without errors
3. /login page renders with form
4. /signup page renders with form
5. Form validation works (email format, password length, password match)
6. Invalid credentials show error toast
7. Root / redirects to /login (when not authenticated)
</verification>

<success_criteria>

- Login page accessible at /login with email/password form
- Signup page accessible at /signup with email/password/confirm form
- Zod validation enforces email format and password requirements
- Supabase auth methods called correctly (signInWithPassword, signUp)
- Error messages displayed via toast on auth failures
- Auth callback handles email confirmation code exchange
- Middleware redirects unauthenticated users from protected routes
- Requirements covered: AUTH-01 (signup), AUTH-02 (login), partial AUTH-03 (session)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-web-application/03-02-SUMMARY.md`
</output>
