---
phase: 03-web-application
plan: 05
type: execute
wave: 3
depends_on: ['03-03']
files_modified:
  - apps/web/src/app/(dashboard)/articles/page.tsx
  - apps/web/src/app/(dashboard)/articles/loading.tsx
  - apps/web/src/components/tables/data-table.tsx
  - apps/web/src/components/tables/data-table-pagination.tsx
  - apps/web/src/components/tables/data-table-toolbar.tsx
  - apps/web/src/components/tables/products/columns.tsx
  - apps/web/src/components/tables/products/product-sheet.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/types/product.ts
  - apps/web/src/components/ui/table.tsx
autonomous: true

must_haves:
  truths:
    - 'Articles page displays products table with data from backend'
    - 'Table has sorting capability on columns'
    - 'Table has filtering/search by product name'
    - 'Table has pagination'
    - 'Clicking a row opens side panel with product details'
  artifacts:
    - path: 'apps/web/src/app/(dashboard)/articles/page.tsx'
      provides: 'Articles page with data fetching'
      min_lines: 30
    - path: 'apps/web/src/components/tables/data-table.tsx'
      provides: 'Reusable TanStack Table wrapper'
      min_lines: 80
    - path: 'apps/web/src/components/tables/products/columns.tsx'
      provides: 'Product table column definitions'
      min_lines: 40
  key_links:
    - from: 'apps/web/src/app/(dashboard)/articles/page.tsx'
      to: 'http://localhost:3001/products'
      via: 'fetch products endpoint'
      pattern: 'fetch.*products'
    - from: 'apps/web/src/components/tables/data-table.tsx'
      to: '@tanstack/react-table'
      via: 'useReactTable hook'
      pattern: 'useReactTable'
---

<objective>
Build the Articles (Products) section with a feature-complete data table using TanStack Table.

Purpose: Create a reusable data table pattern that will be used across all operational sections (Orders, Inventory, Sales, Purchases). The Articles page is the first implementation, establishing patterns for sorting, filtering, pagination, and row detail side panels.

Output: Working Articles page with products table fetched from backend, sortable columns, search filter, pagination, and side panel for viewing product details.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-application/03-RESEARCH.md
@.planning/phases/03-web-application/03-CONTEXT.md
@.planning/phases/02-backend-api-with-mock-data/02-03-SUMMARY.md
@apps/web/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable DataTable component with TanStack Table</name>
  <files>
    apps/web/src/components/tables/data-table.tsx
    apps/web/src/components/tables/data-table-pagination.tsx
    apps/web/src/components/tables/data-table-toolbar.tsx
    apps/web/src/components/ui/table.tsx
    apps/web/src/components/ui/select.tsx
  </files>
  <action>
    1. Add required shadcn/ui components:
       ```bash
       cd apps/web && npx shadcn@latest add table select
       ```

    2. Create DataTable component at `src/components/tables/data-table.tsx`:
       - "use client" directive (table interactivity)
       - Generic component: DataTable<TData, TValue>
       - Accept props: columns, data, onRowClick (optional)
       - Use useReactTable with:
         - getCoreRowModel
         - getPaginationRowModel
         - getSortedRowModel
         - getFilteredRowModel
       - Manage state with useState:
         - sorting: SortingState
         - columnFilters: ColumnFiltersState
         - rowSelection: RowSelectionState (for future bulk actions)
       - Use useMemo for data and columns to prevent re-renders
       - Render using shadcn Table, TableHeader, TableBody, TableRow, TableCell
       - Use flexRender for cell content
       - Style table rows with cursor-pointer and hover:bg-accent when clickable
       - Empty state: "No results found" centered in table body

    3. Create DataTablePagination at `src/components/tables/data-table-pagination.tsx`:
       - Accept table instance as prop
       - Show rows per page selector (10, 20, 30, 50)
       - Show current page info: "Page X of Y"
       - Navigation buttons: First, Previous, Next, Last
       - Use Select component for rows per page
       - Use Button components for navigation
       - Disable buttons appropriately (can't go previous on first page, etc.)

    4. Create DataTableToolbar at `src/components/tables/data-table-toolbar.tsx`:
       - Accept table instance and optional filterColumn prop
       - Render search Input that filters by specified column
       - Placeholder: "Filter by {column}..."
       - Use getColumn(filterColumn).setFilterValue for filtering
       - Optional: Reset filters button
       - Style: flex items-center gap between filters and actions

    5. Update DataTable to include toolbar and pagination:
       - Toolbar above table
       - Pagination below table
       - Configurable via props: showToolbar, showPagination, filterColumn

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - DataTable component can be imported
    - Generic typing works with different data types
  </verify>
  <done>
    Reusable DataTable component with TanStack Table integration. Supports sorting, filtering, pagination, and row click handlers. Ready for use in Articles and other sections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Articles page with products table</name>
  <files>
    apps/web/src/app/(dashboard)/articles/page.tsx
    apps/web/src/app/(dashboard)/articles/loading.tsx
    apps/web/src/components/tables/products/columns.tsx
    apps/web/src/components/tables/products/product-sheet.tsx
    apps/web/src/lib/api.ts
    apps/web/src/types/product.ts
  </files>
  <action>
    1. Create Product type at `src/types/product.ts`:
       - Match backend Product entity:
         - id: string
         - sku: string
         - name: string
         - description: string
         - category: string
         - price: number
         - cost: number
         - status: 'active' | 'inactive' | 'discontinued'
         - createdAt: string
         - updatedAt: string

    2. Update API client `src/lib/api.ts`:
       - Add fetchProducts(params?) function:
         - Fetch from /products endpoint
         - Support pagination params: page, limit
         - Support filter params: status, category, search
         - Return typed response with items and pagination info

    3. Create product columns at `src/components/tables/products/columns.tsx`:
       - "use client" directive
       - Import ColumnDef from @tanstack/react-table
       - Define columns array:
         - SKU: sortable
         - Name: sortable, filterable
         - Category: sortable
         - Price: sortable, formatted as currency
         - Status: badge with color (green=active, yellow=inactive, red=discontinued)
         - Created: formatted date
       - Use header function for sortable columns with sort indicator
       - Use cell function for custom rendering (currency, badge, date)

    4. Create ProductSheet at `src/components/tables/products/product-sheet.tsx`:
       - "use client" directive
       - Import Sheet components from shadcn/ui
       - Accept open, onOpenChange, product props
       - Display product details in organized layout:
         - Header with product name and SKU
         - Description
         - Price and cost (formatted)
         - Category
         - Status with badge
         - Created/Updated timestamps
       - Width: w-[400px] or sm:max-w-md
       - This is read-only view for now (editing in Phase 6)

    5. Create Articles page at `src/app/(dashboard)/articles/page.tsx`:
       - Server Component for initial data fetch
       - Fetch products from backend
       - Client wrapper for table interactivity
       - Pattern: Server fetches, passes to Client component
       - Or: Server renders page shell, Client component fetches
       - Recommended: Server fetches initial data, pass to ArticlesClient component
       - ArticlesClient handles:
         - State for selected product
         - DataTable with onRowClick to open sheet
         - ProductSheet for selected product

    6. Create loading state at `src/app/(dashboard)/articles/loading.tsx`:
       - Skeleton matching table layout
       - Toolbar skeleton
       - Multiple row skeletons
       - Pagination skeleton

    7. Page header:
       - Title "Articles"
       - Optional: Add button (placeholder for future)

  </action>
  <verify>
    - `pnpm build --filter=@objetiva/web` compiles
    - Start backend and web
    - Navigate to /articles
    - Table shows products with all columns
    - Clicking header sorts column
    - Search filters by name
    - Pagination shows and works
    - Clicking row opens side panel with details
  </verify>
  <done>
    Complete Articles page with products table. TanStack Table provides sorting, filtering, pagination. Row click opens side panel with product details. Pattern established for other data sections.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build --filter=@objetiva/web` succeeds
2. Backend running: `pnpm dev --filter=@objetiva/backend`
3. Web running: `pnpm dev --filter=@objetiva/web`
4. Navigate to /articles after login
5. Products table renders with data
6. Click column header to sort (toggle asc/desc)
7. Type in filter to search by name
8. Pagination shows page info and navigation works
9. Click row to open side panel with product details
10. Close panel with X or click outside
</verification>

<success_criteria>

- Articles page displays products from backend /products endpoint
- DataTable component is reusable (generic types)
- Columns: SKU, Name, Category, Price, Status, Created
- Sorting works on sortable columns
- Filtering/search works on name column
- Pagination shows 10/20/30/50 rows per page
- Row click opens Sheet with product details
- Loading skeleton shows during data fetch
- Table is responsive (horizontal scroll on small screens)
- Pattern established for Orders, Inventory, Sales, Purchases tables
  </success_criteria>

<output>
After completion, create `.planning/phases/03-web-application/03-05-SUMMARY.md`
</output>
