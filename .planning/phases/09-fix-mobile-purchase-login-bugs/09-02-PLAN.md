---
phase: 09-fix-mobile-purchase-login-bugs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/types/purchase.ts
  - apps/web/src/components/tables/purchases/columns.tsx
  - apps/web/src/components/tables/purchases/purchase-sheet.tsx
autonomous: true
requirements: [API-06]

must_haves:
  truths:
    - "Web purchase table badges render correctly for 'draft' status purchases (gray badge, not unstyled)"
    - 'Web purchase detail sheet displays correct USD currency amounts for each item (no NaN)'
  artifacts:
    - path: 'apps/web/src/types/purchase.ts'
      provides: 'Corrected PurchaseItem.subtotal field and Purchase.status draft enum'
      contains: 'subtotal: number'
    - path: 'apps/web/src/components/tables/purchases/columns.tsx'
      provides: 'statusVariants and statusColors with draft key instead of pending'
      contains: 'draft'
    - path: 'apps/web/src/components/tables/purchases/purchase-sheet.tsx'
      provides: 'statusVariants and statusColors with draft key, item.subtotal rendering'
      contains: 'draft'
  key_links:
    - from: 'apps/web/src/components/tables/purchases/columns.tsx'
      to: 'apps/web/src/types/purchase.ts'
      via: 'Purchase type import for column defs'
      pattern: 'import.*Purchase.*from.*types/purchase'
    - from: 'apps/web/src/components/tables/purchases/purchase-sheet.tsx'
      to: 'apps/web/src/types/purchase.ts'
      via: 'Purchase type import for sheet props'
      pattern: 'import.*Purchase.*from.*types/purchase'
---

<objective>
Fix web purchase type, badge rendering, and detail sheet to align with backend's 'draft' status enum and 'subtotal' field name.

Purpose: Web purchase table shows unstyled badges for draft purchases (statusColors['draft'] is undefined because map uses 'pending' key). Purchase detail sheet renders NaN for item amounts (item.total is undefined because backend returns item.subtotal).
Output: Corrected web purchase types, badge color maps, and item amount rendering.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fix-mobile-purchase-login-bugs/09-RESEARCH.md

<interfaces>
<!-- Backend source of truth for purchase statuses and fields -->

From apps/backend/src/modules/purchases/dto/purchase-query.dto.ts:

```typescript
@IsIn(['draft', 'ordered', 'received', 'cancelled'])
status?: 'draft' | 'ordered' | 'received' | 'cancelled'
```

From apps/backend/src/db/schema.ts (purchase_items table):

```typescript
unitCost: doublePrecision('unit_cost').notNull(),
subtotal: doublePrecision('subtotal').notNull(),
// NO 'total' field on purchase_items
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix web purchase types</name>
  <files>apps/web/src/types/purchase.ts</files>
  <action>
Two changes in apps/web/src/types/purchase.ts:

1. **PurchaseItem interface (line 7)**: rename `total: number` to `subtotal: number`
2. **Purchase interface (line 20)**: change status union from `'pending' | 'ordered' | 'received' | 'cancelled'` to `'draft' | 'ordered' | 'received' | 'cancelled'`

Do NOT change the `total` field on the `Purchase` interface itself (line 19) — the parent Purchase object has a computed `total` (subtotal + tax + shipping) which is correct. Only the `PurchaseItem.total` is wrong (should be `subtotal`).
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && grep -n "subtotal\|draft" apps/web/src/types/purchase.ts</automated>
</verify>
<done>Web PurchaseItem uses 'subtotal' (not 'total'), Purchase status union includes 'draft' (not 'pending').</done>
</task>

<task type="auto">
  <name>Task 2: Fix web purchase columns and detail sheet</name>
  <files>apps/web/src/components/tables/purchases/columns.tsx, apps/web/src/components/tables/purchases/purchase-sheet.tsx</files>
  <action>
Fix status badge maps and item amount rendering in both files:

1. **columns.tsx** (lines 11-23):
   - In `statusVariants` object: replace `pending: 'secondary'` with `draft: 'secondary'`
   - In `statusColors` object: replace `pending: 'bg-yellow-100 text-yellow-800 hover:bg-yellow-100'` with `draft: 'bg-gray-100 text-gray-800 hover:bg-gray-100'` (gray for draft = initial/inactive state, consistent with mobile StatusBadge)

2. **purchase-sheet.tsx** (lines 21-33 and line 84):
   - In `statusVariants` object (line 22): replace `pending: 'secondary'` with `draft: 'secondary'`
   - In `statusColors` object (line 29): replace `pending: 'bg-yellow-100 text-yellow-800 hover:bg-yellow-100'` with `draft: 'bg-gray-100 text-gray-800 hover:bg-gray-100'`
   - In item rendering (line 84): change `{formatCurrency(item.total)}` to `{formatCurrency(item.subtotal)}`

Do NOT change the `purchase.total` references in the Totals section (lines 111-112) — those refer to the parent Purchase.total which is correct (subtotal + tax + shipping).
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter=@objetiva/web build 2>&1 | tail -20</automated>
</verify>
<done>Web purchase badges render gray for 'draft' status. Purchase detail sheet displays correct item amounts using item.subtotal. Web app compiles without errors.</done>
</task>

</tasks>

<verification>
All web purchase bugs fixed:
1. `grep -n "draft\|subtotal" apps/web/src/types/purchase.ts` shows corrected type
2. `grep -n "draft" apps/web/src/components/tables/purchases/columns.tsx` shows draft key in both maps
3. `grep -n "draft\|item.subtotal" apps/web/src/components/tables/purchases/purchase-sheet.tsx` shows draft keys and subtotal reference
4. `grep -c "pending" apps/web/src/components/tables/purchases/columns.tsx` returns 0 (no remaining 'pending' keys)
5. `pnpm --filter=@objetiva/web build` succeeds with zero errors
</verification>

<success_criteria>

- Web build succeeds (zero TypeScript errors)
- Purchase type uses 'draft' status (not 'pending')
- PurchaseItem type uses 'subtotal' field (not 'total')
- columns.tsx statusVariants and statusColors use 'draft' key (not 'pending')
- purchase-sheet.tsx statusVariants and statusColors use 'draft' key (not 'pending')
- purchase-sheet.tsx renders item.subtotal (not item.total)
  </success_criteria>

<output>
After completion, create `.planning/phases/09-fix-mobile-purchase-login-bugs/09-02-SUMMARY.md`
</output>
