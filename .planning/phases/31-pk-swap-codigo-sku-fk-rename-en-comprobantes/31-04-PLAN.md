---
phase: 31
plan: 04
plan_id: 31-04
type: execute
wave: 3
depends_on:
  - 31-03
files_modified:
  - apps/backend/drizzle/0011_phase31_contract.sql
  - apps/backend/drizzle/meta/_journal.json
  - apps/backend/src/db/schema.ts
  - apps/backend/src/modules/articulos/articulos-helper.ts
  - apps/backend/src/modules/articulos/__tests__/articulos-helper.spec.ts
  - apps/backend/src/modules/existencias/existencias.service.ts
  - apps/backend/src/modules/inventarios/inventarios.service.ts
  - apps/backend/src/modules/existencias/dto/create-existencia.dto.ts
  - apps/backend/src/modules/inventarios/dto/create-inventario-articulo.dto.ts
  - apps/backend/src/db/seed.ts
  - apps/backend/src/db/generators/order.generator.ts
  - apps/backend/src/db/generators/sale.generator.ts
  - apps/backend/src/db/generators/purchase.generator.ts
  - apps/backend/src/db/generators/existencia.generator.ts
  - apps/backend/src/db/generators/inventario.generator.ts
  - apps/web/src/types/order.ts
  - apps/web/src/types/sale.ts
  - apps/web/src/types/purchase.ts
  - apps/web/src/types/existencia.ts
  - apps/web/src/types/inventario.ts
  - apps/web/src/types/dashboard.ts
  - apps/web/src/components/existencias/existencias-columns.tsx
  - apps/web/src/components/existencias/existencias-por-articulo.tsx
  - apps/web/src/components/existencias/existencias-por-deposito.tsx
  - apps/web/src/components/inventarios/conteo-table.tsx
  - apps/web/src/components/dashboard/low-stock-alerts.tsx
  - apps/web/src/components/articulos/articulo-search.tsx
autonomous: false
requirements:
  - VAR-10

must_haves:
  truths:
    - 'Las 5 hijas (order_items, sale_items, purchase_items, existencias, inventarios_articulos) NO tienen columna articulo_codigo (count en information_schema.columns = 0)'
    - 'Backend deja de doble-escribir articulo_codigo (helper simplificado o pass-through, services no setean articuloCodigo en .values())'
    - 'DTOs renombrados: CreateExistenciaDto y CreateInventarioArticuloDto usan articuloSku en vez de articuloCodigo'
    - 'Frontend types quitan articuloCodigo, queda solo articuloSku'
    - 'Componentes frontend leen articuloSku donde antes leian articuloCodigo (existencias-columns, low-stock-alerts, etc)'
    - 'Schema TS sincronizado con DB post-Deploy-3 (sin articuloCodigo en hijas)'
  artifacts:
    - path: 'apps/backend/drizzle/0011_phase31_contract.sql'
      provides: 'Migration Drizzle custom Deploy 3 - DROP COLUMN articulo_codigo en 5 hijas + DROP indices viejos'
      contains: 'DROP COLUMN IF EXISTS articulo_codigo'
    - path: 'apps/backend/drizzle/meta/_journal.json'
      provides: 'Journal entry idx=11 con tag 0011_phase31_contract'
      contains: '0011_phase31_contract'
    - path: 'apps/backend/src/modules/articulos/articulos-helper.ts'
      provides: 'Helper simplificado o pass-through (sin invocacion forzosa en consumidores)'
      contains: 'resolveSku'
  key_links:
    - from: 'apps/backend/src/modules/existencias/existencias.service.ts upsert'
      to: 'existencias.articuloSku (sin articuloCodigo)'
      via: 'DTO direct field'
      pattern: "articuloSku: dto\\.articuloSku"
    - from: 'apps/web/src/types/existencia.ts'
      to: 'Existencia interface (sin articuloCodigo)'
      via: 'type declaration'
      pattern: 'articuloSku: string'
---

<objective>
Deploy 3 (contract): cleanup final. Eliminar columna `articulo_codigo` de las 5 hijas + remover doble-escribe del backend + renombrar DTOs `articuloCodigo` -> `articuloSku` + quitar `articuloCodigo` de frontend types y refactor de consumidores.

Purpose: Cerrar Phase 31 con DB final consistente. Post-Deploy-3 las 5 hijas tienen UNICAMENTE `articulo_sku` (notnull, FK a articulos.sku). El helper queda como pass-through reusable para Phase 32 (variantes).
Output: Migration 0011 aplicada, backend simplificado, frontend types limpio, schema.ts sincronizado.

Esta Wave NO toca `articulos` (sin cambios). NO modifica trigger (sigue funcionando). NO toca payload webhooks (sigue v2 con sku). Es deuda de cleanup pura.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-CONTEXT.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-VALIDATION.md
@apps/backend/drizzle/0007_drop_sector_id_huerfana.sql
@apps/backend/src/db/schema.ts
@apps/backend/src/modules/existencias/existencias.service.ts
@apps/backend/src/modules/inventarios/inventarios.service.ts
@apps/backend/src/modules/articulos/articulos-helper.ts

<interfaces>
0011 migration SQL (research lineas 583-617):
- Header con referencias a D-08, D-13, gate 24-48h post-Deploy-2.
- LOCK TABLE order_items, sale_items, purchase_items, existencias, inventarios_articulos IN ACCESS EXCLUSIVE MODE (defensive, research linea 589-590).
- DROP INDEX IF EXISTS: order_items_articulo_codigo_idx, sale_items_articulo_codigo_idx, purchase_items_articulo_codigo_idx, existencias_articulo_codigo_idx, inv_articulos_articulo_codigo_idx (los que existan — los 3 primeros pueden no existir, pero IF EXISTS evita error).
- 5 x ALTER TABLE <hija> DROP COLUMN IF EXISTS articulo_codigo (research linea 600-604).
- DO block final validacion (research 606-616): SELECT count(*) FROM information_schema.columns WHERE column_name='articulo_codigo' AND table_name IN (...) == 0, sino RAISE EXCEPTION.

Schema.ts cambios Deploy 3:

- order_items, sale_items, purchase_items, existencias, inventarios_articulos: quitar `articuloCodigo: text('articulo_codigo')` de cada tabla.
- Quitar indices articulo_codigo_idx en cada hija (si estaban declarados en TS).
- existencias e inventarios_articulos: la PK compuesta y unique index ya estan sobre articulo_sku (Deploy 2), no cambian.

Backend simplificacion (research 619-628):

- ArticulosHelper.resolveSku: PUEDE simplificarse pero el research recomienda MANTENERLO como pass-through porque Phase 32 (variantes) lo necesita para resolver codigo -> sku cuando se busca el modelo. Por ahora: NO se modifica el codigo del helper. Pero los CALLERS dejan de invocarlo en cada insert/update (porque DTO ya tiene articuloSku).
- existencias.service.ts upsert: el DTO ahora tiene articuloSku directo. Eliminar la linea `const articuloSku = await this.articulosHelper.resolveSku(dto.articuloCodigo)`. En .values() pasar solo `articuloSku: dto.articuloSku`. Eliminar `articuloCodigo: dto.articuloCodigo` (campo ya no existe en DTO ni en tabla). El target del onConflictDoUpdate ya esta en [existencias.articuloSku, existencias.depositoId] desde Deploy 2.
- inventarios.service.ts addArticulo: idem — quitar resolveSku, usar dto.articuloSku directo.
- seed.ts + 5 generators: quitar campo articuloCodigo, dejar solo articuloSku.

DTOs renombrados:

- apps/backend/src/modules/existencias/dto/create-existencia.dto.ts: `articuloCodigo: string` -> `articuloSku: string`. Class-validator decorators (@IsString, @IsNotEmpty) migran inalterados.
- apps/backend/src/modules/inventarios/dto/create-inventario-articulo.dto.ts: idem.
- Otros DTOs que tomen articulo_codigo como path param o body: revisar — generalmente las rutas que toman articuloCodigo en path siguen siendo by-codigo (agrupador), no se renombran.

Frontend types (research 829-831):

- 6 archivos: order.ts, sale.ts, purchase.ts, existencia.ts, inventario.ts, dashboard.ts.
- Quitar `articuloCodigo: string` de las interfaces (OrderItem, SaleItem, PurchaseItem, Existencia, InventarioArticulo, LowStockItem). Queda solo `articuloSku: string`.
- ExistenciaMatrixRow: idem.

Componentes frontend a refactorizar (research linea 830-831):

- existencias-columns.tsx, existencias-por-articulo.tsx, existencias-por-deposito.tsx, conteo-table.tsx, low-stock-alerts.tsx, articulo-search.tsx, existencias-client.tsx: donde lean `e.articuloCodigo` cambiar a `e.articuloSku` donde aplique. Nota research: `existencias-por-articulo` muestra el codigo de la fila como agrupador — Phase 32 va a separar columnas codigo vs sku, asi que Phase 31 puede mantener mostrar `articulo.sku` (que en datos pre-variantes es semanticamente equivalente al codigo).

Helper unit test post-Deploy-3:

- articulos-helper.spec.ts: agregar un test "single-write context" que confirma que en Deploy 3 los services no llaman a resolveSku() porque el DTO ya tiene articuloSku. Es un test de integracion light: mockear ArticulosHelper, spy en resolveSku, hacer un upsert via service, verificar que resolveSku NO fue invocado. (Opcional pero recomendado para evitar regresion accidental.)
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear migration 0011 + journal idx=11 + schema.ts cleanup + simplificacion backend</name>
  <read_first>
    - apps/backend/drizzle/0007_drop_sector_id_huerfana.sql (analog exacto - DROP COLUMN simple)
    - apps/backend/drizzle/0010_phase31_switch.sql (estado post Deploy 2 para entender estructura final)
    - apps/backend/drizzle/meta/_journal.json (idx 10 post Plan 31-03)
    - apps/backend/src/db/schema.ts (estado post Plan 31-03: 5 hijas con articuloCodigo legacy nullable + articuloSku PK FK)
    - apps/backend/src/modules/articulos/articulos-helper.ts (el helper se mantiene as-is per research)
    - apps/backend/src/modules/articulos/__tests__/articulos-helper.spec.ts (4 tests verdes post Plan 31-02)
    - apps/backend/src/modules/existencias/existencias.service.ts (upsert con doble-escribe)
    - apps/backend/src/modules/inventarios/inventarios.service.ts (addArticulo con doble-escribe)
    - apps/backend/src/modules/existencias/dto/create-existencia.dto.ts
    - apps/backend/src/modules/inventarios/dto/create-inventario-articulo.dto.ts
    - apps/backend/src/db/seed.ts + 5 generators
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md (lineas 583-628 Deploy 3 + file inventory 789-801)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md (lineas 156-173 patron 0007 + 533-540 divergencias Deploy 3 + 659-673 generators)
  </read_first>
  <behavior>
    - Test articulos-helper.spec: 4 tests originales siguen verdes (no se rompe). NUEVO test: "post-Deploy-3 existencias.upsert no invoca articulosHelper.resolveSku" - spy en helper, upsert directo, verify spy.toHaveBeenCalledTimes(0).
    - Test smoke: pnpm --filter backend typecheck pasa sin errores post-cleanup (sin referencias a articuloCodigo en hijas).
  </behavior>
  <action>
1. Crear `apps/backend/drizzle/0011_phase31_contract.sql` siguiendo research 583-617 + analog 0007:
   - Header comment:
     `-- Migration 0011: Phase 31 Deploy 3 (contract) — drop articulo_codigo de las 5 hijas`
     `-- Pre-flight: 24-48h post-Deploy-2 sin regresiones`
     `-- Pre-flight: backend deploy con DTOs renombrados (articuloSku) y consumidores simplificados`
     `-- Apply: psql --single-transaction --set ON_ERROR_STOP=1 "$DATABASE_URL" -f 0011_phase31_contract.sql`
     `-- Refs: D-08, D-13. Threats mitigated: T-31-20, T-31-21.`
   - LOCK TABLE order_items, sale_items, purchase_items, existencias, inventarios_articulos IN ACCESS EXCLUSIVE MODE.
   - 5 x `DROP INDEX IF EXISTS <tabla>_articulo_codigo_idx;--> statement-breakpoint` (defensive — los 3 primeros pueden no existir, IF EXISTS lo cubre).
   - 5 x `ALTER TABLE <tabla> DROP COLUMN IF EXISTS articulo_codigo;--> statement-breakpoint`.
   - DO block final (research 606-616):
     ```
     DO $$ DECLARE v_count int; BEGIN
       SELECT count(*) INTO v_count FROM information_schema.columns
       WHERE table_name IN ('order_items','sale_items','purchase_items','existencias','inventarios_articulos')
         AND column_name = 'articulo_codigo';
       IF v_count != 0 THEN RAISE EXCEPTION 'Deploy 3 incompleto: % columnas articulo_codigo persisten', v_count; END IF;
       RAISE NOTICE 'Deploy 3 (contract) OK: articulo_codigo eliminado de las 5 hijas';
     END $$;
     ```

2. Actualizar `apps/backend/drizzle/meta/_journal.json` agregando entry idx=11:
   { "idx": 11, "version": "7", "when": <timestamp ms>, "tag": "0011_phase31_contract", "breakpoints": true }

3. Actualizar `apps/backend/src/db/schema.ts`:
   - Quitar `articuloCodigo: text('articulo_codigo')` de las 5 hijas (order_items, sale_items, purchase_items, existencias, inventarios_articulos).
   - Quitar indices articulo_codigo (si quedaban declarados en TS).
   - articuloSku ya es notnull + FK desde Deploy 2 (Plan 31-03), no cambia.
   - Verificar que existencias.primaryKey y inventariosArticulos.uniqueIndex siguen apuntando a articuloSku.

4. Simplificar `apps/backend/src/modules/existencias/existencias.service.ts`:
   - En upsert(dto): quitar la linea `const articuloSku = await this.articulosHelper.resolveSku(dto.articuloCodigo)`. En `.values()` pasar `articuloSku: dto.articuloSku` directo, sin escribir articuloCodigo. En `.onConflictDoUpdate.set` quitar `articuloSku: sql\`EXCLUDED.articulo_sku\`` (innecesario porque ya es PK + es el field principal). Mantener cantidad/stockMinimo/stockMaximo en el set.
   - Las queries select/join siguen iguales (ya estan sobre articuloSku desde Deploy 2).
   - **MANTENER** la inyeccion de `ArticulosHelper` en el constructor (pass-through para Phase 32 cascade engine). Aunque el upsert ya no invoca `resolveSku()` (el DTO trae `articuloSku` directo), el helper queda disponible para futuros consumers. Decision: NO se remueve del constructor. Aclaracion al checker #6: la opcion "puede removerse" del draft anterior fue descartada — la inyeccion se mantiene en los 5 modules consumidores.

5. Simplificar `apps/backend/src/modules/inventarios/inventarios.service.ts`:
   - addArticulo(inventarioId, dto): quitar resolveSku call. `.values()` usa `articuloSku: dto.articuloSku`. Eliminar `articuloCodigo: dto.articuloCodigo`.
   - **MANTENER** la inyeccion de `ArticulosHelper` en el constructor (idem existencias.service.ts — pass-through para Phase 32). NO remover del constructor ni del module imports.
   - Mantener catch de 23505 (unique constraint sigue activa, ahora sobre (inventario_id, articulo_sku)).

6. Renombrar DTOs:
   - `apps/backend/src/modules/existencias/dto/create-existencia.dto.ts`: campo `articuloCodigo: string` -> `articuloSku: string`. Decorators @IsString @IsNotEmpty migran inalterados.
   - `apps/backend/src/modules/inventarios/dto/create-inventario-articulo.dto.ts`: idem.
   - Buscar otros DTOs que usen articuloCodigo y evaluar caso por caso. Generalmente solo estos 2 estan en hijas; otros (UpdateExistenciaDto, etc) heredan via PartialType.

7. Limpiar `apps/backend/src/db/seed.ts`: en los 5 insertos a hijas (lineas ~102, 169, 205, 243, 284) quitar `articuloCodigo: articulo.codigo` y dejar solo `articuloSku: articulo.sku`.

8. Limpiar `apps/backend/src/db/generators/*.generator.ts` (5 archivos): quitar campo `articuloCodigo: articulo.codigo`, dejar solo `articuloSku: articulo.sku`.

9. Actualizar `apps/backend/src/modules/articulos/__tests__/articulos-helper.spec.ts`:
   - Los 4 tests existentes siguen verdes (helper no cambia).
   - Agregar test smoke: "post-Deploy-3 services no llaman resolveSku":
     - Mockear ArticulosHelper con spy.
     - Crear Test.createTestingModule con ExistenciasService + mock helper.
     - Llamar upsert con DTO `{ articuloSku: 'TEST', depositoId: 1, cantidad: 5 }`.
     - Verify: spy.resolveSku NOT called (toHaveBeenCalledTimes(0)).

10. Verificar typecheck: `pnpm --filter backend typecheck` exit 0. Si hay errores de types/missing fields en consumidores del DTO, agregar adjustments minimos.

Threats mitigados: T-31-20 (Backend sigue doble-escribiendo columna inexistente - simplificacion explicita), T-31-21 (DTOs desactualizados causan validation errors).
</action>
<verify>
<automated>test -f apps/backend/drizzle/0011_phase31_contract.sql && grep -c 'DROP COLUMN IF EXISTS articulo_codigo' apps/backend/drizzle/0011_phase31_contract.sql && grep -q '"tag": "0011_phase31_contract"' apps/backend/drizzle/meta/\_journal.json && cd apps/backend && pnpm typecheck 2>&1 | tail -3 && pnpm test -- articulos-helper.spec.ts 2>&1 | grep -E "Tests:|PASS|FAIL"</automated>
</verify>
<done> - 0011_phase31_contract.sql tiene LOCK + 5 DROP INDEX IF EXISTS + 5 DROP COLUMN IF EXISTS + DO block final. - \_journal.json entry idx=11 con tag 0011_phase31_contract. - schema.ts: las 5 hijas NO declaran articuloCodigo (verificar con `grep -c articuloCodigo apps/backend/src/db/schema.ts` <= 0 dentro del scope de hijas; articulos puede mantener codigo como columna no-PK). - existencias.service.ts y inventarios.service.ts: sin llamadas a articulosHelper.resolveSku() en upsert/addArticulo. - ArticulosHelper sigue inyectado en los 5 modules consumidores (pass-through para Phase 32, decision firme): `grep -l "ArticulosHelper" apps/backend/src/modules/existencias/existencias.module.ts apps/backend/src/modules/inventarios/inventarios.module.ts apps/backend/src/modules/articulos/articulos.module.ts | wc -l == 3` (existencias.module + inventarios.module + articulos.module que lo exporta). Los modules de order-items/sale-items/purchase-items no inyectan helper por separado — los inserts en esas tablas ocurren via existencias o seed; el bullet del checker `5 modules` se reinterpreta como `ArticulosHelper sigue exportado de ArticulosModule + importado donde se consumia historicamente (existencias, inventarios)`. - DTOs renombrados a articuloSku. - seed.ts + 5 generators: sin campo articuloCodigo en .values() de hijas. - articulos-helper.spec.ts tiene 5 tests verdes (4 originales + 1 nuevo smoke). - pnpm --filter backend typecheck exit 0.
</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup frontend - types + componentes que leen articuloCodigo de hijas</name>
  <read_first>
    - apps/web/src/types/order.ts, sale.ts, purchase.ts, existencia.ts, inventario.ts, dashboard.ts
    - apps/web/src/components/existencias/existencias-columns.tsx
    - apps/web/src/components/existencias/existencias-por-articulo.tsx
    - apps/web/src/components/existencias/existencias-por-deposito.tsx
    - apps/web/src/components/inventarios/conteo-table.tsx
    - apps/web/src/components/dashboard/low-stock-alerts.tsx
    - apps/web/src/components/articulos/articulo-search.tsx
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md (lineas 827-831 file inventory Deploy 3 + 976-980 patron type rename)
  </read_first>
  <action>
1. Quitar `articuloCodigo: string` de las 6 type interfaces:
   - `apps/web/src/types/order.ts` OrderItem: quitar `articuloCodigo`, mantener `articuloSku`.
   - `apps/web/src/types/sale.ts` SaleItem: idem.
   - `apps/web/src/types/purchase.ts` PurchaseItem: idem.
   - `apps/web/src/types/existencia.ts` Existencia + ExistenciaMatrixRow: quitar `articuloCodigo`. Linea 9 ya es `articuloSku: string` (notnull desde Deploy 2 Plan 31-03).
   - `apps/web/src/types/inventario.ts` InventarioArticulo: idem.
   - `apps/web/src/types/dashboard.ts` LowStockItem: idem.

2. Refactor componentes consumidores. Para cada componente listado, buscar referencias a `.articuloCodigo` y reemplazar por `.articuloSku` donde aplique:
   - `apps/web/src/components/existencias/existencias-columns.tsx`: si hay una columna que muestra "articuloCodigo", cambiar a "articuloSku". Si la tabla muestra ambas en columnas separadas, mantener la de codigo via JOIN con articulos.codigo desde el backend (que devuelve `articuloSku` y opcionalmente `articuloCodigoAgrupador` o similar — pero por simplicidad Phase 31 muestra SOLO sku; Phase 32 separa).
   - `apps/web/src/components/existencias/existencias-por-articulo.tsx`: research linea 830 dice "muestra el codigo de la fila — Phase 32 va a separar columnas". Para Phase 31 Deploy 3: usar `articulo.sku` como display text (en datos pre-variantes sku ~= codigo semanticamente).
   - `apps/web/src/components/existencias/existencias-por-deposito.tsx`: idem patron.
   - `apps/web/src/components/inventarios/conteo-table.tsx`: si lee `articulo.articuloCodigo`, cambiar a `articulo.articuloSku`.
   - `apps/web/src/components/dashboard/low-stock-alerts.tsx`: si display `item.articuloCodigo`, cambiar a `item.articuloSku`.
   - `apps/web/src/components/articulos/articulo-search.tsx`: si el resultado de la busqueda muestra articulo.codigo (el codigo agrupador, NO articuloCodigo de hija), eso SE MANTIENE — articulos.codigo sigue siendo columna valida de la tabla articulos. Solo cambiar si lee de un type de hija (OrderItem, SaleItem) con articuloCodigo.

3. Verificar typecheck web: `pnpm --filter web typecheck` exit 0.

4. Verificar smoke build: `pnpm --filter web build` exit 0.

5. NO renombrar nada en `apps/web/src/lib/api.client.ts` (ya fue rekey-eado en Plan 31-03). Las funciones fetchExistenciasByArticuloClient(codigo) y similares siguen usando codigo como path param porque el backend mantiene el path by-codigo agrupador.

Threats mitigados: T-31-22 (Type drift post-Deploy-3 - articuloCodigo eliminado de types coincide con DB).
</action>
<verify>
<automated>! grep -l 'articuloCodigo' apps/web/src/types/order.ts apps/web/src/types/sale.ts apps/web/src/types/purchase.ts apps/web/src/types/existencia.ts apps/web/src/types/inventario.ts apps/web/src/types/dashboard.ts && [ "$(grep -rn '\.articuloCodigo' apps/web/src/components/existencias apps/web/src/components/inventarios apps/web/src/components/dashboard apps/web/src/components/articulos 2>/dev/null | wc -l)" = "0" ] && pnpm --filter web typecheck 2>&1 | tail -3</automated>
</verify>
<done> - Los 6 archivos types/\*.ts NO tienen articuloCodigo (grep returns nothing). - Componentes consumidores tampoco leen `.articuloCodigo`: `grep -rn '\.articuloCodigo' apps/web/src/components/{existencias,inventarios,dashboard,articulos} | wc -l == 0` (resuelve checker #8). El `articulo.codigo` directo SI puede aparecer en `articulo-sheet.tsx` y `articulo-search.tsx` cuando display-only del codigo agrupador desde la tabla articulos — eso es valido. El check aplica a `.articuloCodigo` (campo de hijas) NO a `.codigo` (campo de articulos). - pnpm --filter web typecheck exit 0. - pnpm --filter web build exit 0.
</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Operador aplica migration 0011 + valida cleanup + smoke final + cierre Phase</name>
  <what-built>
- Migration 0011 lista para aplicar.
- Backend simplificado (sin doble-escribe).
- Frontend types limpio (sin articuloCodigo).
- Tests passing localmente.

Esta task es BLOCKING humano porque:

1. DROP COLUMN en prod es destructivo (data antigua en articulo_codigo se pierde definitivamente).
2. Requiere 24-48h soak post-Deploy-2 sin abort criteria (research linea 579).
3. pg_dump pre-Deploy-3 opcional pero recomendado por research linea 39.
4. Cierre formal de Phase 31 — el operador firma que el milestone va a Wave/Phase 32.
   </what-built>
   <how-to-verify>
   **Orden:**

5. **Pre-flight final (operador, en VPS):**

   ```bash
   # Confirmar que Plan 31-03 cerró sin abort criteria
   cat .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-03-SUMMARY.md
   bash scripts/phase31-validation.sh all  # debe seguir exit 0 post-Deploy-2 + soak
   ```

6. **pg_dump opcional pre-Deploy-3 (recomendado por research 39):**

   ```bash
   TS=$(date -u +%Y%m%d_%H%M%S)
   docker exec postgres pg_dump -U sanchez -d erp_sanchez -Fc \
     -f /var/backups/erp_sanchez/phase31/pre_deploy3_${TS}.dump
   ```

7. **Deploy backend nuevo (rolling restart, ANTES de migration):**
   - Build container backend con commit que sale de Task 1 (services simplificados + DTOs renombrados).
   - `docker compose up -d --no-deps --build backend`.
   - Health check `/api/health` retorna 200.
   - **Compatibilidad backward:** durante el window entre backend nuevo deploy y migration, el codigo nuevo escribe SOLO articuloSku en .values() pero la columna articulo_codigo todavia existe (nullable). Postgres permite inserts sin especificar columnas nullable — funcionan OK.

8. **Deploy frontend nuevo (ANTES de migration):**
   - Build web con commit de Task 2.
   - `docker compose up -d --no-deps --build web`.
   - Smoke: /articulos, click item, /articulos/[sku]/editar carga.

9. **Aplicar migration 0011:**

   ```bash
   docker cp apps/backend/drizzle/0011_phase31_contract.sql postgres:/tmp/0011_phase31_contract.sql
   docker exec postgres psql -U sanchez -d erp_sanchez \
     --single-transaction --set ON_ERROR_STOP=1 \
     -f /tmp/0011_phase31_contract.sql 2>&1 | tee deploy3_${TS}.log
   ```

   Esperado: ejecucion < 1 min (DROP COLUMN x5 sobre tablas grandes puede ser instant en PG14+). RAISE NOTICE 'Deploy 3 (contract) OK: articulo_codigo eliminado de las 5 hijas' al final.

10. **Registrar la migration en drizzle.\_\_drizzle_migrations (OBLIGATORIO, leccion 260502-tqf):**
    - Ejecutar OBLIGATORIAMENTE post-migration:
      ```bash
      docker exec postgres psql -U sanchez -d erp_sanchez --set ON_ERROR_STOP=1 -c "
        INSERT INTO drizzle.__drizzle_migrations (hash, created_at)
        VALUES (md5(pg_read_file('/tmp/0011_phase31_contract.sql')), extract(epoch from now())*1000);
      "
      ```
    - Verificar el insert con `SELECT count(*) FROM drizzle.__drizzle_migrations WHERE hash = md5(pg_read_file('/tmp/0011_phase31_contract.sql'))` → 1.
    - Sin este step, futuras migrations leen journal desincronizado vs table — silent failure pattern documentado en lecciones 260502-tqf y 260428-mig.

11. **Validacion gate final:**

    ```bash
    # Sanidad: las 5 hijas no tienen articulo_codigo
    docker exec postgres psql -U sanchez -d erp_sanchez -tAc "SELECT count(*) FROM information_schema.columns WHERE column_name='articulo_codigo' AND table_name IN ('order_items','sale_items','purchase_items','existencias','inventarios_articulos')"
    # Debe ser 0

    bash scripts/phase31-validation.sh all  # debe seguir exit 0
    ```

12. **Full stack smoke:**
    - Backend: `pnpm --filter backend test -- articulos-phase31.e2e-spec.ts` → 3 tests passed.
    - Frontend: /articulos, /articulos/{sku}/editar, /settings/webhooks (Alert visible), /articulos/existencias.
    - POST de prueba a /api/articulos/{sku}/existencias o similar: verificar que upsert funciona sin articuloCodigo.

13. **Cierre Phase 31:**
    - Marcar Phase 31 como completed en .planning/ROADMAP.md.
    - Update STATE.md con timestamp de cierre.
    - Tag git opcional: `git tag phase-31-complete`.
    - Reporte final: counts de tablas, paths de los 3 .dumps generados (pre_deploy1, pre_deploy2, pre_deploy3), output final de scripts/phase31-validation.sh all.

14. **Abort criteria (si alguno trigger antes de cierre):**
    - DROP COLUMN falla por dependencia inesperada (algun view/function referencia articulo_codigo) → halt, investigar, considerar restore desde pre_deploy3.
    - Backend rompe post-deploy con drift schema-vs-DB → typecheck Y queries; corregir o rollback.

15. **Reportar al planner:** - Log de migration (deploy3\_<TS>.log). - Output validation all (4 checks ✓). - Output `count articulo_codigo en hijas = 0`. - E2E test results. - Decision sobre tag git / state update / siguiente phase.
    </how-to-verify>
    <resume-signal>Escribir "approved" + log de migration + output validation all 4 checks ✓ + count articulo_codigo en 5 hijas = 0 + E2E tests passed + smoke frontend OK + STATE.md actualizado + ROADMAP marca Phase 31 como completed. Fase cerrada.</resume-signal>
    </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                                                                       |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| psql migration -> Postgres production       | DROP COLUMN es destructivo pero limitado a columna nullable sin FK activa (FK ya eliminada en Deploy 2)                           |
| Backend NestJS services -> DTOs renombrados | Los consumidores externos del backend deben actualizar a articuloSku en payloads POST/PATCH a /api/existencias y /api/inventarios |
| Frontend -> Backend POST con DTO renombrado | Frontend hace POST con articuloSku desde Deploy 3; backend acepta articuloSku                                                     |

## STRIDE Threat Register

| Threat ID | Category                   | Component                                                                                   | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------- | -------------------------- | ------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-31-20   | Tampering / Data Integrity | Backend sigue doble-escribiendo articulo_codigo post-DROP COLUMN                            | mitigate    | Task 1 elimina explicitamente las escrituras de articuloCodigo en services y seed/generators. Test smoke "post-Deploy-3 services no llaman resolveSku" valida. typecheck exit 0 garantiza que no quedan referencias compilables.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T-31-21   | DoS / Validation error     | DTOs renombrados (articuloCodigo→articuloSku) rechazan payloads viejos de clientes externos | mitigate    | Mitigacion explicita (resuelve checker #4): (1) El notice doc-only en /settings/webhooks (D-10) cubre el cambio de payload de eventos `articulo.*` — informa que `sku` ahora viene en el payload. (2) Para clientes que postean a `/api/existencias` o `/api/inventarios/:id/articulos` con `articuloCodigo` en body: deben renombrar el field a `articuloSku` en su lado. NO se agrega `@Expose` alias ni decorator de retrocompatibilidad — per D-08 (rekey total sin compat) el cambio es breaking by design. (3) El operador comunica el cambio de DTO a suscriptores externos conocidos pre-Deploy-3 (window 24h de notice). (4) Documentado en el SUMMARY de 31-04: lista los 2 DTOs afectados (CreateExistenciaDto, CreateInventarioArticuloDto) + los paths /api/existencias y /api/inventarios/:id/articulos. |
| T-31-22   | Tampering                  | Frontend types desincronizados con DB                                                       | mitigate    | Task 2 elimina articuloCodigo de los 6 types. Componentes refactorizados a articuloSku. pnpm typecheck exit 0 garantiza consistency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| T-31-23   | Information Disclosure     | Data antigua en articulo_codigo se pierde irreversiblemente                                 | accept      | El operador hace pg_dump pre-Deploy-3 (paso 2 del checkpoint) si quiere preservar. Post-DROP, recuperacion solo via pg_restore. Aceptado: articulo_codigo en hijas era redundante (articulo_sku ya identifica unicamente) y la informacion del codigo agrupador se preserva en articulos.codigo (columna mantenida indexed no-unique).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T-31-24   | DoS                        | DROP COLUMN bloquea writes ~brief                                                           | accept      | DROP COLUMN en PG12+ es metadata-only operation O(1) — no reescribe la tabla. Lock breve (<1 segundo). Ventana de bajo trafico igual recomendada por consistencia.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| T-31-25   | Repudiation                | Schema.ts no sincronizado con DB post-Deploy-3                                              | mitigate    | Atomic commit obligatorio: schema.ts + 0011.sql + \_journal.json en mismo commit (lección feedback_schema_drift_silencioso.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| T-31-SC   | Supply Chain               | npm install                                                                                 | accept      | No se instalan nuevas deps en este plan.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

</threat_model>

<verification>
- apps/backend/drizzle/0011_phase31_contract.sql aplicada en prod (log con RAISE NOTICE final)
- _journal.json entry idx=11 con tag 0011_phase31_contract
- Las 5 hijas: count en information_schema.columns para articulo_codigo == 0
- schema.ts sincronizado: 0 ocurrencias de articuloCodigo dentro de declaraciones de las 5 hijas
- DTOs renombrados: create-existencia.dto.ts y create-inventario-articulo.dto.ts usan articuloSku
- Backend services: existencias.service.ts y inventarios.service.ts no invocan articulosHelper.resolveSku() en upsert/addArticulo
- Frontend types: order/sale/purchase/existencia/inventario/dashboard.ts sin articuloCodigo
- pnpm --filter backend typecheck exit 0
- pnpm --filter web typecheck && pnpm --filter web build exit 0
- E2E tests articulos-phase31.e2e-spec.ts: 3 tests SC#3a/SC#3b/SC#4 passed
- articulos-helper.spec.ts: 5 tests passed (4 originales + 1 smoke nuevo)
- scripts/phase31-validation.sh all exit 0 final
- Phase 31 cerrada en ROADMAP.md y STATE.md
</verification>

<success_criteria>
Wave 3 completa y Phase 31 cerrada cuando:

1. Migration 0011 aplicada exitosamente (deploy3\_<TS>.log con RAISE NOTICE final).
2. Atomic commit: schema.ts + 0011.sql + \_journal.json en mismo git commit.
3. Las 5 hijas en DB no tienen columna articulo_codigo (count en information_schema = 0).
4. DTOs renombrados, helper queda como pass-through (Phase 32 lo reusara).
5. Frontend types limpios, componentes refactorizados, build/typecheck verdes.
6. SC#1-5 final: scripts/phase31-validation.sh all exit 0.
7. Phase 31 marcada como completed en ROADMAP.md (table progress + section header).
8. STATE.md actualizado con timestamp de cierre + tag opcional phase-31-complete.
9. VAR-10 satisfecho: comprobantes referencian sku como identificador universal, codigo queda como agrupador.
   </success_criteria>

<output>
Create `.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-04-SUMMARY.md` when done documentando: archivos modificados, output de migration log, validation final, paths de los 3 pg_dumps generados (pre_deploy1/2/3), tests E2E results, decisions de cleanup, link a commit atomico, timestamp de cierre Phase 31, ready para Phase 32. **Incluir seccion explicita** "Breaking changes para clientes externos" listando los 2 DTOs renombrados (CreateExistenciaDto y CreateInventarioArticuloDto) y los 2 paths afectados (/api/existencias, /api/inventarios/:id/articulos) — documentacion del threat T-31-21 mitigation.
</output>
