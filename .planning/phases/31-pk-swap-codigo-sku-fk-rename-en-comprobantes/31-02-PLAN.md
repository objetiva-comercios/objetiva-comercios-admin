---
phase: 31
plan: 02
plan_id: 31-02
type: execute
wave: 1
depends_on:
  - 31-01
files_modified:
  - apps/backend/drizzle/0009_phase31_expand.sql
  - apps/backend/drizzle/meta/_journal.json
  - apps/backend/src/db/schema.ts
  - apps/backend/src/modules/articulos/articulos-helper.ts
  - apps/backend/src/modules/articulos/articulos.module.ts
  - apps/backend/src/modules/articulos/__tests__/articulos-helper.spec.ts
  - apps/backend/src/modules/existencias/existencias.service.ts
  - apps/backend/src/modules/existencias/existencias.module.ts
  - apps/backend/src/modules/inventarios/inventarios.service.ts
  - apps/backend/src/modules/inventarios/inventarios.module.ts
  - apps/backend/src/db/seed.ts
  - apps/backend/src/db/generators/order.generator.ts
  - apps/backend/src/db/generators/sale.generator.ts
  - apps/backend/src/db/generators/purchase.generator.ts
  - apps/backend/src/db/generators/existencia.generator.ts
  - apps/backend/src/db/generators/inventario.generator.ts
autonomous: false
requirements:
  - VAR-10

must_haves:
  truths:
    - 'articulos.sku esta poblado al 100% (count WHERE sku IS NULL = 0) con valor stripSep(codigo)'
    - 'Las 5 tablas hijas (order_items, sale_items, purchase_items, existencias, inventarios_articulos) tienen columna articulo_sku TEXT NULL poblada via backfill JOIN'
    - 'Backend doble-escribe articulo_codigo Y articulo_sku en cada nuevo INSERT/UPDATE via ArticulosHelper.resolveSku()'
    - 'apps/backend/src/db/schema.ts esta sincronizado con la DB post-migration (las 5 hijas declaran articuloSku nullable + index)'
    - 'Las 5 queries de integridad referencial SC#5 retornan 0 post-migration'
  artifacts:
    - path: 'apps/backend/drizzle/0009_phase31_expand.sql'
      provides: 'Migration Drizzle custom Deploy 1 - overwrite + ADD COLUMN + backfill + indices'
      contains: 'ADD COLUMN IF NOT EXISTS articulo_sku'
    - path: 'apps/backend/drizzle/meta/_journal.json'
      provides: 'Journal entry idx=9 con tag 0009_phase31_expand'
      contains: '0009_phase31_expand'
    - path: 'apps/backend/src/db/schema.ts'
      provides: 'Schema TS sincronizado: articuloSku text nullable en 5 hijas + indices articulo_sku'
      contains: 'articulo_sku'
    - path: 'apps/backend/src/modules/articulos/articulos-helper.ts'
      provides: 'Injectable ArticulosHelper con resolveSku(codigo) -> Promise<string>'
      contains: 'resolveSku'
    - path: 'apps/backend/src/modules/articulos/articulos.module.ts'
      provides: 'Module exportando ArticulosHelper para consumidores'
      contains: 'ArticulosHelper'
  key_links:
    - from: 'apps/backend/src/modules/existencias/existencias.service.ts upsert/update'
      to: 'ArticulosHelper.resolveSku'
      via: 'constructor injection'
      pattern: "this\\.articulosHelper\\.resolveSku"
    - from: 'apps/backend/src/modules/inventarios/inventarios.service.ts addArticulo'
      to: 'ArticulosHelper.resolveSku'
      via: 'constructor injection'
      pattern: "this\\.articulosHelper\\.resolveSku"
    - from: 'apps/backend/src/db/seed.ts + generators/*.ts'
      to: 'articuloSku: articulo.sku'
      via: 'direct field assignment in .values()'
      pattern: 'articuloSku:'
---

<objective>
Deploy 1 (expand): poblar `articulos.sku` con `stripSep(codigo)` para las 101.021 filas + agregar columna nullable `articulo_sku` en las 5 hijas con backfill via JOIN + cablear backend para doble-escribir ambas columnas via ArticulosHelper.

Purpose: Desacoplar el cambio de datos del cambio de schema (research Decision #1). En Deploy 2 (Plan 31-03) el PK swap encuentra `articulos.sku` ya estable, las 5 hijas con `articulo_sku` 100% backfilled, y solo necesita rotar constraints + recompute trigger.
Output: Migration 0009 aplicada en prod via Docker, helper centralizado en codigo, 5 servicios consumiendo el helper, schema.ts sincronizado en mismo commit.

Esta Wave NO cambia trigger (sigue keyeando por articulo_codigo), NO cambia payload de webhooks (sku ya disponible automaticamente desde Drizzle), NO cambia PK, NO rekey de URLs. Solo agrega columna + datos + helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-CONTEXT.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-VALIDATION.md
@.planning/research/PITFALLS.md
@apps/backend/drizzle/0008_phase30_templates.sql
@apps/backend/src/db/schema.ts
@apps/backend/src/modules/articulos/articulos.module.ts
@apps/backend/src/modules/articulos/articulos.service.ts
@apps/backend/src/modules/existencias/existencias.service.ts
@apps/backend/src/modules/inventarios/inventarios.service.ts
@apps/backend/drizzle/meta/_journal.json

<interfaces>
Contracts existentes verificados (de PATTERNS.md):

ArticulosHelper signature (research 184-224 + patterns 272-296):

```
@Injectable()
export class ArticulosHelper {
  constructor(private readonly drizzle: DrizzleService) {}
  async resolveSku(articuloCodigo: string): Promise<string>
}
```

- SELECT projection { sku: articulos.sku } FROM articulos WHERE codigo = articuloCodigo LIMIT 1
- Lanza NotFoundException(`Articulo ${articuloCodigo} no tiene sku asignado`) si row es null o sku es null
- NUNCA infiere sku como stripSep(codigo) en runtime (research comment line 202-203): siempre lee de articulos. Razón: post-Deploy-2 podrian crearse variantes donde sku != stripSep(codigo).

Schema TS post-Deploy-1 (research 540-545):

- articulos: sin cambios (codigo sigue siendo PK, sku sigue text nullable)
- order_items, sale_items, purchase_items: agregar `articuloSku: text('articulo_sku')` (nullable) + index('order_items_articulo_sku_idx').on(table.articuloSku) etc
- existencias: agregar articuloSku nullable + index('existencias_articulo_sku_idx').on(table.articuloSku); PK compuesta sigue siendo [articuloCodigo, depositoId]
- inventarios_articulos: agregar articuloSku nullable + index('inv_articulos_articulo_sku_idx').on(table.articuloSku); unique index sigue sobre [inventarioId, articuloCodigo]

5 queries SC#5 (gate post-Deploy-1, ya implementado en scripts/phase31-validation.sh integrity):

- SELECT count(\*) FROM <hija> LEFT JOIN articulos a ON <hija>.articulo_sku = a.sku WHERE a.sku IS NULL
- Las 5 deben retornar 0

stripSep regex (Phase 29 D-12, verified in packages/utils): `[-_.\s]+` -> ''
SQL equivalente: `regexp_replace(codigo, '[-_.[:space:]]+', '', 'g')`

Journal entry pattern (de patterns 183-195):

```json
{
  "idx": 9,
  "version": "7",
  "when": <Date.now() at apply time>,
  "tag": "0009_phase31_expand",
  "breakpoints": true
}
```

Patterns para doble-escribe en consumidores (de research 928-957):

- existencias.service.ts upsert(): antes del insert, llama `const articuloSku = await this.articulosHelper.resolveSku(dto.articuloCodigo)`. Luego en .values() escribe AMBAS articuloCodigo y articuloSku. En .onConflictDoUpdate.set agrega `articuloSku: sql\`EXCLUDED.articulo_sku\``.
- inventarios.service.ts addArticulo(): mismo patron, agregar articuloSku al .values().
- existencias.service.ts update(): si el dto incluye articuloCodigo (que tipicamente no incluye porque es PK compuesta), idem.

Generators (apps/backend/src/db/generators/\*.generator.ts): cada factory que toma `articulo: Articulo` y arma un row para la hija, agrega `articuloSku: articulo.sku ?? regexp_replace_equivalent(articulo.codigo)` - PERO research linea 230 indica que en seed/generators el articulo object ya tiene sku poblado post-overwrite por SQL UPDATE, asi que `articuloSku: articulo.sku` es suficiente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear ArticulosHelper + actualizar schema.ts + doble-escribe en 5 servicios/generators</name>
  <read_first>
    - apps/backend/src/modules/articulos/articulos.service.ts (lineas 1-17 imports, 91-95 findOne pattern - el helper sigue el mismo patron)
    - apps/backend/src/modules/articulos/articulos.module.ts (lineas 1-12 wiring actual)
    - apps/backend/src/modules/existencias/existencias.service.ts (lineas 1-30 imports + constructor, 209-254 upsert/update)
    - apps/backend/src/modules/existencias/existencias.module.ts (current module wiring)
    - apps/backend/src/modules/inventarios/inventarios.service.ts (lineas 244-272 addArticulo)
    - apps/backend/src/modules/inventarios/inventarios.module.ts
    - apps/backend/src/db/schema.ts (lineas 50-60 orderItems, 96-106 saleItems, 144-154 purchaseItems, 179-259 articulos, 279-300 existencias con PK compuesta, 361-386 inventariosArticulos con unique index)
    - apps/backend/src/db/seed.ts (lineas 102, 169, 205, 243, 284 insertos en hijas)
    - apps/backend/src/db/generators/order.generator.ts, sale.generator.ts, purchase.generator.ts, existencia.generator.ts, inventario.generator.ts
    - apps/backend/src/modules/articulos/__tests__/articulos-helper.spec.ts (skeleton de Plan 31-01)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md (lineas 184-235 helper completo + 928-957 patron doble-escribe + 760-771 file inventory)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md (lineas 243-303 helper analog + 490-540 existencias patrones + 580-619 inventarios patron + 660-672 seed/generators)
  </read_first>
  <behavior>
    - Test articulos-helper.spec (que se vuelve verde en este task, unskippeado): resolveSku('ABC-001') retorna 'ABC001' cuando articulos.sku='ABC001'. resolveSku('NOEXISTE') lanza NotFoundException con mensaje 'Articulo NOEXISTE no tiene sku asignado'. resolveSku('CODIGO-NULL-SKU') (mockeada articulos.sku=null) lanza NotFoundException.
    - Test que el modulo ArticulosModule exporta ArticulosHelper (Test.createTestingModule + module.get(ArticulosHelper) no es undefined).
  </behavior>
  <action>
1. Crear `apps/backend/src/modules/articulos/articulos-helper.ts` con la clase Injectable exacta del bloque <interfaces>. Imports: `Injectable`, `NotFoundException` from `@nestjs/common`; `eq` from `drizzle-orm`; `DrizzleService` from `'../../db'`; `articulos` from `'../../db/schema'`. Método único `resolveSku(articuloCodigo: string): Promise<string>` que hace SELECT con projection `{ sku: articulos.sku }`, `.limit(1)`, lanza NotFoundException si row.sku es null/undefined.

2. Actualizar `apps/backend/src/modules/articulos/articulos.module.ts`:
   - Import `ArticulosHelper` from './articulos-helper'
   - Agregar `ArticulosHelper` a providers array
   - Agregar `ArticulosHelper` a exports array (al lado de ArticulosService existente)

3. Unskip + implementar tests en `apps/backend/src/modules/articulos/__tests__/articulos-helper.spec.ts`:
   - Reemplazar `it.skip` por `it`
   - Mock DrizzleService.db.select(...).from(...).where(...).limit(1) returning [{ sku: 'ABC001' }] o [] o [{ sku: null }]
   - Test 1: happy path retorna 'ABC001'
   - Test 2: row vacio lanza NotFoundException
   - Test 3: row.sku null lanza NotFoundException con mismo mensaje
   - Test 4 (verifica modulo): Test.createTestingModule({ imports: [ArticulosModule], ... }) y module.get(ArticulosHelper) toBeInstanceOf(ArticulosHelper)

4. Actualizar `apps/backend/src/db/schema.ts` agregando columna nullable + index en las 5 hijas. Cada hija recibe DOS cambios:
   - Agregar campo: `articuloSku: text('articulo_sku')` (sin .notNull(), sin .references() - se agregan en Deploy 2 / Plan 31-03)
   - Agregar index: en el array de constraints/indices, `index('<tabla>_articulo_sku_idx').on(table.articuloSku)`. Nombres exactos (de research 162-166):
     - order_items: `order_items_articulo_sku_idx`
     - sale_items: `sale_items_articulo_sku_idx`
     - purchase_items: `purchase_items_articulo_sku_idx`
     - existencias: `existencias_articulo_sku_idx`
     - inventarios_articulos: `inv_articulos_articulo_sku_idx`
   - NO tocar PK compuesta de existencias (sigue `[articuloCodigo, depositoId]` en Deploy 1).
   - NO tocar unique index de inventarios_articulos (sigue sobre `[inventarioId, articuloCodigo]`).
   - articulos table NO cambia en este task (sigue codigo PK + sku nullable + articulos_sku_idx).

5. Actualizar `apps/backend/src/modules/existencias/existencias.module.ts`:
   - Importar `ArticulosModule` from '../articulos/articulos.module'
   - Agregar a `imports: [ArticulosModule]` en el decorator @Module

6. Actualizar `apps/backend/src/modules/existencias/existencias.service.ts`:
   - Inyectar `private readonly articulosHelper: ArticulosHelper` en el constructor (al lado del DrizzleService existente). Importar `ArticulosHelper` from '../articulos/articulos-helper'.
   - En `upsert(dto)` (linea ~209): ANTES del `.insert()`, llamar `const articuloSku = await this.articulosHelper.resolveSku(dto.articuloCodigo)`. En `.values()` agregar `articuloSku,`. En `.onConflictDoUpdate.set` agregar `articuloSku: sql\`EXCLUDED.articulo_sku\``. Mantener `articuloCodigo: dto.articuloCodigo` (doble-escribe).
   - En `update()` (linea ~233-254): si el update toca articuloCodigo (probablemente NO, ya que es PK compuesta), idem. Si NO toca articuloCodigo, NO modificar.
   - El `target` del onConflictDoUpdate sigue siendo `[existencias.articuloCodigo, existencias.depositoId]` (PK compuesta actual hasta Deploy 2).

7. Aplicar mismos cambios a `apps/backend/src/modules/inventarios/inventarios.module.ts` (importar ArticulosModule) y `apps/backend/src/modules/inventarios/inventarios.service.ts`:
   - Inyectar `ArticulosHelper` en constructor.
   - En `addArticulo(inventarioId, dto)` (linea ~244-272): antes del `.insert()`, `const articuloSku = await this.articulosHelper.resolveSku(dto.articuloCodigo)`. Agregar `articuloSku,` al `.values()`. Mantener `articuloCodigo: dto.articuloCodigo`. La catch de 23505 sigue valida (PK compuesta sin cambios).

8. Actualizar `apps/backend/src/db/seed.ts`: en cada `.insert(<hija>).values(...)` (5 sitios segun research, lineas 102/169/205/243/284), agregar campo `articuloSku: articulo.sku ?? regexpReplace(articulo.codigo)`. PERO como el seed crea articulos primero con sku poblado, `articuloSku: articulo.sku` deberia ser suficiente. Si en el seed el sku NO se popula directamente, agregar un fallback usando stripSep desde `@objetiva/utils`.

9. Actualizar los 5 generators (`apps/backend/src/db/generators/*.generator.ts`): cada factory que recibe `articulo` como parametro y arma un row para la hija, agregar campo `articuloSku: articulo.sku` al objeto retornado.

10. NO crear migration 0009 en este task (es Task 2). NO actualizar \_journal.json (es Task 2). NO aplicar nada en DB (es Task 3).

11. Verificar typecheck: `pnpm --filter backend typecheck` debe pasar sin errores. Si hay errores de DI por imports faltantes o tipos mismatched, corregir.

12. Correr tests de helper: `pnpm --filter backend test -- articulos-helper.spec.ts` debe pasar (todos GREEN).

Threats mitigados: T-31-05 (Backend olvida double-write una ruta — centralización en helper). T-31-04 (FK orphan — schema.ts sincronizado con DB).
</action>
<verify>
<automated>cd apps/backend && pnpm typecheck 2>&1 | tail -5 && pnpm test -- articulos-helper.spec.ts 2>&1 | grep -E "Tests:|PASS|FAIL"</automated>
</verify>
<done> - articulos-helper.ts existe con clase Injectable + resolveSku. - articulos.module.ts exporta ArticulosHelper. - 5 indices `_articulo_sku_idx` (con underscore prefijo) agregados en schema.ts para las 5 hijas (verificar con `grep -c '_articulo_sku_idx' apps/backend/src/db/schema.ts` >= 5; el underscore prefijo distingue los 5 indices de hijas del `articulos_sku_idx` de la tabla articulos). - 5 columnas articuloSku agregadas en schema.ts (verificar con `grep -c 'articulo_sku' apps/backend/src/db/schema.ts` >= 10 incluyendo declaraciones + indices). - `articulos_sku_idx` (sin underscore prefix — el de la tabla `articulos`, NO de hijas) sigue presente: `grep -c '"articulos_sku_idx"' apps/backend/src/db/schema.ts == 1` (Deploy 1 lo mantiene; Plan 31-03 Deploy 2 lo elimina porque la PK lo hace redundante). - existencias.service.ts y inventarios.service.ts inyectan ArticulosHelper y lo invocan en upsert/addArticulo. - seed.ts + 5 generators incluyen articuloSku: articulo.sku. - `pnpm --filter backend typecheck` exit 0. - articulos-helper.spec.ts 4 tests verdes.
</done>
</task>

<task type="auto">
  <name>Task 2: Crear migration 0009_phase31_expand.sql + actualizar _journal.json (atomic commit con schema.ts)</name>
  <read_first>
    - apps/backend/drizzle/0008_phase30_templates.sql (analog primario - header + ALTER TABLE patterns + statement-breakpoint delimiter)
    - apps/backend/drizzle/meta/_journal.json (estructura actual con 9 entries idx 0-8)
    - apps/backend/src/db/migrate-unidades.sql (lineas 15-21 patron DO block pre-check)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md (lineas 119-178 SQL completo del 0009 + 230-235 schema.ts atomic commit rule)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md (lineas 48-90 patron migration 0008 + 175-198 patron journal)
    - .planning/research/PITFALLS.md §P-05 lineas 156-186 (audit script context)
  </read_first>
  <action>
1. Crear `apps/backend/drizzle/0009_phase31_expand.sql` con el SQL COMPLETO del bloque de RESEARCH.md lineas 119-178. NO inventar; copiar literalmente. La estructura es:

- Header comment block (lineas 121-127 del research):
  - `-- Migration 0009: Phase 31 Deploy 1 (expand)` con descripcion
  - `-- Apply: psql --single-transaction --set ON_ERROR_STOP=1 "$DATABASE_URL" -f 0009_phase31_expand.sql`
  - `-- Pre-flight: 31-PREFLIGHT-AUDIT.md generado (informativo)`
  - `-- Pre-flight: pg_dump full safety net en /var/backups/erp_sanchez/phase31/pre_deploy1_<TS>.dump`
  - Referencia a D-01, D-02, D-05, D-06 lockeadas
  - Threats mitigados: T-31-04, T-31-05

- STEP 1: OVERWRITE articulos.sku := regexp*replace(codigo, '[-*.[:space:]]+', '', 'g'), updated_at = now() WHERE sku IS DISTINCT FROM regexp_replace(...). Sigue research lineas 131-135.

- DO block post-overwrite (research 136-145): RAISE EXCEPTION si null_sku > 0; RAISE WARNING si dupes > 0 (no aborta, deja papel para que operador escale).

- STEP 2: 5 x `ALTER TABLE <hija> ADD COLUMN IF NOT EXISTS articulo_sku text;` (sin NOT NULL, sin REFERENCES). Cada statement termina con `;--> statement-breakpoint`.

- STEP 3: 5 x `UPDATE <hija> SET articulo_sku = a.sku FROM articulos a WHERE <hija>.articulo_codigo = a.codigo AND <hija>.articulo_sku IS NULL;`. Literalmente las 5 lineas de research 155-159 (orden: order_items, sale_items, purchase_items, existencias, inventarios_articulos).

- STEP 4: 5 x `CREATE INDEX IF NOT EXISTS <tabla>_articulo_sku_idx ON <tabla>(articulo_sku);`. Nombres exactos de research 162-166.

- STEP 5: DO block FOR loop sobre `['order_items','sale_items','purchase_items','existencias','inventarios_articulos']` que ejecuta `SELECT count(*) FROM %I WHERE articulo_sku IS NULL` para cada uno; RAISE EXCEPTION si > 0; RAISE NOTICE 'Backfill OK' al final. Literal de research 169-177.

NO incluir DDL para FKs (eso es Deploy 2). NO tocar PKs. NO tocar trigger. NO crear tabla articulo_sku_history (eso es Phase 33).

2. Actualizar `apps/backend/drizzle/meta/_journal.json` agregando entry idx=9 al array `entries`:

   ```json
   {
     "idx": 9,
     "version": "7",
     "when": <timestamp UTC en ms al momento de crear el commit>,
     "tag": "0009_phase31_expand",
     "breakpoints": true
   }
   ```

   IMPORTANTE: el campo `when` se setea al momento de aplicar (no es critico al ms preciso pero debe ser razonable). Usar `Date.now()` o un timestamp ISO convertido a ms cuando creas el commit. NO bumpear version del journal (sigue "7").

3. Verificar que la migration sintaxis es valida via dry-run (sin aplicar): `docker exec postgres psql -U sanchez -d erp_sanchez --set ON_ERROR_STOP=1 -c "BEGIN; \i /tmp/sql_dry_check.sql; ROLLBACK;"` con el archivo montado al container, O simplemente `bash -c "psql --no-psqlrc --echo-all -f apps/backend/drizzle/0009_phase31_expand.sql </dev/null"` contra una DB temporal local si esta disponible. Si Docker permission setup no permite esto sin friction, dejar la validacion para Task 3.

4. Verificar que el commit que sube este task incluya ATOMICAMENTE los 3 archivos cambiados desde Task 1 + este task:
   - apps/backend/src/db/schema.ts (modificado en Task 1)
   - apps/backend/drizzle/0009_phase31_expand.sql (nuevo en este task)
   - apps/backend/drizzle/meta/\_journal.json (modificado en este task)
     Plus archivos del helper/services del Task 1. Lección `feedback_schema_drift_silencioso.md`: NUNCA commitear schema.ts sin la migration correspondiente.

Threats mitigados: T-31-06 (Drift schema.ts ↔ DB - atomic commit obligatorio).
</action>
<verify>
<automated>test -f apps/backend/drizzle/0009_phase31_expand.sql && grep -c 'statement-breakpoint' apps/backend/drizzle/0009_phase31_expand.sql && grep -q '"tag": "0009_phase31_expand"' apps/backend/drizzle/meta/\_journal.json && grep -c 'articulo_sku' apps/backend/drizzle/0009_phase31_expand.sql</automated>
</verify>
<done> - 0009_phase31_expand.sql existe con steps 1-5 (overwrite, ADD COLUMN x5, UPDATE backfill x5, CREATE INDEX x5, DO block validation). - El archivo incluye al menos 10 ocurrencias de `articulo_sku` (5 ADD COLUMN + 5 UPDATE). - \_journal.json tiene entry con `"tag": "0009_phase31_expand"` y `"idx": 9`. - Commit atomico planeado: schema.ts + 0009\*.sql + \_journal.json en mismo commit (verificado en git add lista antes de commit).
</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Operador aplica migration 0009 + valida SC#5 + 24-48h soak</name>
  <what-built>
- Migration 0009_phase31_expand.sql lista para aplicar.
- Backend con ArticulosHelper + doble-escribe deployado (ANTES de aplicar migration, segun research linea 180-181).
- Scripts de validacion (phase31-validation.sh integrity) listos para gate-keep.

Esta task es BLOCKING humano porque:

1. Toca DB de produccion del VPS (101.021 filas updated por el overwrite).
2. Requiere coordinacion de despliegue: backend con helper debe estar deployado ANTES de la migration (sino el doble-escribe no funciona en nuevos inserts).
3. Requiere 24-48h de soak monitoreado por el operador antes de gatear a Wave 2.
4. NO se puede automatizar el `docker exec` con flags de produccion sin auth interactiva.
   </what-built>
   <how-to-verify>
   **Orden CRITICO de operaciones:**

5. **Pre-flight final (operador, en VPS):**

   ```bash
   cat .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-CUTOVER-CALENDAR.md  # confirmar horario Deploy 1
   ls -lh /var/backups/erp_sanchez/phase31/pre_deploy1_*.dump  # baseline existe
   bash scripts/phase31-preflight-audit.sh  # re-correr audit por si cambio algo
   ```

6. **Deploy backend code con ArticulosHelper (rolling restart, NO migration aun):**
   - El backend nuevo (commit que sale de Tasks 1+2) se construye y reinicia el container backend.
   - `docker compose up -d --no-deps --build backend` o equivalente del setup actual.
   - Esperar que backend levante (health check `/api/health` retorne 200) — verificar logs sin errores.
   - **Importante:** el backend NUEVO con helper espera `articulos.sku` poblado. Si el helper se invoca antes de la migration, va a tirar `NotFoundException(Articulo X no tiene sku asignado)` en endpoints que escriben en hijas. **Por eso el deploy es:** (a) backend nuevo, (b) inmediatamente despues correr migration, (c) verificar.

7. **Aplicar migration 0009 (operador, en VPS):**

   ```bash
   docker cp apps/backend/drizzle/0009_phase31_expand.sql postgres:/tmp/0009_phase31_expand.sql
   docker exec postgres psql -U sanchez -d erp_sanchez \
     --single-transaction --set ON_ERROR_STOP=1 \
     -f /tmp/0009_phase31_expand.sql 2>&1 | tee deploy1_$(date -u +%Y%m%d_%H%M%S).log
   ```

   Esperado: ejecucion < 2 minutos. RAISE NOTICE 'Backfill OK: 5 hijas con articulo_sku 100% poblado' al final del log.

8. **Registrar la migration en drizzle.\_\_drizzle_migrations (OBLIGATORIO, leccion 260502-tqf):**
   - El `_journal.json` commiteado con idx 9 NO se replica automaticamente en la table `drizzle.__drizzle_migrations` cuando aplicamos la migration via `psql -f` (drizzle-kit no se invoca). Sin este INSERT, queries de drizzle pueden fallar silenciosamente y futuras migrations leen un journal desincronizado (lecciones `260502-tqf`, `260428-mig`).
   - Ejecutar OBLIGATORIAMENTE post-migration:
     ```bash
     docker exec postgres psql -U sanchez -d erp_sanchez --set ON_ERROR_STOP=1 -c "
       INSERT INTO drizzle.__drizzle_migrations (hash, created_at)
       VALUES (md5(pg_read_file('/tmp/0009_phase31_expand.sql')), extract(epoch from now())*1000);
     "
     ```
   - Verificar que la fila se insertó:
     ```bash
     docker exec postgres psql -U sanchez -d erp_sanchez -tAc "SELECT count(*) FROM drizzle.__drizzle_migrations WHERE hash = md5(pg_read_file('/tmp/0009_phase31_expand.sql'))"
     ```
     Esperado: 1.
   - Si la table `drizzle.__drizzle_migrations` o la schema `drizzle` no existen (primera migration aplicada manualmente), crear el shell y reintentar — NO saltarse este paso.

9. **Validacion gate (debe retornar exit 0):**

   ```bash
   bash scripts/phase31-validation.sh integrity
   ```

   Si exit 1: investigar QUE query fallo, NO continuar a Wave 2.

10. **Smoke test de doble-escribe (operador, opcional pero recomendado):**
    - Hacer 1 POST a `/api/articulos` para crear un articulo de prueba.
    - Hacer 1 PATCH a `/api/existencias/articulo/<codigo>` para upsert existencia.
    - Query: `SELECT articulo_codigo, articulo_sku FROM existencias ORDER BY updated_at DESC LIMIT 5` — verificar que las nuevas filas tienen articulo_sku poblado.

11. **24-48h soak:**
    - Cada ~4h durante 24-48h:
      - `bash scripts/phase31-validation.sh integrity` → debe seguir exit 0
      - `docker exec postgres psql -U sanchez -d erp_sanchez -tAc "SELECT count(*) FROM existencias WHERE articulo_sku IS NULL"` → debe seguir 0
      - Revisar logs backend: `docker compose logs backend --tail 200 | grep -i 'error\|exception'` → sin spikes nuevos
      - `docker exec postgres psql -U sanchez -d erp_sanchez -tAc "SELECT a.codigo, a.unidades, COALESCE(SUM(e.cantidad),0) FROM articulos a LEFT JOIN existencias e ON e.articulo_sku=a.sku GROUP BY a.codigo, a.unidades HAVING a.unidades != COALESCE(SUM(e.cantidad),0) LIMIT 5"` → debe retornar 0 filas (trigger sigue funcionando con articulo_codigo)

12. **Abort criteria (definidos en 31-CUTOVER-CALENDAR.md):**
    - Cualquier SC#5 query != 0 durante soak → halt, escalar.
    - Backend log error rate +20% sobre baseline → halt.
    - articulos.unidades desincronizado con SUM(existencias.cantidad) → halt.

13. **Reportar al planner:**
    - Log de la migration (deploy1\_<TS>.log).
    - Output del primer `scripts/phase31-validation.sh integrity` (esperado: 0 0 0 0 0).
    - Timestamp de fin del soak (cuando se aprueba avanzar a Wave 2).
    - Anomalias detectadas (si las hay).
      </how-to-verify>
      <resume-signal>Escribir "approved" + output de `scripts/phase31-validation.sh integrity` (debe ser todos `✓`) + timestamp post-soak de 24h minimo + ningun abort criteria triggered. Si hay sku_dupes detectados post-overwrite (RAISE WARNING en log), reportar para decidir si proceder a Wave 2 o resolver.</resume-signal>
      </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                                                            |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| Backend NestJS app -> Postgres via Drizzle        | ArticulosHelper.resolveSku() es la unica ruta backend que deriva sku desde codigo durante coexistencia |
| psql migration script -> articulos table          | UPDATE de 101.021 filas con regexp_replace; LOCK ROW EXCLUSIVE implicito mientras dura el UPDATE       |
| CI/build pipeline -> apps/backend deploy artifact | Backend nuevo con helper debe ir a prod ANTES de aplicar migration                                     |

## STRIDE Threat Register

| Threat ID | Category                   | Component                                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                               |
| --------- | -------------------------- | ----------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-31-04   | Tampering / Data Integrity | FK orphan post-Deploy-1                               | mitigate    | DO block STEP 5 en 0009 verifica que las 5 hijas tienen articulo_sku 100% poblado; aborta la transaccion si encuentra NULLs. Plus scripts/phase31-validation.sh integrity gate antes de Wave 2                                                                                                                                                                |
| T-31-05   | Tampering / Data Integrity | Backend olvida double-write una ruta                  | mitigate    | Centralizado en ArticulosHelper inyectado en 2 services (existencias, inventarios) + seed + 5 generators. Grep audit antes de commit: `grep -rn 'insert(existencias\|insert(inventariosArticulos\|insert(orderItems\|insert(saleItems\|insert(purchaseItems' apps/backend/src` y verificar que cada hit pasa por helper o el field articuloSku esta presente. |
| T-31-06   | Tampering / Drift          | Schema.ts desincronizado con DB post-migration        | mitigate    | Atomic commit obligatorio: schema.ts + 0009_phase31_expand.sql + \_journal.json en mismo commit (lección feedback_schema_drift_silencioso.md). Post-deploy: `pnpm --filter backend typecheck` debe seguir exit 0                                                                                                                                              |
| T-31-07   | Denial of Service          | UPDATE de 101.021 filas bloquea writes ~30s           | accept      | Operador ejecuta en ventana de bajo trafico definida en 31-CUTOVER-CALENDAR.md (sugerido 03:00-05:00 UTC-3). LOCK ROW EXCLUSIVE permite reads concurrentes.                                                                                                                                                                                                   |
| T-31-08   | Information Disclosure     | RAISE WARNING expone count de dupes en logs           | accept      | Logs solo accesibles a operador con acceso al VPS; no contienen datos sensibles (solo counts agregados)                                                                                                                                                                                                                                                       |
| T-31-09   | Tampering                  | ArticulosHelper SQL injection en articuloCodigo param | mitigate    | Drizzle `eq(articulos.codigo, articuloCodigo)` parametriza automaticamente; el helper recibe articuloCodigo desde DTOs validados con class-validator                                                                                                                                                                                                          |
| T-31-SC   | Supply Chain               | npm install para Task 1                               | accept      | NO se instalan nuevas deps en este plan; usa Drizzle/NestJS ya instalados (Plan 31-01 instalo jest/supertest pero esa task ya cerro su gate de legitimacy)                                                                                                                                                                                                    |

</threat_model>

<verification>
- apps/backend/src/modules/articulos/articulos-helper.ts existe con resolveSku Injectable
- apps/backend/src/db/schema.ts incluye articuloSku + index en 5 hijas (verificar con grep)
- apps/backend/drizzle/0009_phase31_expand.sql aplicado en prod sin errores
- apps/backend/drizzle/meta/_journal.json tiene entry idx=9 con tag 0009_phase31_expand
- `scripts/phase31-validation.sh integrity` exit 0 (las 5 queries SC#5 retornan 0)
- pnpm --filter backend typecheck exit 0
- pnpm --filter backend test (articulos-helper.spec.ts) exit 0 con 4 tests verdes
- existencias.service.ts upsert escribe articulo_codigo Y articulo_sku verificado via smoke INSERT
- 24-48h soak completed sin abort criteria triggered (verificado por operador)
</verification>

<success_criteria>
Wave 1 completa cuando:

1. ArticulosHelper.resolveSku() existe, testeado, exportado desde ArticulosModule, consumido por ExistenciasModule e InventariosModule.
2. schema.ts incluye articuloSku TEXT NULL + indices articulo_sku_idx en las 5 hijas — sincronizado con DB.
3. Migration 0009 aplicada exitosamente en prod (deploy1\_<TS>.log limpio, RAISE NOTICE 'Backfill OK').
4. \_journal.json actualizado con entry idx=9, commiteado atomicamente con schema.ts y .sql.
5. Las 5 queries SC#5 retornan 0 (scripts/phase31-validation.sh integrity exit 0).
6. articulos.sku poblado al 100% via stripSep(codigo); count WHERE sku IS NULL = 0.
7. 24-48h de soak en prod sin abort criteria (operador firma el calendario).
8. Plan 31-03 (Wave 2 Deploy 2) puede arrancar — backend nuevo desplegado, helper funcionando, datos backfilled.
   </success_criteria>

<output>
Create `.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-02-SUMMARY.md` when done documentando: archivos creados/modificados, output del integrity check post-deploy, timestamp de inicio/fin del soak, anomalias detectadas y resoluciones, link al commit atomico (schema.ts + .sql + journal).
</output>
