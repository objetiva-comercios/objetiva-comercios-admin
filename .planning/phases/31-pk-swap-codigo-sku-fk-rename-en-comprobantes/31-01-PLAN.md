---
phase: 31
plan: 01
plan_id: 31-01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/phase31-preflight-audit.sh
  - scripts/phase31-validation.sh
  - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PREFLIGHT-AUDIT.md
  - apps/backend/package.json
  - apps/backend/jest.config.cjs
  - apps/backend/test/jest-setup.ts
  - apps/backend/test/articulos-phase31.e2e-spec.ts
  - apps/backend/src/modules/articulos/__tests__/articulos-helper.spec.ts
  - apps/backend/tsconfig.json
  - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-CUTOVER-CALENDAR.md
autonomous: false
requirements:
  - VAR-10

must_haves:
  truths:
    - 'Existen scripts ejecutables phase31-preflight-audit.sh y phase31-validation.sh en /scripts/ root del repo'
    - 'Backend tiene framework de testing instalado (jest + supertest + @nestjs/testing) capaz de correr articulos-phase31.e2e-spec.ts'
    - 'Existe un .dump pg_dump full de la DB de produccion antes de Deploy 1, validado restore-test'
    - 'Existe 31-PREFLIGHT-AUDIT.md con counts informativos de articulos.sku pre-cutover'
    - 'Existe 31-CUTOVER-CALENDAR.md aprobado por el usuario con horarios concretos para los 3 deploys'
  artifacts:
    - path: 'scripts/phase31-preflight-audit.sh'
      provides: 'Bash script de auditoria informativa de articulos.sku (D-01)'
      contains: 'set -euo pipefail'
    - path: 'scripts/phase31-validation.sh'
      provides: 'Bash script de validacion con sub-comandos integrity/pk-swap/triggers/unidades-sync'
      contains: 'case "$CHECK" in'
    - path: 'apps/backend/jest.config.cjs'
      provides: 'Configuracion Jest para backend tests'
      contains: 'testRegex'
    - path: 'apps/backend/test/articulos-phase31.e2e-spec.ts'
      provides: 'Test E2E que cubre SC#3 (findByCodigo retorna array) y SC#4 (webhook payload incluye sku)'
      contains: "describe('Phase 31"
    - path: 'apps/backend/src/modules/articulos/__tests__/articulos-helper.spec.ts'
      provides: 'Unit test de ArticulosHelper.resolveSku() (placeholder rojo hasta Plan 31-02)'
      contains: "describe('ArticulosHelper"
    - path: '.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PREFLIGHT-AUDIT.md'
      provides: 'Output de la auditoria con null_sku, sku_eq_codigo, sku_eq_stripsep, sku_diff_codigo, sku_dupes, total'
      contains: 'Counts pre-cutover'
    - path: '.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-CUTOVER-CALENDAR.md'
      provides: 'Calendario concreto del cutover con horarios y abort criteria'
      contains: 'Deploy 1'
  key_links:
    - from: 'scripts/phase31-preflight-audit.sh'
      to: 'docker exec postgres psql'
      via: 'docker exec invocation'
      pattern: 'docker exec postgres psql'
    - from: 'scripts/phase31-validation.sh'
      to: '5 queries SC#5'
      via: 'psql -tAc'
      pattern: 'LEFT JOIN articulos a ON .* WHERE a.sku IS NULL'
    - from: 'apps/backend/test/articulos-phase31.e2e-spec.ts'
      to: '/api/articulos/by-codigo/:codigo + webhook_deliveries.payload'
      via: 'supertest'
      pattern: "request\\(app\\.getHttpServer\\(\\)\\)"
---

<objective>
Establecer la red de seguridad completa de Phase 31 ANTES de tocar la DB de produccion: scripts de auditoria informativa, scripts de validacion gate-keeper, framework de testing backend, snapshot pg_dump restoreable y calendario de cutover aprobado.

Purpose: Phase 31 es la fase de mayor riesgo del milestone. Sin estos artefactos, los Deploys 1/2/3 no tienen forma de medir exito ni revertir. El gap pattern mapper #1 (testing framework ausente) y #2 (scripts en /scripts/ root) se resuelven aqui.
Output: 7 archivos nuevos + pg_dump baseline + 31-PREFLIGHT-AUDIT.md generado + 31-CUTOVER-CALENDAR.md aprobado por el usuario.

Esta Wave NO toca migrations, NO toca prod data, NO toca el helper. Solo crea infraestructura de validacion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-CONTEXT.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-VALIDATION.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md
@.planning/research/PITFALLS.md
@scripts/phase38-preflight-backup.sh

<interfaces>
Convenciones del repo (extraidas de patterns + filesystem audit):

- Scripts viven en /scripts/ root (3 existentes: phase38-preflight-backup.sh, restore-selectivo-260502.sh, test-auth.sh). NO en apps/backend/scripts/.
- Postgres corre en container Docker llamado "postgres". User "sanchez", DB "erp_sanchez".
- Migraciones aplicadas via: `psql --single-transaction --set ON_ERROR_STOP=1 "$DATABASE_URL" -f file.sql`
- Backend levantado por docker-compose en puerto 3001. Web en 3000.
- Trigger existente trg_update_articulo_unidades (apps/backend/src/db/migrate-unidades.sql:48-70) usa target_codigo TEXT, keyea por articulo_codigo y articulos.codigo.
- packages/utils ya expone stripSep(codigo) (Phase 29 D-12, regex [-_.\s]+ -> '').
- apps/backend/drizzle/\_journal.json ya tiene 9 entries (idx 0-8). Proximas a agregar: 9, 10, 11.

Script analog (scripts/phase38-preflight-backup.sh lineas 1-27, 31, 64-65):

- Empieza con `#!/usr/bin/env bash` y `set -euo pipefail`
- Verifica dir existe + escribible
- Computa TIMESTAMP="$(date +%y%m%d-%H%M)"
- docker exec postgres pg_dump -U sanchez -d erp_sanchez -F c -f /tmp/...
- docker exec postgres psql -U sanchez -d erp_sanchez -tAc "SELECT ..."

Backend testing: package.json actual NO incluye jest/@nestjs/testing/supertest. tsconfig.json compila a CommonJS (NestJS default). Hay que instalar como dev deps y crear jest.config.cjs + test/ dir.

SQL queries clave (de RESEARCH.md lineas 240-249 + VALIDATION.md):

5 queries SC#5 (integrity check):
SELECT count(_) FROM order_items LEFT JOIN articulos a ON order_items.articulo_sku = a.sku WHERE a.sku IS NULL;
SELECT count(_) FROM sale_items LEFT JOIN articulos a ON sale_items.articulo_sku = a.sku WHERE a.sku IS NULL;
SELECT count(_) FROM purchase_items LEFT JOIN articulos a ON purchase_items.articulo_sku = a.sku WHERE a.sku IS NULL;
SELECT count(_) FROM existencias LEFT JOIN articulos a ON existencias.articulo_sku = a.sku WHERE a.sku IS NULL;
SELECT count(\*) FROM inventarios_articulos LEFT JOIN articulos a ON inventarios_articulos.articulo_sku = a.sku WHERE a.sku IS NULL;

PK check post-Deploy-2:
SELECT a.attname FROM pg_attribute a JOIN pg_constraint c ON a.attnum = ANY(c.conkey) WHERE c.conrelid='articulos'::regclass AND c.contype='p';
(esperado: 'sku')

Trigger enabled check:
SELECT tgenabled FROM pg_trigger WHERE tgname='trg_update_articulo_unidades';
(esperado: 'O')

Unidades-sync check:
SELECT count(\*) FROM articulos a LEFT JOIN (SELECT articulo_sku, SUM(cantidad) AS s FROM existencias GROUP BY articulo_sku) e ON e.articulo_sku=a.sku WHERE a.unidades != COALESCE(e.s, 0);
(esperado: 0)

Audit informativo D-01 (de RESEARCH.md lineas 666-674):
SELECT
count(_) FILTER (WHERE sku IS NULL) AS null_sku,
count(_) FILTER (WHERE sku = codigo) AS sku*eq_codigo,
count(\*) FILTER (WHERE sku = regexp_replace(codigo, '[-*.[:space:]]+', '', 'g')) AS sku_eq_stripsep,
count(_) FILTER (WHERE sku IS NOT NULL AND sku != codigo) AS sku_diff_codigo,
count(_) FILTER (WHERE sku IS NOT NULL) - count(DISTINCT sku) FILTER (WHERE sku IS NOT NULL) AS sku_dupes,
count(\*) AS total
FROM articulos;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear scripts/phase31-preflight-audit.sh y scripts/phase31-validation.sh</name>
  <read_first>
    - scripts/phase38-preflight-backup.sh (lineas 1-100, analog primario)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md (lineas 644-696 entrega phase31-preflight-audit.sh completo; lineas 1011-1018 + 1124-1127 entregan queries de validation)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md (lineas 676-755 detallan divergencias respecto a phase38)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-VALIDATION.md (lineas 92-107 detallan los Critical Failure Modes y queries)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-CONTEXT.md (D-01 non-blocking, decisiones cerradas)
  </read_first>
  <action>
Crear ambos scripts en `/scripts/` root del repo (gap pattern mapper #2: NO en apps/backend/scripts/).

phase31-preflight-audit.sh implementa D-01 (auditoria informativa NON-BLOCKING):

- Shebang `#!/usr/bin/env bash` + `set -euo pipefail`
- TIMESTAMP=$(date -u +%Y-%m-%dT%H:%M:%SZ)
- OUT=".planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PREFLIGHT-AUDIT.md"
- Verifica que container `postgres` esta running (`docker ps --format '{{.Names}}' | grep -q '^postgres$'`)
- Genera markdown con heredoc + invoca docker exec postgres psql -U sanchez -d erp_sanchez -tA -F'|' con la query del bloque <interfaces> "Audit informativo D-01"
- Append section "Interpretacion" explicando cada count (texto literal de RESEARCH lineas 680-686)
- Append section "Decision" notando que D-02 ejecuta overwrite ciego, y que si sku_dupes>0 post-stripSep simulation hay que escalar al usuario
- SIEMPRE exit 0 (es informativo no-blocking por D-01)
- Imprime al stdout "Preflight audit guardado en $OUT"

phase31-validation.sh implementa el gate-keeper entre deploys:

- Shebang + `set -euo pipefail`
- Acepta argumento posicional: `integrity`, `pk-swap`, `triggers`, `unidades-sync`, `all`
- Si no se pasa argumento: imprime usage y exit 1
- Para cada sub-comando, invoca docker exec postgres psql -tAc con la query correspondiente del bloque <interfaces>
- Cada query devuelve un count; el script compara contra el esperado (0 para integrity y unidades-sync, "sku" para pk-swap, "O" para triggers)
- Imprime `✓ <check>: <value>` si pasa, `❌ <check>: <expected> got <got>` si falla
- Exit 0 si todos los checks pasan, exit 1 al primer fallo
- Sub-comando `all`: corre integrity + pk-swap + triggers + unidades-sync en secuencia, exit 1 al primer fallo

NO crear directorio apps/backend/scripts/. NO usar pnpm db:push --force (lección feedback_db_push_force_prod.md).
Ambos scripts deben ser ejecutables (`chmod +x`).

Threats mitigados: T-31-04 (FK orphan post-cutover gateado por integrity check), T-31-SC (legitimacy package N/A - no se instalan deps en esta task).
</action>
<verify>
<automated>test -x scripts/phase31-preflight-audit.sh && test -x scripts/phase31-validation.sh && bash -n scripts/phase31-preflight-audit.sh && bash -n scripts/phase31-validation.sh && scripts/phase31-validation.sh 2>&1 | grep -q -i "usage\|integrity\|pk-swap"</automated>
</verify>
<done> - scripts/phase31-preflight-audit.sh existe, ejecutable, sintaxis bash valida. - scripts/phase31-validation.sh existe, ejecutable, sintaxis bash valida, requiere argumento. - phase31-preflight-audit.sh siempre exit 0 (incluso si counts altos). - phase31-validation.sh exit 1 cuando se invoca sin argumento. - Ambos usan `docker exec postgres psql -U sanchez -d erp_sanchez`.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instalar framework de testing backend (Jest + supertest + @nestjs/testing) y crear skeletons de tests SC#3/SC#4</name>
  <read_first>
    - apps/backend/package.json (verificar deps actuales: no jest, no supertest)
    - apps/backend/tsconfig.json (compilacion CommonJS, paths)
    - apps/backend/src/main.ts (bootstrap de la app NestJS)
    - apps/backend/src/app.module.ts (AppModule completo)
    - apps/backend/src/modules/articulos/articulos.controller.ts (controller actual con rutas :codigo)
    - apps/backend/src/modules/articulos/articulos.service.ts (service con findOne/create/update por codigo)
    - apps/backend/src/modules/webhooks/webhooks.service.ts (lineas 155-215 dispatch, 289-314 deliverWithRetry)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md (lineas 1029-1031 Wave 0 Gaps, lineas 1011-1017 SC#3/SC#4 test contracts)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md (Critical Note #1 lineas 1202: framework gap)
  </read_first>
  <behavior>
    - Test articulos-helper.spec: ArticulosHelper.resolveSku('CODIGO-A') retorna 'CODIGOA' (stripSep aplicado) cuando articulo existe; lanza NotFoundException cuando articulo no existe. PLACEHOLDER - el helper se crea en Plan 31-02, este test queda como skeleton RED.
    - Test articulos-phase31.e2e-spec SC#3: GET /api/articulos/by-codigo/:codigo retorna 200 con array de articulos donde cada item tiene sku y codigo. PLACEHOLDER - la ruta se crea en Plan 31-03, este test queda como skeleton RED hasta entonces.
    - Test articulos-phase31.e2e-spec SC#4: POST /api/articulos con dto valido emite evento articulo.created y crea fila en webhook_deliveries cuyo payload.articulo.sku !== null. PLACEHOLDER - depende de overwrite Plan 31-02, skeleton RED.
  </behavior>
  <action>
Decision Plan 31-01 (cerrando gap pattern mapper #1): INSTALAR framework de testing como Wave 0 prep, NO diferir a smoke scripts. Razon: SC#3 y SC#4 son automated verifications que se corren entre Deploy 2 y Deploy 3; reemplazarlos por smoke manuales convierte el gate en interactive y fragiliza el calendario.

Pasos:

1. Editar `apps/backend/package.json` agregando devDependencies (verificar con Context7 que las versiones son compatibles con NestJS 10 y TypeScript actual del repo):
   - "jest": "^29.7.0"
   - "@types/jest": "^29.5.12"
   - "ts-jest": "^29.1.2"
   - "supertest": "^7.0.0"
   - "@types/supertest": "^6.0.2"
   - "@nestjs/testing": "^10.0.0"
     Agregar scripts:
   - "test": "jest"
   - "test:e2e": "jest --config jest.config.cjs --testPathPattern=e2e-spec"
   - "test:unit": "jest --config jest.config.cjs --testPathPattern=**tests**"

2. Crear `apps/backend/jest.config.cjs` con:
   - preset 'ts-jest'
   - testEnvironment 'node'
   - roots: ['<rootDir>/src', '<rootDir>/test']
   - testRegex: '.\*\\.(spec|e2e-spec)\\.ts$'
   - moduleFileExtensions: ['ts', 'js', 'json']
   - transform usando ts-jest con tsconfig
   - setupFilesAfterEnv: ['<rootDir>/test/jest-setup.ts']
   - testTimeout: 30000 (E2E tests pueden tardar conectando a postgres docker)

3. Crear `apps/backend/test/jest-setup.ts` con:
   - process.env.NODE_ENV = 'test'
   - Loading de .env.test si existe (jest.setEnvironment)

4. Crear `apps/backend/src/modules/articulos/__tests__/articulos-helper.spec.ts` como skeleton:
   - describe('ArticulosHelper', () => { ... })
   - Test 1: 'resolveSku returns canonical sku for existing articulo' - usa Test.createTestingModule mock de DrizzleService; assert resolveSku('ABC-001') === 'ABC001'
   - Test 2: 'resolveSku throws NotFoundException for missing articulo' - mock devuelve [], assert rejects.toThrow(NotFoundException)
   - Marcar tests con `it.skip(...)` o `it.todo(...)` ya que ArticulosHelper aun no existe (lo crea Plan 31-02). Comentar que se unskippa cuando el helper exista.

5. Crear `apps/backend/test/articulos-phase31.e2e-spec.ts` como skeleton:
   - describe('Phase 31 - Articulos rekey + webhook payload v2', () => { ... })
   - beforeAll: NestFactory.create app instance, app.init()
   - afterAll: app.close()
   - Test SC#3a (skipped): 'GET /api/articulos/:sku retorna 1 fila' - usa supertest.expect(200), assert body.sku
   - Test SC#3b (skipped): 'GET /api/articulos/by-codigo/:codigo retorna array de hermanas' - assert Array.isArray(body), body[0].sku
   - Test SC#4 (skipped): 'POST /api/articulos emite webhook con payload.articulo.sku no-null' - inserta articulo, espera 1s, query directa a webhook_deliveries (drizzle), assert payload.articulo.sku !== null
   - Todos los `it.skip` o `it.todo` con comentario explicando que cada test depende de Plan 31-02/03.

6. NO crear ArticulosHelper aqui (es responsabilidad de Plan 31-02). NO crear ruta `/by-codigo/:codigo` aqui (es Plan 31-03). Este task SOLO instala el framework y deja skeletons RED.

7. PREPARAR (no ejecutar) el comando `pnpm install` en `apps/backend/` que el operador correrá en Task 3 (checkpoint). En este Task 2:
   - Editar `apps/backend/package.json` con las nuevas devDependencies + scripts (lo unico que se commitea).
   - NO ejecutar `pnpm install` aún (eso modifica `pnpm-lock.yaml` en el filesystem del developer; el operador del cutover lo ejecuta en Task 3 tras revisar el diff de package.json y dar approve).
   - NO ejecutar `pnpm --filter backend test` aún (depende de install + lock previo).
   - El verify command de este task SOLO confirma que package.json incluye las deps declaradas y que los archivos de test/config existen y compilan sintacticamente (tsc --noEmit sobre los .spec/.e2e-spec).

Package legitimacy gate (cerrado en este Task 2, sin checkpoint humano): jest, ts-jest, supertest, @nestjs/testing son paquetes oficiales canonical en npm registry con millones de downloads/semana — NO `[ASSUMED]` ni `[SUS]`. Context7 confirma versiones compatibles con NestJS 10. Es seguro AGREGAR las deps a `package.json` (texto solamente) sin checkpoint.

IMPORTANTE — separacion de PREPARAR vs EJECUTAR (resuelve contradiccion autonomous/checkpoint identificada por checker #5):

- Task 2 (este) = PREPARA: edita `package.json`, crea jest.config.cjs, crea archivos de test skeleton, sin tocar `pnpm-lock.yaml` ni `node_modules/`.
- Task 3 (checkpoint blocking) = EJECUTA: el operador (que ya tiene que correr pg_dump/restore-test) corre `pnpm install` como un paso mas dentro del checkpoint, tras revisar el diff de `package.json`. La modificacion de `pnpm-lock.yaml` queda gated por la aprobacion humana del cutover plan.
- Por eso `autonomous: false` en el frontmatter del plan: el plan en su conjunto requiere el checkpoint de Task 3 para completarse end-to-end.

Threats mitigados: T-31-T (Testing infrastructure missing = SC#3/SC#4 sin gate automatizado).
</action>
<verify>
<automated>grep -q '"jest"' apps/backend/package.json && grep -q '"@nestjs/testing"' apps/backend/package.json && grep -q '"supertest"' apps/backend/package.json && test -f apps/backend/jest.config.cjs && test -f apps/backend/test/jest-setup.ts && test -f apps/backend/test/articulos-phase31.e2e-spec.ts && test -f apps/backend/src/modules/articulos/**tests**/articulos-helper.spec.ts</automated>
</verify>
<done> - apps/backend/package.json incluye jest, ts-jest, supertest, @nestjs/testing en devDependencies. - apps/backend/jest.config.cjs existe con preset ts-jest y testRegex correcto. - apps/backend/test/jest-setup.ts existe. - apps/backend/test/articulos-phase31.e2e-spec.ts existe con 3 tests skipped (SC#3a, SC#3b, SC#4). - apps/backend/src/modules/articulos/**tests**/articulos-helper.spec.ts existe con 2 tests skipped. - `pnpm --filter backend test` NO se ejecuta en este task (depende de `pnpm install` que corre el operador en Task 3); en su lugar el verify confirma que los archivos existen y package.json declara las deps.
</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operador ejecuta pg_dump full pre-cutover + restore-test drill (D-04)</name>
  <what-built>
Scripts phase31-preflight-audit.sh y phase31-validation.sh listos para usar; framework de testing instalado.

Esta task requiere al humano porque (a) el pg_dump corre contra DB de produccion VPS y necesita credenciales/acceso del operador; (b) el restore-test consume disco temporal real; (c) el calendario de cutover requiere aprobacion humana del horario y del path final del .dump.
</what-built>
<how-to-verify> 0. **Ejecutar `pnpm install` para el framework de testing (gate del legitimacy diff de Task 2):**

- El operador inspecciona el diff de `apps/backend/package.json` que entregó Task 2 (devDependencies jest, ts-jest, supertest, @nestjs/testing + scripts test:\*).
- Confirma que las versiones coinciden con lo declarado y que no se introdujeron paquetes adicionales.
- Ejecuta:
  ```bash
  cd apps/backend && pnpm install
  ```
- Verifica que `pnpm-lock.yaml` se actualizo con los nuevos paquetes y que `pnpm --filter backend test` retorna exit 0 con 0 failed (tests skipped no rompen).
- Si algo del diff sorprende al operador, ABORT y reportar — la fase no avanza.

1. **Crear directorio de backups del Phase 31 en el VPS (manual, una sola vez):**

   ```bash
   sudo mkdir -p /var/backups/erp_sanchez/phase31
   sudo chown $(whoami) /var/backups/erp_sanchez/phase31
   ```

2. **Ejecutar pg_dump full baseline (humano corre desde VPS, en ventana de bajo trafico):**

   ```bash
   TS=$(date -u +%Y%m%d_%H%M%S)
   docker exec postgres pg_dump -U sanchez -d erp_sanchez -Fc \
     -f /var/backups/erp_sanchez/phase31/pre_deploy1_${TS}.dump
   docker exec postgres ls -lh /var/backups/erp_sanchez/phase31/pre_deploy1_${TS}.dump
   ```

   Esperar tamaño > 0 (esperado 40-100 MB segun research).

3. **Smoke restore-test a DB temporal (humano corre desde VPS):**

   ```bash
   docker exec postgres createdb -U sanchez erp_phase31_smoke
   docker exec postgres pg_restore -U sanchez -d erp_phase31_smoke /var/backups/erp_sanchez/phase31/pre_deploy1_${TS}.dump
   docker exec postgres psql -U sanchez -d erp_phase31_smoke -tAc "SELECT count(*) FROM articulos"
   docker exec postgres dropdb -U sanchez erp_phase31_smoke
   ```

   Esperar count >= 100000 (segun CONTEXT.md son 101.021).

4. **Ejecutar el preflight audit:**

   ```bash
   bash scripts/phase31-preflight-audit.sh
   cat .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PREFLIGHT-AUDIT.md
   ```

   Verificar que el archivo se creo y los counts son numericos.

5. **Crear 31-CUTOVER-CALENDAR.md con horarios aprobados:**
   El operador escribe en `.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-CUTOVER-CALENDAR.md`:
   - Deploy 1 (expand) fecha/hora UTC-3 propuesta
   - Deploy 2 (switch) fecha/hora UTC-3 propuesta (24-48h despues de Deploy 1)
   - Deploy 3 (contract) fecha/hora UTC-3 propuesta (24-48h despues de Deploy 2)
   - Abort criteria (de RESEARCH lineas 1147-1150)
   - Metricas a monitorear durante soak (de RESEARCH lineas 1131-1144)
   - Validar contra ventana de bajo trafico real (ejemplo 03:00-05:00 UTC-3 segun research, pero el operador confirma).

6. **Reportar resultados al planner:**
   - Path absoluto del .dump generado (lo necesita Plan 31-02 como "pre-Deploy-1 baseline").
   - Counts del preflight audit (null_sku, sku_dupes en particular).
   - Si sku_dupes > 0 post-overwrite simulado, INVESTIGAR antes de aprobar continuacion a Wave 1.

  </how-to-verify>
  <resume-signal>Escribir "approved" + path del .dump + counts del audit + confirmacion de 31-CUTOVER-CALENDAR.md aprobado. Si sku_dupes > 0 reportar como blocker.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                                                          |
| -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| Operador humano -> Docker postgres container | El humano ejecuta pg_dump y restore con privilegios del usuario postgres dentro del container; el path /var/backups/erp_sanchez/ esta en el host VPS |
| Bash scripts -> Postgres via docker exec     | Los scripts no aceptan input no-confiable; las queries SQL son hardcoded sin string interpolation de usuario                                         |
| Plan 31-02 -> ArticulosHelper.resolveSku     | El helper se crea en Plan 31-02; Plan 31-01 solo provee el skeleton del test                                                                         |

## STRIDE Threat Register

| Threat ID | Category                                                  | Component                                           | Disposition                                                                                                                                                      | Mitigation Plan                                                                                                                                                                 |
| --------- | --------------------------------------------------------- | --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-31-01   | Tampering                                                 | scripts/phase31-validation.sh queries SQL           | mitigate                                                                                                                                                         | Queries hardcoded sin interpolation de variables de entorno; psql `-tAc` ejecuta literalmente sin parsear como string template                                                  |
| T-31-02   | Information Disclosure                                    | pg_dump output en /var/backups/erp_sanchez/phase31/ | mitigate                                                                                                                                                         | Directorio creado con `chown $(whoami)`; permisos default 750; el .dump es binario custom (-Fc) que requiere pg_restore para leer; no exponer ruta a usuarios sin acceso al VPS |
| T-31-03   | Repudiation                                               | Operador olvida pg_dump pre-cutover                 | mitigate                                                                                                                                                         | Checkpoint blocking en Task 3 que requiere reportar path absoluto del .dump como `resume-signal`; sin esta evidencia, Wave 1 NO arranca                                         |
| T-31-04   | Tampering / Data Integrity                                | FK orphan post-cutover detectado tarde              | mitigate                                                                                                                                                         | phase31-validation.sh integrity check obligatorio entre cada deploy; las 5 queries SC#5 deben retornar 0                                                                        |
| T-31-T    | Testing infrastructure missing                            | Backend sin Jest -> SC#3/SC#4 sin automated gate    | mitigate                                                                                                                                                         | Task 2 instala framework como Wave 0; tests quedan skeletons RED hasta Plan 31-02/03 los implementen GREEN                                                                      |
| T-31-SC   | Supply chain (npm install jest/supertest/@nestjs/testing) | accept                                              | jest/@nestjs/testing/supertest son packages oficiales canonical (millones de downloads/semana, mantenidos por NestJS team y Facebook); no son [ASSUMED] ni [SUS] |
| T-31-DR   | DR drill failure (dump no restorable)                     | mitigate                                            | Task 3 hace smoke restore-test obligatorio a DB temporal `erp_phase31_smoke`; si pg_restore falla, halt y reportar                                               |

</threat_model>

<verification>
- scripts/phase31-preflight-audit.sh + scripts/phase31-validation.sh existen, ejecutables, syntax check OK
- apps/backend/package.json tiene devDependencies jest, ts-jest, supertest, @nestjs/testing
- pnpm --filter backend test corre y reporta 0 failed (skeletons skipped/todo)
- .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PREFLIGHT-AUDIT.md generado con counts numericos
- .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-CUTOVER-CALENDAR.md aprobado por usuario
- pg_dump pre-Deploy-1 baseline existe en /var/backups/erp_sanchez/phase31/pre_deploy1_*.dump, tamaño > 0, restoreable
- Si sku_dupes > 0 post-stripSep: bloquear avance a Wave 1 hasta resolver
</verification>

<success_criteria>
Wave 0 completa cuando:

1. Ambos scripts (preflight + validation) viven en `/scripts/` root del repo, ejecutables, syntax-valid.
2. Backend testing framework instalado: `pnpm --filter backend test` exit 0.
3. 5 archivos de test skeleton creados (jest.config.cjs, jest-setup.ts, articulos-phase31.e2e-spec.ts, articulos-helper.spec.ts, + ts-jest config implícita).
4. pg_dump baseline existe y se valido restore en DB temporal con count(\*) FROM articulos >= 100000.
5. 31-PREFLIGHT-AUDIT.md commiteado con counts informativos de 6 columnas (null_sku, sku_eq_codigo, sku_eq_stripsep, sku_diff_codigo, sku_dupes, total).
6. 31-CUTOVER-CALENDAR.md aprobado por usuario con horarios concretos + abort criteria + metricas de soak.
7. Plan 31-02 (Wave 1) puede arrancar sin blockers — todos los gates de validacion ya existen.
   </success_criteria>

<output>
Create `.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-01-SUMMARY.md` when done documentando: paths de scripts, hash del .dump pg_dump, counts del preflight audit, decisiones del calendario aprobado, links a archivos creados.
</output>
