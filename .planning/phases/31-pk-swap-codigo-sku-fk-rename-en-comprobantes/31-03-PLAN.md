---
phase: 31
plan: 03
plan_id: 31-03
type: execute
wave: 2
depends_on:
  - 31-02
files_modified:
  - apps/backend/drizzle/0010_phase31_switch.sql
  - apps/backend/drizzle/meta/_journal.json
  - apps/backend/src/db/schema.ts
  - apps/backend/src/modules/articulos/articulos.controller.ts
  - apps/backend/src/modules/articulos/articulos.service.ts
  - apps/backend/src/modules/articulos/articulos-imagenes.controller.ts
  - apps/backend/src/modules/articulos/articulos-imagenes.service.ts
  - apps/backend/src/modules/existencias/existencias.service.ts
  - apps/backend/src/modules/inventarios/inventarios.service.ts
  - apps/backend/src/modules/dashboard/dashboard.service.ts
  - apps/backend/test/articulos-phase31.e2e-spec.ts
  - apps/web/src/app/(dashboard)/articulos/[sku]/editar/page.tsx
  - apps/web/src/app/(dashboard)/articulos/articulos-client.tsx
  - apps/web/src/components/articulos/articulo-sheet.tsx
  - apps/web/src/components/articulos/imagen-slot.tsx
  - apps/web/src/components/articulos/imagen-slot-grid.tsx
  - apps/web/src/lib/api.client.ts
  - apps/web/src/types/order.ts
  - apps/web/src/types/sale.ts
  - apps/web/src/types/purchase.ts
  - apps/web/src/types/existencia.ts
  - apps/web/src/types/inventario.ts
  - apps/web/src/types/dashboard.ts
  - apps/web/src/components/settings/webhooks/webhooks-client.tsx
autonomous: false
requirements:
  - VAR-10

must_haves:
  truths:
    - "articulos tiene sku como PRIMARY KEY (verificable via pg_constraint con contype='p' devuelve attname='sku')"
    - 'Las 5 hijas tienen FK articulo_sku apuntando a articulos.sku (5 constraints con sufijo _articulo_sku_fkey)'
    - 'Backend rekey completo: /api/articulos/:sku (GET/PATCH/DELETE), nuevo /api/articulos/by-codigo/:codigo retorna Articulo[]'
    - 'Frontend rekey: /articulos/[sku]/editar carga sin 404; api.client.ts usa :sku en URLs'
    - 'Webhook payload v2: cada delivery post-Deploy-2 tiene payload.articulo.sku no-null (E2E test passing)'
    - 'Trigger trg_update_articulo_unidades reescrito (keyea por articulo_sku), ENABLED post-cutover, articulos.unidades recomputed'
    - 'Alert visible en /settings/webhooks informando del cambio de payload v2'
  artifacts:
    - path: 'apps/backend/drizzle/0010_phase31_switch.sql'
      provides: 'Migration Drizzle custom Deploy 2 - 9-step ordered transaction con LOCK ACCESS EXCLUSIVE'
      contains: 'LOCK TABLE articulos, order_items, sale_items, purchase_items, existencias, inventarios_articulos IN ACCESS EXCLUSIVE MODE'
    - path: 'apps/backend/src/modules/articulos/articulos.controller.ts'
      provides: 'Controller rekey con findByCodigo declarado ANTES de findOne(:sku)'
      contains: 'by-codigo'
    - path: 'apps/backend/src/db/schema.ts'
      provides: 'Schema TS: articulos.sku PK, articulos.codigo NOT NULL drop + index codigo, 5 hijas notnull FK a articulos.sku, existencias PK [articuloSku, depositoId], inv_articulos unique [inventarioId, articuloSku]'
      contains: 'primaryKey({ columns: [table.sku]'
    - path: 'apps/web/src/app/(dashboard)/articulos/[sku]/editar/page.tsx'
      provides: 'Page renombrada de [codigo] a [sku], useParams<{sku:string}>'
      contains: 'useParams<{ sku: string }>'
    - path: 'apps/web/src/components/settings/webhooks/webhooks-client.tsx'
      provides: 'Notice <Alert> sobre payload v2 antes de la tabla de webhooks'
      contains: 'Cambio en el payload de articulo'
  key_links:
    - from: 'apps/backend/src/modules/articulos/articulos.controller.ts'
      to: 'apps/backend/src/modules/articulos/articulos.service.ts findByCodigo'
      via: 'controller -> service call'
      pattern: 'findByCodigo'
    - from: 'apps/web/src/lib/api.client.ts'
      to: '/api/articulos/:sku y /api/articulos/by-codigo/:codigo'
      via: 'fetch URL'
      pattern: "/api/articulos/(\\$\\{|by-codigo)"
    - from: 'trigger update_articulo_unidades function'
      to: 'existencias.articulo_sku + articulos.sku'
      via: 'CREATE OR REPLACE FUNCTION'
      pattern: 'WHERE articulo_sku = target_sku'
---

<objective>
Deploy 2 (switch): la fase de mayor riesgo del milestone. Ejecutar el 9-step ordered transaction de PITFALLS §P-01 con LOCK ACCESS EXCLUSIVE para hacer el PK swap (codigo -> sku) en articulos + recrear las 5 FKs hijas apuntando a articulos.sku + reescribir el trigger trg_update_articulo_unidades para keyear por sku + rekey API backend a /api/articulos/:sku + rekey frontend a /articulos/[sku]/editar + agregar notice en /settings/webhooks. El payload webhook v2 incluye sku automaticamente (bump semantico).

Purpose: Materializar VAR-10 — los comprobantes (orders/sales/purchases items, existencias, inventarios_articulos) referencian sku como identificador universal. Despues de este Deploy: sku es PK estable, codigo queda como agrupador indexado NOT UNIQUE listo para variantes de Phase 32.
Output: Migration 0010 aplicada, backend + frontend rekey-eados, E2E tests passing, notice visible.

Esta Wave NO elimina articulo_codigo columna de las hijas (eso es Deploy 3 / Plan 31-04). NO crea articulo_sku_history (Phase 33). NO agrega guard pg_trigger_depth (D-16, diferido a Phase 33).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-CONTEXT.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md
@.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-VALIDATION.md
@.planning/research/PITFALLS.md
@apps/backend/src/db/migrate-unidades.sql
@apps/backend/drizzle/0008_phase30_templates.sql
@apps/backend/src/db/schema.ts
@apps/backend/src/modules/articulos/articulos.controller.ts
@apps/backend/src/modules/articulos/articulos.service.ts
@apps/backend/src/modules/webhooks/webhooks.listener.ts
@apps/web/src/app/(dashboard)/articulos/articulos-client.tsx
@apps/web/src/components/articulos/articulo-sheet.tsx
@apps/web/src/lib/api.client.ts
@apps/web/src/components/ui/alert.tsx
@apps/web/src/components/settings/webhooks/webhooks-client.tsx

<interfaces>
9-step ordered transaction (RESEARCH.md lineas 278-433, sigue PITFALLS §P-01 lineas 28-67 literalmente):

STEP 1: LOCK TABLE articulos, order_items, sale_items, purchase_items, existencias, inventarios_articulos IN ACCESS EXCLUSIVE MODE.

STEP 2: DO block pre-check con 6 sub-checks (research 291-311):

- count(\*) FROM articulos WHERE sku IS NULL == 0 sino RAISE EXCEPTION
- count(\*) - count(DISTINCT sku) FROM articulos == 0 sino RAISE EXCEPTION sku dupes
- count(\*) FROM order_items WHERE articulo_sku IS NULL == 0 (idem para 5 hijas)

STEP 3: ALTER TABLE existencias DISABLE TRIGGER trg_update_articulo_unidades.

STEP 4 (PK swap articulos):

- ALTER TABLE articulos DROP CONSTRAINT articulos_pkey CASCADE (elimina las 5 FKs viejas automaticamente)
- ALTER TABLE articulos ALTER COLUMN sku SET NOT NULL
- ALTER TABLE articulos ADD CONSTRAINT articulos_pkey PRIMARY KEY (sku)
- ALTER TABLE articulos ALTER COLUMN codigo DROP NOT NULL
- DROP INDEX IF EXISTS articulos_sku_idx (redundante con PK)
- CREATE INDEX IF NOT EXISTS articulos_codigo_idx ON articulos(codigo) (NO unique - codigo es agrupador)

STEP 5 (FK rebuild en 5 hijas):

- 5.1 existencias: DROP CONSTRAINT existencias_pkey, ALTER COLUMN articulo_sku SET NOT NULL, ADD CONSTRAINT existencias_pkey PRIMARY KEY (articulo_sku, deposito_id)
- 5.2 inventarios_articulos: DROP INDEX inv_articulos_unique_idx, ALTER COLUMN articulo_sku SET NOT NULL, CREATE UNIQUE INDEX inv_articulos_unique_idx ON inventarios_articulos(inventario_id, articulo_sku)
- 5.3: order_items, sale_items, purchase_items - ALTER COLUMN articulo_sku SET NOT NULL
- 5.4: 5 hijas ADD CONSTRAINT <tabla>\_articulo_sku_fkey FOREIGN KEY (articulo_sku) REFERENCES articulos(sku) ON UPDATE CASCADE ON DELETE RESTRICT

STEP 6 (trigger function rewrite via CREATE OR REPLACE - mismo nombre por D-14):

- CREATE OR REPLACE FUNCTION update_articulo_unidades() RETURNS TRIGGER con cuerpo: target_sku := OLD/NEW.articulo_sku segun TG_OP, UPDATE articulos SET unidades = COALESCE((SELECT SUM(cantidad) FROM existencias WHERE articulo_sku = target_sku), 0) WHERE sku = target_sku, RETURN COALESCE(NEW, OLD).

STEP 7 (recompute manual D-13 P-02 capa 1):
UPDATE articulos a SET unidades = COALESCE((SELECT SUM(e.cantidad) FROM existencias e WHERE e.articulo_sku = a.sku), 0).

STEP 8: ALTER TABLE existencias ENABLE TRIGGER trg_update_articulo_unidades.

STEP 9 (validacion final DO block):

- pg_constraint count where table='articulos' AND contype='p' == 1
- pg_constraint count where conname IN (5 nombres \_articulo_sku_fkey) == 5
- pg_trigger count where tgname='trg_update_articulo_unidades' AND tgenabled='O' == 1
- RAISE NOTICE 'Deploy 2 (switch) OK'

Backend controller rekey (research 438-501 + patterns 395-444):

- articulos.controller.ts: PRIMERO declarar @Get('by-codigo/:codigo') findByCodigo() - ANTES de @Get(':sku') por orden de path-specificity (research §A6). Luego @Get(':sku'), @Patch(':sku'), @Patch(':sku/toggle'), @Delete(':sku') (rekey 5 rutas). Mantener @UseGuards(RolesGuard) @Roles('admin') sin cambios.
- articulos-imagenes.controller.ts: rekey 3 rutas (@Post(':sku/imagenes'), @Post(':sku/imagenes/from-url'), @Delete(':sku/imagenes/:tipo/:slot')).

Service rekey:

- articulos.service.ts: findOne(sku) usa eq(articulos.sku, sku). findByCodigo(codigo): Promise<typeof articulos.$inferSelect[]> retorna db.select().from(articulos).where(eq(articulos.codigo, codigo)).orderBy(asc(articulos.sku)). update/toggleActive/softDelete keyean por sku. NotFoundException message cambia "Articulo con codigo X" -> "Articulo con sku X" en los keyeados-por-sku, mantener "codigo" en findByCodigo.
- articulos-imagenes.service.ts: todos los eq(articulos.codigo, ...) -> eq(articulos.sku, ...).
- existencias.service.ts: eq(existencias.articuloCodigo, ...) y joins en articulos.codigo -> eq(existencias.articuloSku, ...) y eq(articulos.sku, ...). El target del onConflictDoUpdate cambia a [existencias.articuloSku, existencias.depositoId]. El path /api/existencias/articulo/:articuloCodigo se MANTIENE como agrupador (research linea 507) — el controller no cambia.
- inventarios.service.ts: joins por articulo_sku. La catch de 23505 sigue valida.
- dashboard.service.ts: aggregations por articulo_sku.

Webhook payload (D-09/D-11): NO CAMBIA CODIGO. articulo emit en articulos.service.ts ya es rows[0] (fila completa de Drizzle) que post-Deploy-1 contiene sku. Bump v2 es semantico (research linea 503-505).

Frontend rekey:

- RENAME dir apps/web/src/app/(dashboard)/articulos/[codigo]/ -> [sku]/. Dentro de editar/page.tsx: useParams<{ codigo: string }>() -> <{ sku: string }>, decodeURIComponent(params.codigo) -> decodeURIComponent(params.sku), llamadas a api.client.ts cambiar de fetchArticuloByCodigoClient(codigo) -> fetchArticuloBySkuClient(sku), deleteArticulo(articulo.codigo) -> deleteArticulo(articulo.sku), toggleArticuloActivo(articulo.codigo) -> toggleArticuloActivo(articulo.sku). Props articuloCodigo en ImagenSlotGrid/ImagenSlot -> articuloSku.
- articulos-client.tsx linea 154: cambiar a router.push(/articulos/${encodeURIComponent(articulo.sku)}/editar).
- articulo-sheet.tsx linea 199: <Link href={/articulos/${encodeURIComponent(articulo.sku)}/editar}>. Linea 162 MANTIENE fetchExistenciasByArticuloClient(articulo.codigo) (path by-codigo agrupador). Linea 190 display text mantiene {articulo.codigo} (Phase 32 va a separar codigo vs sku en columnas).
- imagen-slot.tsx + imagen-slot-grid.tsx: rename prop articuloCodigo -> articuloSku + internal calls (uploadArticuloImagen, deleteArticuloImagen).
- api.client.ts: rename 6 funciones (research 532-538): fetchArticuloByCodigoClient -> fetchArticuloBySkuClient (URL cambia a /api/articulos/{sku}). updateArticulo/toggleArticuloActivo/deleteArticulo: param codigo -> sku + URL change. uploadArticuloImagen/deleteArticuloImagen: idem. Agregar NUEVA funcion fetchArticulosByCodigoClient(codigo): Promise<Articulo[]> que llama a /api/articulos/by-codigo/:codigo.
- Types order/sale/purchase/existencia/inventario/dashboard.ts: agregar articuloSku: string notnull al lado de articuloCodigo: string existente (coexistencia hasta Deploy 3 segun research §"Open Decisions" #4). Existencia.ts linea 9: cambiar articuloSku: string | null a articuloSku: string. ExistenciaMatrixRow tambien agrega articuloSku: string.

Notice Alert en webhooks-client.tsx (research 553-571):

- Import: import { Alert, AlertTitle, AlertDescription } from '@/components/ui/alert' + import { Info } from 'lucide-react'.
- Posicionado dentro del top-level <div className="space-y-4"> (linea 273), ANTES del condicional {webhooks.length === 0 ? ... : ...}.
- Texto literal en es-AR:
  AlertTitle: "Cambio en el payload de articulo.\* desde v1.3"
  AlertDescription: "Los eventos articulo.created, articulo.updated y articulo.deleted ahora incluyen el campo sku dentro del objeto articulo, ademas del codigo existente. sku es el identificador unico de cada fila; codigo puede agrupar variantes cuando una misma referencia tiene atributos multiples. Los suscriptores que solo leen codigo siguen funcionando sin cambios."

Schema.ts cambios Deploy 2 (research 540-545):

- articulos: codigo deja de tener .primaryKey() (mover constraint a primaryKey({ columns: [table.sku] }) en el array de constraints). Agregar index('articulos_codigo_idx').on(table.codigo). QUITAR el index('articulos_sku_idx') existente (redundante con PK).
- 5 hijas: articuloSku pasa a .notNull().references(() => articulos.sku, { onDelete: 'restrict', onUpdate: 'cascade' }). articuloCodigo se mantiene (nullable, sin .references — la FK ya fue dropped por CASCADE).
- existencias: primaryKey({ columns: [table.articuloCodigo, table.depositoId] }) -> primaryKey({ columns: [table.articuloSku, table.depositoId] }).
- inventarios_articulos: uniqueIndex('inv_articulos_unique_idx').on(table.inventarioId, table.articuloCodigo) -> .on(table.inventarioId, table.articuloSku).

E2E test articulos-phase31.e2e-spec.ts (unskippear 3 tests SC#3a, SC#3b, SC#4):

- SC#3a: request(app).get(/api/articulos/${createdSku}).expect(200), assert body.sku.
- SC#3b: request(app).get(/api/articulos/by-codigo/${createdCodigo}).expect(200), assert Array.isArray(body), body[0].sku.
- SC#4: POST /api/articulos con dto valido + setup de webhook activo + wait 2s + drizzle select FROM webhook_deliveries ORDER BY created_at DESC LIMIT 1, assert delivery.payload.articulo.sku !== null.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear migration 0010 + journal idx=10 + schema.ts swap + E2E tests verdes</name>
  <read_first>
    - apps/backend/src/db/migrate-unidades.sql (lineas 15-21 patron pre-check DO block + 48-70 cuerpo trigger function + 88-92 patron recompute)
    - apps/backend/drizzle/0008_phase30_templates.sql (patron header + statement-breakpoint delimiter)
    - apps/backend/drizzle/meta/_journal.json (estructura actual con idx 9 post Plan 31-02)
    - apps/backend/src/db/schema.ts (estado post Plan 31-02: 5 hijas con articulo_sku nullable + indices)
    - .planning/research/PITFALLS.md §P-01 lineas 9-67, §P-02 lineas 69-115
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md (lineas 278-433 SQL completo 9-step + 539-545 schema.ts changes)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md (lineas 92-152 patron migration 0010)
    - apps/backend/test/articulos-phase31.e2e-spec.ts (skeleton skipped de Plan 31-01)
    - apps/backend/src/main.ts (bootstrap app)
    - apps/backend/src/app.module.ts (AppModule)
  </read_first>
  <behavior>
    - Test SC#3a: POST /api/articulos con codigo TEST31-001 crea fila con sku TEST31001. GET /api/articulos/TEST31_001 retorna status 200 con body.sku === 'TEST31_001'.
    - Test SC#3b: GET /api/articulos/by-codigo/TEST31-001 retorna status 200 con Array.isArray(body)===true, body.length === 1, body[0].sku === 'TEST31_001'.
    - Test SC#4: POST /api/articulos con webhook configurado para articulo.created. Esperar 2s. SELECT FROM webhook_deliveries ORDER BY created_at DESC LIMIT 1. Assert delivery.payload.articulo.sku === 'TEST31_001'.
  </behavior>
  <action>
1. Crear apps/backend/drizzle/0010_phase31_switch.sql con el SQL COMPLETO del bloque <interfaces> (9 steps). Copia LITERAL del RESEARCH.md lineas 278-433. Estructura:
   - Header comment (research 279-286): incluir referencia a pg_dump full path /var/backups/erp_sanchez/phase31/pre_deploy2_<TS>.dump, gate D-07 (5 queries SC#5 = 0), gate 24-48h soak desde Deploy 1, comando apply "psql --single-transaction --set ON_ERROR_STOP=1 -f 0010_phase31_switch.sql", referencias a D-08, D-13, D-14, D-15, P-01.
   - STEP 1 LOCK ACCESS EXCLUSIVE en las 6 tablas (research 287-289).
   - STEP 2 DO block pre-check con 6 sub-checks (research 291-311) — bloquea si encuentra NULL en articulos.sku, sku dupes, o NULL en articulo_sku de cualquiera de las 5 hijas.
   - STEP 3 DISABLE TRIGGER (research 313-314).
   - STEP 4 PK swap articulos (research 316-326).
   - STEP 5 (5.1-5.4) FK rebuild en 5 hijas (research 328-367).
   - STEP 6 CREATE OR REPLACE FUNCTION update_articulo_unidades() con cuerpo nuevo keyeando por sku (research 371-391). MISMO NOMBRE — D-14.
   - STEP 7 UPDATE articulos a SET unidades = COALESCE((SELECT SUM(e.cantidad) FROM existencias e WHERE e.articulo_sku = a.sku), 0) (research 395-398).
   - STEP 8 ENABLE TRIGGER (research 404).
   - STEP 9 DO block validacion final (research 407-433).
   - Cada statement termina con ";--> statement-breakpoint".

2. Actualizar apps/backend/drizzle/meta/\_journal.json agregando entry idx=10:
   { "idx": 10, "version": "7", "when": <timestamp ms al commit>, "tag": "0010_phase31_switch", "breakpoints": true }

3. Actualizar apps/backend/src/db/schema.ts segun bloque <interfaces> "Schema.ts cambios Deploy 2":
   - articulos: mover .primaryKey() de codigo a sku via array constraint primaryKey({ columns: [table.sku] }). Agregar index('articulos_codigo_idx').on(table.codigo). QUITAR el index articulos_sku_idx existente.
   - 5 hijas: articuloSku pasa a .notNull().references(() => articulos.sku, { onDelete: 'restrict', onUpdate: 'cascade' }). articuloCodigo se mantiene (nullable, sin .references — la FK ya fue dropped por CASCADE).
   - existencias: primaryKey({ columns: [table.articuloSku, table.depositoId] }).
   - inventarios_articulos: uniqueIndex('inv_articulos_unique_idx').on(table.inventarioId, table.articuloSku).

4. Unskippear y completar apps/backend/test/articulos-phase31.e2e-spec.ts:
   - beforeAll: NestFactory.create(AppModule), app.init(), inject DrizzleService desde app.get(DrizzleService).
   - Setup: crear un webhook configurado para articulo.created con secret de test antes de los tests; capturar webhookId para cleanup.
   - Cleanup en afterEach: borrar articulo TEST31-001 (db delete) y deliveries asociados.
   - afterAll: app.close().
   - Test SC#3a: const dto = { codigo: 'TEST31-001', nombre: 'Producto E2E 31' }; POST /api/articulos; expect 201; sku derivado de overwrite Drizzle = 'TEST31_001'. Luego GET /api/articulos/TEST31_001 expect(200), expect(body.sku).toBe('TEST31_001').
   - Test SC#3b: precondiciones del SC#3a + GET /api/articulos/by-codigo/TEST31-001 expect(200), expect(Array.isArray(body)).toBe(true), expect(body).toHaveLength(1), expect(body[0].sku).toBe('TEST31_001').
   - Test SC#4: POST + await new Promise(r => setTimeout(r, 2000)) + drizzle.db.select().from(webhookDeliveries).where(eq(webhookDeliveries.evento, 'articulo.created')).orderBy(desc(webhookDeliveries.createdAt)).limit(1). Assert delivery[0].payload.articulo.sku === 'TEST31_001'.

5. Verificar typecheck: pnpm --filter backend typecheck exit 0. Si schema.ts cambios rompen alguna query en services, dejar errores para Task 2 (que rekey services).

Threats mitigados: T-31-10 (PK swap a mitad - LOCK + transaction atomica), T-31-11 (Trigger broken - recompute manual antes de ENABLE como gate), T-31-12 (inv_articulos_unique_idx rota - STEP 5.2 explicito), T-31-13 (existencias PK compuesta rota - STEP 5.1 explicito).
</action>
<verify>
<automated>test -f apps/backend/drizzle/0010_phase31_switch.sql && grep -c 'LOCK TABLE' apps/backend/drizzle/0010_phase31_switch.sql | grep -q '^[1-9]' && grep -q '"tag": "0010_phase31_switch"' apps/backend/drizzle/meta/\_journal.json && grep -q 'CREATE OR REPLACE FUNCTION update_articulo_unidades' apps/backend/drizzle/0010_phase31_switch.sql</automated>
</verify>
<done> - 0010_phase31_switch.sql existe con los 9 steps (LOCK + pre-check + DISABLE + PK swap + FK rebuild + function replace + recompute + ENABLE + validacion). - \_journal.json tiene entry con tag 0010_phase31_switch idx=10. - schema.ts cambios atomicos: articulos.sku como PK, 5 hijas con FK a articulos.sku. - `articulos_sku_idx` ELIMINADO de schema.ts en este Deploy (redundante con PK sobre `sku`): `grep -c '"articulos_sku_idx"' apps/backend/src/db/schema.ts == 0`. Plus `articulos_codigo_idx` agregado: `grep -c '"articulos_codigo_idx"' apps/backend/src/db/schema.ts == 1`. - articulos-phase31.e2e-spec.ts tiene 3 tests no-skipped (it() en vez de it.skip()). - Tests SE EJECUTAN pero pueden fallar contra DB sin migration aplicada — eso es esperado; el gate real es ejecutarlos POST-migration aplicada en Task 4 (checkpoint).
</done>
</task>

<task type="auto">
  <name>Task 2a: Backend rekey (controllers + services + tests SC#3/SC#4)</name>
  <read_first>
    - apps/backend/src/modules/articulos/articulos.controller.ts (lineas 31-67 patrones rutas actuales)
    - apps/backend/src/modules/articulos/articulos.service.ts (lineas 91-159 findOne/update/toggleActive/softDelete por codigo)
    - apps/backend/src/modules/articulos/articulos-imagenes.controller.ts (3 rutas con :codigo)
    - apps/backend/src/modules/articulos/articulos-imagenes.service.ts
    - apps/backend/src/modules/existencias/existencias.service.ts (lineas 55-71 JOIN pattern + 209-254 upsert)
    - apps/backend/src/modules/inventarios/inventarios.service.ts (joins + addArticulo)
    - apps/backend/src/modules/dashboard/dashboard.service.ts (aggregations linea 23-fwd)
    - apps/backend/test/articulos-phase31.e2e-spec.ts (skeleton skipped post Plan 31-01)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md (lineas 438-507 backend rekey)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md (lineas 395-672 analogs backend)
  </read_first>
  <action>
Esta task SOLO toca backend. El split de Task 2 en 2a (backend) + 2b (frontend) responde a checker issue #3: el monolitico Task 2 cubria 22+ archivos, excediendo threshold de 15. File ownership disjunto entre 2a y 2b permite ejecucion paralela controlada o secuencial sin interferencia.

1. apps/backend/src/modules/articulos/articulos.controller.ts:
   - Declarar @Get('by-codigo/:codigo') findByCodigo() ANTES de @Get(':sku') por orden de path-specificity (research §A6).
   - Rekey 5 rutas: @Get(':sku'), @Patch(':sku'), @Patch(':sku/toggle'), @Delete(':sku'), @Param('sku'). El metodo findOne sigue lanzando NotFoundException si articulo no existe (cambiar mensaje a "Articulo con sku ${sku} no encontrado").
   - @UseGuards(RolesGuard) @Roles('admin') migran inalterados en las 4 write routes (POST sin param se mantiene igual).

2. apps/backend/src/modules/articulos/articulos.service.ts:
   - findOne(sku): cambiar parametro y `eq(articulos.codigo, codigo)` -> `eq(articulos.sku, sku)`.
   - Agregar findByCodigo(codigo): Promise<typeof articulos.$inferSelect[]>: retorna db.select().from(articulos).where(eq(articulos.codigo, codigo)).orderBy(asc(articulos.sku)).
   - update(sku, dto): cambiar parametro + eq(articulos.sku, sku). NotFoundException message: "Articulo con sku ${sku} no encontrado".
   - toggleActive(sku) y softDelete(sku): idem.
   - eventEmitter.emit() NO cambia — rows[0] ya incluye sku desde Deploy 1 (bump semantico).

3. apps/backend/src/modules/articulos/articulos-imagenes.controller.ts: rekey @Post(':sku/imagenes'), @Post(':sku/imagenes/from-url'), @Delete(':sku/imagenes/:tipo/:slot'). @Param('sku') reemplaza @Param('codigo'). Mantener guards.

4. apps/backend/src/modules/articulos/articulos-imagenes.service.ts: todos los eq(articulos.codigo, ...) -> eq(articulos.sku, ...).

5. apps/backend/src/modules/existencias/existencias.service.ts:
   - JOIN con articulos pasar de eq(existencias.articuloCodigo, articulos.codigo) a eq(existencias.articuloSku, articulos.sku) en select queries.
   - WHERE clauses por articulo: eq(existencias.articuloCodigo, X) -> eq(existencias.articuloSku, X). Aclaración: el path GET /api/existencias/articulo/:articuloCodigo SE MANTIENE como agrupador (research linea 507), pero internamente la query pasa por articulo_sku ya que ahora codigo no es PK. Implementacion: el controller sigue tomando articuloCodigo como path param; el service hace JOIN existencias × articulos y filtra por articulos.codigo (efecto: retorna todas las existencias de hermanas del codigo - en Phase 31 sigue siendo 1 articulo, Phase 32 podria ser N).
   - upsert(dto): la target del onConflictDoUpdate cambia a [existencias.articuloSku, existencias.depositoId] (PK nueva). El helper sigue invocandose para mantener doble-escribe durante Deploy 2->3 (articuloCodigo se sigue escribiendo en .values() hasta Deploy 3).

6. apps/backend/src/modules/inventarios/inventarios.service.ts: joins por articulo_sku. La catch de 23505 sigue valida (el unique index ahora se llama igual pero sobre articulo_sku).

7. apps/backend/src/modules/dashboard/dashboard.service.ts: cambiar projections y joins de existencias.articuloCodigo -> existencias.articuloSku. La LowStockItem projection puede mantener field name articuloCodigo si frontend lee asi, o renombrar a articuloSku — research linea 627 dice "renombra el field" pero el types frontend agrega articuloSku al lado de articuloCodigo en Deploy 2, asi que mejor: agregar articuloSku al projection AL LADO de articuloCodigo (que sigue obteniendose desde un JOIN si necesario, o se quita del LowStockItem en Deploy 3).

8. Confirmar que el E2E test articulos-phase31.e2e-spec.ts (creado en Task 1 con `it()` no-skipped) compila contra el shape rekey-eado: la ruta GET /api/articulos/:sku y GET /api/articulos/by-codigo/:codigo deben coincidir con lo que el test invoca via supertest. Si typecheck rompe, ajustar fixtures (no la ruta). webhooks.listener.ts NO cambia estructuralmente (PATTERNS Critical Note #8: bump v2 es semantico).

9. Verificar: `pnpm --filter backend typecheck` exit 0. NO correr E2E tests aca (corren contra DB con migration aplicada, eso es Task 4).

Archivos tocados (~9 backend): articulos.controller.ts, articulos.service.ts, articulos-imagenes.controller.ts, articulos-imagenes.service.ts, existencias.service.ts, inventarios.service.ts, dashboard.service.ts. Bajo el threshold de 15.

Threats mitigados: T-31-14 (Bypass de RolesGuard al renombrar rutas - decorators migran inalterados), T-31-15 (Auth path-specificity - by-codigo antes de :sku).
</action>
<verify>
<automated>pnpm --filter backend typecheck 2>&1 | tail -3 && grep -q "by-codigo/:codigo" apps/backend/src/modules/articulos/articulos.controller.ts && grep -q "findByCodigo" apps/backend/src/modules/articulos/articulos.service.ts && grep -q "asc(articulos.sku)" apps/backend/src/modules/articulos/articulos.service.ts</automated>
</verify>
<done> - articulos.controller.ts tiene @Get('by-codigo/:codigo') DECLARADO ANTES de @Get(':sku'). - articulos.service.ts tiene metodo findByCodigo con asc(articulos.sku) ordering. - articulos-imagenes.controller.ts y service rekey-eados a :sku. - existencias.service.ts, inventarios.service.ts, dashboard.service.ts: joins/queries por articulo_sku. - pnpm --filter backend typecheck exit 0.
</done>
</task>

<task type="auto">
  <name>Task 2b: Frontend rekey (page rename + types + api client + componentes + Alert webhooks)</name>
  <read_first>
    - apps/web/src/app/(dashboard)/articulos/[codigo]/editar/page.tsx (lineas 28-79 useParams + handlers)
    - apps/web/src/app/(dashboard)/articulos/articulos-client.tsx (lineas 152-157 handleEdit)
    - apps/web/src/components/articulos/articulo-sheet.tsx (lineas 162, 190, 199)
    - apps/web/src/components/articulos/imagen-slot.tsx (lineas 37-44 props interface)
    - apps/web/src/components/articulos/imagen-slot-grid.tsx
    - apps/web/src/lib/api.client.ts (lineas 128-223 6 funciones articulo)
    - apps/web/src/types/order.ts, sale.ts, purchase.ts, existencia.ts, inventario.ts, dashboard.ts
    - apps/web/src/components/settings/webhooks/webhooks-client.tsx (lineas 272-282 return block)
    - apps/web/src/components/ui/alert.tsx (componente Alert shadcn instalado)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-RESEARCH.md (lineas 510-571 frontend rekey + Alert)
    - .planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-PATTERNS.md (lineas 759-1052 analogs frontend)
  </read_first>
  <action>
Esta task SOLO toca frontend. Depende de Task 1 (schema.ts + migration aplicada en commit anterior). File ownership disjunto con Task 2a: 2a toca `apps/backend/src/**`, 2b toca `apps/web/src/**`. Ambos pueden ejecutarse en paralelo dentro de la misma wave si el setup de execute-phase lo permite; en secuencia es seguro tambien.

1. git mv apps/web/src/app/(dashboard)/articulos/[codigo] apps/web/src/app/(dashboard)/articulos/[sku]. Dentro de editar/page.tsx:
   - useParams<{ codigo: string }>() -> useParams<{ sku: string }>()
   - const codigo = decodeURIComponent(params.codigo) -> const sku = decodeURIComponent(params.sku)
   - loadArticulo callback: cambiar dependency array, llamar fetchArticuloBySkuClient(sku) (funcion nueva en api.client.ts).
   - handleConfirmToggle: deleteArticulo(articulo.codigo) -> deleteArticulo(articulo.sku); toggleArticuloActivo(articulo.codigo) -> toggleArticuloActivo(articulo.sku).
   - Props pasados a <ImagenSlotGrid articuloCodigo={articulo.codigo}> -> articuloSku={articulo.sku}. Idem <ImagenSlot>.

2. apps/web/src/app/(dashboard)/articulos/articulos-client.tsx linea 154: router.push(`/articulos/${encodeURIComponent(articulo.sku)}/editar`).

3. apps/web/src/components/articulos/articulo-sheet.tsx:
   - Linea 199: <Link href={`/articulos/${encodeURIComponent(articulo.sku)}/editar`}>.
   - Linea 162: MANTENER fetchExistenciasByArticuloClient(articulo.codigo) - este path sigue siendo by-codigo (agrupador).
   - Linea 190: mantener display text {articulo.codigo} - es el identificador visible.

4. apps/web/src/components/articulos/imagen-slot.tsx y imagen-slot-grid.tsx:
   - Props interface: articuloCodigo: string -> articuloSku: string.
   - Internal calls uploadArticuloImagen(articuloCodigo, ...) -> uploadArticuloImagen(articuloSku, ...). deleteArticuloImagen idem.

5. apps/web/src/lib/api.client.ts:
   - Rename fetchArticuloByCodigoClient -> fetchArticuloBySkuClient(sku: string). URL: /api/articulos/${encodeURIComponent(sku)}.
   - Rename updateArticulo(codigo, data) -> updateArticulo(sku, data). URL idem.
   - Rename toggleArticuloActivo(codigo) -> toggleArticuloActivo(sku). URL: /api/articulos/${sku}/toggle.
   - Rename deleteArticulo(codigo) -> deleteArticulo(sku).
   - Rename uploadArticuloImagen(codigo, ...) -> uploadArticuloImagen(sku, ...).
   - Rename deleteArticuloImagen(codigo, ...) -> deleteArticuloImagen(sku, ...).
   - AGREGAR nueva funcion: export async function fetchArticulosByCodigoClient(codigo: string): Promise<Articulo[]>. Hace fetch a /api/articulos/by-codigo/${encodeURIComponent(codigo)} con auth headers, retorna response.json() tipado como array.

6. apps/web/src/types/{order,sale,purchase,existencia,inventario,dashboard}.ts: agregar `articuloSku: string` (notnull) al lado de `articuloCodigo: string` existente. Existencia.ts linea 9: cambiar `articuloSku: string | null` a `articuloSku: string`. ExistenciaMatrixRow tambien agrega articuloSku. NO quitar articuloCodigo aun (coexistencia para Deploy 3).

7. apps/web/src/components/settings/webhooks/webhooks-client.tsx (Alert webhooks v2):
   - Agregar imports al top: import { Alert, AlertTitle, AlertDescription } from '@/components/ui/alert' + import { Info } from 'lucide-react'.
   - Insertar el Alert dentro del top-level <div className="space-y-4"> (linea 273), ANTES del condicional {webhooks.length === 0 ? ... : ...}. Texto literal del bloque <interfaces> "Notice Alert" — es-AR exacto.

8. Verificar: `pnpm --filter web typecheck` exit 0. Si rompe algo en consumidores de types (existencias-columns, low-stock-alerts, etc) que asumen el shape sin articuloSku, agregar el field al objeto o desestructuracion correspondiente.

Archivos tocados (~13 frontend): page.tsx (rename dir [codigo]->[sku]), articulos-client.tsx, articulo-sheet.tsx, imagen-slot.tsx, imagen-slot-grid.tsx, api.client.ts, 6× types/\*.ts, webhooks-client.tsx. Bajo el threshold de 15.

Threats mitigados: T-31-16 (Type drift frontend - articuloSku agregado coexistente con articuloCodigo).
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter web typecheck 2>&1 | tail -3 && test -f "apps/web/src/app/(dashboard)/articulos/[sku]/editar/page.tsx" && ! test -d "apps/web/src/app/(dashboard)/articulos/[codigo]" && grep -q "fetchArticulosByCodigoClient" apps/web/src/lib/api.client.ts && grep -q "Cambio en el payload de articulo" apps/web/src/components/settings/webhooks/webhooks-client.tsx</automated>
</verify>
<done> - Directorio renombrado: apps/web/src/app/(dashboard)/articulos/[sku]/editar/page.tsx existe; el [codigo]/ dir ya no esta. - api.client.ts tiene fetchArticuloBySkuClient + fetchArticulosByCodigoClient + 4 funciones rekey a sku. - 6 archivos types/\*.ts incluyen articuloSku: string al lado de articuloCodigo: string. - webhooks-client.tsx tiene <Alert> con texto exacto del notice. - pnpm --filter web typecheck exit 0.
</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Operador aplica migration 0010 + valida SC#1-5 + E2E + frontend smoke + 24-48h soak</name>
  <what-built>
- Migration 0010 lista para aplicar (9-step transaction).
- Backend rekey deployado.
- Frontend rekey deployado.
- E2E tests passing localmente (los 3 tests SC#3a/SC#3b/SC#4).
- Notice Alert visible en /settings/webhooks.

Esta task es BLOCKING humano porque:

1. Es el cutover de mayor riesgo del milestone (PK swap + 6 tablas locked + trigger replacement).
2. pg_dump full pre-Deploy-2 es OBLIGATORIO (D-04) y se ejecuta desde el VPS.
3. Orden de deploys importa: backend nuevo, frontend nuevo, migration. Si la migration corre antes del backend nuevo, las rutas `/api/articulos/:codigo` viejas devuelven 500 contra schema cambiado.
4. 24-48h de soak en prod con tráfico real obligatorio antes de Wave 3.
5. Rollback es restore desde pg_dump — operación riesgosa que solo el operador autoriza.
   </what-built>
   <how-to-verify>
   **Orden CRITICO (research lineas 259-274):**

6. **Pre-flight DR drill (operador, en VPS, antes del cutover):**

   ```bash
   TS=$(date -u +%Y%m%d_%H%M%S)
   docker exec postgres pg_dump -U sanchez -d erp_sanchez -Fc \
     -f /var/backups/erp_sanchez/phase31/pre_deploy2_${TS}.dump
   docker exec postgres ls -lh /var/backups/erp_sanchez/phase31/pre_deploy2_${TS}.dump  # > 0 bytes
   ```

   DR drill: crear DB temporal + pg_restore + count articulos = 101.021. Sin esto NO se procede.

7. **Gate D-07 pre-Deploy-2 (CRITICAL):**

   ```bash
   bash scripts/phase31-validation.sh integrity  # debe exit 0
   ```

   Si exit 1: halt, investigar. NO continuar.

8. **Deploy backend nuevo (rolling restart, ANTES de migration):**
   - Build container backend con commit que sale de Task 1+2 (controller rekey + service rekey).
   - `docker compose up -d --no-deps --build backend`.
   - Health check `/api/health` retorna 200.
   - **IMPORTANTE:** el backend nuevo declara rutas `/api/articulos/:sku`. Hasta que la migration corra, queries con eq(articulos.sku, X) van a fallar porque sku no es PK aun pero el shape esta correcto. Las URL viejas `/api/articulos/:codigo` ya NO existen — el frontend viejo si todavia esta puede romper. Por eso se hace deploy backend + frontend juntos (paso 4).

9. **Deploy frontend nuevo (ANTES de migration):**
   - Build web con commit que sale de Task 2 (page rename + api.client rekey).
   - `docker compose up -d --no-deps --build web`.
   - Smoke: visitar /articulos, click cualquier item, verificar redirect a `/articulos/${sku}/editar`.
   - **Si el SKU aun no es PK pero esta poblado (post-Deploy-1), la URL funciona — el backend find por sku con eq(articulos.sku, ...) usa el index articulos_sku_idx (todavia existente).** No hay 404 esperado.

10. **Aplicar migration 0010 (operador):**

    ```bash
    docker cp apps/backend/drizzle/0010_phase31_switch.sql postgres:/tmp/0010_phase31_switch.sql
    docker exec postgres psql -U sanchez -d erp_sanchez \
      --single-transaction --set ON_ERROR_STOP=1 \
      -f /tmp/0010_phase31_switch.sql 2>&1 | tee deploy2_${TS}.log
    ```

    Duracion estimada 5-15 min (locks + recompute manual). RAISE NOTICE 'Deploy 2 (switch) OK: PK=sku, 5 FKs activas, trigger ENABLED, articulos.unidades recomputed' al final.

11. **Registrar la migration en drizzle.\_\_drizzle_migrations (OBLIGATORIO, leccion 260502-tqf):**
    - Ejecutar OBLIGATORIAMENTE post-migration (NO opcional):
      ```bash
      docker exec postgres psql -U sanchez -d erp_sanchez --set ON_ERROR_STOP=1 -c "
        INSERT INTO drizzle.__drizzle_migrations (hash, created_at)
        VALUES (md5(pg_read_file('/tmp/0010_phase31_switch.sql')), extract(epoch from now())*1000);
      "
      ```
    - Verificar el insert con `SELECT count(*) FROM drizzle.__drizzle_migrations WHERE hash = md5(pg_read_file('/tmp/0010_phase31_switch.sql'))` → 1.
    - Sin este step, futuras migrations leen journal desincronizado vs table — silent failure pattern documentado en lecciones 260502-tqf y 260428-mig.

12. **Validacion gate post-Deploy-2:**

    ```bash
    bash scripts/phase31-validation.sh all  # all = integrity + pk-swap + triggers + unidades-sync
    ```

    Esperado: 4 checks ✓. Si exit 1: halt, considerar rollback desde pg_dump.

13. **E2E tests contra DB prod (smoke, no destructivo):**

    ```bash
    cd apps/backend && pnpm test -- articulos-phase31.e2e-spec.ts
    ```

    Los 3 tests SC#3a, SC#3b, SC#4 deben pasar. Si fallan: investigar.

14. **Frontend smoke (manual o playwright):**
    - Visitar https://erp.sanchezrepuestos.com.ar/articulos
    - Click cualquier articulo → verificar URL navega a `/articulos/{sku}/editar` (no /{codigo}/).
    - Formulario carga, save funciona.
    - Visitar `/settings/webhooks` → verificar Alert visible con texto "Cambio en el payload de articulo.\* desde v1.3".

15. **Validar webhook payload v2 contra prod:**
    - Crear webhook test con endpoint webhook.site o similar.
    - POST /api/articulos → esperar 5s → revisar webhook.site → assert payload.articulo.sku no-null.
    - Borrar webhook test post-validacion.

16. **24-48h soak (research lineas 1139-1144):**
    - Cada ~4h:
      - `scripts/phase31-validation.sh all` → exit 0
      - 5 queries SC#5 retornan 0
      - Frontend `/articulos/{sku}/editar` carga sin 404 (sampling 5 articulos random)
      - Webhook deliveries post-Deploy-2 todos con sku no-null:
        `SELECT count(*) FROM webhook_deliveries WHERE created_at > '<deploy2_timestamp>' AND payload->'articulo'->>'sku' IS NULL` → 0
      - Backend log error rate sin spike +20%
    - Abort criteria (research 1147-1150): cualquier SC# != 0, error rate spike, 404 spike → halt.

17. **Reportar al planner:** - Log de la migration (deploy2*<TS>.log). - Output de `scripts/phase31-validation.sh all` (4 checks ✓). - Resultado E2E tests (3 passed). - Screenshot del Alert en /settings/webhooks. - Webhook delivery test resultado. - Timestamp post-soak para gatear Wave 3.
    </how-to-verify>
    <resume-signal>Escribir "approved" + log de migration + output validation all 4 checks ✓ + E2E 3 tests passed + screenshot Alert + timestamp post-soak 24h minimo + ningun abort criteria triggered. Si rollback: reportar y aplicar pre_deploy2*<TS>.dump via pg_restore.</resume-signal>
    </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                                |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| psql migration -> Postgres production        | 9-step transaction con LOCK ACCESS EXCLUSIVE en 6 tablas; bloquea reads y writes durante ~5-15 min                         |
| Backend NestJS controller -> path param :sku | Drizzle parameterizado evita SQL injection; class-validator no aplica a path params pero NestJS rechaza paths > 2000 chars |
| Frontend Next.js -> /api/articulos/:sku      | encodeURIComponent al armar URL evita injection en URL; auth via Supabase JWT                                              |
| Webhook subscriber -> payload v2 con sku     | Sin breaking change esperado (sku es additive); HMAC sha256 mantiene integridad                                            |

## STRIDE Threat Register

| Threat ID | Category                                | Component                                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                        |
| --------- | --------------------------------------- | ------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-31-10   | Tampering / Data Integrity              | PK swap interrumpido a mitad                                  | mitigate    | --single-transaction --set ON_ERROR_STOP=1 + LOCK ACCESS EXCLUSIVE upfront. Si falla, ROLLBACK automatico restaura estado pre-Deploy-2. Plus pg_dump full safety net pre-cutover (D-04) — rollback final = pg_restore.                                                                                 |
| T-31-11   | Tampering / Trigger consistency         | Trigger function rota post-CREATE OR REPLACE                  | mitigate    | STEP 7 ejecuta recompute manual ANTES de ENABLE TRIGGER. Si la function nueva tiene bug, el recompute falla y la transaccion aborta. Plus E2E test SC#4 valida que webhook delivery post-Deploy-2 tiene sku no-null.                                                                                   |
| T-31-12   | Information Disclosure / Data integrity | inv_articulos_unique_idx eliminado por CASCADE sin recreacion | mitigate    | STEP 5.2 explicito: DROP INDEX inv_articulos_unique_idx + CREATE UNIQUE INDEX sobre (inventario_id, articulo_sku) dentro de la transaccion. Validation script `--check=integrity` corre query sobre pg_indexes para verificar que el index existe con articulo_sku.                                    |
| T-31-13   | Data integrity                          | existencias PK compuesta no rebuildeada                       | mitigate    | STEP 5.1 explicito: DROP CONSTRAINT existencias_pkey + ADD CONSTRAINT PK (articulo_sku, deposito_id). DROP CONSTRAINT articulos_pkey CASCADE NO elimina automaticamente la PK compuesta de existencias (research linea 990-993).                                                                       |
| T-31-14   | Elevation of Privilege                  | Bypass RolesGuard al rekey rutas                              | mitigate    | Decorators @UseGuards(RolesGuard) @Roles('admin') migran inalterados en el rekey (research §V4). El path param cambia de :codigo a :sku pero los guards quedan intactos. Verificacion: grep ":sku" + grep "RolesGuard" en articulos.controller.ts post-rekey - cada write route mantiene su decorator. |
| T-31-15   | Information Disclosure                  | NestJS path-specificity colision /by-codigo vs /:sku          | mitigate    | by-codigo declarado ANTES de :sku en el controller (research §A6). NestJS path matching es por orden de declaracion en clase + path-specificity literal > parametrizado. Test E2E SC#3b valida que GET /api/articulos/by-codigo/X resuelve a findByCodigo, no a findOne.                               |
| T-31-16   | Tampering                               | Drift type frontend ↔ DB schema                               | mitigate    | articuloSku agregado en types coexiste con articuloCodigo durante Deploy 2; los componentes que leen articuloCodigo no rompen porque backend sigue devolviendo ambos campos (la columna existe en DB hasta Deploy 3). Plan 31-04 quita articuloCodigo del type cuando la columna se DROP.              |
| T-31-17   | Repudiation                             | Migration aplicada pero schema.ts/journal desincronizados     | mitigate    | Atomic commit obligatorio (lección feedback_schema_drift_silencioso.md): schema.ts + 0010.sql + \_journal.json en mismo commit. Post-deploy: pnpm typecheck debe pasar; si rompe, drift detectado tempranamente.                                                                                       |
| T-31-18   | DoS                                     | LOCK ACCESS EXCLUSIVE bloquea writes ~5-15 min                | accept      | Operador ejecuta en ventana de bajo trafico (03:00-05:00 UTC-3 segun 31-CUTOVER-CALENDAR.md). Frontend muestra toast "Migración en curso, reintentar en 1 min" cuando recibe 503 (decision operador).                                                                                                  |
| T-31-19   | Information Disclosure                  | Webhook payload tampering en transito                         | accept      | HMAC sha256 con secret pre-shared (heredado de Phase 24); sin cambios estructurales en envelope. Subscribers verifican signature.                                                                                                                                                                      |
| T-31-SC   | Supply Chain                            | npm install para Task 1/2                                     | accept      | No se instalan nuevas deps en este plan; deps de Plan 31-01 ya pasaron gate de legitimacy.                                                                                                                                                                                                             |

</threat_model>

<verification>
- apps/backend/drizzle/0010_phase31_switch.sql aplicada en prod, log limpio con "Deploy 2 (switch) OK"
- _journal.json tiene entry idx=10 con tag 0010_phase31_switch
- schema.ts post-Deploy-2 sincronizado: articulos.sku PK, articulos.codigo nullable + index, 5 hijas con FK a articulos.sku, existencias PK [articuloSku, depositoId], inv_articulos unique [inventarioId, articuloSku]
- scripts/phase31-validation.sh all exit 0 (4 checks: integrity, pk-swap, triggers, unidades-sync)
- pnpm --filter backend test -- articulos-phase31.e2e-spec.ts: 3 tests passed (SC#3a, SC#3b, SC#4)
- Frontend smoke: /articulos/{sku}/editar carga sin 404 (sampling 5 articulos)
- Alert visible en /settings/webhooks con texto literal del bloque <interfaces>
- Webhook delivery test contra webhook.site retorna payload.articulo.sku no-null
- 24-48h soak en prod completed sin abort criteria triggered
- Trigger trg_update_articulo_unidades tgenabled='O' post-cutover
- articulos.unidades sincronizado con SUM(existencias.cantidad) (unidades-sync check exit 0)
</verification>

<success_criteria>
Wave 2 completa cuando:

1. Migration 0010_phase31_switch.sql aplicada exitosamente en prod (log con RAISE NOTICE final).
2. Schema.ts post-Deploy-2 sincronizado en mismo commit con 0010.sql y journal idx=10.
3. SC#1 (articulos.sku PK), SC#2 (5 FKs articulo_sku), SC#3 (findOne/findByCodigo), SC#4 (webhook payload v2 incluye sku), SC#5 (5 queries integrity = 0): TODOS verdes via scripts/phase31-validation.sh all + E2E tests.
4. Backend rekey: /api/articulos/:sku, /api/articulos/by-codigo/:codigo, articulos-imagenes/:sku/imagenes funcionan.
5. Frontend rekey: /articulos/[sku]/editar carga, api.client.ts usa URLs nuevas.
6. Notice Alert visible en /settings/webhooks con texto es-AR literal.
7. 24-48h soak en prod sin abort criteria triggered.
8. Plan 31-04 (Wave 3 Deploy 3 contract) puede arrancar — schema estable, backend listo para simplificar el helper.
   </success_criteria>

<output>
Create `.planning/phases/31-pk-swap-codigo-sku-fk-rename-en-comprobantes/31-03-SUMMARY.md` when done documentando: archivos creados/modificados, output de scripts/phase31-validation.sh all, log de migration aplicada, E2E test results, screenshot del Alert, timestamp del soak, anomalias y resoluciones, commits atomicos (schema.ts + 0010.sql + journal en mismo commit).
</output>
