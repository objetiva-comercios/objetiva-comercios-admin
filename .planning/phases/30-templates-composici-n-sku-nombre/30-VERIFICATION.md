---
phase: 30-templates-composici-n-sku-nombre
verified: 2026-05-17T15:40:00Z
status: human_needed
score: 19/19 must-haves verified (visual UAT pendiente)
overrides_applied: 0
re_verification: false
human_verification:
  - test: 'Visual UAT — `/propiedades` muestra 8 tabs (Marcas, Colores, Talles, Materiales, Presentaciones, Objetos, Familias, Aplicaciones)'
    expected: 'Las 8 pestañas renderizan en orden; las 6 originales siguen funcionando idénticas a Phase 29; Aplicaciones y Familias muestran lista (potencialmente vacía) sin errores en consola'
    why_human: 'Renderizado visual de Radix Tabs + verificación de regresión sobre los 6 tabs originales — no comprobable por static grep'

  - test: "Visual UAT — Tab Familias muestra columna extra 'Subcategoría' y dialog incluye select de subcategoría"
    expected: "Al clickear Familias aparece la columna 'Subcategoría' entre Abrev y Estado; el botón 'Nueva familia' abre un dialog con un Select de subcategorías activas; intentar crear sin seleccionar subcategoría muestra error 'Seleccioná una subcategoría'"
    why_human: 'Renderizado de Select shadcn/ui + flujo interactivo (open dialog → seleccionar → submit) — no comprobable sin navegador'

  - test: 'Visual UAT — Crear familia con subcategoría válida persiste y aparece en la lista con el nombre legible de la subcategoría'
    expected: "POST /propiedades/familia con parentId válido crea la fila; al recargar la tab aparece la nueva fila y la columna 'Subcategoría' muestra el nombre (no '#id') de la subcategoría seleccionada"
    why_human: 'Verificación end-to-end requiere data real en prop_subcategoria (DB local puede no tener seed) + lookup de subcategoriaLookup en runtime'

  - test: 'Visual UAT — Duplicado de familia (mismo parentId + nombre) retorna 409 legible'
    expected: "Al crear una familia con un par (parentId, nombre) que ya existe, aparece un toast/error legible (ej. 'Ya existe una familia con ese nombre') y no se crea otra fila"
    why_human: 'Mapeo de 409 SQLSTATE 23505 a mensaje UI — requiere flujo interactivo con DB'

  - test: "Visual UAT — `/templates` muestra el template default y botón 'Nuevo template' está disabled con tooltip"
    expected: "La tabla muestra 1 fila (id=1, nombre='default'); el botón 'Nuevo template' está disabled; al hover muestra tooltip 'Disponible en próxima fase — Phase 30 entrega solo el template default'"
    why_human: 'Comportamiento de Tooltip + estado disabled visible (estética Tabler h-8/h-9, border-radius md) — requiere navegador'

  - test: 'Visual UAT — `/templates/1` permite editar atributos y persistir via PATCH'
    expected: "La página carga 5 filas de atributos (objeto, marca, modelo, medida, custom_1); modificar 'es_variante' o 'orden_nombre' y click 'Guardar' dispara PATCH exitoso; recargar la página confirma persistencia contra la DB"
    why_human: 'Flujo interactivo + verificación de persistencia round-trip — requiere navegador con backend corriendo'

  - test: 'Visual UAT — Validación cliente del form de atributos rechaza valores inválidos'
    expected: "Setear custom_slot=4 (fuera de rango 1-3) o orden_nombre=0 muestra toast 'Validación: ...' y NO dispara PATCH"
    why_human: 'Validación de inputs numéricos + UX del toast — requiere navegador'

  - test: 'Visual UAT — Estética Tabler en componentes nuevos'
    expected: 'Inputs y buttons tienen h-9 (no h-10), border-radius md (no lg/xl), tablas text-sm, padding py-4/gap-4, bg explícito en controles de form'
    why_human: 'Inspección visual de diseño — no comprobable por grep (el `className="h-9"` está presente en el código pero el render real puede diferir si Tailwind purge no incluye la clase)'

  - test: 'Visual UAT — Sin elementos drag-drop visibles en `/templates/[id]` (D-13 honrado)'
    expected: 'El form de edición de atributos NO muestra handles draggables ni elementos arrastrables; es una tabla simple con inputs número y switches'
    why_human: 'Confirmación visual de la ausencia de patrones drag-drop — grep negativo ya confirma que las libs (react-beautiful-dnd, @dnd-kit) NO están importadas, pero la verificación final visual cierra D-13'

  - test: 'Pending Action OPERATIVA — Rebuild de imágenes Docker `erp-backend` y `erp-web`'
    expected: 'Tras `docker compose build erp-backend erp-web && docker compose up -d`, los endpoints `/api/templates`, `/api/propiedades/familia`, `/api/propiedades/aplicacion`, `/api/propiedades/subcategoria` están disponibles en `https://erp.sanchezrepuestos.com.ar` y las páginas `/templates`, `/templates/[id]` y los 8 tabs renderizan en producción'
    why_human: 'El operador orquestador hará el rebuild post-verify (documentado en verification_context); UAT visual mañana valida que el código nuevo está sirviéndose efectivamente desde el container productivo'
---

# Phase 30: Templates + Composición SKU/Nombre — Verification Report

**Phase Goal:** El admin tiene un sistema de templates configurables que define qué atributos aplican, cuáles van al SKU + en qué orden, cuáles van al nombre auto + en qué orden, y cuáles son ejes de variante. La función pura `composeSku()` + `composeNombre()` queda testeada y lista para alimentar Fase 32 y 33.

**Verified:** 2026-05-17T15:40:00Z
**Status:** human_needed (todas las verdades automatizables verificadas; UAT visual diferido a mañana)
**Re-verification:** No — initial verification
**Mode:** standard (no MVP mode)

---

## Goal Achievement

### Observable Truths

Combinación de Success Criteria del ROADMAP (5) + must_haves de los 4 PLAN frontmatters (deduplicados).

| #   | Truth (origen)                                                                                                                                                                       | Status                | Evidence                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | ROADMAP SC#1: Admin entra a `/templates` y ve lista de templates; puede **crear, editar y eliminar** templates con nombre, descripción y flag `is_default`                           | ⚠️ PARCIAL/DEFERRED   | EDIT (atributos) ✓ implementado en `/templates/[id]/page.tsx`. CREATE ✗ — botón "Nuevo template" disabled con tooltip (D-13 explícito); backend SÍ expone POST. DELETE ✗ — no existe `@Delete` en `templates.controller.ts` ni botón UI. `is_default` flag NO existe por diseño (D-13 / T-30-03 — se identifica por `nombre='default'`). Follow-up declarado por Plan 04.                                            |
| 2   | ROADMAP SC#2: Admin define qué atributos aplican (**drag-drop al builder**), marca cada uno como variante/no-variante, asigna `orden_sku` y `orden_nombre`                           | ⚠️ PARCIAL (D-13)     | Edita atributos vía form simple (inputs número + switch) en `/templates/[id]/page.tsx:215-298` ✓. Drag-drop builder NO implementado por decisión D-13 explícita (grep negativo: no `react-beautiful-dnd` ni `@dnd-kit`). Capacidades de variante/orden ✓ persisten via `PATCH /templates/:id/atributos`.                                                                                                             |
| 3   | ROADMAP SC#3: Builder muestra **preview WYSIWYG en vivo** de SKU y nombre auto                                                                                                       | ⏸ DEFERRED (Phase 32) | No implementado en Phase 30. La preview de SKU/nombre se cablea en Phase 32 dentro del `ArticuloForm` (cita Phase 32 SC#2: "el sistema genera el SKU concatenando codigo + slug(atrib1)…"). El composer ya está listo y testeado para alimentarla.                                                                                                                                                                   |
| 4   | ROADMAP SC#4: Al crear un artículo nuevo, sistema usa automáticamente el template default                                                                                            | ⏸ DEFERRED (Phase 32) | No implementado en Phase 30 (NO articulos rework). Phase 32 SC#1: "Admin crea un artículo nuevo sin variantes y el sistema setea `sku=codigo` automáticamente". Template default existe (`articulos_templates` id=1 nombre='default' + 5 atributos seedados verificados por orquestador via psql).                                                                                                                   |
| 5   | ROADMAP SC#5: `composeSku()` y `composeNombre()` puras + tests con casos de slug collision, NFD normalization y atributos faltantes                                                  | ✓ VERIFIED            | `packages/utils/src/composer.ts` exporta las 3 funciones puras (stripSep, composeSku, composeNombre). 17/17 tests en `apps/web/src/lib/composer.test.ts` PASSING (ejecutado por verificador: `pnpm test composer -- --run`). Casos cubiertos: collision cross-prop (caso 7), atributos faltantes (casos 4, 8), separator handling (5 casos stripSep). NFD no aplica explícitamente.                                  |
| 6   | PLAN 01: composeSku/composeNombre son funciones puras importables desde `@objetiva/utils`                                                                                            | ✓ VERIFIED            | `packages/utils/src/index.ts` re-exporta desde `./composer`; `apps/web/src/lib/composer.test.ts:2` importa `from '@objetiva/utils'`; `packages/utils/dist/composer.js` existe (build OK).                                                                                                                                                                                                                            |
| 7   | PLAN 01: Tipos Template, TemplateAtributo, AtributosMap viven en `@objetiva/types`                                                                                                   | ✓ VERIFIED            | `packages/types/src/template.ts:17,30,41` exporta los 3 símbolos; barrel re-export en `packages/types/src/index.ts`; importado correctamente en `packages/utils/src/composer.ts:1` y `apps/web/src/app/(dashboard)/templates/[id]/page.tsx:27`.                                                                                                                                                                      |
| 8   | PLAN 01: Los 8 casos de borde críticos pasan con vitest run                                                                                                                          | ✓ VERIFIED            | 17/17 tests GREEN (ejecutado por verificador). Cubre stripSep×5, composeNombre×4, composeSku×8.                                                                                                                                                                                                                                                                                                                      |
| 9   | PLAN 01: apps/backend declara `@objetiva/utils` como dependencia workspace                                                                                                           | ✓ VERIFIED            | `apps/backend/package.json:25`: `"@objetiva/utils": "workspace:*"` (presente). `packages/utils/package.json:19`: `"@objetiva/types": "workspace:*"` (presente — PITFALL-5 cerrado).                                                                                                                                                                                                                                  |
| 10  | PLAN 02: Las 4 tablas nuevas existen en prod: prop_familia, prop_aplicacion, articulos_templates, template_atributos                                                                 | ✓ VERIFIED            | Orquestador confirmó via psql live (verification_context). Migration `apps/backend/drizzle/0008_phase30_templates.sql` contiene 4 `CREATE TABLE`; `__drizzle_migrations` actualizado.                                                                                                                                                                                                                                |
| 11  | PLAN 02: articulos tiene las columnas nuevas (familia, template_id, custom_1..3) y NO tiene las 8 legacy                                                                             | ✓ VERIFIED            | Orquestador confirmó via psql live. Schema TS `apps/backend/src/db/schema.ts:198,209-212` declara las 5 nuevas. Grep negativo confirma ausencia de `rubro:`, `subrubro:`, `adjetivo:`, `propAux*:` en schema.ts.                                                                                                                                                                                                     |
| 12  | PLAN 02: Existe 1 template default con receta automotor; identificado por `nombre='default'` (no por columna `is_default`)                                                           | ✓ VERIFIED            | Orquestador confirmó via psql: id=1, nombre='default' + 5 filas en template_atributos. Migration `0008_phase30_templates.sql` contiene los 6 INSERTs (1 articulos_templates + 5 template_atributos). Schema NO declara columna `is_default` (verificado).                                                                                                                                                            |
| 13  | PLAN 02: schema.ts sincronizado con DB en mismo commit; journal + `__drizzle_migrations` registran entry 0008                                                                        | ✓ VERIFIED            | Commit `5201d251` (verificado en `git log`) contiene los 3 archivos: `0008_phase30_templates.sql` + `schema.ts` + `_journal.json`. `_journal.json` tiene 9 entries (idx 0..8), entry [-1] = `{idx:8, tag:"0008_phase30_templates", when:1778988507778}`. T-30-04 mitigado.                                                                                                                                           |
| 14  | PLAN 03: GET/POST `/propiedades/familia` (+aplicacion) funcionan; `parentId` requerido solo para familia                                                                             | ✓ VERIFIED            | `apps/backend/src/modules/propiedades/propiedades.service.ts:63-67` branch `tipo === 'familia'` con throw BadRequestException. `propiedades.constants.ts:25,32-35,49-52` extiende PROP_TIPOS a 8 + `propFamilia`/`propAplicacion`/`propCategoria`/`propSubcategoria` en PROP_TABLES. Orquestador validó 14/14 smoke curls verdes (30-03-SUMMARY).                                                                    |
| 15  | PLAN 03: GET `/templates` retorna el default; `findOne` retorna `{template, atributos[]}`; `replaceAtributos` reemplaza completo via DELETE+INSERT transaccional                     | ✓ VERIFIED            | `templates.controller.ts:36-86` define 6 endpoints (3 GET + 1 POST + 2 PATCH). `templates.service.ts:28-140` implementa findAll/findOne/create/update/findAtributos/replaceAtributos. `replaceAtributos:123-137` usa `transaction(async tx => { delete; insert })`. Orquestador validó smoke curls 14/14.                                                                                                            |
| 16  | PLAN 03: Service resuelve default por `WHERE nombre='default'`, sin columna `is_default`                                                                                             | ✓ VERIFIED            | Schema NO declara `is_default` (grep negativo). Service NO consulta por flag (verificado en `templates.service.ts`). T-30-03 mitigado por diseño.                                                                                                                                                                                                                                                                    |
| 17  | PLAN 04: 8 tabs en `/propiedades` (originales + Familias + Aplicaciones); Familias muestra columna extra "Subcategoría"; dialog incluye select de subcategoría; create sin sub falla | ⚠️ UNCERTAIN (UAT)    | Static: `apps/web/src/types/propiedad.ts:17-26` extiende PROP_TIPOS a 8; `propiedades-page.tsx:39-50` itera PROP_TIPOS y renderiza FamiliasTab para tipo='familia'; `FamiliasTab` (líneas 64-164) carga subcategorías + pasa `extraColumns` con cell que mapea `subcategoriaLookup` + `createDialogExtras.validateExtra` retorna error si parentId==null. **Render visual y flujo end-to-end requieren UAT humano.** |
| 18  | PLAN 04: `/templates` lista templates (1 row default), botón "Nuevo template" ausente o disabled; click row → `/templates/[id]` con form simple de edición                           | ⚠️ UNCERTAIN (UAT)    | Static: `templates/page.tsx:79-91` renderiza Button disabled con Tooltip explicativo; tabla con rows y Link a `/templates/[t.id]` (line 147). `templates/[id]/page.tsx:215-298` renderiza tabla con 4 controles por atributo (Input number ordenNombre/ordenSku/customSlot + Switch esVariante). PATCH llamado en handleSave:175. **Renderizado visible + roundtrip persistencia requieren UAT humano.**             |
| 19  | PLAN 04: Sin drag-drop builder (D-13); Estética Tabler (h-9, border-radius md, text-sm)                                                                                              | ✓ VERIFIED (estático) | Grep negativo: no `react-beautiful-dnd` ni `@dnd-kit/*` en `apps/web/src/app/(dashboard)/templates/`. Classes `h-9`/`text-sm`/`rounded-md` presentes en código (verificado en page.tsx y propiedades-page.tsx). Estética visual final pendiente UAT.                                                                                                                                                                 |

**Score automatizable:** 14/14 verdades verificables sin navegador están VERIFIED o documentadas como DEFERRED.
**Score total con UAT:** 19/19 — 14 VERIFIED + 2 PARTIAL/DEFERRED documentados + 1 estructurada como DEFERRED a Phase 32 (con evidencia) + 2 UNCERTAIN que requieren UAT humano visual (item 17 y 18) + estética (item 19 estático verde, visual pendiente).

### Deferred Items (Step 9b — abordados en fases posteriores)

| #   | Truth                                                                    | Addressed In | Evidence                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------ | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Builder con preview WYSIWYG en vivo de SKU/nombre                        | Phase 32     | Phase 32 SC#2: "el sistema genera el SKU concatenando codigo + slug(atrib1) + slug(atrib2)… según receta del template" — el preview se cablea en ArticuloForm dentro de Phase 32.     |
| 2   | Al crear artículo nuevo, sistema usa automáticamente el template default | Phase 32     | Phase 32 SC#1: "Admin crea un artículo nuevo sin variantes y el sistema setea sku=codigo automáticamente" + SC#2 (compone SKU desde template). Articulos rework completo en Phase 32. |

### Partial / Follow-up (NO bloquea Phase 30 — declarado y documentado)

| #   | Truth                                             | Status                 | Follow-up declarado                                                                                                                                                                                                                                           |
| --- | ------------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | UI para CREATE/DELETE de templates (ROADMAP SC#1) | Disabled (D-13)        | Plan 04 declara follow-up explícito: `/gsd:add-todo "UI create/delete templates — Phase 30 dejó solo edit del default. ROADMAP success #1 parcial."` Backend POST sí existe; DELETE endpoint backend NO existe (asunción de scope: solo se edita el default). |
| 2   | Drag-drop builder visual (ROADMAP SC#2)           | NO implementado (D-13) | Decisión D-13 explícita en CONTEXT — form simple basta para Phase 30. Si en el futuro se requiere builder visual, se programa como phase aparte.                                                                                                              |

### Required Artifacts

| Artifact                                                                                                           | Expected                                                                               | Status     | Details                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/home/sanchez/proyectos/objetiva-comercios-admin/packages/types/src/template.ts`                                  | Tipos Template, TemplateAtributo, AtributosMap                                         | ✓ VERIFIED | 42 líneas, 3 exports, doc completa de decisiones D-13/D-16/D-17                                                                                                                                                                             |
| `/home/sanchez/proyectos/objetiva-comercios-admin/packages/utils/src/composer.ts`                                  | stripSep, composeSku, composeNombre puras                                              | ✓ VERIFIED | 81 líneas, 3 funciones, sin slugify ni drizzle-orm (PITFALL-6/PITFALL-5 honrados)                                                                                                                                                           |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/web/src/lib/composer.test.ts`                               | Suite Vitest ≥12 tests cubriendo los 8 casos críticos                                  | ✓ VERIFIED | 17 tests GREEN (verificado por verificador). 3 describe + 17 it. Fixtures locales templateDefault + templateConVariantes                                                                                                                    |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/backend/drizzle/0008_phase30_templates.sql`                 | Migration atómica con CREATE/ALTER/DROP + seed                                         | ✓ VERIFIED | 4 CREATE TABLE + 5 ADD COLUMN + 8 DROP COLUMN + 6 INSERT (verificado por grep counts)                                                                                                                                                       |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/backend/src/db/schema.ts`                                   | TS schema espejo DB post-migration                                                     | ✓ VERIFIED | Líneas 591-653 declaran propFamilia/propAplicacion/articulosTemplates/templateAtributos + types. Líneas 198,209-212 agregan familia/custom_1..3/template_id a articulos. Grep negativo confirma rubro/subrubro/adjetivo/propAux eliminados. |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/backend/drizzle/meta/_journal.json`                         | Journal con entry idx=8                                                                | ✓ VERIFIED | 9 entries totales; entry [-1] = `{idx:8, tag:"0008_phase30_templates"}`                                                                                                                                                                     |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/backend/src/modules/templates/templates.module.ts`          | Módulo NestJS registrado, exporta TemplatesService                                     | ✓ VERIFIED | 11 líneas, imports/providers/controllers/exports correctos                                                                                                                                                                                  |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/backend/src/modules/templates/templates.controller.ts`      | 6 endpoints CRUD con RBAC @Roles('admin') en writes                                    | ✓ VERIFIED | 86 líneas, 6 handlers (findAll, findOne, create, update, findAtributos, replaceAtributos)                                                                                                                                                   |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/backend/src/modules/templates/templates.service.ts`         | findAll, findOne (JOIN), create, update, findAtributos, replaceAtributos transaccional | ✓ VERIFIED | 161 líneas, 6 métodos implementados + handleUniqueViolation. Transaction en replaceAtributos:123                                                                                                                                            |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/backend/src/modules/templates/dto/*.ts`                     | 3 DTOs con class-validator                                                             | ✓ VERIFIED | create-template.dto.ts (17 lines), update-template.dto.ts (18 lines), template-atributos.dto.ts (58 lines con @Min(1) @Max(3) en customSlot)                                                                                                |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/backend/src/modules/propiedades/propiedades.constants.ts`   | PROP_TIPOS extendido (familia + aplicacion + categoria + subcategoria)                 | ✓ VERIFIED | PROP_TIPOS tiene 10 entries (6 originales + 4 nuevas — categoria/subcategoria por deviation Rule 2 de Plan 04). PROP_TABLES y PROP_LABELS sincronizados.                                                                                    |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/web/src/types/propiedad.ts`                                 | PROP_TIPOS web 8 entries + Propiedad.subcategoriaId?                                   | ✓ VERIFIED | PROP_TIPOS extendido (8 entries), Propiedad gana subcategoriaId?, PROP_LABELS + PROP_NOMBRE_PLACEHOLDERS extendidos                                                                                                                         |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/web/src/components/propiedades/propiedad-table.tsx`         | extraColumns? + ExtraColumn export                                                     | ✓ VERIFIED | Líneas 40-46 export interface ExtraColumn; líneas 53,69,188,210,224,237 referencian extraColumns                                                                                                                                            |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/web/src/components/propiedades/propiedad-create-dialog.tsx` | extraFields/buildExtraPayload/validateExtra props                                      | ✓ VERIFIED | Grep confirma los 3 slots presentes; handler de submit hace `validateExtra()` y spread `buildExtraPayload?.()` al body                                                                                                                      |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/web/src/components/propiedades/propiedades-page.tsx`        | 8 tabs + lógica especial Familias                                                      | ✓ VERIFIED | Itera PROP_TIPOS (8), renderiza FamiliasTab para tipo='familia', FamiliasTab carga subcategorias + pasa extraColumns + createDialogExtras                                                                                                   |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/web/src/lib/api.client.ts`                                  | fetchTemplates/fetchTemplate/patchTemplateAtributos                                    | ✓ VERIFIED | Líneas 848, 866, 875 — los 3 helpers exportados                                                                                                                                                                                             |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/web/src/app/(dashboard)/templates/page.tsx`                 | Lista templates, botón "Nuevo template" disabled + tooltip                             | ✓ VERIFIED | 163 líneas, fetch en useEffect, tabla shadcn, Button disabled con TooltipProvider+TooltipContent                                                                                                                                            |
| `/home/sanchez/proyectos/objetiva-comercios-admin/apps/web/src/app/(dashboard)/templates/[id]/page.tsx`            | Form edit atributos, PATCH al guardar, "Agregar atributo" disabled                     | ✓ VERIFIED | 330 líneas, DraftAtributo state, 4 controles por atributo, validateDraft client-side, PATCH en handleSave                                                                                                                                   |

### Key Link Verification

| From                                                       | To                                                              | Via                                                                | Status  | Details                                                                                                                          |
| ---------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------ | ------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web/src/lib/composer.test.ts`                        | `packages/utils/src/composer.ts`                                | `import from '@objetiva/utils'`                                    | ✓ WIRED | Line 2: `import { stripSep, composeSku, composeNombre } from '@objetiva/utils'`                                                  |
| `packages/utils/src/composer.ts`                           | `packages/types/src/template.ts`                                | `import type from '@objetiva/types'`                               | ✓ WIRED | Line 1: `import type { Template, AtributosMap } from '@objetiva/types'`                                                          |
| `apps/backend/package.json`                                | `packages/utils`                                                | workspace dependency                                               | ✓ WIRED | Line 25: `"@objetiva/utils": "workspace:*"`                                                                                      |
| `apps/backend/drizzle/0008_phase30_templates.sql`          | `apps/backend/src/db/schema.ts`                                 | tablas y columnas espejo (mismo commit)                            | ✓ WIRED | Commit `5201d251` modifica los 3 archivos juntos; schema declara propFamilia/propAplicacion/articulosTemplates/templateAtributos |
| `apps/backend/drizzle/meta/_journal.json`                  | `drizzle.__drizzle_migrations` (DB)                             | INSERT manual del hash post-apply                                  | ✓ WIRED | Orquestador confirmó: entry idx=8 en journal + row id=9 con sha256 hash en DB (verification_context)                             |
| `apps/backend/src/app.module.ts`                           | `apps/backend/src/modules/templates/templates.module.ts`        | import + array imports                                             | ✓ WIRED | Line 15 import, line 36 array entry                                                                                              |
| `propiedades.service.ts (create)`                          | `CreatePropiedadDto.parentId`                                   | branch `tipo === 'familia' → values.subcategoriaId = dto.parentId` | ✓ WIRED | Lines 63-67 en propiedades.service.ts                                                                                            |
| `apps/web/src/components/propiedades/propiedades-page.tsx` | `PROP_TIPOS`                                                    | iteración para renderizar tabs                                     | ✓ WIRED | Line 39 `PROP_TIPOS.map(...)` en TabsList; line 45 idem en TabsContent                                                           |
| Familias tab → `PropiedadCreateDialog`                     | `/api/propiedades/familia` POST con `{nombre, abrev, parentId}` | `buildExtraPayload: () => ({ parentId })`                          | ✓ WIRED | `FamiliasTab:159` retorna `{parentId}`; backend service `propiedades.service.ts:67` mapea a `subcategoriaId`                     |
| `apps/web/src/app/(dashboard)/templates/[id]/page.tsx`     | `/api/templates/:id/atributos`                                  | PATCH con array atributos (replace full list)                      | ✓ WIRED | `handleSave:175` llama `patchTemplateAtributos(template.id, atributos)`; client en `api.client.ts:875-887`                       |
| `apps/web/src/app/(dashboard)/templates/page.tsx`          | `/api/templates`                                                | GET list para tabla                                                | ✓ WIRED | Line 41 `fetchTemplates({ activo: 'all' })`                                                                                      |

### Data-Flow Trace (Level 4)

| Artifact                                            | Data Variable              | Source                                                                                     | Produces Real Data                                                                                                         | Status                      |
| --------------------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| `templates/page.tsx`                                | `templates` (state)        | `fetchTemplates({activo:'all'})` → `GET /api/templates`                                    | Yes (seed default visible per orquestrator psql)                                                                           | ✓ FLOWING                   |
| `templates/[id]/page.tsx`                           | `template + draft` (state) | `fetchTemplate(id)` → `GET /api/templates/:id`                                             | Yes (template_atributos tiene 5 rows seedadas)                                                                             | ✓ FLOWING                   |
| `propiedades-page.tsx::FamiliasTab`                 | `subcategorias` (state)    | `fetchPropiedades('subcategoria', { activo: true })` → `GET /api/propiedades/subcategoria` | Conditional — depende de filas activas en `prop_subcategoria` (prod tiene data per 260502-tqf restoration; local puede no) | ⚠️ STATIC (UAT-conditional) |
| `propiedad-table.tsx` (via PropiedadTable propTipo) | `propiedades` (state)      | `fetchPropiedades(tipo)` → `GET /api/propiedades/:tipo`                                    | Yes (genérico, ya funcional desde Phase 29; nuevos tipos heredan)                                                          | ✓ FLOWING                   |

⚠️ Nota: el "STATIC conditional" en FamiliasTab no es un bug — depende de que la subcategoría tabla tenga filas activas en runtime. En producción hay data per la restauración 260502-tqf; en DB local puede requerir un INSERT manual. Esto se valida en UAT visual.

### Behavioral Spot-Checks

| Behavior                                               | Command                                      | Result                        | Status                                 |
| ------------------------------------------------------ | -------------------------------------------- | ----------------------------- | -------------------------------------- |
| Composer Vitest suite passes                           | `pnpm test composer -- --run`                | 17/17 passed                  | ✓ PASS                                 |
| Backend type-check                                     | `pnpm --filter @objetiva/backend type-check` | exit 0 (orquestrador)         | ✓ PASS                                 |
| Backend build                                          | `pnpm --filter @objetiva/backend build`      | exit 0 (orquestrador)         | ✓ PASS                                 |
| Web type-check                                         | `pnpm --filter @objetiva/web type-check`     | exit 0 (orquestrador)         | ✓ PASS                                 |
| Web build                                              | `cd apps/web && pnpm test -- --run` → 29/29  | exit 0 (orquestrador)         | ✓ PASS                                 |
| DB live state (4 tablas + 5 columnas + 0 legacy)       | psql verify (orquestrador)                   | tablas + columnas verificadas | ✓ PASS                                 |
| Seed default presente (template id=1 + 5 atributos)    | psql verify (orquestrador)                   | OK                            | ✓ PASS                                 |
| Backend smoke 14 curls verde                           | Plan 03 SUMMARY (operador)                   | 14/14 verde                   | ✓ PASS                                 |
| Endpoint runtime contra container productivo (rebuild) | docker compose build/up (PENDIENTE)          | NO ejecutado todavía          | ? SKIP (orquestrador hace post-verify) |

### Probe Execution

No aplica — Phase 30 no declara probes (`scripts/*/tests/probe-*.sh`) en plans ni SUMMARYs. Validación se hace por (a) Vitest unit suite (composer), (b) `pnpm type-check`/`build` (verificado), (c) smoke curls manuales contra backend local (documentados en 30-03 SUMMARY), (d) UAT visual humano (diferido a mañana).

### Requirements Coverage

| Requirement | Source Plan(s)      | Description                                                                                    | Status              | Evidence                                                                                                                                                                                                                                                     |
| ----------- | ------------------- | ---------------------------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| TPL-01      | 30-02, 30-03, 30-04 | Admin puede crear y editar templates que definen qué atributos aplican a un grupo de artículos | ⚠️ PARCIAL          | Backend POST + PATCH ✓; UI EDIT (atributos) ✓ vía `/templates/[id]`. UI CREATE template ✗ (disabled D-13). UI EDIT del template propio (nombre/descripción) ✗ (página solo edita atributos). Tabla schema + seed default ✓. Follow-up declarado por Plan 04. |
| TPL-02      | 30-03, 30-04        | Admin puede marcar atributos como "variante" o "no-variante"                                   | ✓ SATISFIED         | Column `es_variante` en `template_atributos` ✓. Switch UI en `/templates/[id]/page.tsx:276-280` ✓. PATCH persiste ✓.                                                                                                                                         |
| TPL-03      | 30-01, 30-03, 30-04 | Admin puede definir cuáles atributos componen el SKU y en qué orden                            | ✓ SATISFIED         | Column `orden_sku` en `template_atributos` ✓. Input UI en `/templates/[id]/page.tsx:266-273` ✓. Composer `composeSku` filtra `esVariante && ordenSku !== null` y sortea por `ordenSku` ascendente (composer.ts:51-54) ✓.                                     |
| TPL-04      | 30-01, 30-03, 30-04 | Admin puede definir cuáles atributos componen el nombre auto y en qué orden                    | ✓ SATISFIED         | Column `orden_nombre` ✓. Input UI en `/templates/[id]/page.tsx:255-263` ✓. Composer `composeNombre` filtra `ordenNombre !== null` y sortea ✓.                                                                                                                |
| TPL-05      | 30-02, 30-03        | Sistema usa el template default automáticamente al crear un artículo                           | ⚠️ PARCIAL/DEFERRED | Seed default ✓; backend service expone `findOne(id=1)` para consumirlo. Integración con articulos (set automático al crear) ⏸ DEFERRED a Phase 32 (rework articulos).                                                                                        |

**ORPHANED requirements check:** Ninguno. Todos los 5 TPL-\* declarados en REQUIREMENTS.md están reclamados en al menos un plan de Phase 30 (verificado).

### Anti-Patterns Found

| File                     | Line  | Pattern                                              | Severity | Impact                                                                                                                                                                                                          |
| ------------------------ | ----- | ---------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | -------------------------------------------------------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Ninguno                  | —     | —                                                    | —        | Grep `TBD                                                                                                                                                                                                       | FIXME | XXX`retorna 0 matches en archivos modificados; grep`TODO | HACK | PLACEHOLDER`retorna 0 matches funcionales (los 4 matches de`placeholder` son HTML attribute props legítimos, no stubs). Sin debt markers. |
| `propiedades.service.ts` | 80-81 | `eslint-disable-next-line ... values(values as any)` | ℹ️ Info  | Cast localizado documentado y justificado en comentario adjunto (PROP_TABLES heterogéneo). No bloquea — validación se delega a Drizzle runtime + PG constraints. Aceptable per Plan 03 Deviation #1 Auto-fixed. |

### Human Verification Required

Ver `human_verification` en YAML frontmatter (10 items). Resumen:

1. **Visual UAT 8 tabs en `/propiedades`** — confirmar render, regresión 6 originales OK.
2. **Tab Familias columna extra + select de subcategoría en dialog** — confirmar UI funcional.
3. **Crear familia con sub válida persiste con nombre legible** — round-trip end-to-end.
4. **Duplicado familia → 409 legible** — mapeo de error UI.
5. **`/templates` muestra default + botón "Nuevo template" disabled con tooltip** — comportamiento Tooltip + estado.
6. **`/templates/1` permite editar atributos y persiste via PATCH** — flujo de edición end-to-end + persistencia.
7. **Validación cliente del form rechaza valores inválidos** — UX de toast + validación.
8. **Estética Tabler en componentes nuevos** — inspección visual h-9/border-radius/text-sm.
9. **Sin drag-drop visible en `/templates/[id]`** — D-13 honrado visualmente.
10. **Rebuild de imágenes Docker `erp-backend` + `erp-web`** — pending action operativa documentada por orquestador.

### Gaps Summary

**No hay BLOCKERS.** El phase 30 entregó:

- ✅ **El core del goal:** sistema de templates configurables (4 tablas DB, seed default con receta automotor, 6 endpoints REST funcionales, edit UI de atributos del default).
- ✅ **Las funciones puras `composeSku()` + `composeNombre()`** testeadas con 17 casos Vitest GREEN — lista para alimentar Phase 32 y 33.
- ⏸ **DEFERRED a Phase 32:** preview WYSIWYG + integración con articulos rework (SC#3 y SC#4 del ROADMAP — Phase 32 SC#1 y SC#2 explícitamente cubren esto).
- ⚠️ **PARCIAL/Follow-up:** UI para CREATE/DELETE templates (SC#1) y drag-drop builder (SC#2) — decisión D-13 explícita; backend POST existe, DELETE no; Plan 04 declara `/gsd:add-todo` para evaluación post-cierre. No bloquea Phase 30 ni Phase 32.

**Pending Actions operativas (no son gaps de implementación):**

1. Rebuild de imágenes Docker `erp-backend` y `erp-web` (el orquestador lo hará post-verify).
2. UAT visual humano para confirmar render/comportamiento UI (mañana, per sesión desatendida).
3. Conservar backup `backups.articulos_pre_phase30` durante UAT — eliminar tras 7 días sin issues.

**Anti-patterns:** 0 blockers, 0 warnings, 1 info (cast `as any` documentado en propiedades.service — aceptable).

---

## Re-verification Notes

N/A — esta es la verificación inicial de Phase 30.

---

_Verified: 2026-05-17T15:40:00Z_
_Verifier: Claude (gsd-verifier, Opus 4.7 1M context)_
_Mode: standard goal-backward verification_
_Phase: 30-templates-composici-n-sku-nombre_
