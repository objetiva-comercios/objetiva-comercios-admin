---
phase: 30-templates-composici-n-sku-nombre
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - packages/types/src/template.ts
  - packages/types/src/index.ts
  - packages/utils/src/composer.ts
  - packages/utils/src/index.ts
  - apps/web/src/lib/composer.test.ts
  - apps/backend/package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - TPL-03
  - TPL-04
tags:
  - composer
  - shared-utils
  - tdd
  - typescript
  - vitest

must_haves:
  truths:
    - "composeSku/composeNombre son funciones puras importables desde @objetiva/utils"
    - "Los tipos Template, TemplateAtributo, AtributosMap viven en @objetiva/types"
    - "Los 8 casos de borde críticos pasan con vitest run"
    - "apps/backend declara @objetiva/utils como dependencia workspace"
  artifacts:
    - path: "packages/types/src/template.ts"
      provides: "Tipos compartidos Template, TemplateAtributo, AtributosMap"
      contains: "export interface Template"
    - path: "packages/utils/src/composer.ts"
      provides: "Funciones puras stripSep, composeSku, composeNombre"
      exports: ["stripSep", "composeSku", "composeNombre"]
    - path: "apps/web/src/lib/composer.test.ts"
      provides: "Suite Vitest cubriendo los 8 casos de borde de RESEARCH.md"
      min_lines: 60
  key_links:
    - from: "apps/web/src/lib/composer.test.ts"
      to: "packages/utils/src/composer.ts"
      via: "import from '@objetiva/utils'"
      pattern: "from ['\"]@objetiva/utils['\"]"
    - from: "packages/utils/src/composer.ts"
      to: "packages/types/src/template.ts"
      via: "import type from '@objetiva/types'"
      pattern: "import.*@objetiva/types"
    - from: "apps/backend/package.json"
      to: "packages/utils"
      via: "workspace dependency"
      pattern: "@objetiva/utils.*workspace:\\*"
---

<objective>
Establecer el shared composer del Phase 30: tipos en `@objetiva/types`, implementación en `@objetiva/utils`, y suite Vitest cubriendo los 8 casos de borde críticos (RESEARCH.md §"Casos de borde críticos para el composer"). Es el Wave 0 que las olas posteriores consumen.

Purpose: Una sola fuente de verdad para la composición SKU/Nombre. La función vive en `packages/utils` porque (a) `apps/web` ya depende de `@objetiva/utils`, (b) `apps/backend` puede agregarla, (c) los tests viven en `apps/web` donde Vitest ya está configurado (backend NO tiene tests automatizados, PITFALL-4).

Output:
- `packages/types/src/template.ts` — tipos `Template`, `TemplateAtributo`, `AtributosMap`
- `packages/utils/src/composer.ts` — `stripSep`, `composeSku`, `composeNombre`
- `apps/web/src/lib/composer.test.ts` — Vitest suite de 8 casos de borde
- `apps/backend/package.json` — agrega `@objetiva/utils: workspace:*` (PITFALL-5)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-templates-composici-n-sku-nombre/30-CONTEXT.md
@.planning/phases/30-templates-composici-n-sku-nombre/30-RESEARCH.md
@.planning/phases/30-templates-composici-n-sku-nombre/30-PATTERNS.md
@.planning/phases/30-templates-composici-n-sku-nombre/30-VALIDATION.md
@apps/web/src/lib/abrev.test.ts
@apps/web/vitest.config.ts
@packages/utils/src/formatters.ts
@packages/utils/src/index.ts
@packages/types/src/index.ts

<interfaces>
<!-- Contratos que este plan CREA. Olas posteriores los consumen. -->

From packages/types/src/template.ts (NEW):
```typescript
export interface TemplateAtributo {
  atributoTipo: string             // 'objeto' | 'marca' | 'modelo' | 'medida' | 'custom_1' | ...
  ordenNombre: number | null       // NULL = no aparece en nombre
  ordenSku: number | null          // NULL = no aparece en SKU
  esVariante: boolean              // si es eje de variante
  customSlot: number | null        // NULL si no es custom; 1, 2 o 3 si lo es
}

export interface Template {
  id: number
  nombre: string
  atributos: TemplateAtributo[]
}

export type AtributosMap = Record<string, string | undefined>
```

From packages/utils/src/composer.ts (NEW):
```typescript
export function stripSep(codigo: string): string
export function composeSku(codigo: string, atributos: AtributosMap, template: Template): string
export function composeNombre(atributos: AtributosMap, template: Template): string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear tipos compartidos en packages/types</name>
  <read_first>
    - packages/types/src/index.ts (estructura barrel + interfaces existentes)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-PATTERNS.md (sección "packages/types/src/template.ts (NEW)")
    - .planning/phases/30-templates-composici-n-sku-nombre/30-RESEARCH.md (sección "Composer functions — Tipos compartidos")
  </read_first>
  <behavior>
    - Test 1 (implícito por TS compile): `Template` tiene campos `id: number`, `nombre: string`, `atributos: TemplateAtributo[]`
    - Test 2: `TemplateAtributo` tiene `atributoTipo: string`, `ordenNombre: number | null`, `ordenSku: number | null`, `esVariante: boolean`, `customSlot: number | null`
    - Test 3: `AtributosMap` permite `Record<string, string | undefined>`
    - Test 4: Los tipos se re-exportan desde `@objetiva/types`
  </behavior>
  <action>
Crear archivo nuevo `packages/types/src/template.ts` con exports `interface TemplateAtributo`, `interface Template`, `type AtributosMap` (signatures exactas según el bloque interfaces de este PLAN — campos en camelCase per convención TS, sin Zod, sin lógica). Actualizar `packages/types/src/index.ts` agregando al final del archivo la línea `export * from './template'` (sin tocar las secciones existentes de Zod schemas, getPasswordStrength, AppRole — solo append). Per D-13, D-16, D-17.
  </action>
  <acceptance_criteria>
    - source: archivo `packages/types/src/template.ts` existe y exporta los 3 símbolos (`TemplateAtributo`, `Template`, `AtributosMap`).
    - source: `packages/types/src/index.ts` contiene la línea `export * from './template'` (grep `grep -c "export \* from './template'" packages/types/src/index.ts` retorna `1`).
    - behavior: `import { Template } from '@objetiva/types'` se resuelve sin error en `apps/web` (verificable luego con type-check en task 4).
    - cli: `grep -E "interface (Template|TemplateAtributo)" packages/types/src/template.ts` retorna 2 líneas.
  </acceptance_criteria>
  <verify>
    <automated>test -f packages/types/src/template.ts && grep -q "export interface Template" packages/types/src/template.ts && grep -q "export interface TemplateAtributo" packages/types/src/template.ts && grep -q "export type AtributosMap" packages/types/src/template.ts && grep -q "export \* from './template'" packages/types/src/index.ts</automated>
  </verify>
  <done>Archivo `packages/types/src/template.ts` con los 3 exports + barrel actualizado. Sin breaking changes en exports previos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Escribir tests RED del composer (Vitest)</name>
  <read_first>
    - apps/web/src/lib/abrev.test.ts (patrón canónico de tests puros)
    - apps/web/vitest.config.ts (config: `environment: 'node'`, include `src/**/*.{test,spec}.{ts,tsx}`, alias `@`)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-RESEARCH.md (sección "Casos de borde críticos para el composer (Wave 0)" — 8 casos)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-VALIDATION.md (sección "Casos de Borde Críticos (composer)" — los 8 casos consolidados)
  </read_first>
  <behavior>
    - Test stripSep #1: `stripSep('AMOR-001')` → `'AMOR001'`
    - Test stripSep #2: `stripSep('X.001.A')` → `'X001A'`
    - Test stripSep #3: `stripSep('AMOR 001')` → `'AMOR001'`
    - Test stripSep #4: `stripSep('A_B_C')` → `'ABC'` (underscores también)
    - Test stripSep #5: `stripSep('AB-_. C')` → `'ABC'` (mezcla de separadores consecutivos)
    - Test composeNombre #1 (caso 4 VALIDATION): atributos vacíos/undefined se omiten sin doble espacio → `composeNombre({ objeto: 'Amortiguador', marca: '', medida: undefined }, templateDefault)` → `'Amortiguador'`
    - Test composeNombre #2 (caso 6 VALIDATION): respeta `ordenNombre` ascending del template
    - Test composeNombre #3: template con todos los campos completos → `'Amortiguador Sachs C24-A 345mm Fiat Cronos 1.3'` (receta D-14)
    - Test composeSku #1 (caso 5 VALIDATION): template default con `atributos_variante=[]` → return `stripSep(codigo)` shortcut (D-15)
    - Test composeSku #2: template con variantes → `stripSep(codigo) + '-' + valores.join('-')`
    - Test composeSku #3 (caso 7 VALIDATION): variantes con texto repetido cross-prop (`talle:'XL'`, `color:'XL'`) ambos aparecen sin colisión → `'XYZ-XL-XL'`
    - Test composeSku #4 (caso 8 VALIDATION): atributo marcado `es_variante` pero valor `undefined` en `atributos` → se omite del SKU sin error
  </behavior>
  <action>
Crear `apps/web/src/lib/composer.test.ts` siguiendo exactamente el patrón de `apps/web/src/lib/abrev.test.ts`: cabecera comment explicando que es RED-phase (TDD strict), import `{ describe, it, expect } from 'vitest'`, import `{ stripSep, composeSku, composeNombre } from '@objetiva/utils'` y `{ Template, TemplateAtributo, AtributosMap } from '@objetiva/types'`. Tres `describe` blocks: uno por función. Cubrir los 12+ tests listados en `<behavior>` arriba. Para `composeNombre`/`composeSku` definir un fixture `templateDefault` local con receta D-14 (`[objeto, marca, modelo, medida, custom_1]` con ordenNombre 1..5, ordenSku NULL, esVariante false) y un `templateConVariantes` con dos atributos es_variante=true y ordenSku 1,2. Los tests DEBEN fallar (RED) porque el composer todavía no existe — está esperado.
  </action>
  <acceptance_criteria>
    - source: archivo `apps/web/src/lib/composer.test.ts` existe.
    - source: `grep -c "describe(" apps/web/src/lib/composer.test.ts` retorna `>= 3` (uno por función).
    - source: `grep -c "it(" apps/web/src/lib/composer.test.ts` retorna `>= 12` (los 12+ casos).
    - source: importa desde `@objetiva/utils` y `@objetiva/types` (no relative paths a `packages/`).
    - behavior (esperado RED): `cd apps/web && pnpm test composer` muestra failures porque composer.ts todavía no exporta funciones. Esto es correcto en este task.
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/web/src/lib/composer.test.ts && [ $(grep -c "describe(" apps/web/src/lib/composer.test.ts) -ge 3 ] && [ $(grep -c "^\s*it(" apps/web/src/lib/composer.test.ts) -ge 12 ] && grep -q "from '@objetiva/utils'" apps/web/src/lib/composer.test.ts</automated>
  </verify>
  <done>Suite de tests escrita con los 12+ casos. Los tests están RED (esperado — composer.ts no implementado todavía).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementar composer (GREEN phase) + barrel export</name>
  <read_first>
    - apps/web/src/lib/composer.test.ts (tests escritos en Task 2 — usar como spec)
    - packages/utils/src/formatters.ts (patrón función pura en utils)
    - packages/utils/src/index.ts (barrel export)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-RESEARCH.md (sección "Composer functions" — algoritmo de cada función con código de referencia)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-CONTEXT.md (D-15, D-16, D-17 — algoritmo exacto)
  </read_first>
  <behavior>
    - Test suite RED del Task 2 pasa a GREEN tras implementar las 3 funciones.
    - Sin nuevas dependencias externas (PITFALL-6: NO instalar slugify).
    - Sin side effects, sin imports de `apps/` o de `drizzle-orm`.
  </behavior>
  <action>
Crear `packages/utils/src/composer.ts`:
- Importar tipos: `import type { Template, AtributosMap } from '@objetiva/types'`.
- Exportar `stripSep(codigo: string): string` → reemplaza `/[-_.\s]+/g` por `''` (Phase 29 D-12 pattern).
- Exportar `composeSku(codigo, atributos, template)` per D-16: filtra `template.atributos` con `esVariante && ordenSku !== null`, sort por `ordenSku` asc, mapea a `atributos[a.atributoTipo]`, filtra `Boolean`. Si lista resultante vacía → return `stripSep(codigo)` (shortcut D-15). Si no vacía → `stripSep(codigo) + '-' + partes.join('-')`.
- Exportar `composeNombre(atributos, template)` per D-17: filtra `template.atributos` con `ordenNombre !== null`, sort por `ordenNombre` asc, mapea a `atributos[a.atributoTipo]`, filtra `Boolean`, join con `' '` (espacio). Sin slugificación. Sin separador entre clasificador y descriptor.
Actualizar `packages/utils/src/index.ts` añadiendo `export * from './composer'` después de la línea existente `export * from './formatters'`. Verificar que `cd apps/web && pnpm test composer` retorna GREEN (todos los tests del Task 2 pasan).
  </action>
  <acceptance_criteria>
    - source: archivo `packages/utils/src/composer.ts` existe y exporta `stripSep`, `composeSku`, `composeNombre`.
    - source: `packages/utils/src/index.ts` contiene `export * from './composer'` (grep retorna 1).
    - source: `composer.ts` NO contiene imports de `slugify` (PITFALL-6) ni de `drizzle-orm` ni rutas relativas a `apps/`.
    - test: `cd apps/web && pnpm test composer` retorna exit code 0 con todos los tests GREEN.
    - behavior: `composeSku('AMOR-001', {}, { id:1, nombre:'default', atributos:[] })` retorna `'AMOR001'` (shortcut D-15).
    - behavior: `composeNombre({ objeto:'Amortiguador', marca:'', medida:undefined }, templateDefault)` retorna `'Amortiguador'` sin doble espacio (caso 4).
  </acceptance_criteria>
  <verify>
    <automated>test -f packages/utils/src/composer.ts && grep -q "export function stripSep" packages/utils/src/composer.ts && grep -q "export function composeSku" packages/utils/src/composer.ts && grep -q "export function composeNombre" packages/utils/src/composer.ts && grep -q "export \* from './composer'" packages/utils/src/index.ts && ! grep -q "slugify" packages/utils/src/composer.ts && cd apps/web && pnpm test composer -- --run 2>&1 | grep -E "Test Files\s+[0-9]+ passed"</automated>
  </verify>
  <done>composer.ts implementado, tests del Task 2 pasan GREEN, barrel export actualizado, sin dependencias externas nuevas.</done>
</task>

<task type="auto">
  <name>Task 4: Wire workspace dep + type-check end-to-end</name>
  <read_first>
    - apps/backend/package.json (sección `dependencies` — línea con `@objetiva/types: workspace:*`)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-PATTERNS.md (sección "apps/backend/package.json (MODIFY)")
    - .planning/phases/30-templates-composici-n-sku-nombre/30-RESEARCH.md (PITFALL-5)
  </read_first>
  <action>
Editar `apps/backend/package.json`: en `dependencies`, agregar `"@objetiva/utils": "workspace:*",` inmediatamente después de la línea `"@objetiva/types": "workspace:*",` (orden alfabético: `@objetiva/types` viene antes de `@objetiva/utils`). Correr `pnpm install` desde la raíz para sincronizar `pnpm-lock.yaml` y crear los symlinks de workspace. Correr `pnpm --filter @objetiva/utils build` para compilar `dist/` (el backend importa desde `./dist` según `packages/utils/package.json:main`). Correr `pnpm --filter @objetiva/backend type-check` para confirmar que el backend resuelve `import { composeSku } from '@objetiva/utils'` (sin uso real todavía — un type-check OK valida la resolución). PITFALL-5: sin esta dep declarada, Plan 03 (backend module templates) o un futuro consumidor backend explotaría en build.
  </action>
  <acceptance_criteria>
    - source: `apps/backend/package.json` contiene línea `"@objetiva/utils": "workspace:*"` en `dependencies` (no en `devDependencies`).
    - cli: `pnpm install` desde raíz completa exit 0.
    - cli: `pnpm --filter @objetiva/utils build` genera `packages/utils/dist/composer.js` y `composer.d.ts`.
    - cli: `pnpm --filter @objetiva/backend type-check` retorna exit 0 (no rompe el backend; la dep está disponible aunque no se use todavía).
    - cli: `pnpm --filter @objetiva/web type-check` retorna exit 0 (composer importable desde web).
    - test: `cd apps/web && pnpm test composer -- --run` sigue verde tras la instalación (no se regresó nada).
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"@objetiva/utils": "workspace:\*"' apps/backend/package.json && pnpm install --frozen-lockfile=false 2>&1 | tail -5 && pnpm --filter @objetiva/utils build && test -f packages/utils/dist/composer.js && pnpm --filter @objetiva/backend type-check && pnpm --filter @objetiva/web type-check && cd apps/web && pnpm test composer -- --run 2>&1 | grep -E "Test Files\s+[0-9]+ passed"</automated>
  </verify>
  <done>Backend declara `@objetiva/utils` workspace dep, `dist/` compilado, ambos apps type-check OK, suite Vitest sigue GREEN. Wave 0 cerrada — Wave 1+ pueden consumir el composer.</done>
</task>

</tasks>

<verification>
- Tests Vitest verdes: `cd apps/web && pnpm test composer -- --run` → exit 0.
- Type-check backend + web: ambos exit 0.
- `packages/utils/dist/composer.js` existe (build OK para consumo backend).
- Sin imports prohibidos (`slugify`, `drizzle-orm`, paths a `apps/`) en `composer.ts`.
</verification>

<success_criteria>
- ✅ `@objetiva/utils` exporta `stripSep`, `composeSku`, `composeNombre`.
- ✅ `@objetiva/types` exporta `Template`, `TemplateAtributo`, `AtributosMap`.
- ✅ Suite Vitest cubre los 8 casos de borde de RESEARCH.md/VALIDATION.md.
- ✅ `apps/backend` declara `@objetiva/utils` como dep workspace (PITFALL-5 cerrado).
- ✅ Tests GREEN, ambos apps type-check OK.
- ✅ Wave 0 lista para que Wave 1 (migration) y Wave 2 (backend module) la consuman.
</success_criteria>

<output>
After completion, create `.planning/phases/30-templates-composici-n-sku-nombre/30-01-SUMMARY.md` documenting:
- Funciones públicas exportadas (signatures completas)
- Tests escritos (lista de cases con números 1-12+)
- Decisiones de implementación tomadas dentro del Claude's Discretion (estructura interna, etc.)
- Dependencias agregadas (workspace deps, NO externas)
</output>
