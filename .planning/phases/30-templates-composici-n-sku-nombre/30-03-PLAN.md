---
phase: 30-templates-composici-n-sku-nombre
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - apps/backend/src/modules/propiedades/propiedades.constants.ts
  - apps/backend/src/modules/propiedades/propiedades.service.ts
  - apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts
  - apps/backend/src/modules/templates/templates.module.ts
  - apps/backend/src/modules/templates/templates.controller.ts
  - apps/backend/src/modules/templates/templates.service.ts
  - apps/backend/src/modules/templates/dto/create-template.dto.ts
  - apps/backend/src/modules/templates/dto/update-template.dto.ts
  - apps/backend/src/modules/templates/dto/template-atributos.dto.ts
  - apps/backend/src/app.module.ts
autonomous: false
requirements:
  - TPL-01
  - TPL-02
  - TPL-03
  - TPL-04
  - TPL-05
tags:
  - backend
  - nestjs
  - crud
  - propiedades
  - templates

must_haves:
  truths:
    - "GET /propiedades/familia retorna 200 con JSON array (puede estar vacío inicialmente)"
    - "GET /propiedades/aplicacion retorna 200 con JSON array (puede estar vacío inicialmente)"
    - "POST /propiedades/familia con {nombre, abrev, parentId=<subcategoria_id_existente>} crea fila"
    - "POST /propiedades/familia SIN parentId retorna 400 BadRequest"
    - "GET /templates retorna 200 con array que incluye el template default seedeado (identificado por `nombre='default'`, NO por columna `is_default` — D-13 + T-30-03)"
    - "El service `templates.service.ts` resuelve el template default por `WHERE nombre = 'default'` (no por flag); ningún query/endpoint asume columna `is_default`"
    - "GET /templates/1 retorna template + array atributos con los 5 esperados (objeto/marca/modelo/medida/custom_1)"
    - "PATCH /templates/1/atributos reemplaza la lista completa de atributos (upsert)"
  artifacts:
    - path: "apps/backend/src/modules/templates/templates.module.ts"
      provides: "Módulo NestJS registrado en AppModule"
      exports: ["TemplatesService"]
    - path: "apps/backend/src/modules/templates/templates.controller.ts"
      provides: "CRUD endpoints templates + template_atributos"
      exports: ["TemplatesController"]
    - path: "apps/backend/src/modules/templates/templates.service.ts"
      provides: "Service con findAll, findOne (con JOIN atributos), create, update, upsertAtributos"
      exports: ["TemplatesService"]
    - path: "apps/backend/src/modules/propiedades/propiedades.constants.ts"
      provides: "PROP_TIPOS extendido a 8 tipos (6 originales + familia + aplicacion)"
      contains: "'familia'"
  key_links:
    - from: "apps/backend/src/app.module.ts"
      to: "apps/backend/src/modules/templates/templates.module.ts"
      via: "import + array imports"
      pattern: "TemplatesModule"
    - from: "apps/backend/src/modules/propiedades/propiedades.service.ts"
      to: "apps/backend/src/db/schema.ts (propFamilia, propAplicacion)"
      via: "PROP_TABLES map"
      pattern: "familia:\\s*propFamilia"
    - from: "apps/backend/src/modules/propiedades/propiedades.service.ts (create)"
      to: "CreatePropiedadDto.parentId"
      via: "branch tipo === 'familia' → values.subcategoriaId = dto.parentId"
      pattern: "subcategoriaId"
---

<objective>
Crear el módulo `templates` (NestJS) con CRUD completo de `articulos_templates` + `template_atributos`, y extender el módulo `propiedades` existente con 2 nuevos tipos: `familia` (con FK a `prop_subcategoria`, requiere campo `parentId`) y `aplicacion` (factory genérico estándar). El CRUD de `familia` y `aplicacion` aparece "automágicamente" gracias al patrón parametrizado por `:tipo` de Phase 29 — solo hay que actualizar `PROP_TIPOS`, `PROP_TABLES`, `PROP_LABELS` y agregar la rama `parentId` en `create()`.

Purpose: Cierra TPL-01 (crear/editar templates), TPL-02 (es_variante), TPL-03 (orden_sku), TPL-04 (orden_nombre), TPL-05 (template default consumible vía GET /templates). Habilita Wave 3 (frontend) que consume estos endpoints.

Output:
- Módulo NestJS `templates` con 6 endpoints (GET list, GET detail con JOIN, POST, PATCH template, GET atributos, PATCH atributos como replace).
- Extensión de `propiedades` para los nuevos tipos `familia` (con `parentId`) y `aplicacion`.
- App module registra el nuevo `TemplatesModule`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-templates-composici-n-sku-nombre/30-CONTEXT.md
@.planning/phases/30-templates-composici-n-sku-nombre/30-RESEARCH.md
@.planning/phases/30-templates-composici-n-sku-nombre/30-PATTERNS.md
@apps/backend/src/modules/propiedades/propiedades.constants.ts
@apps/backend/src/modules/propiedades/propiedades.service.ts
@apps/backend/src/modules/propiedades/propiedades.controller.ts
@apps/backend/src/modules/propiedades/propiedades.module.ts
@apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts
@apps/backend/src/modules/propiedades/dto/update-propiedad.dto.ts
@apps/backend/src/app.module.ts
@apps/backend/src/db/schema.ts
@apps/backend/src/modules/webhooks/webhooks.controller.ts

<interfaces>
<!-- Lo que este plan CREA. La UI (Plan 04) los consume. -->

API endpoints:
```
GET    /propiedades/familia[?activo=true|false|all]   → 200, Array<{id,subcategoriaId,nombre,abrev,activo,createdAt,updatedAt}>
GET    /propiedades/familia/:id                        → 200 | 404
POST   /propiedades/familia (admin)                    → 201, body={nombre, abrev, parentId} (parentId requerido)
PATCH  /propiedades/familia/:id (admin)                → 200, body={nombre?, abrev?}
PATCH  /propiedades/familia/:id/toggle (admin)         → 200
(idem para 'aplicacion' sin parentId)

GET    /templates[?activo=true|false|all]              → 200, Array<{id,nombre,descripcion,activo,...}>
GET    /templates/:id                                  → 200 con atributos[], 404 si no existe
POST   /templates (admin)                              → 201, body={nombre, descripcion?}
PATCH  /templates/:id (admin)                          → 200, body={nombre?, descripcion?, activo?}
GET    /templates/:id/atributos                        → 200, Array<TemplateAtributo>
PATCH  /templates/:id/atributos (admin)                → 200, body=Array<{atributoTipo, ordenNombre?, ordenSku?, esVariante?, customSlot?}> (reemplaza completo)
```

Shapes (devueltos por el endpoint):
```typescript
interface ArticulosTemplateResponse {
  id: number
  nombre: string
  descripcion: string | null
  activo: boolean
  createdAt: string
  updatedAt: string
  atributos?: TemplateAtributoResponse[]   // solo en GET /:id y opcionalmente en GET list
}

interface TemplateAtributoResponse {
  templateId: number
  atributoTipo: string
  ordenNombre: number | null
  ordenSku: number | null
  esVariante: boolean
  customSlot: number | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender propiedades para 'familia' y 'aplicacion' (PROP_TIPOS + DTO parentId + service branch)</name>
  <read_first>
    - apps/backend/src/modules/propiedades/propiedades.constants.ts (líneas 1-44 — PROP_TIPOS, PROP_TABLES, PROP_LABELS)
    - apps/backend/src/modules/propiedades/propiedades.service.ts (líneas 57-69 — método create actual)
    - apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts (líneas 1-17 — DTO base)
    - apps/backend/src/modules/propiedades/propiedades.controller.ts (líneas 65-70 — POST endpoint con @Roles('admin'))
    - .planning/phases/30-templates-composici-n-sku-nombre/30-PATTERNS.md (secciones de propiedades — patrón exacto a aplicar)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-RESEARCH.md (PITFALL-7 — duplicación PROP_TIPOS backend/frontend)
  </read_first>
  <behavior>
    - Test smoke #1: `GET /propiedades/familia` retorna 200 (array, posiblemente vacío).
    - Test smoke #2: `GET /propiedades/aplicacion` retorna 200 (array, posiblemente vacío).
    - Test smoke #3: `POST /propiedades/familia` con `{nombre:"Amortiguadores", abrev:"AMORT", parentId:1}` crea fila (asumiendo que existe `prop_subcategoria.id=1`); response 201 con la fila creada incluyendo `subcategoriaId: 1`.
    - Test smoke #4: `POST /propiedades/familia` con `{nombre:"X", abrev:"X"}` SIN `parentId` retorna 400 BadRequest (`subcategoria_id requerido para familia`).
    - Test smoke #5: `POST /propiedades/aplicacion` con `{nombre:"Fiat Cronos 1.3", abrev:"FCRON13"}` (sin parentId) retorna 201 — `aplicacion` NO requiere parentId.
    - Test smoke #6: POST duplicado de `familia` con mismo `(parentId, nombre)` retorna 409 Conflict por UNIQUE compuesto.
  </behavior>
  <action>
**A. `propiedades.constants.ts`:**
- Importar `propFamilia, propAplicacion` desde `'../../db/schema'`.
- Extender `PROP_TIPOS` agregando `'familia', 'aplicacion'` al final del array (manteniendo el orden de los 6 originales) — total 8 elementos.
- Extender `PROP_TABLES` con `familia: propFamilia,` y `aplicacion: propAplicacion,`.
- Extender `PROP_LABELS` con: `familia: { singular: 'familia', plural: 'familias' }` y `aplicacion: { singular: 'aplicación', plural: 'aplicaciones' }`.

**B. `dto/create-propiedad.dto.ts`:**
- Agregar import de `IsInt, IsOptional` desde `class-validator`.
- Agregar campo opcional `@IsOptional() @IsInt() parentId?: number` al DTO. NO marcar como required en el DTO — la validación de presencia se hace condicional al tipo en el service (`familia` la requiere; los 7 restantes la ignoran).

**C. `propiedades.service.ts` `create()`:**
- Cambiar la construcción del objeto values:
  ```ts
  const values: Record<string, unknown> = { nombre: dto.nombre, abrev: dto.abrev }
  if (tipo === 'familia') {
    if (dto.parentId === undefined || dto.parentId === null) {
      throw new BadRequestException('subcategoria_id requerido para familia')
    }
    values.subcategoriaId = dto.parentId
  }
  ```
- Cambiar `.values({ nombre: dto.nombre, abrev: dto.abrev })` por `.values(values)`.
- `BadRequestException` ya está importado al inicio del archivo.
- NO tocar `handleUniqueViolation` — el patrón de matching por nombre del constraint (`prop_familia_nombre_lower_uniq`) seguirá funcionando con el prefijo correcto.

**D. NO tocar el controller** — el endpoint `POST /propiedades/:tipo` ya pasa el DTO completo (incluyendo `parentId` cuando viene) al service. La validación de tipo (`PROP_TIPOS.includes(tipo)`) ya cubre los nuevos `familia` y `aplicacion` automáticamente.

**E. Smoke local con curl** (post-build):
```bash
pnpm --filter @objetiva/backend dev &   # o el container docker-compose
# Esperar 5s al boot
curl -s -X GET "http://localhost:3001/propiedades/familia" -H "Authorization: Bearer $TOKEN"
# Debe retornar [] o JSON válido (no 404, no 500)
curl -s -X GET "http://localhost:3001/propiedades/aplicacion" -H "Authorization: Bearer $TOKEN"
```
Per CLAUDE.md memory `feedback_docker_compose.md`: si el setup es docker, usar `docker compose up` en lugar de `pnpm dev`. El executor decide el método según el estado del entorno.
  </action>
  <acceptance_criteria>
    - source: `propiedades.constants.ts` contiene `'familia'` y `'aplicacion'` en `PROP_TIPOS` (grep retorna 2+ líneas).
    - source: `propiedades.constants.ts` contiene `familia: propFamilia` y `aplicacion: propAplicacion` en `PROP_TABLES`.
    - source: `create-propiedad.dto.ts` contiene `parentId?: number` con `@IsInt() @IsOptional()`.
    - source: `propiedades.service.ts` contiene la rama `if (tipo === 'familia')` con throw `BadRequestException`.
    - cli: `pnpm --filter @objetiva/backend type-check` retorna exit 0.
    - cli: `pnpm --filter @objetiva/backend build` retorna exit 0.
    - behavior: `curl GET /propiedades/familia` retorna 200 con body JSON parseable como array.
    - behavior: `curl GET /propiedades/aplicacion` retorna 200 con body JSON parseable como array.
    - behavior: `curl POST /propiedades/familia` sin `parentId` retorna 400 con mensaje que contiene "subcategoria_id".
  </acceptance_criteria>
  <verify>
    <automated>grep -q "'familia'" apps/backend/src/modules/propiedades/propiedades.constants.ts && grep -q "'aplicacion'" apps/backend/src/modules/propiedades/propiedades.constants.ts && grep -q "familia: propFamilia" apps/backend/src/modules/propiedades/propiedades.constants.ts && grep -q "parentId" apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts && grep -q "tipo === 'familia'" apps/backend/src/modules/propiedades/propiedades.service.ts && pnpm --filter @objetiva/backend type-check && pnpm --filter @objetiva/backend build</automated>
  </verify>
  <done>Backend `propiedades` soporta los 8 tipos. Smoke curl OK para los 2 nuevos endpoints (test manual durante implementación).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear módulo templates (module + controller + service + DTOs)</name>
  <read_first>
    - apps/backend/src/modules/propiedades/propiedades.module.ts (patrón módulo NestJS mínimo)
    - apps/backend/src/modules/propiedades/propiedades.service.ts (patrón service con DrizzleService + handleUniqueViolation)
    - apps/backend/src/modules/webhooks/webhooks.controller.ts (patrón controller con RBAC + CRUD)
    - apps/backend/src/modules/propiedades/propiedades.controller.ts (patrón @Roles('admin') en métodos write, reads abiertos)
    - apps/backend/src/modules/propiedades/dto/create-propiedad.dto.ts (patrón DTO con class-validator + Transform)
    - apps/backend/src/db/schema.ts (exports articulosTemplates, templateAtributos del Plan 02)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-PATTERNS.md (secciones del módulo templates: module, controller, service, dto)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-RESEARCH.md (sección "Backend — módulo templates")
  </read_first>
  <behavior>
    - Test smoke #1: `GET /templates` retorna 200 con array que contiene el template seedeado `nombre='default'`.
    - Test smoke #2: `GET /templates/1` (asumiendo el seed insertó id=1) retorna 200 con `{id, nombre, descripcion, activo, atributos: [5 elementos]}`.
    - Test smoke #3: `GET /templates/1/atributos` retorna 200 con array de 5 elementos ordenados por `ordenNombre`.
    - Test smoke #4: `POST /templates` con `{nombre:"Indumentaria"}` (admin) crea fila → 201.
    - Test smoke #5: `POST /templates` con `{nombre:"default"}` retorna 409 Conflict (UNIQUE violation en `nombre`).
    - Test smoke #6: `PATCH /templates/1/atributos` con body `[{atributoTipo:'objeto', ordenNombre:1, ordenSku:null, esVariante:false, customSlot:null}, ...]` reemplaza atributos del template (DELETE + INSERT en transacción).
    - Test smoke #7: `POST /templates` con body inválido (sin `nombre`) retorna 400 BadRequest.
  </behavior>
  <action>
**A. Crear `apps/backend/src/modules/templates/templates.module.ts`:**
Copiar la estructura de `propiedades.module.ts` cambiando los nombres a `TemplatesController`, `TemplatesService`, `TemplatesModule`. Exportar `TemplatesService` (Phase 32 puede consumirlo).

**B. Crear DTOs en `apps/backend/src/modules/templates/dto/`:**
- `create-template.dto.ts`: clase `CreateTemplateDto` con:
  - `@IsString() @IsNotEmpty() @MaxLength(100) @Transform(({value}) => typeof value === 'string' ? value.trim() : value) nombre!: string`
  - `@IsString() @IsOptional() @MaxLength(500) descripcion?: string`
- `update-template.dto.ts`: clase `UpdateTemplateDto` con `nombre?`, `descripcion?`, `activo?` (todos `@IsOptional()`). NO usar PartialType de @nestjs/mapped-types si no está instalado — declarar campos opcionales manualmente (patrón `update-propiedad.dto.ts`).
- `template-atributos.dto.ts`:
  - `TemplateAtributoDto` con `atributoTipo!: string` (`@IsString() @IsNotEmpty() @MaxLength(50)`), `ordenNombre?: number | null` (`@IsInt() @IsOptional()`), `ordenSku?: number | null` (`@IsInt() @IsOptional()`), `esVariante?: boolean` (`@IsBoolean() @IsOptional()`), `customSlot?: number | null` (`@IsInt() @IsOptional() @Min(1) @Max(3)`).
  - `ReplaceTemplateAtributosDto` con `@IsArray() @ValidateNested({each:true}) @Type(() => TemplateAtributoDto) atributos!: TemplateAtributoDto[]` (usar `class-transformer` `@Type`).

**C. Crear `apps/backend/src/modules/templates/templates.service.ts`:**
Inyectar `DrizzleService`. Métodos:
- `async findAll(opts: { activo?: boolean } = {})` → SELECT * FROM articulosTemplates [WHERE activo=?] ORDER BY nombre ASC. Patrón idéntico a `propiedades.service.findAll`.
- `async findOne(id: number)` → SELECT del template + un segundo SELECT de `templateAtributos` filtrado por `templateId=id` ordenado por `ordenNombre NULLS LAST, atributoTipo ASC`. Retornar `{...template, atributos: [...]}`. Throw `NotFoundException` si template no existe.
- `async create(dto: CreateTemplateDto)` → INSERT con `.returning()`. Wrap try/catch para `handleUniqueViolation` (SQLSTATE 23505 sobre `nombre` UNIQUE → `ConflictException('Ya existe un template con ese nombre')`).
- `async update(id: number, dto: UpdateTemplateDto)` → UPDATE SET con `updatedAt: new Date()`. Throw `NotFoundException` si no existe.
- `async findAtributos(templateId: number)` → SELECT FROM templateAtributos WHERE templateId=? ORDER BY ordenNombre NULLS LAST.
- `async replaceAtributos(templateId: number, atributos: TemplateAtributoDto[])` → Validar primero que el template existe (`findOne` o SELECT simple). Drizzle no tiene `upsert` con PK compuesta limpio → usar transacción `this.drizzle.db.transaction(async tx => { await tx.delete(templateAtributos).where(eq(templateAtributos.templateId, id)); if (atributos.length) await tx.insert(templateAtributos).values(atributos.map(a => ({...a, templateId: id}))) })`. Retornar la lista nueva via `findAtributos`.

Reutilizar el patrón de `handleUniqueViolation` del propiedades.service (copiarlo o extraerlo a un helper común; copiar es válido por ahora).

**D. Crear `apps/backend/src/modules/templates/templates.controller.ts`:**
- `@Controller('templates')` sin guards a nivel clase. Importar `RolesGuard`, `Roles`.
- `@Get()` `findAll(@Query('activo') activo?: string)` — replicar la lógica de parsing de `activo` del `propiedades.controller` (default `true`, `'all'` → undefined, `'true'/'false'` → boolean, otros → 400).
- `@Get(':id')` `findOne(@Param('id', ParseIntPipe) id: number)` — retorna template con atributos.
- `@UseGuards(RolesGuard) @Roles('admin') @Post() create(@Body() dto: CreateTemplateDto)` — 201.
- `@UseGuards(RolesGuard) @Roles('admin') @Patch(':id') update(@Param('id', ParseIntPipe) id, @Body() dto: UpdateTemplateDto)`.
- `@Get(':id/atributos') findAtributos(@Param('id', ParseIntPipe) id)`.
- `@UseGuards(RolesGuard) @Roles('admin') @Patch(':id/atributos') replaceAtributos(@Param('id', ParseIntPipe) id, @Body() dto: ReplaceTemplateAtributosDto)` — invoca `service.replaceAtributos(id, dto.atributos)`.

No declarar `JwtAuthGuard` — `CompositeAuthGuard` global en `app.module.ts` ya lo aplica.
  </action>
  <acceptance_criteria>
    - source: existen los 5 archivos (`templates.module.ts`, `templates.controller.ts`, `templates.service.ts`, `dto/create-template.dto.ts`, `dto/update-template.dto.ts`, `dto/template-atributos.dto.ts`).
    - source: `templates.module.ts` exporta `TemplatesService`.
    - source: `templates.controller.ts` define los 6 endpoints listados (3 GET + 1 POST + 2 PATCH).
    - source: `templates.service.ts` define `findAll`, `findOne`, `create`, `update`, `findAtributos`, `replaceAtributos` (grep `async (findAll|findOne|create|update|findAtributos|replaceAtributos)` retorna 6 matches).
    - source: `templates.controller.ts` aplica `@Roles('admin')` solo a `POST` y `PATCH`, no a `GET`.
    - source: `template-atributos.dto.ts` valida `customSlot` con `@Min(1) @Max(3)`.
    - cli: `pnpm --filter @objetiva/backend type-check` retorna exit 0.
    - cli: `pnpm --filter @objetiva/backend build` retorna exit 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/backend/src/modules/templates/templates.module.ts && test -f apps/backend/src/modules/templates/templates.controller.ts && test -f apps/backend/src/modules/templates/templates.service.ts && test -f apps/backend/src/modules/templates/dto/create-template.dto.ts && test -f apps/backend/src/modules/templates/dto/update-template.dto.ts && test -f apps/backend/src/modules/templates/dto/template-atributos.dto.ts && grep -q "export class TemplatesModule" apps/backend/src/modules/templates/templates.module.ts && grep -q "@Roles('admin')" apps/backend/src/modules/templates/templates.controller.ts && [ $(grep -cE "async (findAll|findOne|create|update|findAtributos|replaceAtributos)" apps/backend/src/modules/templates/templates.service.ts) -ge 6 ] && pnpm --filter @objetiva/backend type-check && pnpm --filter @objetiva/backend build</automated>
  </verify>
  <done>Módulo `templates` completo y compilando. Aún no registrado en AppModule (próximo task).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Registrar TemplatesModule en AppModule + smoke runtime</name>
  <read_first>
    - apps/backend/src/app.module.ts (líneas 1-51 — patrón de registro de módulo)
    - apps/backend/src/modules/templates/templates.module.ts (creado en Task 2)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-PATTERNS.md (sección "apps/backend/src/app.module.ts (MODIFY)")
  </read_first>
  <behavior>
    - Test smoke #1: arrancar backend → log indica `[TemplatesModule] dependencies initialized` (NestJS lifecycle).
    - Test smoke #2: `curl GET /templates` retorna 200 con array que incluye el template seedeado `nombre='default'`.
    - Test smoke #3: `curl GET /templates/1` retorna template con `atributos: [5 elementos]` y el primer atributo es `objeto` con `ordenNombre=1`.
    - Test smoke #4: `curl GET /templates/1/atributos` retorna 5 elementos.
    - Test smoke #5: `curl GET /templates/999` retorna 404 NotFound.
    - Test smoke #6: `curl POST /templates` sin auth admin retorna 401/403.
  </behavior>
  <action>
**A. Modificar `apps/backend/src/app.module.ts`:**
- Agregar import: `import { TemplatesModule } from './modules/templates/templates.module'` (después de los imports existentes de módulos, manteniendo orden alfabético si lo hay).
- Agregar `TemplatesModule` al array `imports` del decorator `@Module({})`, después de `PropiedadesModule` (mantener proximidad temática).

**B. Build + smoke runtime:**
- `pnpm --filter @objetiva/backend build` → exit 0.
- Arrancar backend (`pnpm --filter @objetiva/backend dev` o `docker compose up backend` según el entorno; ver memory `feedback_docker_compose.md`).
- Curl smokes (necesita JWT válido — usar el token de admin del entorno de dev):
  - `curl -s "http://localhost:3001/templates" -H "Authorization: Bearer $TOKEN"` → 200 con `[{"id":1,"nombre":"default",...}]`.
  - `curl -s "http://localhost:3001/templates/1" -H "Authorization: Bearer $TOKEN" | jq '.atributos | length'` → `5`.
  - `curl -s "http://localhost:3001/templates/1/atributos" -H "Authorization: Bearer $TOKEN" | jq '.[0].atributoTipo'` → `"objeto"` (primer atributo, ordenNombre=1).
  - `curl -s "http://localhost:3001/propiedades/familia" -H "Authorization: Bearer $TOKEN"` → 200 con `[]` (sin filas todavía).
  - `curl -s "http://localhost:3001/propiedades/aplicacion" -H "Authorization: Bearer $TOKEN"` → 200 con `[]`.
  - `curl -s "http://localhost:3001/templates/999" -H "Authorization: Bearer $TOKEN" -w "%{http_code}"` → `404`.

**C. Si el smoke falla**, lo más probable es algo de los siguientes:
- `_journal.json` no sync → drizzle queries del runtime fallan silenciosamente (lección 2026-05-15). Volver a verificar Plan 02 Task 3.
- Auth: token expirado → re-generar con Supabase login.
- Backend no arrancó (orden de imports incorrecto) → revisar log NestJS.
  </action>
  <acceptance_criteria>
    - source: `app.module.ts` contiene `import { TemplatesModule }` (grep retorna 1 línea).
    - source: `app.module.ts` contiene `TemplatesModule` en el array `imports` (grep retorna 2 lines: el import + el array entry).
    - cli: `pnpm --filter @objetiva/backend build` retorna exit 0.
    - behavior: arrancar backend → no crash; log NestJS muestra `TemplatesModule dependencies initialized`.
    - behavior: `curl GET /templates` → 200, array no vacío, primer item `nombre='default'`.
    - behavior: `curl GET /templates/1` → 200, response tiene `atributos: [5 items]`, `atributos[0].atributoTipo === 'objeto'`.
    - behavior: `curl GET /templates/1/atributos` → 200, length=5.
    - behavior: `curl GET /templates/999` → 404.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "TemplatesModule" apps/backend/src/app.module.ts | grep -E "^2$" && pnpm --filter @objetiva/backend build && echo "Smoke curl manual — operador valida con su token" && exit 0</automated>
  </verify>
  <done>TemplatesModule registrado, backend arranca, curl smokes OK. Wave 2 cerrada. Wave 3 (frontend) puede arrancar.

  **Nota operativa (autonomous:false):** El `<verify><automated>` corre grep + build pero termina con `exit 0` deliberado — el smoke runtime requiere humano. ANTES de marcar Task 3 done, el operador DEBE correr manualmente los curl smokes con un JWT viewer/admin válido (los 6 listados en `<behavior>`) y confirmar que `GET /templates` retorna el template default con sus 5 atributos. El automated grep+build pasa por archivos pero no valida runtime — esto es exactamente el patrón que falló en 2026-05-15 (drizzle schema drift silencioso). Sin este smoke manual, Wave 3 puede partir de un backend roto.</done>
</task>

</tasks>

<verification>
- Build OK + type-check OK del backend completo.
- 8 tipos en `PROP_TIPOS` (6 originales + familia + aplicacion).
- `TemplatesModule` registrado en AppModule.
- 6 endpoints templates accesibles (3 reads, 1 create, 2 patches).
- Smoke curl: `GET /templates` retorna el seed, `GET /propiedades/familia` y `GET /propiedades/aplicacion` retornan arrays.
- `POST /propiedades/familia` sin `parentId` retorna 400; con `parentId` válido retorna 201.
</verification>

<success_criteria>
- ✅ TPL-01: CRUD templates funciona (create, list, detail, update).
- ✅ TPL-02: `es_variante` persiste en `template_atributos` via PATCH `/templates/:id/atributos`.
- ✅ TPL-03: `orden_sku` persiste correctamente.
- ✅ TPL-04: `orden_nombre` persiste correctamente.
- ✅ TPL-05: `GET /templates` retorna el template default seedeado (id=1, nombre='default') con sus 5 atributos.
- ✅ Familia y aplicación expuestos vía `/propiedades/familia` y `/propiedades/aplicacion` con CRUD completo.
- ✅ Smoke curl 100% verde en local.
</success_criteria>

<output>
After completion, create `.planning/phases/30-templates-composici-n-sku-nombre/30-03-SUMMARY.md` documenting:
- Endpoints expuestos (lista completa con método y path)
- Sample responses de `GET /templates/1` (con atributos)
- Test commands curl usados para smoke (con outputs redactados)
- Cambios en `propiedades` (PROP_TIPOS, DTO, service create branch)
</output>
