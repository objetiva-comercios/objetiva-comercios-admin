---
phase: 30-templates-composici-n-sku-nombre
plan: 04
type: execute
wave: 3
depends_on: ["01", "02", "03"]
files_modified:
  - apps/web/src/types/propiedad.ts
  - apps/web/src/components/propiedades/propiedad-table.tsx
  - apps/web/src/components/propiedades/propiedad-create-dialog.tsx
  - apps/web/src/components/propiedades/propiedades-page.tsx
  - apps/web/src/lib/api.client.ts
  - apps/web/src/app/(dashboard)/templates/page.tsx
  - apps/web/src/app/(dashboard)/templates/[id]/page.tsx
autonomous: false
requirements:
  - TPL-01
  - TPL-02
  - TPL-03
  - TPL-04
tags:
  - frontend
  - nextjs
  - propiedades-tabs
  - templates-ui
  - human-verify
  - tabler

must_haves:
  truths:
    - "Admin navega a /propiedades y ve 8 tabs: Marcas, Colores, Talles, Materiales, Presentaciones, Objetos, Familias, Aplicaciones"
    - "Tab Aplicaciones funciona idéntica a las 6 originales: list + create + edit + toggle"
    - "Tab Familias muestra una columna extra 'Subcategoría' (nombre legible) además de las 4 estándar"
    - "El dialog de create de Familias incluye un select de subcategoría (envía parentId al backend)"
    - "Crear Familia sin subcategoría seleccionada falla con mensaje legible (validación cliente o error 400 del backend mostrado)"
    - "Admin navega a /templates y ve tabla con 1 row: el template default (identificado por nombre='default')"
    - "Admin click en row de /templates → entra a /templates/[id] con form simple (inputs número + checkbox) para editar atributos del template"
    - "Form de detalle/edit permite editar orden_sku, orden_nombre, es_variante, custom_slot para cada atributo y guarda via PATCH /templates/:id/atributos"
    - "Phase 30 NO ofrece UI para crear/eliminar templates — el botón 'Nuevo template' está ausente o disabled (el default basta); creación queda diferida a phase futura"
    - "Sin drag-drop builder visual (D-13) — form simple con inputs número y checkbox por atributo"
    - "Estética Tabler en componentes nuevos: border-radius md, altura h-9, text-sm (per shadcn-tabler-mcp)"
  artifacts:
    - path: "apps/web/src/components/propiedades/propiedad-table.tsx"
      provides: "Componente extendido con prop extraColumns? para customización"
      contains: "extraColumns"
    - path: "apps/web/src/components/propiedades/propiedad-create-dialog.tsx"
      provides: "Dialog extendido con prop extraFields? + buildExtraPayload? + validateExtra?"
      contains: "extraFields"
    - path: "apps/web/src/components/propiedades/propiedades-page.tsx"
      provides: "Página tabs cableada con 8 tipos + lógica especial para Familias (extraColumns + extraFields)"
      contains: "extraColumns"
    - path: "apps/web/src/types/propiedad.ts"
      provides: "PROP_TIPOS extendido a 8 tipos + tipo extendido Propiedad con subcategoriaId opcional"
      contains: "'familia', 'aplicacion'"
    - path: "apps/web/src/app/(dashboard)/templates/page.tsx"
      provides: "Página list de templates (tabla shadcn) — botón 'Nuevo template' ausente o disabled en Phase 30"
      contains: "templates"
    - path: "apps/web/src/app/(dashboard)/templates/[id]/page.tsx"
      provides: "Página detail/edit de template — form simple con inputs número + checkbox por atributo"
      contains: "PATCH"
  key_links:
    - from: "apps/web/src/components/propiedades/propiedades-page.tsx"
      to: "PROP_TIPOS"
      via: "iteración para renderizar tabs"
      pattern: "PROP_TIPOS"
    - from: "Familias tab → PropiedadCreateDialog"
      to: "/api/propiedades/familia"
      via: "POST con body {nombre, abrev, parentId}"
      pattern: "parentId"
    - from: "apps/web/src/app/(dashboard)/templates/[id]/page.tsx"
      to: "/api/templates/:id/atributos"
      via: "PATCH con array de atributos editados (replace full list)"
      pattern: "PATCH.*templates.*atributos"
    - from: "apps/web/src/app/(dashboard)/templates/page.tsx"
      to: "/api/templates"
      via: "GET list para tabla"
      pattern: "fetch.*templates"
---

<objective>
Cerrar Wave 3 (frontend) entregando:
1. Dos tabs nuevas en `/propiedades`: **Aplicaciones** (configuración estándar) y **Familias** (con `extraColumns` + `extraFields` para subcategoría — PITFALL-9).
2. Página `/templates` simple (list + detail/edit) que cumple TPL-01 desde la UI sin Builder visual (honra D-13). En Phase 30 la tabla solo muestra 1 row (el default) y la edición permite ajustar `orden_sku`/`orden_nombre`/`es_variante`/`custom_slot` por atributo.

Purpose: La promesa de Phase 30 incluye que el admin pueda (a) gestionar `familia` y `aplicacion` desde el mismo panel `/propiedades` y (b) ver/editar el template default sin esperar a Phase 32. Este plan cumple TPL-01..TPL-04 desde la UI. NO incluye cableado al `ArticuloForm` (eso es Phase 32 per D-19 + Deferred del CONTEXT) ni Builder drag-drop (Deferred).

Output:
- `propiedad.ts` extendido (PROP_TIPOS=8, PROP_LABELS, PROP_NOMBRE_PLACEHOLDERS, `Propiedad` con `subcategoriaId?`).
- `propiedad-table.tsx` con `extraColumns?` prop (backward-compat).
- `propiedad-create-dialog.tsx` con `extraFields?` + `buildExtraPayload?` + `validateExtra?` props.
- `propiedades-page.tsx` cableado con 8 tabs + lógica especial Familias.
- `templates/page.tsx` (list) + `templates/[id]/page.tsx` (detail/edit con form simple).
- `api.client.ts` extendido con helpers `fetchTemplates`, `fetchTemplate(id)`, `patchTemplateAtributos(id, atributos)`.
- Checkpoint humano final para validar visualmente (Playwright o navegación manual).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-templates-composici-n-sku-nombre/30-CONTEXT.md
@.planning/phases/30-templates-composici-n-sku-nombre/30-RESEARCH.md
@.planning/phases/30-templates-composici-n-sku-nombre/30-PATTERNS.md
@apps/web/src/types/propiedad.ts
@apps/web/src/components/propiedades/propiedad-table.tsx
@apps/web/src/components/propiedades/propiedad-create-dialog.tsx
@apps/web/src/components/propiedades/propiedades-page.tsx
@apps/web/src/app/(dashboard)/propiedades/page.tsx

<interfaces>
<!-- Las extensiones de tipos que este plan crea -->

```typescript
// apps/web/src/types/propiedad.ts (extendido):
export const PROP_TIPOS = [
  'marca', 'color', 'talle', 'material', 'presentacion', 'objeto',
  'familia', 'aplicacion',
] as const

export interface Propiedad {
  id: number
  nombre: string
  abrev: string
  activo: boolean
  createdAt: string
  updatedAt: string
  // Phase 30: solo presente en filas de tipo 'familia' — el backend lo devuelve.
  subcategoriaId?: number
}

// apps/web/src/components/propiedades/propiedad-table.tsx:
import type { ReactNode } from 'react'

export interface ExtraColumn {
  header: string
  cell: (row: Propiedad) => ReactNode
  className?: string
}

export interface PropiedadTableProps {
  propTipo: PropTipo
  extraColumns?: ExtraColumn[]   // se renderizan ANTES de "Estado"
}

// apps/web/src/components/propiedades/propiedad-create-dialog.tsx:
export interface PropiedadCreateDialogProps {
  propTipo: PropTipo
  onCreated?: (created: Propiedad) => void
  trigger?: ReactNode
  open?: boolean
  onOpenChange?: (open: boolean) => void
  extraFields?: ReactNode              // se renderiza entre 'abrev' y DialogFooter
  buildExtraPayload?: () => Record<string, unknown>   // contribuye al body POST
  validateExtra?: () => string | null  // retorna error message si extra es inválido; null = OK
}

// apps/web/src/lib/api.client.ts (helpers nuevos):
export async function fetchTemplates(): Promise<ArticulosTemplate[]>
export async function fetchTemplate(id: number): Promise<ArticulosTemplate & { atributos: TemplateAtributo[] }>
export async function patchTemplateAtributos(id: number, atributos: TemplateAtributo[]): Promise<TemplateAtributo[]>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender tipos web + extender PropiedadTable con extraColumns</name>
  <read_first>
    - apps/web/src/types/propiedad.ts (líneas completas — PROP_TIPOS, Propiedad interface, PROP_LABELS, copyFor, PROP_NOMBRE_PLACEHOLDERS)
    - apps/web/src/components/propiedades/propiedad-table.tsx (líneas 31-269 — props, render header, render body)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-PATTERNS.md (sección "apps/web/src/types/propiedad.ts (MODIFY)" + "propiedad-table.tsx (MODIFY)")
    - .planning/phases/30-templates-composici-n-sku-nombre/30-RESEARCH.md (sección 10 "Frontend — PropiedadesPage" + PITFALL-9)
  </read_first>
  <behavior>
    - Test #1 (smoke web): `cd apps/web && pnpm type-check` retorna exit 0 con los nuevos tipos.
    - Test #2 (smoke UI): navegar a `/propiedades` muestra 8 tabs (las 6 originales siguen funcionando + Familias + Aplicaciones).
    - Test #3 (backward compat): los 6 tabs originales no pasan `extraColumns` → render idéntico al anterior (sin columna extra en el header/body).
    - Test #4: `PropiedadTable` con `extraColumns={[{header:'Test', cell:row => row.id}]}` renderiza una nueva columna entre las existentes y "Estado".
  </behavior>
  <action>
**A. Extender `apps/web/src/types/propiedad.ts`:**
- `PROP_TIPOS`: agregar `'familia', 'aplicacion'` al final del array (`as const`).
- `Propiedad` interface: agregar campo opcional `subcategoriaId?: number` (solo presente cuando el backend lo retorna en filas de `familia`).
- `PROP_LABELS`: agregar entries
  - `familia: { singular: 'Familia', plural: 'Familias', gender: 'f' }`
  - `aplicacion: { singular: 'Aplicación', plural: 'Aplicaciones', gender: 'f' }`
- `PROP_NOMBRE_PLACEHOLDERS`: agregar entries
  - `familia: 'Ej: Amortiguadores delanteros'`
  - `aplicacion: 'Ej: Fiat Cronos 1.3 2020-2024'`
- NO tocar `copyFor` — ya es genérica.

**B. Extender `apps/web/src/components/propiedades/propiedad-table.tsx`:**
- Importar `ReactNode` desde `'react'`.
- Definir y exportar `interface ExtraColumn { header: string; cell: (row: Propiedad) => ReactNode; className?: string }`.
- Cambiar `PropiedadTableProps` a `{ propTipo: PropTipo; extraColumns?: ExtraColumn[] }`.
- En el `<TableHeader>`: después de las `<TableHead>` existentes para "ID", "Nombre", "Abrev" e INMEDIATAMENTE ANTES de la `<TableHead>` "Estado", iterar `extraColumns?.map((col, idx) => <TableHead key={idx} className={col.className}>{col.header}</TableHead>)`.
- En el `<TableBody>`: dentro del `propiedades.map(p => <TableRow>...`, después de las `<TableCell>` de ID/Nombre/Abrev y ANTES de la "Estado", iterar `extraColumns?.map((col, idx) => <TableCell key={idx} className={col.className}>{col.cell(p)}</TableCell>)`.
- Reglas de orden importantes: las extra columns SIEMPRE van entre "Abrev" y "Estado" (no al final donde están las acciones).
- **Estética Tabler (W4 / shadcn-tabler-mcp):** mantener border-radius `md` (no `lg`/`xl`), altura `h-9` (no `h-10`), text-sm en cells, padding consistente con tabs originales. Si dudas, invocar `shadcn-tabler-mcp` `transform-component` para verificar antes de commitear.
- Los 6 tabs existentes no pasan `extraColumns` → render idéntico al anterior. Verificar con `pnpm test` del web app si hay tests (probable hay un test de PropiedadCreateDialog; el de PropiedadTable puede no existir todavía).
  </action>
  <acceptance_criteria>
    - source: `PROP_TIPOS` en `propiedad.ts` contiene 8 elementos (grep `grep -oE "'(marca|color|talle|material|presentacion|objeto|familia|aplicacion)'" apps/web/src/types/propiedad.ts | sort -u | wc -l` retorna `8`).
    - source: `Propiedad` interface contiene `subcategoriaId?: number`.
    - source: `PROP_LABELS` tiene 8 entries (grep retorna 8 líneas).
    - source: `propiedad-table.tsx` exporta `interface ExtraColumn`.
    - source: `PropiedadTableProps` tiene `extraColumns?: ExtraColumn[]`.
    - source: en el `TableHeader` y `TableBody` del componente hay un map sobre `extraColumns`.
    - cli: `pnpm --filter @objetiva/web type-check` retorna exit 0.
    - cli: `pnpm --filter @objetiva/web build` retorna exit 0.
    - cli: `cd apps/web && pnpm test -- --run` (tests existentes — `abrev.test.ts`, `composer.test.ts` de Plan 01, `propiedad-create-dialog.test.tsx`) retornan todos verdes.
  </acceptance_criteria>
  <verify>
    <automated>[ $(grep -oE "'(marca|color|talle|material|presentacion|objeto|familia|aplicacion)'" apps/web/src/types/propiedad.ts | sort -u | wc -l) -eq 8 ] && grep -q "subcategoriaId" apps/web/src/types/propiedad.ts && grep -q "extraColumns" apps/web/src/components/propiedades/propiedad-table.tsx && grep -q "export interface ExtraColumn" apps/web/src/components/propiedades/propiedad-table.tsx && pnpm --filter @objetiva/web type-check && pnpm --filter @objetiva/web build && cd apps/web && pnpm test -- --run 2>&1 | grep -E "Test Files\s+[0-9]+ passed"</automated>
  </verify>
  <done>Tipos extendidos, PROP_TIPOS=8, PropiedadTable acepta `extraColumns?`. Backward compat verificada con type-check + tests existentes. Estética Tabler respetada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extender PropiedadCreateDialog con extraFields + buildExtraPayload</name>
  <read_first>
    - apps/web/src/components/propiedades/propiedad-create-dialog.tsx (líneas 1-193 — props, form, submit handler)
    - apps/web/src/components/propiedades/propiedad-create-dialog.test.tsx (test existente — para no romperlo)
    - apps/web/src/lib/api.client.ts (firma del POST a `/propiedades/:tipo`)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-PATTERNS.md (sección "propiedad-create-dialog.tsx (MODIFY)")
  </read_first>
  <behavior>
    - Test #1: Dialog sin `extraFields` (los 6 originales) sigue funcionando 100% igual — el test existente `propiedad-create-dialog.test.tsx` pasa sin modificación.
    - Test #2 (manual smoke): Dialog con `extraFields={<SelectSubcategoria ...>}` muestra el select entre "abrev" y los botones del footer.
    - Test #3: Submit del Dialog: si `buildExtraPayload` está definido, el body POST incluye los keys/values que retorna además de `{nombre, abrev}`. Si `validateExtra` retorna un string no-null, NO se hace submit y se muestra el error.
  </behavior>
  <action>
**A. Extender `PropiedadCreateDialogProps`:**
```typescript
extraFields?: ReactNode
buildExtraPayload?: () => Record<string, unknown>
validateExtra?: () => string | null
```

**B. Render del extraFields:**
En el JSX del Dialog, ubicar la sección donde se renderizan los FormField de `nombre` y `abrev`. Inmediatamente DESPUÉS del FormField de `abrev` y ANTES del `DialogFooter` (botones), insertar:
```tsx
{extraFields ?? null}
```

**C. Submit handler:**
En el `onSubmit` actual (donde se llama al backend con `{nombre, abrev}`), modificar para:
- Antes del fetch: si `validateExtra` está definido, llamar y si retorna no-null → setear el error en un Form-level error / toast y `return` (no submit).
- Construir el body: `const extra = buildExtraPayload?.() ?? {}; const body = { nombre, abrev, ...extra }`.
- Llamar al fetch con `body` (en lugar de `{nombre, abrev}`).

**D. Estética Tabler (W4 / shadcn-tabler-mcp):** los inputs de los extraFields que se inyecten desde el componente padre deben respetar `h-9`, border-radius `md`, text-sm, bg explícito en controles de formulario. Documentar esta restricción en el JSDoc del prop `extraFields` (comentar arriba del tipo).

**E. NO romper el test existente:**
Correr `cd apps/web && pnpm test propiedad-create-dialog -- --run` después de los cambios. Si falla, revisar la lógica de submit — el test asume que el body es `{nombre, abrev}` cuando `extraFields` no se pasa, lo cual debe seguir siendo cierto (spread de `{}` no cambia nada).

**F. Type check & build:**
- `pnpm --filter @objetiva/web type-check`.
- `pnpm --filter @objetiva/web build`.
  </action>
  <acceptance_criteria>
    - source: `PropiedadCreateDialogProps` contiene `extraFields?: ReactNode`, `buildExtraPayload?: () => Record<string, unknown>`, `validateExtra?: () => string | null`.
    - source: render del componente contiene `{extraFields ?? null}` o equivalente entre FormField de abrev y DialogFooter.
    - source: handler de submit usa `buildExtraPayload` para extender el body (grep busca `buildExtraPayload?.()` o similar).
    - source: handler de submit llama `validateExtra` antes del fetch.
    - cli: `pnpm --filter @objetiva/web type-check` exit 0.
    - cli: `pnpm --filter @objetiva/web build` exit 0.
    - test: `cd apps/web && pnpm test propiedad-create-dialog -- --run` retorna exit 0 (test existente sigue verde — backward compat).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "extraFields" apps/web/src/components/propiedades/propiedad-create-dialog.tsx && grep -q "buildExtraPayload" apps/web/src/components/propiedades/propiedad-create-dialog.tsx && grep -q "validateExtra" apps/web/src/components/propiedades/propiedad-create-dialog.tsx && pnpm --filter @objetiva/web type-check && pnpm --filter @objetiva/web build && cd apps/web && pnpm test propiedad-create-dialog -- --run 2>&1 | grep -E "Test Files\s+[0-9]+ passed"</automated>
  </verify>
  <done>Dialog extendido con slot para campos extra. Tests existentes verdes (no rompió las 6 tabs originales). Estética Tabler documentada en JSDoc del prop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire propiedades-page.tsx (8 tabs + lógica Familias) + páginas /templates (list + detail/edit)</name>
  <read_first>
    - apps/web/src/components/propiedades/propiedades-page.tsx (líneas 1-39 — itera PROP_TIPOS para generar 6 tabs; ahora 8)
    - apps/web/src/app/(dashboard)/propiedades/page.tsx (estructura de página dashboard — pattern simple `<div><h2/><Component/></div>`)
    - apps/web/src/types/propiedad.ts (PROP_TIPOS extendido en Task 1)
    - apps/web/src/components/propiedades/propiedad-table.tsx (extraColumns implementado en Task 1)
    - apps/web/src/components/propiedades/propiedad-create-dialog.tsx (extraFields implementado en Task 2)
    - apps/web/src/lib/api.client.ts (firma de fetchPropiedades — para reutilizar y agregar helpers de templates)
    - apps/backend/src/modules/templates/templates.controller.ts (del Plan 03 — endpoints disponibles)
    - .planning/phases/30-templates-composici-n-sku-nombre/30-CONTEXT.md (D-13: NO Builder visual)
  </read_first>
  <behavior>
    - Test #1 (smoke web): `cd apps/web && pnpm type-check` exit 0.
    - Test #2 (smoke web): `cd apps/web && pnpm build` exit 0.
    - Test #3 (smoke runtime): navegar a `/propiedades` → 8 tabs visibles. Click Aplicaciones → tabla vacía con botón "Nueva aplicación". Click Familias → tabla con columna extra "Subcategoría", dialog con select de subcategoría obligatorio.
    - Test #4 (smoke runtime): navegar a `/templates` → tabla con 1 row (`default`). Botón "Nuevo template" ausente o disabled (Phase 30: el default basta).
    - Test #5 (smoke runtime): click en row `/templates` → entra a `/templates/[id]` con form simple: por cada atributo del template, 4 inputs/controles (`orden_sku` número, `orden_nombre` número, `es_variante` checkbox, `custom_slot` número 1-3 nullable). Guardar → PATCH `/templates/:id/atributos` retorna 200 con la lista nueva.
  </behavior>
  <action>
**A. Modificar `apps/web/src/components/propiedades/propiedades-page.tsx`:**
- El componente ya itera `PROP_TIPOS` dinámicamente. Con el array extendido a 8 en Task 1, automáticamente renderiza 2 tabs más (Familias + Aplicaciones).
- **Lógica especial para Familias** (PITFALL-9): para `tipo === 'familia'` no se monta `<PropiedadTable propTipo={tipo} />` directo — se monta una variante que:
  - Carga subcategorías activas: `useEffect(() => { fetchPropiedades('subcategoria', { activo: true }).then(setSubcategorias) }, [])` (per Decisión Q3 RESOLVED: solo activas).
  - Construye `subcategoriaLookup: Record<number, string>` a partir del array.
  - Mantiene state `parentId: number | null` para el dialog.
  - Monta `<PropiedadTable propTipo="familia" extraColumns={[{header:'Subcategoría', cell: row => subcategoriaLookup[row.subcategoriaId ?? -1] ?? '—'}]} />`.
  - El `PropiedadCreateDialog` interno de la tabla recibe (vía prop nuevo `createDialogExtras` en `PropiedadTable` que reenvía al dialog, O renderizando el dialog fuera de la tabla y componiendo en este componente — executor elige la opción menos invasiva): `extraFields={<SelectSubcategoria value={parentId} onChange={setParentId} options={subcategorias} />}` + `buildExtraPayload={() => ({ parentId })}` + `validateExtra={() => parentId == null ? 'Seleccioná una subcategoría' : null}`.
- Componente `SelectSubcategoria` (interno o nuevo archivo): wraps shadcn/ui `Select` con `h-9`, border-radius `md`, bg explícito (estética Tabler per W4 / shadcn-tabler-mcp).
- Para los 6 tipos originales + `aplicacion`: render igual al actual (`<PropiedadTable propTipo={tipo} />` sin extras).

**B. Extender `apps/web/src/lib/api.client.ts` con helpers de templates:**
- `fetchTemplates(): Promise<ArticulosTemplate[]>` → `GET /templates`.
- `fetchTemplate(id: number): Promise<ArticulosTemplate & { atributos: TemplateAtributo[] }>` → `GET /templates/:id`.
- `patchTemplateAtributos(id: number, atributos: TemplateAtributo[]): Promise<TemplateAtributo[]>` → `PATCH /templates/:id/atributos` con body `{ atributos }`.
- Tipos `ArticulosTemplate` y `TemplateAtributo` se importan desde `@objetiva/types` (Plan 01 los exporta).
- Si helper `fetchSubcategorias` no existe, NO agregar — usar `fetchPropiedades('subcategoria', { activo: true })` genérico.

**C. Crear `apps/web/src/app/(dashboard)/templates/page.tsx` (list view):**
- Server-side: `'use client'` page con state local.
- Estructura: header `<h2>Templates</h2>` + descripción + tabla shadcn.
- Carga: `useEffect` con `fetchTemplates()` → state `templates`.
- Tabla shadcn con columnas: ID, Nombre, Descripción, Activo (badge), Acciones (Link a `/templates/:id` con button "Editar").
- Botón "Nuevo template" en header de tabla → **disabled** con tooltip `"Disponible en próxima fase — Phase 30 entrega solo el template default."` (per must_haves: ausente o disabled). Documentar la decisión en el JSDoc del componente.
- Estética Tabler: `h-9` en buttons, `text-sm` en cells, border-radius `md` en card y tabla, padding `py-4` (no `py-6`). Si dudas, invocar `shadcn-tabler-mcp` `transform-component` antes de commitear.

**D. Crear `apps/web/src/app/(dashboard)/templates/[id]/page.tsx` (detail/edit view):**
- `'use client'` page con `useParams()` para leer `id`.
- Carga: `useEffect` con `fetchTemplate(Number(id))` → state `template` (incluye `atributos`).
- Header: `<h2>Template: {template.nombre}</h2>` + descripción + link "← Volver a /templates".
- Form simple (NO drag-drop visual — D-13): tabla con una fila por atributo de `template.atributos`. Columnas:
  - `atributo_tipo` (read-only, texto).
  - `orden_nombre` → `<Input type="number" min="1" />` (nullable: empty string = null).
  - `orden_sku` → `<Input type="number" min="1" />` (nullable: empty string = null).
  - `es_variante` → `<Switch>` (shadcn/ui).
  - `custom_slot` → `<Input type="number" min="1" max="3" />` (nullable).
- Estado local con copia editable del array `atributos`. Cada cambio actualiza el local state, no dispara fetch.
- Botón "Guardar cambios" en footer → llama `patchTemplateAtributos(id, atributosEditados)` (replace full list per endpoint contract del Plan 03 Task 2). Toast de éxito/error.
- Validación cliente mínima: `custom_slot` debe ser 1, 2, 3 o vacío; números deben ser ≥ 1 cuando no son null.
- NO permite agregar/eliminar filas de atributos en Phase 30 (solo edita los 5 existentes del default); botón "Agregar atributo" ausente o disabled con tooltip. Documentar.
- Estética Tabler: inputs `h-9`, table cells `text-sm`, padding consistente, border-radius `md` en card y controles, bg explícito en inputs (per shadcn-tabler-mcp).

**E. Type check & build:**
- `pnpm --filter @objetiva/web type-check`.
- `pnpm --filter @objetiva/web build`.
- `cd apps/web && pnpm test -- --run` (tests existentes siguen verdes).

**F. Notas operativas:**
- Per CLAUDE.md memory `feedback_docker_compose.md`: backend disponible vía `docker compose up backend` o `pnpm --filter @objetiva/backend dev` según entorno. Web en `http://localhost:3000` o `erp.sanchezrepuestos.com.ar`.
- Backend del Plan 03 debe estar corriendo para que las páginas `/templates` funcionen al hacer smoke runtime en Task 4.
  </action>
  <acceptance_criteria>
    - source: `apps/web/src/components/propiedades/propiedades-page.tsx` referencia `extraColumns` y `subcategoria` (lógica especial Familias).
    - source: `apps/web/src/lib/api.client.ts` exporta `fetchTemplates`, `fetchTemplate`, `patchTemplateAtributos`.
    - source: `apps/web/src/app/(dashboard)/templates/page.tsx` existe con tabla y botón "Nuevo template" disabled o ausente.
    - source: `apps/web/src/app/(dashboard)/templates/[id]/page.tsx` existe con form de edición de atributos (inputs número + switch).
    - source: NO se introducen librerías de drag-drop (`react-beautiful-dnd`, `@dnd-kit/*`, etc.) — D-13 honrado. `grep -r "dnd" apps/web/src/app/\(dashboard\)/templates/` retorna vacío.
    - cli: `pnpm --filter @objetiva/web type-check` retorna exit 0.
    - cli: `pnpm --filter @objetiva/web build` retorna exit 0.
    - cli: `cd apps/web && pnpm test -- --run` retorna exit 0 (tests existentes siguen verdes).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "extraColumns" apps/web/src/components/propiedades/propiedades-page.tsx && grep -q "fetchTemplates" apps/web/src/lib/api.client.ts && grep -q "patchTemplateAtributos" apps/web/src/lib/api.client.ts && test -f apps/web/src/app/\(dashboard\)/templates/page.tsx && test -f apps/web/src/app/\(dashboard\)/templates/\[id\]/page.tsx && ! grep -rq "react-beautiful-dnd\|@dnd-kit" apps/web/src/app/\(dashboard\)/templates/ && pnpm --filter @objetiva/web type-check && pnpm --filter @objetiva/web build && cd apps/web && pnpm test -- --run 2>&1 | grep -E "Test Files\s+[0-9]+ passed"</automated>
  </verify>
  <done>Wiring completo: propiedades-page con 8 tabs + lógica Familias; /templates list + /templates/[id] edit funcionando. Backend del Plan 03 consumido vía api.client. Sin Builder drag-drop. Estética Tabler aplicada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [CHECKPOINT] Validar visualmente las 2 tabs nuevas en /propiedades + página /templates</name>
  <what-built>Frontend extendido:
- 8 tabs en `/propiedades` (Marcas, Colores, Talles, Materiales, Presentaciones, Objetos, **Familias**, **Aplicaciones**).
- Página `/templates` (list view con 1 row: el default; botón "Nuevo template" ausente/disabled per Phase 30 scope).
- Página `/templates/[id]` (detail/edit view con form simple — inputs número + switch — para editar `orden_sku`, `orden_nombre`, `es_variante`, `custom_slot` por atributo).

Per project memory `feedback_playwright_cli.md`: usar **playwright-cli skill** (NO MCP) para el smoke visual reproducible si el operador lo prefiere; navegación manual también es válida.

Pre-condición: Task 3 (auto) completó wiring de `propiedades-page.tsx` + creó las 2 páginas `/templates`. Backend del Plan 03 debe estar corriendo.</what-built>
  <read_first>
    - apps/web/src/components/propiedades/propiedades-page.tsx (8 tabs cableados en Task 3)
    - apps/web/src/app/(dashboard)/templates/page.tsx (list view creado en Task 3)
    - apps/web/src/app/(dashboard)/templates/[id]/page.tsx (detail/edit view creado en Task 3)
    - apps/web/src/types/propiedad.ts (PROP_TIPOS extendido en Task 1)
  </read_first>
  <how-to-verify>
**Preparación (asume backend del Plan 03 corriendo + frontend dev server):**
```bash
# Backend (si docker compose) o pnpm dev
docker compose up backend     # o pnpm --filter @objetiva/backend dev
# Frontend
pnpm --filter @objetiva/web dev   # arranca en http://localhost:3000
```

Si docker está configurado per CLAUDE.md memory, usar `docker compose up` y la app queda en `erp.sanchezrepuestos.com.ar` (Traefik). El operador valida en el host correcto.

**Paso 1: Validación visual `/propiedades` (humano + Playwright CLI opcional)**

1. Navegar a `/propiedades` → confirmar visualmente que aparecen 8 tabs.
2. Clickear tab **Aplicaciones** → la tabla aparece vacía con botón "Nueva aplicación". Click → dialog abre con campos Nombre+Abrev. Submit con `{nombre:'Fiat Cronos 1.3 2020-2024', abrev:'FCRON13'}` → toast de éxito, fila aparece en tabla.
3. Toggle activo/inactivo de la fila → estado cambia.
4. Click "Editar" → dialog de edición funciona.
5. Clickear tab **Familias** → la tabla aparece con columnas: ID, Nombre, Abrev, **Subcategoría**, Estado, (acciones). Click "Nueva familia" → dialog con Nombre + Abrev + **Select de subcategoría** (entre abrev y los botones; solo subcategorías activas per Q3 RESOLVED). Si el select está vacío y se intenta submit → error legible "Seleccioná una subcategoría". Seleccionar una subcategoría existente + nombre + abrev → submit → fila aparece con la subcategoría correcta en la nueva columna.
6. Intentar crear duplicado `(parentId, nombre)` → toast de error "ya existe una familia con el nombre..." (409 del backend mapeado).
7. Validar que las 6 tabs originales siguen funcionando idénticas (no se rompió Marcas/Colores/etc.).

**Paso 2: Validación visual `/templates` y `/templates/[id]`**

1. Navegar a `/templates` → tabla muestra 1 row con `nombre='default'`. Botón "Nuevo template" disabled (o ausente) con tooltip explicando que se difiere a phase futura.
2. Click en row (o botón "Editar") → navegación a `/templates/1` (asumiendo id=1 del seed).
3. Página detail muestra:
   - Header con nombre del template + link "← Volver".
   - Tabla/form con 5 filas (atributos `objeto`, `marca`, `modelo`, `medida`, `custom_1`).
   - Cada fila tiene 4 controles editables: `orden_nombre` (número), `orden_sku` (número), `es_variante` (switch), `custom_slot` (número 1-3).
4. Modificar algún valor (ej cambiar `orden_nombre` de `marca` de 2 a 3) → click "Guardar" → toast de éxito.
5. Recargar página → cambios persistieron (validar contra backend con curl `GET /templates/1/atributos`).
6. Sin elementos de drag-drop visibles (D-13 honrado).

**Paso 3: Tabler aesthetic check**

Validar que en las 3 vistas nuevas:
- Buttons tienen `h-9` (no `h-10`).
- Inputs tienen `h-9`, bg explícito, border-radius `md`.
- Tablas usan `text-sm` en cells.
- Padding `py-4` / `gap-4` (no `py-6`/`gap-6`).
- Sin radius `xl` o `lg` heredados de shadcn defaults.

Si dudas, invocar `shadcn-tabler-mcp` `query-aesthetic` con el componente en cuestión.

**Paso 4: Screenshots (recomendado, no obligatorio)**

Capturar screenshots con playwright-cli skill o navegación manual para archivar en `.planning/phases/30-templates-composici-n-sku-nombre/screenshots/`:
- `tabs-overview.png` — vista de las 8 tabs
- `aplicaciones-list.png` — tab Aplicaciones con al menos 1 fila
- `familias-list.png` — tab Familias con columna Subcategoría visible
- `familias-create-dialog.png` — dialog de Familias con el select de subcategoría
- `templates-list.png` — tabla con el row `default` + botón "Nuevo template" disabled
- `templates-edit.png` — form de edición de atributos con inputs número + switch
  </how-to-verify>
  <resume-signal>Operador responde:
- "OK visual: las 8 tabs renderizan, Aplicaciones CRUD funciona, Familias muestra subcategoría y el dialog incluye el select; duplicados retornan 409 legible. /templates list muestra default, /templates/[id] edita atributos y PATCH persiste. Tabler aesthetic OK." → Plan 04 cerrado, Phase 30 puede declararse done.
- O lista de issues encontrados → executor itera fixes y vuelve a este checkpoint.

Si el operador NO tiene tiempo para validar manualmente, opciones aceptables:
1. Aprobar basado solo en `pnpm type-check + pnpm build + pnpm test verde` + revisión de PR / diff.
2. Diferir el checkpoint a `/gsd:verify-work 30` posterior.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @objetiva/web type-check` exit 0.
- `pnpm --filter @objetiva/web build` exit 0.
- `cd apps/web && pnpm test -- --run` todos los tests verdes (composer + abrev + propiedad-create-dialog).
- Visual smoke (humano): 8 tabs en `/propiedades`, las 2 nuevas funcionan idénticas a las 6 originales, con la columna extra en Familias.
- Visual smoke (humano): `/templates` list muestra el default; `/templates/[id]` edita atributos via PATCH y persiste.
- Sin librerías drag-drop introducidas (D-13 honrado).
</verification>

<success_criteria>
- ✅ TPL-01 cerrado desde UI: admin puede ver/editar templates desde `/templates` (este Plan 04) y los catálogos `prop_familia` + `prop_aplicacion` desde `/propiedades`.
- ✅ TPL-02 cerrado desde UI: admin marca `es_variante` por atributo via switch en `/templates/[id]`.
- ✅ TPL-03 cerrado desde UI: admin edita `orden_sku` por atributo en `/templates/[id]`.
- ✅ TPL-04 cerrado desde UI: admin edita `orden_nombre` por atributo en `/templates/[id]`.
- ✅ PropiedadTable extendido con `extraColumns?` (backward-compat, 6 tabs originales intactos).
- ✅ PropiedadCreateDialog extendido con `extraFields?` + `buildExtraPayload?` + `validateExtra?`.
- ✅ Wiring de `propiedades-page.tsx` separado del checkpoint visual (Task 3 auto, Task 4 checkpoint).
- ✅ depends_on incluye `["01", "02", "03"]` (composer del 01 + schema del 02 + endpoints del 03).
- ✅ Smoke visual humano OK (o aprobación por diff si el operador delega).
- ✅ Wave 3 cerrada. Phase 30 lista para `/gsd:verify-work 30`.
</success_criteria>

<output>
After completion, create `.planning/phases/30-templates-composici-n-sku-nombre/30-04-SUMMARY.md` documenting:
- Cambios en `propiedad.ts` (lista de exports nuevos/modificados)
- Decisión de implementación: cómo se inyectan `extraFields`/`extraColumns` al PropiedadTable (composición externa vs prop nuevo)
- Estructura de las páginas `/templates` y `/templates/[id]` (componentes usados, librerías evitadas — D-13)
- Lista de screenshots archivados (si aplica)
- Output del checkpoint humano (issues reportados + resolución)
- Cualquier follow-up identificado (delegado a un todo de `/gsd:add-todo`)
</output>
