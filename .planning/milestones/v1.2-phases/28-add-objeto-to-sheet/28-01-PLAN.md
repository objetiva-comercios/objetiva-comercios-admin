---
phase: 28-add-objeto-to-sheet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/articulos/articulo-sheet.tsx
autonomous: true
requirements:
  - VIEW-02
gap_closure: true

must_haves:
  truths:
    - 'El campo objeto aparece en el panel lateral de detalle (ArticuloSheet) dentro de la seccion Propiedades'
    - "El campo muestra label 'Tipo / Objeto' con el valor del articulo"
    - "El campo respeta isCampoVisible('objeto') — desaparece si el usuario lo oculta en configuracion"
  artifacts:
    - path: 'apps/web/src/components/articulos/articulo-sheet.tsx'
      provides: 'FieldRow de objeto en grid Propiedades'
      contains: "isCampoVisible('objeto')"
  key_links:
    - from: 'apps/web/src/components/articulos/articulo-sheet.tsx'
      to: 'apps/web/src/hooks/use-articulos-config.ts'
      via: "isCampoVisible('objeto') call"
      pattern: "isCampoVisible\\('objeto'\\)"
---

<objective>
Agregar el campo `objeto` al panel lateral de detalle ArticuloSheet en la seccion Propiedades.

Purpose: Cerrar gap INT-02 del audit v1.2 — el campo `objeto` existe en todo el stack (DB, tipo, config, formulario) pero falta en la vista de detalle lateral.
Output: ArticuloSheet muestra el campo objeto como primer campo del grid de Propiedades, con label "Tipo / Objeto".
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-add-objeto-to-sheet/28-RESEARCH.md

@apps/web/src/components/articulos/articulo-sheet.tsx
</context>

<interfaces>
<!-- FieldRow ya definido en articulo-sheet.tsx (linea ~39) -->
```tsx
function FieldRow({ label, value }: { label: string; value: string | null | undefined })
```

<!-- isCampoVisible del hook existente -->

```tsx
// from apps/web/src/hooks/use-articulos-config.ts
isCampoVisible(campo: string): boolean
```

<!-- Tipo Articulo ya tiene objeto -->

```tsx
// from apps/web/src/types/articulo.ts
objeto: string | null
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Insertar FieldRow de objeto en ArticuloSheet Propiedades</name>
  <files>apps/web/src/components/articulos/articulo-sheet.tsx</files>
  <action>
    Insertar una linea en el grid de Propiedades de ArticuloSheet, como PRIMER campo dentro del `<div className="mt-2 grid grid-cols-2 gap-x-6 gap-y-1">`, ANTES del FieldRow de `marca`:

    ```tsx
    {isCampoVisible('objeto') && <FieldRow label="Tipo / Objeto" value={articulo.objeto} />}
    ```

    Formato inline (una sola linea), mismo estilo que marca, modelo, talle, color. No usar parentesis multi-linea.

    El bloque resultante debe quedar:
    ```tsx
    <div className="mt-2 grid grid-cols-2 gap-x-6 gap-y-1">
      {isCampoVisible('objeto') && <FieldRow label="Tipo / Objeto" value={articulo.objeto} />}
      {isCampoVisible('marca') && <FieldRow label="Marca" value={articulo.marca} />}
      {isCampoVisible('modelo') && <FieldRow label="Modelo" value={articulo.modelo} />}
      ...
    </div>
    ```

  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter web build 2>&1 | tail -5</automated>
  </verify>
  <done>Build de Next.js compila sin errores. El grid de Propiedades en ArticuloSheet tiene objeto como primer campo con label "Tipo / Objeto" y condicion isCampoVisible('objeto').</done>
</task>

</tasks>

<verification>
1. `pnpm --filter web build` compila sin errores TypeScript
2. Grep confirma presencia: `grep "isCampoVisible('objeto')" apps/web/src/components/articulos/articulo-sheet.tsx`
3. Grep confirma label: `grep "Tipo / Objeto" apps/web/src/components/articulos/articulo-sheet.tsx`
</verification>

<success_criteria>

- Build verde de Next.js
- ArticuloSheet contiene FieldRow de objeto con label "Tipo / Objeto" en seccion Propiedades
- El campo respeta visibilidad condicional via isCampoVisible
- Posicion: primer campo del grid (antes de marca)
  </success_criteria>

<output>
After completion, create `.planning/phases/28-add-objeto-to-sheet/28-01-SUMMARY.md`
</output>
