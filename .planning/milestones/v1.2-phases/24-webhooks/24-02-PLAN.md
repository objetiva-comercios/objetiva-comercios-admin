---
phase: 24-webhooks
plan: 02
type: execute
wave: 2
depends_on: ['24-01']
files_modified:
  - apps/web/src/lib/api.client.ts
  - apps/web/src/components/settings/settings-nav.tsx
  - apps/web/src/app/(dashboard)/settings/webhooks/page.tsx
  - apps/web/src/components/settings/webhooks/webhooks-client.tsx
autonomous: true
requirements: [HOOK-01, HOOK-02]

must_haves:
  truths:
    - 'Admin can see a list of webhooks with name, URL (truncated), events (badges), estado (toggle), and actions menu'
    - 'Admin can create a webhook via Dialog selecting name, URL (HTTPS only), and multiple events — secret shown once with copy button'
    - 'Admin can edit a webhook (name, URL, events) via same Dialog pre-filled'
    - 'Admin can toggle a webhook active/inactive directly from the table'
    - 'Admin can soft-delete a webhook via AlertDialog'
    - 'Webhooks nav item visible only for admin users in Settings'
  artifacts:
    - path: 'apps/web/src/lib/api.client.ts'
      provides: 'Webhook API client functions and TypeScript interfaces'
      contains: 'fetchWebhooks'
    - path: 'apps/web/src/components/settings/webhooks/webhooks-client.tsx'
      provides: 'Main webhooks list + create/edit dialogs + toggle + delete'
      exports: ['WebhooksClient']
    - path: 'apps/web/src/app/(dashboard)/settings/webhooks/page.tsx'
      provides: 'Server component with admin-only guard'
      contains: 'notFound'
  key_links:
    - from: 'apps/web/src/components/settings/webhooks/webhooks-client.tsx'
      to: '/api/webhooks'
      via: 'api.client.ts functions'
      pattern: 'fetchWebhooks|createWebhook|updateWebhook'
    - from: 'apps/web/src/components/settings/settings-nav.tsx'
      to: '/settings/webhooks'
      via: 'nav item with Webhook icon'
      pattern: 'webhooks'
---

<objective>
Build the webhooks list page in Settings with CRUD dialogs (create with two-step secret reveal, edit, toggle active/inactive, soft-delete), following the exact API Keys UI patterns from Phase 23.

Purpose: Admins can manage webhook subscriptions from the web UI — create, edit, toggle, and delete webhooks.
Output: Functional webhooks settings page with full CRUD interactions.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-webhooks/24-CONTEXT.md
@.planning/phases/24-webhooks/24-RESEARCH.md
@.planning/phases/23-api-keys/23-02-SUMMARY.md

<interfaces>
<!-- API Keys client pattern to replicate -->

From apps/web/src/lib/api.client.ts:

```typescript
export interface ApiKeyItem {
  id: number
  name: string
  prefix: string
  createdAt: string
  lastUsedAt: string | null
  revokedAt: string | null
}
export interface ApiKeyCreated extends ApiKeyItem { fullKey: string }
export async function fetchApiKeys(): Promise<ApiKeyItem[]> { ... }
export async function createApiKey(name: string): Promise<ApiKeyCreated> { ... }
export async function revokeApiKey(id: number): Promise<void> { ... }
```

From apps/web/src/components/settings/settings-nav.tsx:

```typescript
// settingsNavItems array — add Webhooks item
// SettingsNav({ userRole }) — filter admin-only items
const visibleItems = settingsNavItems.filter(
  item => item.href !== '/settings/api-keys' || userRole === 'admin'
)
```

From apps/web/src/app/(dashboard)/settings/api-keys/page.tsx (admin guard pattern):

```typescript
import { createClient } from '@/lib/supabase/server'
import { notFound } from 'next/navigation'
// Check role, notFound() if not admin
```

Backend endpoints (from Plan 01):

- GET /api/webhooks — list all (no secret field)
- POST /api/webhooks — create, returns { ...webhook, fullSecret }
- PATCH /api/webhooks/:id — update name/url/events
- PATCH /api/webhooks/:id/toggle — flip active
- DELETE /api/webhooks/:id — soft-delete (revokedAt)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: API client functions + Settings nav webhook item</name>
  <files>
    apps/web/src/lib/api.client.ts,
    apps/web/src/components/settings/settings-nav.tsx
  </files>
  <action>
    1. api.client.ts — Add interfaces and functions at the end of the file (after existing apiKeys functions):

       Interfaces:
       - `WebhookItem`: id (number), name (string), url (string), entity (string), events (string[]), active (boolean), createdAt (string), revokedAt (string | null)
       - `WebhookCreated extends WebhookItem`: fullSecret (string)

       Functions (follow existing pattern with getAuthHeaders + throwIfError):
       - `fetchWebhooks(): Promise<WebhookItem[]>` — GET /api/webhooks
       - `createWebhook(data: { name: string; url: string; events: string[] }): Promise<WebhookCreated>` — POST /api/webhooks
       - `updateWebhook(id: number, data: Partial<{ name: string; url: string; events: string[] }>): Promise<WebhookItem>` — PATCH /api/webhooks/:id
       - `toggleWebhook(id: number): Promise<WebhookItem>` — PATCH /api/webhooks/:id/toggle
       - `revokeWebhook(id: number): Promise<void>` — DELETE /api/webhooks/:id

    2. settings-nav.tsx — Add Webhooks item to `settingsNavItems` array AFTER the API Keys item:
       - title: 'Webhooks', href: '/settings/webhooks', icon: Webhook (from lucide-react), description: 'Configurá las notificaciones a URLs externas'
       - Update the `visibleItems` filter to also hide '/settings/webhooks' for non-admin: change the condition to check both '/settings/api-keys' and '/settings/webhooks'

  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin/apps/web && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>API client has 5 webhook functions with proper TypeScript interfaces. Settings nav shows "Webhooks" item for admin users only.</done>
</task>

<task type="auto">
  <name>Task 2: Webhooks page + WebhooksClient component (list, create, edit, toggle, delete)</name>
  <files>
    apps/web/src/app/(dashboard)/settings/webhooks/page.tsx,
    apps/web/src/components/settings/webhooks/webhooks-client.tsx
  </files>
  <action>
    1. page.tsx — Server component with admin-only guard. Same pattern as API Keys page:
       - Import createClient from '@/lib/supabase/server', notFound from 'next/navigation'
       - Get user, check app_metadata.role === 'admin', notFound() if not
       - Render `<WebhooksClient />`

    2. webhooks-client.tsx — 'use client' component. Follow api-keys-client.tsx structure closely but with these differences:

       **State:** webhooks list, loading, dialog open/step, editing webhook ref, form fields (name, url, events multi-select)

       **Table columns per CONTEXT.md:**
       - Nombre: full text
       - URL: truncated with `truncate max-w-[200px]` class
       - Eventos: badges for each event (created/updated/deleted) using Badge component with variant="secondary" and text-xs
       - Estado: clickable badge "Activo" (green) / "Pausa" (yellow) that calls toggleWebhook on click
       - Acciones: DropdownMenu with Editar and Eliminar options

       **Create Dialog (two-step per CONTEXT.md):**
       - Step 1 (form): name input, url input (placeholder "https://"), events multi-select (3 checkboxes: Crear, Actualizar, Eliminar), entity selector (disabled, value "Articulos"). Button "Crear Webhook"
       - Step 2 (reveal): show fullSecret with monospace font, warning "Este secreto no se mostrara de nuevo", copy button with clipboard, "Entendido" button to close. Block onOpenChange during this step (prevent accidental close)
       - URL validation: must start with https:// (client-side check before submit)

       **Edit Dialog:** Same Dialog component. Pre-fill name, url, events from selected webhook. Show "Guardar Cambios" button. Call updateWebhook on submit.

       **Delete AlertDialog per CONTEXT.md:** message "El webhook dejara de recibir eventos inmediatamente. Las entregas pendientes se cancelaran." Call revokeWebhook on confirm.

       **Toggle:** Direct on badge click, optimistic update. Call toggleWebhook, update local state. Revert on error.

       **Empty state:** "No hay webhooks configurados" with CTA button to create one.

       **Styling:** Tabler aesthetic — text-sm base, border-radius reduced (rounded-md not rounded-xl), compact heights (h-8/h-9 buttons), py-4 not py-6 spacing.

       Use useCallback for handlers, useEffect for initial load. Toast via sonner for success/error messages.

  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin/apps/web && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Admin can navigate to Settings > Webhooks, see list of webhooks, create new ones (with two-step secret reveal), edit existing ones, toggle active/inactive, and soft-delete with AlertDialog. Viewers get 404.</done>
</task>

</tasks>

<verification>
- Web app builds without errors
- /settings/webhooks route exists and renders WebhooksClient
- Settings nav shows Webhooks item for admin users only
- Create dialog has two-step flow (form -> secret reveal)
- Edit dialog pre-fills webhook data
- Toggle badge changes between Activo/Pausa
- Delete AlertDialog with correct warning message
</verification>

<success_criteria>

- Admin sees webhooks list table with name, URL, events badges, status toggle, and actions menu
- Create webhook shows form -> secret reveal flow identical to API Keys
- Edit webhook pre-fills form and saves changes
- Toggle active/inactive updates the badge inline
- Delete shows AlertDialog and removes webhook from list
- Non-admin users get 404 on /settings/webhooks
  </success_criteria>

<output>
After completion, create `.planning/phases/24-webhooks/24-02-SUMMARY.md`
</output>
