---
phase: 24-webhooks
plan: 03
type: execute
wave: 3
depends_on: ['24-02']
files_modified:
  - apps/web/src/lib/api.client.ts
  - apps/web/src/components/settings/webhooks/webhooks-client.tsx
  - apps/web/src/components/settings/webhooks/webhook-detail.tsx
autonomous: true
requirements: [HOOK-04, HOOK-05]

must_haves:
  truths:
    - 'Admin can click a webhook row to see its detail view with info and delivery log'
    - 'Delivery log shows evento, estado (OK/Fail), codigo HTTP, and fecha for each delivery'
    - 'Admin can click a delivery row to expand and see attempt N/3, payload, and response body'
    - 'Admin can send a test ping and see inline result (success/fail + HTTP code + duration)'
    - 'Admin can resend a failed delivery from the expanded row'
    - 'Admin can regenerate webhook secret with AlertDialog warning'
    - "Delivery log shows last 20 entries with 'Cargar mas' pagination"
    - 'Test ping deliveries appear tagged as (test) in the log'
  artifacts:
    - path: 'apps/web/src/components/settings/webhooks/webhook-detail.tsx'
      provides: 'Webhook detail panel with delivery log table, ping, resend, regenerate'
      exports: ['WebhookDetail']
    - path: 'apps/web/src/lib/api.client.ts'
      provides: 'API functions for deliveries, ping, resend, regenerate-secret'
      contains: 'fetchWebhookDeliveries'
  key_links:
    - from: 'apps/web/src/components/settings/webhooks/webhook-detail.tsx'
      to: '/api/webhooks/:id/deliveries'
      via: 'fetchWebhookDeliveries'
      pattern: 'fetchWebhookDeliveries'
    - from: 'apps/web/src/components/settings/webhooks/webhook-detail.tsx'
      to: '/api/webhooks/:id/ping'
      via: 'pingWebhook'
      pattern: 'pingWebhook'
    - from: 'apps/web/src/components/settings/webhooks/webhooks-client.tsx'
      to: 'apps/web/src/components/settings/webhooks/webhook-detail.tsx'
      via: 'selectedWebhook state triggers detail render'
      pattern: 'WebhookDetail|selectedWebhook'
---

<objective>
Build the webhook detail view with delivery log table (expandable rows), test ping with inline result, resend failed deliveries, and regenerate secret — completing the admin's ability to monitor and troubleshoot webhooks.

Purpose: Admins can inspect delivery history, verify connectivity with test pings, retry failed deliveries, and rotate secrets without recreating webhooks.
Output: Complete webhook detail panel with interactive delivery log, ping, resend, and secret regeneration.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-webhooks/24-CONTEXT.md
@.planning/phases/24-webhooks/24-RESEARCH.md

<interfaces>
<!-- Interfaces created by Plan 02 that this plan consumes -->

From apps/web/src/lib/api.client.ts (Plan 02 adds these):

```typescript
export interface WebhookItem {
  id: number
  name: string
  url: string
  entity: string
  events: string[]
  active: boolean
  createdAt: string
  revokedAt: string | null
}
export async function fetchWebhooks(): Promise<WebhookItem[]>
export async function toggleWebhook(id: number): Promise<WebhookItem>
export async function revokeWebhook(id: number): Promise<void>
```

Backend endpoints (from Plan 01):

- GET /api/webhooks/:id/deliveries?page=1&limit=20 — paginated deliveries
- POST /api/webhooks/:id/ping — returns { success, httpStatus, durationMs, error? }
- POST /api/webhooks/:id/deliveries/:deliveryId/resend — resend failed delivery
- POST /api/webhooks/:id/regenerate-secret — returns { fullSecret }
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: API client functions for deliveries, ping, resend, regenerate-secret</name>
  <files>
    apps/web/src/lib/api.client.ts
  </files>
  <action>
    Add interfaces and functions AFTER the webhook functions added by Plan 02:

    Interfaces:
    - `WebhookDeliveryItem`: id (number), webhookId (number), deliveryId (string), eventName (string), payload (unknown), attempt (number), success (boolean), httpStatus (number | null), responseBody (string | null), isTest (boolean), createdAt (string)
    - `PingResult`: success (boolean), httpStatus (number | null), durationMs (number), error? (string)

    Functions:
    - `fetchWebhookDeliveries(webhookId: number, page?: number): Promise<{ data: WebhookDeliveryItem[]; meta: { total: number; page: number; limit: number; totalPages: number } }>` — GET /api/webhooks/:id/deliveries?page=N&limit=20
    - `pingWebhook(id: number): Promise<PingResult>` — POST /api/webhooks/:id/ping
    - `resendWebhookDelivery(webhookId: number, deliveryId: string): Promise<void>` — POST /api/webhooks/:id/deliveries/:deliveryId/resend
    - `regenerateWebhookSecret(id: number): Promise<{ fullSecret: string }>` — POST /api/webhooks/:id/regenerate-secret

    Follow existing pattern with getAuthHeaders + throwIfError.

  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin/apps/web && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>4 new API client functions for deliveries, ping, resend, and regenerate-secret with proper TypeScript interfaces.</done>
</task>

<task type="auto">
  <name>Task 2: WebhookDetail component + wire into WebhooksClient</name>
  <files>
    apps/web/src/components/settings/webhooks/webhook-detail.tsx,
    apps/web/src/components/settings/webhooks/webhooks-client.tsx
  </files>
  <action>
    1. webhook-detail.tsx — 'use client' component that receives a WebhookItem as prop and an onBack callback.

       **Layout (per CONTEXT.md — detail view):**
       - Top: Back button (ArrowLeft icon + "Volver a webhooks"), webhook name as heading
       - Info section: URL (full, monospace text-xs), Entity (badge "Articulos"), Events (badges), Estado (Activo/Pausa badge), Creado (formatted date)
       - Action buttons row: "Test Ping" button (primary), "Regenerar Secreto" button (outline/destructive)

       **Test Ping section:**
       - Button triggers pingWebhook(webhook.id)
       - Show loading spinner while waiting
       - Inline result below button: check icon + "200 OK (142ms)" green text on success, or X icon + "Fail: 500 (234ms)" or "Fail: Network error" red text on failure
       - Result persists until next ping or component unmount

       **Regenerate Secret:**
       - AlertDialog: title "Regenerar Secreto", message "El secreto anterior dejara de funcionar inmediatamente. Los endpoints que verifican la firma dejaran de validar hasta actualizar el nuevo secreto."
       - On confirm: call regenerateWebhookSecret(webhook.id)
       - Show new secret in monospace with copy button + warning "Este secreto no se mostrara de nuevo". Same pattern as create two-step reveal.

       **Delivery Log table:**
       - Fetch on mount with fetchWebhookDeliveries(webhook.id)
       - Columns: Evento (eventName, with "(test)" suffix for isTest=true), Estado (Badge: green "OK" / red "Fallo"), Codigo HTTP (httpStatus or "—" for null), Fecha (formatted createdAt)
       - Expandable rows: click row toggles expansion showing:
         - Intento: "N/3" (attempt value)
         - Payload: JSON.stringify(payload, null, 2) in a pre/code block with max-height and overflow-auto
         - Respuesta: responseBody text in pre/code if present, or "Sin respuesta" if null
         - If !success: "Reenviar" button that calls resendWebhookDelivery(webhook.id, delivery.deliveryId), shows toast on success/error, and refetches deliveries
       - Only show "Reenviar" on failed deliveries (not on successful ones)
       - Bottom: "Cargar mas" button if totalPages > current page. Append results (not replace).
       - Empty state: "No hay entregas registradas" message

       **Styling:** Tabler aesthetic — text-sm, compact spacing, rounded-md, muted colors for secondary info. Use cn() for conditional classes.

    2. webhooks-client.tsx — Add state for selectedWebhook (WebhookItem | null). When a webhook row is clicked, set selectedWebhook. When selectedWebhook is set, render WebhookDetail instead of the table. WebhookDetail's onBack sets selectedWebhook to null and refetches the list.

  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-admin/apps/web && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Clicking a webhook row opens detail view with info, delivery log table (expandable rows with payload/response/resend), test ping with inline result, and regenerate secret with AlertDialog. Back button returns to list.</done>
</task>

</tasks>

<verification>
- Web app builds without errors
- WebhookDetail component renders when a webhook row is clicked
- Delivery log table shows with expandable rows
- Test ping button triggers POST and shows inline result
- Regenerate secret shows AlertDialog and reveals new secret once
- Resend button appears only on failed deliveries
- "Cargar mas" loads next page of deliveries
- Back button returns to webhooks list
</verification>

<success_criteria>

- Admin can click any webhook to see its detail view
- Delivery log shows evento, estado, codigo HTTP, fecha with expandable rows
- Test ping shows inline result (checkmark/X + status + duration)
- Failed deliveries have "Reenviar" button that creates new delivery
- Regenerate secret confirms via AlertDialog and shows new secret once
- Pagination works with "Cargar mas" appending results
- Test ping deliveries appear with "(test)" tag in log
  </success_criteria>

<output>
After completion, create `.planning/phases/24-webhooks/24-03-SUMMARY.md`
</output>
