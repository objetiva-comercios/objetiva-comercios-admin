---
phase: 27-add-objeto-to-form
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/articulos/articulo-form.tsx
autonomous: true
requirements:
  - INT-01
gap_closure: true

must_haves:
  truths:
    - "Campo 'Tipo / Objeto' visible en formulario de crear articulo (seccion Propiedades)"
    - "Campo 'objeto' se popula con valor existente en modo edicion"
    - "Campo respeta isCampoVisible('objeto') — se oculta si el campo esta deshabilitado en settings"
    - 'Seccion Propiedades se muestra si solo objeto esta visible (wrapper condition)'
  artifacts:
    - path: 'apps/web/src/components/articulos/articulo-form.tsx'
      provides: 'objeto field in Zod schema, defaultValues, and JSX'
      contains: 'objeto'
  key_links:
    - from: 'articuloFormSchema'
      to: "FormField name='objeto'"
      via: 'Zod schema field drives form validation'
      pattern: "objeto.*z\\.string"
    - from: 'defaultValues'
      to: 'articulo?.objeto'
      via: 'Pre-population in edit mode'
      pattern: "objeto.*articulo\\?"
---

<objective>
Add the `objeto` field to ArticuloForm so users can create/edit articulos with this field.

Purpose: Close integration gap INT-01 from v1.2 audit — the field exists in DB and list view (Phase 22) but was missing from the create/edit form.
Output: Updated articulo-form.tsx with objeto in Zod schema, defaultValues, and JSX.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/components/articulos/articulo-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add objeto to ArticuloForm Zod schema, defaultValues, and JSX</name>
  <files>apps/web/src/components/articulos/articulo-form.tsx</files>
  <action>
Make 4 targeted edits in articulo-form.tsx:

1. **Zod schema** (`articuloFormSchema`): Add `objeto: z.string().optional().or(z.literal(''))` — same pattern as marca, material, etc. Place it near the other Propiedades fields.

2. **defaultValues** (in `useForm` call): Add `objeto: articulo?.objeto ?? ''` — same pattern as other optional string fields.

3. **Wrapper condition** (the `||` chain that guards the Propiedades section): Add `isCampoVisible('objeto') ||` as the FIRST condition in the expression, before `isCampoVisible('marca')`.

4. **FormField JSX**: Add a new FormField block as the FIRST child inside the Propiedades grid (`<div className="grid gap-3 sm:grid-cols-2">`), BEFORE the marca field:

```tsx
{
  isCampoVisible('objeto') && (
    <FormField
      control={form.control}
      name="objeto"
      render={({ field }) => (
        <FormItem>
          <FormLabel>Tipo / Objeto</FormLabel>
          <FormControl>
            <Input className="h-9" {...field} />
          </FormControl>
          <FormMessage />
        </FormItem>
      )}
    />
  )
}
```

No placeholder, no FormDescription — consistent with all other Propiedades fields.
Do NOT add parameter tables/Select/Combobox — that is deferred to a future phase.
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && pnpm --filter web build 2>&1 | tail -5</automated>
</verify>
<done> - articuloFormSchema includes objeto field with z.string().optional().or(z.literal('')) - defaultValues includes objeto: articulo?.objeto ?? '' - Propiedades wrapper condition includes isCampoVisible('objeto') - FormField for objeto renders as first field in Propiedades grid with label "Tipo / Objeto" - TypeScript build passes without errors
</done>
</task>

</tasks>

<verification>
1. `pnpm --filter web build` completes without TypeScript errors
2. Grep confirms objeto in schema: `grep -n "objeto" apps/web/src/components/articulos/articulo-form.tsx`
3. Visual: open /articulos/nuevo, verify "Tipo / Objeto" appears as first field in Propiedades section
</verification>

<success_criteria>

- Build passes (zero TypeScript errors)
- objeto field appears in create form under Propiedades section
- objeto field populates with existing value in edit mode
- objeto field respects isCampoVisible toggle from settings
  </success_criteria>

<output>
After completion, create `.planning/phases/27-add-objeto-to-form/27-01-SUMMARY.md`
</output>
