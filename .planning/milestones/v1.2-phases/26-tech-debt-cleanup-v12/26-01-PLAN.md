---
phase: 26-tech-debt-cleanup-v12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/webhooks/webhook-events.ts
  - apps/backend/src/modules/webhooks/webhooks.service.ts
  - apps/backend/src/modules/webhooks/webhooks.listener.ts
  - apps/backend/src/modules/articulos/articulos.service.ts
  - apps/backend/src/modules/api-keys/api-keys.service.ts
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - 'dispatchEvent rechaza strings arbitrarios en compilacion — solo acepta WebhookEvent'
    - 'Revocar una API key ya revocada devuelve 409 Conflict con mensaje explicito'
    - 'Revocar un webhook ya revocado devuelve 409 Conflict con mensaje explicito'
    - 'Segundo revoke preserva el revokedAt original (no lo sobreescribe)'
  artifacts:
    - path: 'apps/backend/src/modules/webhooks/webhook-events.ts'
      provides: 'Constantes tipadas WEBHOOK_EVENTS, type WebhookEvent, mapeo EVENT_TO_DB'
      exports: ['WEBHOOK_EVENTS', 'WebhookEvent', 'EVENT_TO_DB']
    - path: 'apps/backend/src/modules/webhooks/webhooks.service.ts'
      provides: 'dispatchEvent tipado con WebhookEvent, revoke con idempotency guard, findOneAny privado'
      contains: 'ConflictException'
    - path: 'apps/backend/src/modules/api-keys/api-keys.service.ts'
      provides: 'revoke con idempotency guard y ConflictException'
      contains: 'ConflictException'
  key_links:
    - from: 'apps/backend/src/modules/articulos/articulos.service.ts'
      to: 'apps/backend/src/modules/webhooks/webhook-events.ts'
      via: 'import WEBHOOK_EVENTS'
      pattern: "WEBHOOK_EVENTS\\.ARTICULO_"
    - from: 'apps/backend/src/modules/webhooks/webhooks.listener.ts'
      to: 'apps/backend/src/modules/webhooks/webhook-events.ts'
      via: 'import WEBHOOK_EVENTS para @OnEvent decorators'
      pattern: "WEBHOOK_EVENTS\\.ARTICULO_"
    - from: 'apps/backend/src/modules/webhooks/webhooks.service.ts'
      to: 'apps/backend/src/modules/webhooks/webhook-events.ts'
      via: 'import WebhookEvent y EVENT_TO_DB para dispatchEvent'
      pattern: "EVENT_TO_DB\\[eventName\\]"
---

<objective>
Corregir 3 defectos tech debt del audit v1.2: type safety de webhook events, idempotencia de re-revoke en API keys, e idempotencia de re-revoke en webhooks.

Purpose: Eliminar string splitting fragil en dispatchEvent, prevenir sobreescritura silenciosa de revokedAt, y devolver HTTP semantics correctos (409 vs silencio/404).
Output: 5 archivos modificados (1 nuevo + 4 existentes), compilacion limpia, comportamiento idempotente verificable.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-tech-debt-cleanup-v12/26-CONTEXT.md
@.planning/phases/26-tech-debt-cleanup-v12/26-RESEARCH.md

<interfaces>
<!-- Archivos existentes que se modifican — estructura actual relevante -->

From apps/backend/src/modules/webhooks/webhooks.service.ts:

```typescript
async findOne(id: number) // filtra isNull(revokedAt) — callers: update, toggle, ping, findDeliveries, resendDelivery, regenerateSecret, revoke
async revoke(id: number) // usa findOne → 404 si ya revocado (bug)
async dispatchEvent(eventName: string, payload: unknown) // split('.')[1] fragil (bug)
```

From apps/backend/src/modules/api-keys/api-keys.service.ts:

```typescript
async revoke(id: number) // update directo sin verificar estado previo (bug)
```

From apps/backend/src/modules/webhooks/webhooks.listener.ts:

```typescript
@OnEvent('articulo.created') // string literal hardcodeado
@OnEvent('articulo.updated') // string literal hardcodeado
@OnEvent('articulo.deleted') // string literal hardcodeado
```

From apps/backend/src/modules/articulos/articulos.service.ts:

```typescript
this.eventEmitter.emit('articulo.created', { articulo }) // string literal
this.eventEmitter.emit('articulo.updated', { articulo }) // string literal
this.eventEmitter.emit('articulo.deleted', { articulo }) // string literal
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webhook-events.ts and wire type-safe events across emitter, listener, and dispatcher</name>
  <files>apps/backend/src/modules/webhooks/webhook-events.ts, apps/backend/src/modules/webhooks/webhooks.service.ts, apps/backend/src/modules/webhooks/webhooks.listener.ts, apps/backend/src/modules/articulos/articulos.service.ts</files>
  <action>
1. Create `apps/backend/src/modules/webhooks/webhook-events.ts` — pure leaf module, zero imports:
   - `WEBHOOK_EVENTS` as const object with keys ARTICULO_CREATED, ARTICULO_UPDATED, ARTICULO_DELETED mapping to 'articulo.created', 'articulo.updated', 'articulo.deleted'
   - `WebhookEvent` type derived via `typeof WEBHOOK_EVENTS[keyof typeof WEBHOOK_EVENTS]`
   - `EVENT_TO_DB` record mapping full event name to DB short-form: 'articulo.created' -> 'created', etc.

2. Update `webhooks.service.ts`:
   - Import `WebhookEvent` and `EVENT_TO_DB` from './webhook-events'
   - Change `dispatchEvent(eventName: string, ...)` to `dispatchEvent(eventName: WebhookEvent, ...)`
   - Replace `eventName.split('.')[1]` with `EVENT_TO_DB[eventName]`
   - No other changes to this file in this task

3. Update `webhooks.listener.ts`:
   - Import `WEBHOOK_EVENTS` from './webhook-events'
   - Replace string literals in @OnEvent decorators: `@OnEvent(WEBHOOK_EVENTS.ARTICULO_CREATED)`, etc.
   - Replace string literals in dispatchEvent calls: `this.webhooksService.dispatchEvent(WEBHOOK_EVENTS.ARTICULO_CREATED, payload)`, etc.

4. Update `articulos.service.ts`:
   - Import `WEBHOOK_EVENTS` from '../webhooks/webhook-events'
   - Replace all `this.eventEmitter.emit('articulo.created', ...)` with `this.eventEmitter.emit(WEBHOOK_EVENTS.ARTICULO_CREATED, ...)`
   - Same for 'articulo.updated' and 'articulo.deleted' in create(), update(), toggleActive(), softDelete()
     </action>
     <verify>
     <automated>cd apps/backend && npx tsc --noEmit 2>&amp;1 | head -30</automated>
     </verify>
     <done>webhook-events.ts exporta WEBHOOK_EVENTS, WebhookEvent y EVENT_TO_DB. Los 4 archivos compilan sin error. Ningun string literal de evento queda en articulos.service, webhooks.listener, o webhooks.service.dispatchEvent.</done>
     </task>

<task type="auto">
  <name>Task 2: Add idempotency guards to api-keys.revoke() and webhooks.revoke()</name>
  <files>apps/backend/src/modules/api-keys/api-keys.service.ts, apps/backend/src/modules/webhooks/webhooks.service.ts</files>
  <action>
1. Update `api-keys.service.ts`:
   - Add `ConflictException` and `NotFoundException` to @nestjs/common imports
   - Rewrite `revoke(id: number)`:
     a. Query `apiKeys` by `eq(apiKeys.id, id)` WITHOUT isNull filter, `.limit(1)`
     b. If no row found: `throw new NotFoundException('API key no encontrada')`
     c. If `existing.revokedAt !== null`: `throw new ConflictException('API key ya fue revocada')`
     d. Only then: update revokedAt to new Date() where eq(apiKeys.id, id)

2. Update `webhooks.service.ts`:
   - Add `ConflictException` to @nestjs/common imports (NotFoundException already imported)
   - Add private method `findOneAny(id: number)` that queries webhooks by eq(webhooks.id, id) WITHOUT isNull filter, returns `rows[0] ?? null`. Keep public `findOne(id)` unchanged (still filters isNull) so all other callers (update, toggle, ping, findDeliveries, resendDelivery, regenerateSecret) keep returning 404 for revoked webhooks.
   - Rewrite `revoke(id: number)`:
     a. Call `this.findOneAny(id)` instead of `this.findOne(id)`
     b. If not found: `throw new NotFoundException('Webhook no encontrado')`
     c. If `existing.revokedAt !== null`: `throw new ConflictException('Webhook ya fue revocado')`
     d. Only then: update revokedAt to new Date() where eq(webhooks.id, id)
     </action>
     <verify>
     <automated>cd apps/backend && npx tsc --noEmit 2>&amp;1 | head -30</automated>
     </verify>
     <done>api-keys.service.revoke() lanza NotFoundException si no existe, ConflictException si ya revocado, y solo actualiza si esta activa. webhooks.service.revoke() usa findOneAny privado con el mismo patron. findOne publica sin cambios. Compilacion limpia.</done>
     </task>

</tasks>

<verification>
1. `cd apps/backend && npx tsc --noEmit` — compilacion TypeScript limpia sin errores
2. Verificar que no quedan string literals de eventos: `grep -rn "'articulo\." apps/backend/src/modules/articulos/articulos.service.ts apps/backend/src/modules/webhooks/webhooks.listener.ts` debe devolver vacio
3. Verificar ConflictException en ambos servicios: `grep -n "ConflictException" apps/backend/src/modules/api-keys/api-keys.service.ts apps/backend/src/modules/webhooks/webhooks.service.ts`
4. Verificar findOneAny es privado: `grep -n "private.*findOneAny" apps/backend/src/modules/webhooks/webhooks.service.ts`
</verification>

<success_criteria>

- TypeScript compila sin errores (tsc --noEmit exit 0)
- webhook-events.ts existe con WEBHOOK_EVENTS, WebhookEvent, EVENT_TO_DB exportados
- Cero string literals de eventos en articulos.service.ts, webhooks.listener.ts, webhooks.service.dispatchEvent
- api-keys.service.revoke() tiene check de revokedAt + ConflictException
- webhooks.service.revoke() usa findOneAny + check de revokedAt + ConflictException
- webhooks.service.findOne() publica sin cambios (sigue filtrando isNull)
  </success_criteria>

<output>
After completion, create `.planning/phases/26-tech-debt-cleanup-v12/26-01-SUMMARY.md`
</output>
