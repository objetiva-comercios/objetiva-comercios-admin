---
phase: 14-schema-foundation-articulos-depositos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/db/schema.ts
  - apps/backend/src/db/seed.ts
  - apps/backend/src/db/generators/articulo.generator.ts
  - apps/backend/src/db/generators/deposito.generator.ts
  - apps/backend/src/modules/settings/settings.controller.ts
  - packages/types/src/index.ts
autonomous: true
requirements:
  - MIG-01
  - MIG-04
  - MIG-06
  - MIG-07
  - ART-08
  - ART-09
  - ART-10
  - ART-11
  - ART-12
  - DEBT-01
  - DEBT-04

must_haves:
  truths:
    - 'Drizzle schema defines articulos table with text PK (codigo), ~30 fields across 6 groups, and numeric(10,2) for precio/costo'
    - 'Drizzle schema defines depositos table with serial PK, nombre/direccion/descripcion/activo fields'
    - 'Running db:push && db:seed produces a working database with articulos, depositos, and all v1.0 tables'
    - "Settings write endpoints (PATCH, POST logo, DELETE logo) are protected with @Roles('admin')"
    - 'packages/types no longer exports unused Product/Inventory-related types'
  artifacts:
    - path: 'apps/backend/src/db/schema.ts'
      provides: 'articulos and depositos table definitions with type exports'
      contains: 'articulos'
    - path: 'apps/backend/src/db/seed.ts'
      provides: 'Seed script for articulos, depositos, and v1.0 tables'
      contains: 'articulos'
    - path: 'apps/backend/src/db/generators/articulo.generator.ts'
      provides: 'Faker-based articulo data generator'
      exports: ['generateArticulos']
    - path: 'apps/backend/src/db/generators/deposito.generator.ts'
      provides: 'Faker-based deposito data generator'
      exports: ['generateDepositos']
  key_links:
    - from: 'apps/backend/src/db/seed.ts'
      to: 'apps/backend/src/db/schema.ts'
      via: 'import schema tables'
      pattern: "schema\\.articulos"
    - from: 'apps/backend/src/db/seed.ts'
      to: 'apps/backend/src/db/generators/articulo.generator.ts'
      via: 'import generator'
      pattern: 'generateArticulos'
---

<objective>
Create the Drizzle schema for articulos (text PK, ~30 fields, numeric monetary) and depositos tables, rewrite seed data for the new model, fix Settings RBAC gap (DEBT-01), and clean unused shared package exports (DEBT-04).

Purpose: Establishes the data foundation that all backend modules and frontend features depend on.
Output: Working schema + seed, secured settings endpoints, cleaned shared types.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-schema-foundation-articulos-depositos/14-CONTEXT.md
@.planning/phases/14-schema-foundation-articulos-depositos/14-RESEARCH.md

@apps/backend/src/db/schema.ts
@apps/backend/src/db/seed.ts
@apps/backend/src/modules/settings/settings.controller.ts
@packages/types/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define articulos and depositos tables in Drizzle schema</name>
  <files>apps/backend/src/db/schema.ts</files>
  <action>
Add `articulos` and `depositos` table definitions to the existing schema.ts file. Keep ALL existing tables (products, orders, orderItems, inventory, sales, saleItems, purchases, purchaseItems, businessSettings) unchanged — they still have FK references that Phase 16 will migrate. Add the new tables after businessSettings.

Import `numeric`, `boolean`, and `jsonb` from `drizzle-orm/pg-core` (add to existing import).

**articulos table** (follows RESEARCH.md Pattern 1 exactly):

- PK: `codigo: text('codigo').primaryKey()` (ART-12)
- Identification: nombre (varchar 255 notNull), sku (varchar 50), codigoBarras (varchar 50), observaciones (text)
- Properties (ART-08): marca (varchar 100), modelo (varchar 100), talle (varchar 50), color (varchar 50), material (varchar 100), presentacion (varchar 100), medida (varchar 50)
- Prices (MIG-07): `precio: numeric('precio', { precision: 10, scale: 2 })`, `costo: numeric('costo', { precision: 10, scale: 2 })`
- Images (ART-09): imagenesProducto (jsonb, string[] default []), imagenesEtiqueta (jsonb, string[] default []), etiquetasOcr (jsonb, string[] default []), jsonArticulo (jsonb)
- ERP (ART-10): erpId (varchar 50), erpCodigo (varchar 50), erpNombre (varchar 255), erpPrecio (numeric 10,2), erpCosto (numeric 10,2), erpUnidades (integer), erpDatos (jsonb), erpSincronizado (boolean default false), erpFechaSync (timestamp)
- Origin (ART-11): originSource (varchar 50), originSyncId (varchar 100), originSyncedAt (timestamp)
- State: activo (boolean notNull default true), createdAt (timestamp notNull defaultNow), updatedAt (timestamp notNull defaultNow)
- Indexes: nombre, sku, codigoBarras, erpCodigo, activo, marca

**depositos table** (MIG-04):

- PK: `id: serial('id').primaryKey()`
- Fields: nombre (varchar 100 notNull), direccion (varchar 255), descripcion (text), activo (boolean notNull default true), createdAt/updatedAt (timestamp notNull defaultNow)
- Index: activo

Export types: `Articulo`, `NewArticulo`, `Deposito`, `NewDeposito` using `$inferSelect` / `$inferInsert`.
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin/apps/backend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
</verify>
<done>schema.ts compiles with articulos (text PK, numeric monetary, jsonb, ~30 fields) and depositos tables defined alongside existing v1.0 tables. Type exports available.</done>
</task>

<task type="auto">
  <name>Task 2: Create generators and rewrite seed for articulos + depositos</name>
  <files>apps/backend/src/db/generators/articulo.generator.ts, apps/backend/src/db/generators/deposito.generator.ts, apps/backend/src/db/seed.ts</files>
  <action>
**articulo.generator.ts:** Create a generator that produces realistic Mexican commerce articulo data using @faker-js/faker. Follow the pattern of existing `product.generator.ts`. Generate ~300 articulos with:
- codigo: sequential like "ART-001", "ART-002" (simple, no special chars for now)
- nombre: product names in Spanish (use faker.commerce.productName)
- sku: faker.string.alphanumeric(8).toUpperCase()
- codigoBarras: faker.string.numeric(13) (EAN-13 format)
- marca, modelo, talle, color, material, presentacion, medida: mix of populated and null (60% populated)
- precio: random between 10.00-9999.99 as string
- costo: ~60-80% of precio as string
- imagenesProducto: empty array
- imagenesEtiqueta: empty array
- ~20% have ERP fields populated (erpId, erpCodigo, erpNombre, etc.)
- ~10% have origin fields populated
- activo: 90% true, 10% false
- Export function `generateArticulos(count: number)`

**deposito.generator.ts:** Generate 5 depositos with realistic Mexican warehouse names:

- "Deposito Central", "Sucursal Norte", "Sucursal Sur", "Almacen Auxiliar", "Deposito Temporal"
- All with direccion and descripcion
- All activo: true
- Export function `generateDepositos()`

**seed.ts:** Rewrite the seed to:

1. TRUNCATE all tables (add articulos and depositos to the truncate list, keep all existing tables)
2. Seed depositos first (5 records)
3. Seed articulos (300 records) — use `db.insert(schema.articulos).values(...)` with text PK (no `.returning()` needed for ID mapping since PK is the codigo itself)
4. Keep existing product seeding (products table still exists for Phase 16 FK compat) but reduce to 100 products
5. Keep existing order, sale, purchase, inventory seeding with reduced counts to match 100 products
6. Import new generators, remove old product count logging, add articulos/depositos logging

IMPORTANT: The TRUNCATE must include `articulos, depositos` in the list. Use `ON CONFLICT DO NOTHING` or ensure codigo uniqueness in generator.
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin/apps/backend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
</verify>
<done>Generators create realistic articulo/deposito data. Seed script compiles and includes both new tables and existing v1.0 tables. Running `pnpm db:push && pnpm db:seed` will populate the database (full runtime verification deferred to Plan 02 after backend modules exist).</done>
</task>

<task type="auto">
  <name>Task 3: Fix Settings RBAC and clean unused shared exports</name>
  <files>apps/backend/src/modules/settings/settings.controller.ts, packages/types/src/index.ts</files>
  <action>
**DEBT-01 — Settings RBAC fix:**
In `settings.controller.ts`, add `@UseGuards(RolesGuard)` and `@Roles('admin')` to these methods:
- `update()` (PATCH)
- `uploadLogo()` (POST logo/:type)
- `deleteLogo()` (DELETE logo/:type)

Import `RolesGuard` from `../../common/guards/roles.guard` and `Roles` from `../../common/decorators/roles.decorator`. The GET endpoint stays `@Public()`. Follow the exact pattern from products.controller.ts.

**DEBT-04 — Clean unused shared package exports:**
In `packages/types/src/index.ts`, the file currently exports auth-related schemas (emailSchema, passwordSchema, loginSchema, signupSchema, getPasswordStrength, User, AppRole, ApiResponse). These are all actively used by the web and mobile apps.

Check if any exports are unused:

- `signupSchema` — verify if signup flow exists in web/mobile. If signup is not implemented, this is dead code but harmless. Leave it since it's a validation schema that may be used.
- The file does NOT export Product, Order, etc. types (those come from Drizzle schema on backend and local types on web). So the "unused shared package exports" likely refers to checking for any import of `@objetiva/types` that pulls unused items.

Audit: Search the codebase for imports from `@objetiva/types`. If all exports are used somewhere, document that DEBT-04 is clean. If any are unused, remove them.

Also check `packages/ui/src/index.ts` (the @objetiva/ui package) for unused exports and clean if found.
</action>
<verify>
<automated>cd /home/sanchez/proyectos/objetiva-comercios-admin && npx turbo build --filter=@objetiva/types --filter=@objetiva/ui 2>&1 | tail -10</automated>
</verify>
<done>Settings PATCH/POST/DELETE endpoints protected with @Roles('admin'). Shared packages audited and cleaned of unused exports. Turbo build passes for shared packages.</done>
</task>

</tasks>

<verification>
- `apps/backend/src/db/schema.ts` contains `articulos` table with text PK and `depositos` table with serial PK
- `apps/backend/src/db/schema.ts` exports `Articulo`, `NewArticulo`, `Deposito`, `NewDeposito` types
- Schema compiles without errors via `npx tsc --noEmit`
- Seed script references new tables and generators
- Settings controller has `@UseGuards(RolesGuard)` + `@Roles('admin')` on write endpoints
- Shared packages build cleanly
</verification>

<success_criteria>

- articulos table defined with text PK (codigo), ~30 fields across 6 groups, numeric(10,2) for precio/costo
- depositos table defined with serial PK and 5 fields
- Seed generators produce realistic data for both tables
- Seed script truncates and populates all tables (new + v1.0)
- Settings write endpoints require admin role
- Shared packages have no unused exports
- TypeScript compilation passes for the backend
  </success_criteria>

<output>
After completion, create `.planning/phases/14-schema-foundation-articulos-depositos/14-01-SUMMARY.md`
</output>
