---
phase: 01-foundation-monorepo
plan: 03
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/tailwind.config.ts
  - apps/web/postcss.config.js
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/globals.css
  - apps/web/components.json
  - apps/web/.env.example
  - apps/mobile/package.json
  - apps/mobile/tsconfig.json
  - apps/mobile/vite.config.ts
  - apps/mobile/tailwind.config.ts
  - apps/mobile/postcss.config.js
  - apps/mobile/index.html
  - apps/mobile/src/main.tsx
  - apps/mobile/src/App.tsx
  - apps/mobile/src/index.css
  - apps/mobile/.env.example
autonomous: true

must_haves:
  truths:
    - 'Web app starts on localhost:3000 and displays home page'
    - 'Mobile app starts on localhost:5173 and displays home page'
    - 'Both apps successfully import design tokens from @objetiva/ui'
    - 'Tailwind CSS styling works in both apps'
  artifacts:
    - path: 'apps/web/src/app/page.tsx'
      provides: 'Next.js home page component'
      contains: '@objetiva/ui'
    - path: 'apps/mobile/src/App.tsx'
      provides: 'Mobile app root component'
      contains: '@objetiva/ui'
    - path: 'apps/web/tailwind.config.ts'
      provides: 'Tailwind config using design tokens'
      contains: 'tokens'
    - path: 'apps/mobile/tailwind.config.ts'
      provides: 'Tailwind config using design tokens'
      contains: 'tokens'
  key_links:
    - from: 'apps/web/package.json'
      to: '@objetiva/ui'
      via: 'workspace dependency'
      pattern: "workspace:\\*"
    - from: 'apps/mobile/package.json'
      to: '@objetiva/ui'
      via: 'workspace dependency'
      pattern: "workspace:\\*"
    - from: 'apps/*/tailwind.config.ts'
      to: 'packages/ui/tokens'
      via: 'design token import'
      pattern: '@objetiva/ui/tokens'
---

<objective>
Create the web (Next.js) and mobile (Vite + React) app skeletons that consume the shared packages. Both apps use Tailwind CSS with design tokens from packages/ui.

Purpose: Frontend apps demonstrate that workspace packages work correctly. This proves the monorepo structure is functional before adding features.
Output: Running web app on port 3000 and mobile app on port 5173, both using shared design tokens.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-monorepo/01-CONTEXT.md
@.planning/phases/01-foundation-monorepo/01-RESEARCH.md
@.planning/phases/01-foundation-monorepo/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js web app skeleton</name>
  <files>
    apps/web/package.json
    apps/web/tsconfig.json
    apps/web/next.config.ts
    apps/web/tailwind.config.ts
    apps/web/postcss.config.js
    apps/web/src/app/layout.tsx
    apps/web/src/app/page.tsx
    apps/web/src/app/globals.css
    apps/web/components.json
    apps/web/.env.example
  </files>
  <action>
Create the Next.js web application with App Router:

1. **apps/web/package.json**:

- name: "@objetiva/web"
- version: "1.0.0"
- scripts: dev (next dev), build (next build), start (next start), lint, type-check
- dependencies:
  - next@^14.0.0
  - react@^18.3.0
  - react-dom@^18.3.0
  - @objetiva/ui: "workspace:\*"
  - @objetiva/types: "workspace:\*"
  - @objetiva/utils: "workspace:\*"
- devDependencies:
  - typescript@^5.3.0
  - @types/react@^18.3.0
  - @types/node@^20.0.0
  - tailwindcss@^3.4.0
  - postcss@^8.4.0
  - autoprefixer@^10.4.0

2. **apps/web/tsconfig.json**:

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "plugins": [{ "name": "next" }],
    "jsx": "preserve",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowJs": true,
    "noEmit": true,
    "incremental": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

3. **apps/web/next.config.ts**:

```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  transpilePackages: ['@objetiva/ui'],
  experimental: {
    // Enable turbopack for faster dev
  },
}

export default nextConfig
```

4. **apps/web/tailwind.config.ts** - Using design tokens:

```typescript
import type { Config } from 'tailwindcss'
import { colors, spacing, typography } from '@objetiva/ui/tokens'

const config: Config = {
  content: ['./src/**/*.{js,ts,jsx,tsx,mdx}', '../../packages/ui/src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        ...colors,
      },
      spacing: spacing,
      fontFamily: typography.fontFamily,
      fontSize: typography.fontSize,
    },
  },
  plugins: [],
}

export default config
```

5. **apps/web/postcss.config.js**:

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

6. **apps/web/src/app/globals.css**:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --background: #ffffff;
  --foreground: #0f172a;
}

body {
  background: var(--background);
  color: var(--foreground);
  font-family: Inter, system-ui, sans-serif;
}
```

7. **apps/web/src/app/layout.tsx**:

```typescript
import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'Objetiva Comercios Admin',
  description: 'Commercial administration application',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

8. **apps/web/src/app/page.tsx** - Home page using design tokens:

```typescript
import { colors, cn } from '@objetiva/ui'

export default function Home() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center p-8">
      <h1 className="text-4xl font-bold mb-4">
        Objetiva Comercios Admin
      </h1>
      <p className="text-muted-foreground mb-8">
        Web Application - Foundation Ready
      </p>
      <div className="flex gap-4">
        <div
          className={cn(
            "w-16 h-16 rounded-lg",
            "bg-primary"
          )}
          title={`Primary: ${colors.primary.DEFAULT}`}
        />
        <div
          className={cn(
            "w-16 h-16 rounded-lg",
            "bg-secondary"
          )}
          title={`Secondary: ${colors.secondary.DEFAULT}`}
        />
        <div
          className={cn(
            "w-16 h-16 rounded-lg",
            "bg-destructive"
          )}
          title={`Destructive: ${colors.destructive.DEFAULT}`}
        />
      </div>
      <p className="mt-8 text-sm text-muted-foreground">
        Design tokens imported from @objetiva/ui
      </p>
    </main>
  )
}
```

9. **apps/web/components.json** - shadcn/ui config for later:

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/app/globals.css",
    "baseColor": "slate",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@objetiva/ui/lib/utils"
  }
}
```

10. **apps/web/.env.example**:

```
# Supabase Auth
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# API
NEXT_PUBLIC_API_URL=http://localhost:3001
```

Run `pnpm install` from root after creating files.
</action>
<verify>
Run: `pnpm install` from repo root
Run: `turbo dev --filter=@objetiva/web` - should start Next.js dev server
Check: Browser at http://localhost:3000 shows "Objetiva Comercios Admin"
Check: Page displays 3 colored squares (design tokens working)
</verify>
<done>
Next.js web app exists and runs on port 3000. App imports and uses design tokens from @objetiva/ui. Tailwind CSS styling works correctly.
</done>
</task>

<task type="auto">
  <name>Task 2: Create Vite + React mobile app skeleton</name>
  <files>
    apps/mobile/package.json
    apps/mobile/tsconfig.json
    apps/mobile/tsconfig.node.json
    apps/mobile/vite.config.ts
    apps/mobile/tailwind.config.ts
    apps/mobile/postcss.config.js
    apps/mobile/index.html
    apps/mobile/src/main.tsx
    apps/mobile/src/App.tsx
    apps/mobile/src/index.css
    apps/mobile/src/vite-env.d.ts
    apps/mobile/.env.example
  </files>
  <action>
Create the Vite + React mobile application (Capacitor will be added in Phase 4):

1. **apps/mobile/package.json**:

- name: "@objetiva/mobile"
- version: "1.0.0"
- type: "module"
- scripts: dev (vite), build (vite build), preview (vite preview), lint, type-check
- dependencies:
  - react@^18.3.0
  - react-dom@^18.3.0
  - @objetiva/ui: "workspace:\*"
  - @objetiva/types: "workspace:\*"
  - @objetiva/utils: "workspace:\*"
- devDependencies:
  - vite@^5.0.0
  - @vitejs/plugin-react@^4.0.0
  - typescript@^5.3.0
  - @types/react@^18.3.0
  - @types/react-dom@^18.3.0
  - tailwindcss@^3.4.0
  - postcss@^8.4.0
  - autoprefixer@^10.4.0

2. **apps/mobile/tsconfig.json**:

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

3. **apps/mobile/tsconfig.node.json**:

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts"]
}
```

4. **apps/mobile/vite.config.ts**:

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
  },
  // Optimize workspace packages
  optimizeDeps: {
    include: ['@objetiva/ui'],
  },
})
```

5. **apps/mobile/tailwind.config.ts** - Same pattern as web:

```typescript
import type { Config } from 'tailwindcss'
import { colors, spacing, typography } from '@objetiva/ui/tokens'

const config: Config = {
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}',
    '../../packages/ui/src/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        ...colors,
      },
      spacing: spacing,
      fontFamily: typography.fontFamily,
      fontSize: typography.fontSize,
    },
  },
  plugins: [],
}

export default config
```

6. **apps/mobile/postcss.config.js**:

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

7. **apps/mobile/index.html**:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <title>Objetiva Comercios Mobile</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

8. **apps/mobile/src/main.tsx**:

```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

9. **apps/mobile/src/App.tsx** - Mobile home using design tokens:

```typescript
import { colors, cn } from '@objetiva/ui'

function App() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center p-8 bg-background">
      <h1 className="text-3xl font-bold mb-4 text-foreground">
        Objetiva Comercios Mobile
      </h1>
      <p className="text-muted-foreground mb-8">
        Mobile Application - Foundation Ready
      </p>
      <div className="flex gap-4">
        <div
          className={cn(
            "w-14 h-14 rounded-lg",
            "bg-primary"
          )}
          title={`Primary: ${colors.primary.DEFAULT}`}
        />
        <div
          className={cn(
            "w-14 h-14 rounded-lg",
            "bg-secondary"
          )}
          title={`Secondary: ${colors.secondary.DEFAULT}`}
        />
        <div
          className={cn(
            "w-14 h-14 rounded-lg",
            "bg-destructive"
          )}
          title={`Destructive: ${colors.destructive.DEFAULT}`}
        />
      </div>
      <p className="mt-8 text-sm text-muted-foreground">
        Design tokens imported from @objetiva/ui
      </p>
    </main>
  )
}

export default App
```

10. **apps/mobile/src/index.css**:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --background: #ffffff;
  --foreground: #0f172a;
}

body {
  background: var(--background);
  color: var(--foreground);
  font-family: Inter, system-ui, sans-serif;
  margin: 0;
  padding: 0;
  /* Safe area insets for mobile */
  padding-top: env(safe-area-inset-top);
  padding-bottom: env(safe-area-inset-bottom);
}
```

11. **apps/mobile/src/vite-env.d.ts**:

```typescript
/// <reference types="vite/client" />
```

12. **apps/mobile/.env.example**:

```
# Supabase Auth
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# API
VITE_API_URL=http://localhost:3001
```

Run `pnpm install` from root after creating files.
</action>
<verify>
Run: `pnpm install` from repo root
Run: `turbo dev --filter=@objetiva/mobile` - should start Vite dev server
Check: Browser at http://localhost:5173 shows "Objetiva Comercios Mobile"
Check: Page displays 3 colored squares (design tokens working)
Run: `turbo dev` - should start all apps concurrently (web, mobile, backend)
</verify>
<done>
Vite + React mobile app exists and runs on port 5173. App imports and uses design tokens from @objetiva/ui. Tailwind CSS styling works correctly. Both apps can run concurrently via turbo dev.
</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `turbo build` compiles web and mobile apps successfully
2. `turbo dev --filter=@objetiva/web` starts on http://localhost:3000
3. `turbo dev --filter=@objetiva/mobile` starts on http://localhost:5173
4. Both apps show colored squares demonstrating design tokens work
5. `turbo dev` starts all apps concurrently (web:3000, mobile:5173, backend:3001)
6. TypeScript resolves @objetiva/ui, @objetiva/types, @objetiva/utils imports
</verification>

<success_criteria>

- Web app runs and displays home page with design tokens
- Mobile app runs and displays home page with design tokens
- Both apps successfully import from @objetiva/ui
- Tailwind CSS works with shared design tokens in both apps
- turbo dev runs all apps concurrently
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-monorepo/01-03-SUMMARY.md`
</output>
